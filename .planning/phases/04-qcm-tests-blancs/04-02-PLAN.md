---
phase: 04-qcm-tests-blancs
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - qhse-cesi/outils.html
autonomous: true
requirements: [QUIZ-01, QUIZ-02, QUIZ-03, SRS-03]
requirements_addressed: [QUIZ-01, QUIZ-02, QUIZ-03, SRS-03]
must_haves:
  truths:
    - "Owner opens the QCM tab, picks a theme (or 'Tous les thèmes'), and sees a question stem with 4 answer choices rendered from window.BANK (QUIZ-01)"
    - "Clicking a choice immediately reveals correct/incorrect badge + canonical answer + explanation + source — single-click, no Valider button (QUIZ-02 / D-01)"
    - "Clicking a wrong choice writes one row to localStorage qhse-srs-v1[item.id] using window.SRS.schedule(state, 'rate') — never re-implements SM-2 (QUIZ-03 / SRS-03 / D-03)"
    - "Per-session in-memory Set<itemId> prevents double-penalty on the same card during a single panel sitting; resets on tab switch or reload (D-04)"
    - "qhse-prefs-v1 gains key lastQcmTheme via merge-safe writer that preserves Phase 3 keys (lastTheme, lastMode, newCardsPerDay)"
    - "Suivant button advances to next item; keyboard Enter/Space on Suivant equivalent to click; arrow keys pass through to browser default (D-02 — explicit Suivant, no auto-advance, no click-anywhere-on-verso)"
    - "Empty pool (theme has no QCM items) renders inline French message; bank-load failure renders inline degraded message — no console error, no broken page"
  artifacts:
    - path: "qhse-cesi/outils.html"
      provides: "Replacement DOM scaffold for #panel-qcm (replaces lines 133-136 placeholder) + new QCM engine IIFE appended after the existing Flashcards IIFE (~after line 795)"
      contains: "<h2 class=\"sr-only\">QCM — révision rapide</h2>, <nav class=\"qz-theme\">, <article class=\"qz-card\" data-qz-card>, [data-qz-question], .qz-choices, .qz-reveal[hidden], .qz-reveal__badge, [data-qz-answer], [data-qz-explanation], [data-qz-source], [data-qz-next] button, IIFE with window.__qzQcmBooted guard + DCL boot"
  key_links:
    - from: "qhse-cesi/outils.html #panel-qcm IIFE"
      to: "window.BANK (outils-data.js)"
      via: "BANK.filter(i => i.type === 'qcm' && (theme === 'all' || i.theme === theme))"
      pattern: "BANK\\.filter.*type.*qcm"
    - from: "qhse-cesi/outils.html #panel-qcm IIFE"
      to: "localStorage qhse-srs-v1"
      via: "window.SRS.schedule(state, 'rate') on wrong click, then setItem('qhse-srs-v1', ...)"
      pattern: "SRS\\.schedule\\(.*,\\s*'rate'"
    - from: "qhse-cesi/outils.html #panel-qcm IIFE"
      to: "localStorage qhse-prefs-v1"
      via: "merge-safe writePrefs({lastQcmTheme})"
      pattern: "lastQcmTheme"
---

<objective>
Ship the QCM révision rapide mode inside `#panel-qcm` of `outils.html`. This plan closes QUIZ-01 (themed/global QCM), QUIZ-02 (auto-reveal feedback with answer + explanation + source), QUIZ-03 (wrong-answer feeds SRS), and the write-half of SRS-03.

Purpose: Give the owner the daily-revision rhythm they asked for — one click reveals the truth, Suivant moves on, wrong answers re-enter the SM-2 queue exactly as a flashcard "raté" would. No new SM-2 math; the IIFE composes the existing frozen `window.SRS.schedule` and `window.BANK`.

Output: One commit modifying `outils.html` — (a) the `#panel-qcm` placeholder `<p>` is replaced with a real scaffold; (b) a new IIFE is appended after the Phase 3 Flashcards IIFE, following the same DCL-boot + double-load-guard + panel-scoped-listener discipline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-qcm-tests-blancs/04-CONTEXT.md
@.planning/phases/04-qcm-tests-blancs/04-UI-SPEC.md
@.planning/phases/04-qcm-tests-blancs/04-PATTERNS.md
@.planning/phases/04-qcm-tests-blancs/04-RESEARCH.md
@.planning/phases/04-qcm-tests-blancs/04-01-SUMMARY.md
@qhse-cesi/outils.html
@qhse-cesi/outils-data.js
@qhse-cesi/srs.js
</context>

<interfaces>
<!-- window.BANK QCM item schema (frozen Phase 2 contract — outils-data.js:187-208) -->
{
  id: string,                       // e.g. 'duerp-qcm-001'
  type: 'qcm',                      // filter on this
  theme: string,                    // 15-theme closed vocabulary (one of the option values in the theme picker)
  question: string,                 // raw stem text (Fraunces; assign via .textContent, NEVER .innerHTML)
  answer: string,                   // canonical answer prose (rendered in [data-qz-answer] post-reveal)
  choices: string[],                // 4 strings, order is FIXED (D-08: do NOT shuffle; preserves correct index)
  correct: number,                  // 0-based index into choices[]
  explanation: string,              // long-form explanation (rendered in [data-qz-explanation] post-reveal)
  source: {
    authority: string,              // e.g. 'INRS'
    ref: string,                    // e.g. 'Art. R4121-1' — wrap in <code> via createElement
    url: string,                    // 'http…' — wrap in <a target="_blank" rel="noopener noreferrer">
    verified: string                // ISO date, not displayed
  },
  difficulty: number
}

<!-- window.SRS API (frozen Phase 3 — srs.js) — Phase 4 calls only ONE function on the wrong-click write path -->
window.SRS.schedule(stateOrNull, grade, todayISO?) → newState   // grade ∈ {'rate','hard','bien','easy'}; Phase 4 always passes 'rate' from QCM
window.SRS.todayLocal()                                          → 'yyyy-mm-dd' local civil day (pure function — no store mutation)
window.SRS.GRADE                                                 → {'rate','hard','bien','easy'} constants (optional reference; the string 'rate' is fine)
window.SRS.DEFAULTS                                              → defaults object (not directly needed in QCM IIFE)
// Object.freeze(window.SRS) per P3 contract — do not assign to its keys.

<!-- localStorage keys (PERSIST-01 contract) -->
qhse-srs-v1   : {  [itemId]: { ease, interval, due, lapses, reps, introduced }  }   // object map; Phase 4 mutates ONE row per wrong QCM
qhse-prefs-v1 : { lastTheme, lastMode, newCardsPerDay, lastQcmTheme, lastTestTheme }  // P4 adds lastQcmTheme via merge-safe write; lastTestTheme added in Plan 04-03
qhse-scores-v1: array — NOT touched by this plan (Plan 04-03 owns it)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace #panel-qcm placeholder with the QCM scaffold DOM</name>
  <files>qhse-cesi/outils.html</files>
  <read_first>
    - qhse-cesi/outils.html (FULL — you MUST see lines 60-126 for the #panel-flashcards scaffold analog, lines 133-136 for the current #panel-qcm placeholder to replace, and the surrounding tabpanel `[hidden]` discipline)
    - .planning/phases/04-qcm-tests-blancs/04-UI-SPEC.md §Composition: #panel-qcm (top-to-bottom diagram), §Component Inventory (lines 525-540 QCM rows), §Copywriting Contract — QCM révision rapide (every visible French string)
    - .planning/phases/04-qcm-tests-blancs/04-PATTERNS.md §Surface 1 (scaffold pattern + 16-option theme list to copy verbatim) and §Pattern S7 (BANK item schema)
    - .planning/phases/04-qcm-tests-blancs/04-CONTEXT.md `<decisions>` — D-01 (auto-reveal), D-02 (Suivant button), and Claude's Discretion §"Module layout" (mount inside #panel-qcm, single IIFE per panel)
  </read_first>
  <action>
Edit `qhse-cesi/outils.html` lines 133-136 (the existing `#panel-qcm` placeholder `<p>`) to replace its inner content with the QCM scaffold. The `<div role="tabpanel" id="panel-qcm" aria-labelledby="tab-qcm" tabindex="0" hidden>` wrapper itself MUST NOT be touched — only its inner children. The `hidden` attribute on the tabpanel stays (ARIA tablist IIFE manages it).

**Children to add inside `#panel-qcm` (replace the placeholder `<p>`):**

1. `<h2 class="sr-only">QCM — révision rapide</h2>` — first child; screen-reader landmark.

2. Theme picker block — mirror the Flashcards `.fc-theme` structure from outils.html:81-101 but with `.qz-theme` class and new IDs:
   ```
   <nav class="qz-theme" aria-label="Choisir un thème">
     <label for="qz-qcm-theme-select">Thème</label>
     <select id="qz-qcm-theme-select" data-qz-qcm-theme>
       <option value="all">Tous les thèmes</option>
       <option value="duerp">DUERP</option>
       <option value="principes-generaux">Principes généraux de prévention</option>
       <option value="iso-9001">ISO 9001</option>
       <option value="iso-14001">ISO 14001</option>
       <option value="iso-45001">ISO 45001</option>
       <option value="tms">TMS</option>
       <option value="risque-routier">Risque routier</option>
       <option value="risque-chimique">Risque chimique</option>
       <option value="rps">RPS</option>
       <option value="espaces-confines">Espaces confinés</option>
       <option value="acronymes">Acronymes</option>
       <option value="metiers">Métiers</option>
       <option value="calendrier">Calendrier alternance</option>
       <option value="icpe-seveso">ICPE / Seveso</option>
       <option value="rncp">RNCP / Blocs de compétences</option>
     </select>
   </nav>
   ```
   Option values + order are verbatim from outils.html:84-100 (Flashcards picker) per PATTERNS §Surface 1. DO NOT alter the slugs.

3. Card scaffold — the focal element:
   ```
   <article class="qz-card" data-qz-card>
     <h3 id="qz-qcm-question" data-qz-question></h3>
     <div class="qz-choices" role="group" aria-labelledby="qz-qcm-question" data-qz-choices>
       <!-- 4 <button type="button" data-qz-choice data-qz-choice-idx="0..3"> nodes injected by IIFE -->
     </div>
     <div class="qz-reveal" data-qz-reveal hidden>
       <p class="qz-reveal__badge" data-qz-badge></p>
       <p data-qz-answer><span class="sr-only">Réponse correcte : </span><span data-qz-answer-text></span></p>
       <p data-qz-explanation><span class="sr-only">Explication : </span><span data-qz-explanation-text></span></p>
       <p data-qz-source><span class="sr-only">Source : </span></p>
     </div>
     <button type="button" data-qz-next hidden>Suivant<span data-qz-next-hint> Espace ou Entrée</span></button>
   </article>
   ```
   - `[data-qz-reveal]` uses `hidden` attribute (NEVER `display:none`) — screen readers skip pre-reveal (mirrors `[data-fc-verso]` chassis discipline at outils.html:108).
   - `[data-qz-next]` is also `hidden` pre-reveal; IIFE un-hides it on click and focuses it.
   - The 4 choice buttons are injected by the IIFE (not hard-coded in HTML) — they re-render on each new question.
   - The sr-only spans before answer/explanation/source carry the prefix copy from UI-SPEC for screen readers; the visible content goes into the second span.
   - The 2px accent underline on `[data-qz-question]` is not used in QCM mode (only in `.qz-correction__question` per UI-SPEC); do not add an `::after` pseudo here.

4. Comment marker after the scaffold (still inside `#panel-qcm`) for grep/maintenance:
   `<!-- Phase 4 QCM engine — IIFE appended after Flashcards IIFE; see outils.html#qcm-iife -->`

**Forbidden moves:**
- Modifying the `<div role="tabpanel" id="panel-qcm" …>` wrapper attributes.
- Touching `#panel-fiches`, `#panel-flashcards`, `#panel-tests`, the tablist (`role="tablist"`), or any other line outside the `#panel-qcm` block.
- Adding inline `style="…"` or inline `onclick="…"` / `onchange="…"`.
- Changing the existing 16 theme option values.
- Adding `class="placeholder"` anywhere (the placeholder is being deleted, not repurposed).
  </action>
  <verify>
    <automated>
      grep -nE "<h2 class=\"sr-only\">QCM — révision rapide</h2>" qhse-cesi/outils.html
      grep -nE "id=\"qz-qcm-theme-select\"" qhse-cesi/outils.html
      grep -nE "data-qz-card|data-qz-question|data-qz-choices|data-qz-reveal|data-qz-badge|data-qz-answer|data-qz-explanation|data-qz-source|data-qz-next" qhse-cesi/outils.html | head -20
      <!-- The 16 theme slugs are present in the new picker: -->
      grep -cE "value=\"(all|duerp|principes-generaux|iso-9001|iso-14001|iso-45001|tms|risque-routier|risque-chimique|rps|espaces-confines|acronymes|metiers|calendrier|icpe-seveso|rncp)\"" qhse-cesi/outils.html
      <!-- Expected count: ≥ 32 (16 for Flashcards + 16 for QCM; Plan 03 adds 16 more for Tests). -->
      <!-- The placeholder string for QCM is gone: -->
      grep -c "Ce mode arrive en Phase 4 — questions à choix multiples" qhse-cesi/outils.html
      <!-- Expected: 0 -->
      <!-- The other panels' placeholders are intact: -->
      grep -c "Ce mode arrive en Phase 4 — tests blancs chronométrés" qhse-cesi/outils.html
      <!-- Expected: 1 (will be removed in Plan 04-03) -->
      grep -c "Ce mode arrive en Phase 5" qhse-cesi/outils.html
      <!-- Expected: 1 (Phase 5 fiches placeholder intact) -->
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `#panel-qcm` no longer contains the string `Ce mode arrive en Phase 4 — questions à choix multiples` (placeholder removed).
    - Source: `outils.html` contains exactly one `<h2 class="sr-only">QCM — révision rapide</h2>`.
    - Source: `outils.html` contains the `<select id="qz-qcm-theme-select" data-qz-qcm-theme>` element with 16 `<option>` children whose values are: `all, duerp, principes-generaux, iso-9001, iso-14001, iso-45001, tms, risque-routier, risque-chimique, rps, espaces-confines, acronymes, metiers, calendrier, icpe-seveso, rncp` — in that order.
    - Source: `outils.html` contains the `<article class="qz-card" data-qz-card>` element with these direct/indirect descendants: `[data-qz-question]`, `[data-qz-choices]`, `[data-qz-reveal][hidden]`, `[data-qz-badge]`, `[data-qz-answer]`, `[data-qz-explanation]`, `[data-qz-source]`, `[data-qz-next][hidden]`.
    - Source: `#panel-fiches`, `#panel-flashcards`, `#panel-tests` are byte-identical to pre-edit (`git diff` confirms changes only inside the `#panel-qcm` `<div>` block).
    - Source: the `[hidden]` attribute is used on `[data-qz-reveal]` and `[data-qz-next]` (NOT `style="display:none"`).
    - Behavior: opening `outils.html` in a browser before Task 2 lands — the QCM tab renders the picker + empty card. No JS has wired it yet, so clicking does nothing; this is expected.
  </acceptance_criteria>
  <done>HTML scaffold lands; grep gates pass; `git diff` is bounded to the `#panel-qcm` block plus possibly a comment line above it.</done>
</task>

<task type="auto">
  <name>Task 2: Append the QCM engine IIFE (boot, queue, render, click-reveal, wrong-answer SRS write, advance)</name>
  <files>qhse-cesi/outils.html</files>
  <read_first>
    - qhse-cesi/outils.html (lines 206-795 — the entire Phase 3 Flashcards IIFE; you MUST copy its boot/guard/localStorage/event-wiring discipline verbatim where applicable)
    - .planning/phases/04-qcm-tests-blancs/04-PATTERNS.md §Surface 2 (every excerpt — IIFE shell, DCL boot, pre-flight BANK+SRS guard, readPrefs/writePrefs merge-safe, readStore/writeStore for qhse-srs-v1, Fisher-Yates note (NOT used here per D-08), source-line builder, panel-scoped listeners) and §Pattern S2, S3, S4, S5
    - .planning/phases/04-qcm-tests-blancs/04-UI-SPEC.md §Keyboard contract (#panel-qcm rows), §ARIA contract (#panel-qcm rules — role=group, aria-labelledby, aria-pressed semantics, aria-describedby on canonical correct choice), §Layout & Interaction Contracts §Card visual treatment (instant `[hidden]` toggle, no animations), §Choice button geometry (leading letter span structure)
    - .planning/phases/04-qcm-tests-blancs/04-CONTEXT.md `<decisions>` — D-01 (auto-reveal on click), D-02 (Suivant explicite + Space/Enter), D-03 (full 'raté' SM-2 on wrong), D-04 (1 écriture SRS par session de panel), and Claude's Discretion §Module layout + §Keyboard discipline
    - qhse-cesi/srs.js (the full file — read once to confirm the schedule signature and todayLocal() availability)
  </read_first>
  <action>
Append a new inline `<script>` IIFE to `outils.html` AFTER the closing `</script>` of the Phase 3 Flashcards IIFE (which ends near line 795 — locate via the `})()` IIFE closer of the existing block). Insert the new IIFE BEFORE the `</body>` tag. Use the same indentation and surrounding whitespace conventions as the Flashcards IIFE.

**IIFE shell + boot pattern (verbatim from Flashcards IIFE per PATTERNS §Surface 2, Pattern S1):**

- Banner comment: `/* ============ IIFE: QCM révision rapide — auto-reveal click → SRS wrong-feed → Suivant advance ============ */`
- `'use strict';` at IIFE top.
- Double-load guard: `if (window.__qzQcmBooted) return; window.__qzQcmBooted = true;` (unique guard name per IIFE — never reuse `__fcViewBooted`).
- `function boot() { ... }` body, dispatched via `if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', boot, { once: true }); else boot();` (Phase 3 hotfix `0553899` — inline scripts ignore `defer`, must wait for deferred outils-data.js + srs.js to evaluate).

**Inside `boot()`:**

1. **Resolve mount points**, then run the pre-flight guard (mirror outils.html:222-237):
   - `const panel = document.getElementById('panel-qcm');`
   - If `panel` is null, log `[QCM]` warn and return.
   - If `!window.BANK || !Array.isArray(window.BANK) || !window.SRS || typeof window.SRS.schedule !== 'function'`: append a `<p class="qz-error">` (chassis rule already scopes `#panel-qcm .qz-error`) inside `[data-qz-card]` with the copy `Impossible de charger la banque de questions. Recharge la page ou vérifie que outils-data.js est bien servi.`, log `console.error('[QCM] BANK or SRS missing — cannot boot view.', { BANK: !!window.BANK, SRS: !!window.SRS });`, return.

2. **Cache DOM handles** into a single `els` object: `themeSelect, card, question, choices, reveal, badge, answerEl, answerText, explanationEl, explanationText, sourceEl, nextBtn, nextHint`.

3. **Module-top constants and per-session state:**
   - `const DEFAULT_PREFS = { lastQcmTheme: 'all' };` (extended; merge-safe writer protects the Phase 3 sibling keys).
   - `const THEME_SLUGS` — same 16-slug array as Flashcards (copy from outils.html ~line 242).
   - `const state = { theme: 'all', pool: [], cursor: 0, currentItem: null, picked: null, isRevealed: false, srsWrittenThisSession: new Set() };` — the in-memory Set is the D-04 enforcement.

4. **localStorage helpers** (copy verbatim from outils.html:257-305 — `readPrefs`, `writePrefs`, `readStore`, `writeStore`), with log tags changed to `[QCM]`. `readPrefs` MUST type-check `parsed.lastQcmTheme` as string and fall back to `'all'` when absent — this is the merge-safety contract (Pattern S2). Mirror the Flashcards readPrefs which preserves `lastTheme`, `lastMode`, `newCardsPerDay`; the QCM `readPrefs` also returns those fields untouched so subsequent `writePrefs({lastQcmTheme})` merges cleanly. **Implementation note:** the simplest path is one shared `readPrefs/writePrefs` pattern reading the whole object, returning all keys; the QCM IIFE uses `prefs.lastQcmTheme` and writes via `writePrefs({ lastQcmTheme: nextTheme })`. Pattern S2 already proves the `Object.assign({}, existing, partial)` merge preserves Flashcards' keys.

5. **`buildPool(theme)`**: returns `window.BANK.filter(item => item.type === 'qcm' && (theme === 'all' || item.theme === theme))`. **DO NOT shuffle** (D-08 — option order stays valid; the natural pool order is fine for QCM révision rapide). Optionally shuffle the POOL order itself (questions, not choices) for daily variety — owner discretion, per RESEARCH it is acceptable; if shuffling, use Fisher-Yates from outils.html:344-351. Pick one approach and document it inline as a comment.

6. **`renderEmpty()`**: clears `[data-qz-card]` and renders `<p class="qz-no-pool">Aucun QCM pour ce thème. Choisis un autre thème.</p>` (UI-SPEC Copywriting). Focus stays on the theme select.

7. **`renderQuestion(item)`** — the canonical question render (will be shared with Plan 04-03 conceptually, but each IIFE owns its own copy per CONTEXT.md Claude's Discretion §Module layout). It:
   - Resets reveal state: `els.reveal.hidden = true; els.nextBtn.hidden = true; state.isRevealed = false; state.picked = null;`.
   - Assigns the stem: `els.question.textContent = item.question;` (NEVER `.innerHTML` — Pattern S3 / T-04-02-01 XSS gate).
   - Empties `els.choices` (`while (els.choices.firstChild) els.choices.removeChild(els.choices.firstChild);`).
   - Builds 4 `<button type="button" data-qz-choice data-qz-choice-idx="…">` children inside `els.choices`. Each button structure:
     - `<button type="button" data-qz-choice data-qz-choice-idx="0" aria-label="Choix A : <choice text>">`
     - Inside: `<span class="qz-choice__letter">A · </span>` then a text node `item.choices[0]`. Letters `A B C D` cycle.
     - Set `aria-label` attribute by concatenating `'Choix ' + letter + ' : ' + item.choices[idx]` (UI-SPEC Copywriting).
     - Default `data-qz-choice-state="idle"`.
   - Resets the choices container disabled state: `els.choices.removeAttribute('data-qz-disabled');`.
   - Clears the reveal panel contents (badge, answer, explanation, source) — see Source line builder below for the source clear.
   - Initial focus: focus the first choice button (`els.choices.firstElementChild.focus()`) only if focus is currently outside the choices area (use `panel.contains(document.activeElement)` test to avoid stealing focus from the select).

8. **`renderReveal(item, pickedIdx, isCorrect)`** — fires on click:
   - Updates each choice button's `data-qz-choice-state`:
     - The picked button → `"picked-correct"` (if `pickedIdx === item.correct`) OR `"picked-incorrect"`.
     - The canonical correct button (if the user picked wrong) → `"unpicked-correct"`.
     - The other two distractor buttons → `"unpicked-distractor"`.
   - Sets `aria-pressed="true"` on the picked button; `aria-pressed="false"` on the others.
   - On the picked button: set `aria-describedby="qz-qcm-reveal-id"` so SR users hear the badge after the choice text (assign `id="qz-qcm-reveal-id"` to `[data-qz-reveal]` if not already set; do this once at boot rather than per-render).
   - Disables further clicks: `els.choices.setAttribute('data-qz-disabled', 'true');` (the CSS rule from Plan 01 sets `pointer-events: none` on children).
   - Populates the badge: `els.badge.textContent = isCorrect ? '✓ Correct' : '✗ Incorrect'; els.badge.dataset.qzBadgeState = isCorrect ? 'correct' : 'incorrect';`.
   - Populates `els.answerText.textContent = item.answer;` and `els.explanationText.textContent = item.explanation;`.
   - Builds the source line via the verbatim PATTERNS Surface 2 / Pattern S3 builder (copy from outils.html:465-490): clear `els.sourceEl` non-sr-only children (loop while `els.sourceEl.childNodes.length > 1` and remove last), then append authority text, ` — `, `<code>` ref, ` `, `<a target="_blank" rel="noopener noreferrer">` URL. Always use `document.createElement` + `textContent`; never `innerHTML`.
   - Un-hides reveal + next: `els.reveal.hidden = false; els.nextBtn.hidden = false;`.
   - Moves focus to the Suivant button: `els.nextBtn.focus();`.

9. **`onChoiceClick(event)`** — event delegate on `els.choices`:
   - If `state.isRevealed` → return (re-click no-op).
   - Resolve the clicked button (`event.target.closest('[data-qz-choice]')`); if not a button, return.
   - `const pickedIdx = Number(button.dataset.qzChoiceIdx);`.
   - `const isCorrect = (pickedIdx === state.currentItem.correct);`.
   - `state.picked = pickedIdx; state.isRevealed = true;`.
   - Call `renderReveal(state.currentItem, pickedIdx, isCorrect);`.
   - **SRS WRITE (the only call site for `SRS.schedule` in this IIFE — D-03, D-04):**
     ```
     if (!isCorrect && !state.srsWrittenThisSession.has(state.currentItem.id)) {
       const today = window.SRS.todayLocal();
       const store = readStore();
       const existingRow = store[state.currentItem.id] || null;
       const newRow = window.SRS.schedule(existingRow, 'rate', today);
       store[state.currentItem.id] = newRow;
       writeStore(store);
       state.srsWrittenThisSession.add(state.currentItem.id);
     }
     ```
     **Comment this block** with `// D-03 + D-04: one 'rate' write per (panel-session, item); SM-2 math owned by window.SRS.schedule, never re-implemented.`

10. **`onNextClick()`** — advances:
    - `state.cursor = (state.cursor + 1) % state.pool.length;` (wrap to start so daily revision can loop indefinitely; documents in inline comment).
    - `state.currentItem = state.pool[state.cursor];`.
    - `renderQuestion(state.currentItem);`.
    - Focus moves to the first choice (handled inside `renderQuestion`).

11. **`onThemeChange()`** — fires on `<select>` change:
    - `state.theme = els.themeSelect.value;`.
    - `writePrefs({ lastQcmTheme: state.theme });` (merge-safe — preserves P3 keys; **the only writePrefs call in this IIFE**).
    - **Reset `state.srsWrittenThisSession` to a fresh `new Set()`** — switching theme starts a new sub-session (a generous interpretation of D-04 "session de panel"; document in inline comment). Alternative: keep the Set across theme switches within one panel mount — pick one and comment. Recommended: reset on theme change (less surprising for the owner).
    - Rebuild pool: `state.pool = buildPool(state.theme);`.
    - If `state.pool.length === 0`: call `renderEmpty()` and `return`.
    - Else: `state.cursor = 0; state.currentItem = state.pool[0]; renderQuestion(state.currentItem);` (re-mount card if it was replaced by `renderEmpty`; you may need to re-write the card scaffold inner HTML, OR keep the scaffold and only swap its inner via the render functions — pick the latter, it's simpler: stash a clone of the original card children at boot via `card.cloneNode(true)`, and on theme change restore them before `renderQuestion` runs).

12. **Event wiring (panel-scoped only — Pattern S4):**
    - `els.themeSelect.addEventListener('change', onThemeChange);`
    - `els.choices.addEventListener('click', onChoiceClick);` (event delegation — choices are re-created each render but the container is stable).
    - `els.nextBtn.addEventListener('click', onNextClick);`
    - **Keyboard:** Suivant button accepts native Enter/Space (browser default on `<button>`); no manual keydown handler needed on it. Choices accept native Enter/Space on focused button. **DO NOT** attach `document.addEventListener('keydown', ...)`. The `panel.addEventListener('keydown', e => { if (e.target.matches('select, input, textarea')) return; … })` pattern is NOT needed here because all interactive elements are native buttons and the select — arrow keys pass through to browser default (UI-SPEC line 468).

13. **Boot sequence at end of `boot()`:**
    - Read prefs: `const prefs = readPrefs(); state.theme = prefs.lastQcmTheme || 'all';`.
    - Restore picker: `els.themeSelect.value = state.theme;` (falls back to first option if value not found — defensive).
    - Build initial pool: `state.pool = buildPool(state.theme);`.
    - If empty → `renderEmpty()`; else → `state.cursor = 0; state.currentItem = state.pool[0]; renderQuestion(state.currentItem);`.

**Anti-patterns to refuse (from PATTERNS Surface 2/3 anti-pattern checklist):**
- `document.addEventListener('keydown', …)` anywhere.
- `el.innerHTML = …` on bank content (Pattern S3, T-04-02-01).
- Re-implementing SM-2 math (D-03 hard line — only `window.SRS.schedule` writes the row).
- `setItem('qhse-prefs-v1', JSON.stringify({lastQcmTheme}))` without merging (Pitfall 3; would clobber Phase 3 prefs).
- Writing to `qhse-srs-v1` on a CORRECT QCM click (D-V2-03 invariant + D-03 specifies wrong-only).
- Writing to `qhse-srs-v1` twice for the same item in one panel session (D-04 — `srsWrittenThisSession` Set).
- Calling `SRS.schedule` from any other place than `onChoiceClick` (group-(e) precursor — verify-quiz.cjs in Plan 04 asserts one call site).
- Adding `beforeunload` listener (out of scope; D-14 sibling principle).
- Auto-advance on click (D-02 forbids; Suivant is explicit).
- Shuffling `item.choices[]` (D-08; would invalidate `item.correct`).
  </action>
  <verify>
    <automated>
      <!-- 1. IIFE boots — guard name unique: -->
      grep -c "window.__qzQcmBooted" qhse-cesi/outils.html
      <!-- Expected: 2 (guard check + assignment) -->

      <!-- 2. DCL pattern present: -->
      grep -nE "document\\.readyState === 'loading'" qhse-cesi/outils.html | wc -l
      <!-- Expected: ≥ 2 (Flashcards IIFE + QCM IIFE) -->

      <!-- 3. Single SRS.schedule call site in QCM IIFE — must equal 1 inside the QCM block: -->
      grep -nE "SRS\\.schedule\\(" qhse-cesi/outils.html
      <!-- Across the file expect: 1 in Flashcards IIFE + 1 in QCM IIFE = 2; Tests blancs IIFE (Plan 03) MUST add ZERO. -->

      <!-- 4. SRS write is guarded on wrong-answer + Set membership: -->
      grep -nE "srsWrittenThisSession" qhse-cesi/outils.html
      <!-- Expected: ≥ 3 (declaration + has-check + add) -->

      <!-- 5. The strict 'rate' grade literal is used (never 'hard'/'bien'/'easy' for wrong QCM): -->
      grep -nE "SRS\\.schedule\\([^,]+,\\s*'rate'" qhse-cesi/outils.html
      <!-- Expected: ≥ 1 inside the QCM IIFE -->

      <!-- 6. Merge-safe writePrefs: lastQcmTheme appears, Phase 3 keys are preserved by reading existing object first. -->
      grep -nE "lastQcmTheme" qhse-cesi/outils.html
      grep -nE "Object\\.assign\\(\\{\\}, existing, partial\\)" qhse-cesi/outils.html
      <!-- Both expected ≥ 1 -->

      <!-- 7. No document-level keydown listeners: -->
      grep -c "document\\.addEventListener.\\?'keydown'" qhse-cesi/outils.html
      <!-- Expected: 0 -->

      <!-- 8. No innerHTML assignments on bank content: -->
      grep -nE "\\.innerHTML\\s*=" qhse-cesi/outils.html
      <!-- Expected: empty or only literal-string innerHTML if any (none in Phase 3 — must stay 0 for bank text). -->

      <!-- 9. Source line uses createElement/textContent for code+a wrappers: -->
      grep -nE "createElement\\('code'\\)|createElement\\('a'\\)" qhse-cesi/outils.html
      <!-- Expected: ≥ 2 (Flashcards source builder + QCM source builder) -->

      <!-- 10. Browser smoke (manual until Plan 04 ships verify-quiz.cjs):
            - Open https://mes-apps-claude.vercel.app/qhse-cesi/outils.html after deploy
            - Click QCM tab → see picker + first question + 4 choices
            - Click a wrong choice → see ✗ Incorrect badge + canonical answer + explanation + source
            - DevTools → Application → Local Storage → confirm qhse-srs-v1 has a new entry with interval=1, lapses≥1
            - Click Suivant → next QCM appears
            - Click the SAME wrong card again (after wrapping) → confirm qhse-srs-v1 row unchanged (Set guard)
            - Switch theme → confirm picker writes lastQcmTheme into qhse-prefs-v1 AND that lastTheme/lastMode/newCardsPerDay are still present
      -->
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `outils.html` contains exactly one `window.__qzQcmBooted` declaration AND one assignment (the guard pattern).
    - Source: `outils.html` contains exactly one new IIFE banner: `/* ============ IIFE: QCM révision rapide — auto-reveal click → SRS wrong-feed → Suivant advance ============ */` (or near-verbatim).
    - Source: across the entire file, `SRS.schedule(` appears exactly 2 times (Flashcards IIFE + QCM IIFE); the QCM IIFE call passes the string literal `'rate'` as the grade argument.
    - Source: the QCM IIFE's SRS-write block contains both `state.srsWrittenThisSession.has(` and `state.srsWrittenThisSession.add(` (D-04 enforcement).
    - Source: `outils.html` contains `lastQcmTheme` references AND `Object.assign({}, existing, partial)` (Pattern S2 merge-safe writer).
    - Source: zero `document.addEventListener('keydown'` occurrences anywhere in the file (Pattern S4).
    - Source: zero `.innerHTML =` assignments anywhere; the QCM source line builder uses `createElement('code')` and `createElement('a')` (Pattern S3 / T-04-02-01).
    - Behavior: open `outils.html` in browser, click QCM tab, click any choice — within 100ms the reveal panel un-hides (`[hidden]` attribute removed), the badge displays `✓ Correct` or `✗ Incorrect`, the explanation + source render, and `[data-qz-next]` un-hides and receives focus.
    - Behavior: clicking a wrong choice writes one new key to `localStorage.qhse-srs-v1` whose value has `interval === 1`, `lapses >= 1`, `due === todayLocal() + 1 day` (verifiable via `JSON.parse(localStorage.getItem('qhse-srs-v1'))[itemId]` in DevTools).
    - Behavior: clicking a correct choice does NOT add a new key to `qhse-srs-v1` (no SRS write on correct, per D-03).
    - Behavior: clicking Suivant advances to the next pool item; pressing Space or Enter on the focused Suivant button has identical effect (native button keyboard semantics).
    - Behavior: changing the theme picker (a) updates `qhse-prefs-v1.lastQcmTheme` to the new slug, (b) preserves `qhse-prefs-v1.lastTheme` / `lastMode` / `newCardsPerDay` from Phase 3 (manually verifiable in DevTools: read prefs before and after the change).
    - Behavior: reloading the page restores the previously-selected QCM theme (`prefs.lastQcmTheme` re-applied to the picker on boot).
    - Behavior: choosing a theme whose QCM pool is empty (none currently in the bank with 0 items — all 15 themes have ≥1 QCM, so this is mostly defensive) shows the `Aucun QCM pour ce thème.` message; no crash.
    - Behavior: opening the page with `localStorage.removeItem('qhse-prefs-v1')` first — IIFE boots with `lastQcmTheme` defaulting to `'all'`, no error.
    - Behavior: opening the page with `delete window.BANK` injected before DCL — IIFE renders the `Impossible de charger la banque de questions.` inline message, no white-screen crash.
    - Behavior: Phase 3 Flashcards tab still renders identically — switching to Flashcards, picking a theme, grading a card — all works. The Phase 3 `qhse-srs-v1` data written by Flashcards remains compatible (same schema).
    - Behavior: opening the Tests tab still shows the Phase 4 placeholder (`Ce mode arrive en Phase 4 — tests blancs chronométrés`) — that surface is owned by Plan 04-03.
  </acceptance_criteria>
  <done>QCM engine boots and renders; all grep gates pass; manual browser smoke confirms auto-reveal + SRS wrong-write + Suivant advance + theme persistence + Phase 3 regression-free. Commit message: `🚀 Phase 4 Plan 02 — outils.html: QCM révision rapide engine (QUIZ-01/02/03 + SRS-03 write-half)`. Push to main; verify Vercel deploys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outils-data.js (BANK content) → outils.html DOM | Bank prose flows into innerText surfaces; XSS risk if any path uses innerHTML |
| user click → localStorage qhse-srs-v1 | Wrong-QCM clicks mutate persistent SRS state; risk = double-penalty, missed write, or write of wrong schema |
| user → localStorage qhse-prefs-v1 | Theme change writes prefs; risk = clobbering Phase 3 keys via non-merge write |
| browser tab background → setInterval (none in this plan) | N/A — QCM mode has no timer; Tests plan owns that boundary |
| Phase 3 Flashcards state → Phase 4 QCM state | Shared window.SRS + qhse-srs-v1 store; risk = double-write conflict or schema drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | renderReveal source line (bank `source.url`, `source.ref`, `item.explanation`) | mitigate | XSS-safe build per Pattern S3: `textContent` + `createElement('code')` + `createElement('a')` with `rel="noopener noreferrer"`. No `innerHTML` on bank fields. Verified by grep gate. |
| T-04-02-02 | Tampering | qhse-srs-v1 schema (Phase 3 contract) | mitigate | Phase 4 NEVER mutates the row shape; only `window.SRS.schedule()` returns a valid row; we write the returned object whole. `introduced` field preserved automatically (srs.js carries it through). |
| T-04-02-03 | Tampering | qhse-prefs-v1 (Phase 3 keys) | mitigate | Pattern S2 merge-safe `writePrefs(partial)` (Object.assign over existing parsed JSON). Verified by grep gate + Plan 04-04 verify-quiz.cjs group (d). |
| T-04-02-04 | Repudiation | wrong-QCM double-penalty | mitigate | D-04 in-memory `Set<itemId>` blocks repeated SRS writes for the same item within a single panel session. Reset on theme change or reload. |
| T-04-02-05 | Information disclosure | localStorage keys | accept | Single-user app; localStorage is owner-only on owner's devices. No PII beyond grading patterns. |
| T-04-02-06 | Denial of service | qhse-srs-v1 quota exhaustion | accept | At 92 QCM + 134 flashcards = ~226 possible keys × ~80 bytes ≈ 18KB. localStorage cap is 5–10MB. Quota exhaustion implausible. |
| T-04-02-07 | Denial of service | corrupt localStorage JSON | mitigate | All readers wrap `JSON.parse` in try/catch (Pattern S5), return safe defaults (`{}` for srs store, `{}` for prefs). IIFE still boots; user re-grades cards. |
| T-04-02-08 | Elevation of privilege | bank tampering via DevTools (BANK reassign) | accept | Owner-only single-user; if owner edits their own BANK in DevTools they implicitly own the consequences. window.BANK is Object.frozen by Phase 2 — DevTools edits would silently fail in strict mode and noisily fail otherwise. |
| T-04-02-09 | Tampering | localStorage theme injection (e.g., setting lastQcmTheme to an unknown slug) | mitigate | `readPrefs` type-checks and falls back to `'all'`; `buildPool('garbage-slug')` returns empty array → `renderEmpty()` shows the no-pool message. Graceful degradation, no crash. |
| T-04-02-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | This plan installs zero packages. Pure inline JS edit. |
</threat_model>

<verification>
- The Vercel deploy succeeds within ~60s; visit `https://mes-apps-claude.vercel.app/qhse-cesi/outils.html` and confirm console is clear (no `[QCM]` errors).
- Phase 3 verify-srs.cjs gate STILL passes (run `node .planning/phases/03-flashcards-srs/verify-srs.cjs` — exit 0 required; Phase 4 does not regress Phase 3).
- Manual eyeball gate (the owner is the human-verify checkpoint): one full QCM cycle (pick wrong → see incorrect badge + reveal → Suivant → next question), then DevTools confirms `qhse-srs-v1[lastWrongItemId].lapses >= 1` and `qhse-prefs-v1.lastQcmTheme === '<picked-slug>'` while `qhse-prefs-v1.lastTheme === <Phase 3 value>` (untouched).
</verification>

<success_criteria>
- QUIZ-01 closed: themed/global QCM render, owner-verifiable.
- QUIZ-02 closed: auto-reveal feedback with answer + explanation + source on every click.
- QUIZ-03 closed: wrong-answer SRS write produces a row equivalent to `window.SRS.schedule(state, 'rate')`.
- SRS-03 write-half closed: Phase 3 supplied the schema; Phase 4 supplies the wrong-QCM write path.
- Phase 3 Flashcards and Hub reading content show zero visual or functional regression.
- One atomic commit; one push to main.
</success_criteria>

<output>
Create `.planning/phases/04-qcm-tests-blancs/04-02-SUMMARY.md` after completion. Record: IIFE lines added, single `SRS.schedule` call-site line number, the new `lastQcmTheme` key write line, DOM scaffold replacement bounds, smoke-test result, commit SHA, deploy URL, regression check on Phase 3 (Flashcards still working).
</output>
