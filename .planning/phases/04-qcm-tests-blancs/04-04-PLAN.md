---
phase: 04-qcm-tests-blancs
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
autonomous: true
requirements: []
requirements_addressed: []
# Verification-only plan: no new requirement closes here. This plan ships the executable
# contract that ASSERTS the requirements already closed by Plans 02 (QUIZ-01/02/03 + SRS-03
# write-half) and 03 (TEST-01/02/03) are correctly implemented. Mirrors the Plan 04-01
# `requirements: []` convention (foundation plans + verification gates do not close requirements).
must_haves:
  truths:
    - "Running `node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs` after Plans 02 + 03 ship exits 0 with all 6 named PASS assertion groups (a)..(f) printed"
    - "Group (a) asserts the QCM wrong-answer write produces a state equivalent to window.SRS.schedule(state, 'rate') against synthetic fixtures — codifies QUIZ-03 / SRS-03 / D-03"
    - "Group (b) asserts test composition: BANK.filter(type==='qcm') yields a 20-item shape-valid queue (choices.length===4, correct ∈ 0..3) — codifies TEST-01 / D-05 / D-08"
    - "Group (c) asserts qhse-scores-v1 round-trip + cap 50 FIFO: insert 51 items, oldest dropped, newest first — codifies TEST-03 / D-10 / D-11 / D-12"
    - "Group (d) asserts qhse-prefs-v1 merge-safety: Phase 3 keys (lastTheme, lastMode, newCardsPerDay) AND Plan 02's lastQcmTheme survive a Plan 03-style lastTestTheme write — codifies PERSIST-01"
    - "Group (e) asserts the D-V2-03 hard invariant: a simulated Tests blancs session (in-memory snapshot before + after) does NOT mutate qhse-srs-v1 — codifies the cross-phase boundary"
    - "Group (f) asserts schema compatibility with Phase 3 verify-srs.cjs: a row written by SRS.schedule still contains every field the P3 schema gate expects (ease, interval, due, lapses, reps, introduced)"
    - "Running `node .planning/phases/03-flashcards-srs/verify-srs.cjs` still exits 0 after this plan ships (zero regression — verify-quiz.cjs is a sibling, not a replacement)"
    - "The gate uses Node built-ins only (require, assert, path); zero npm dependencies, no test runner"
    - "The gate loads qhse-cesi/srs.js under Node via a `global.window = {}` shim, mirroring verify-srs.cjs:23-39 exactly"
  artifacts:
    - path: ".planning/phases/04-qcm-tests-blancs/verify-quiz.cjs"
      provides: "Plain-Node verification gate for Phase 4; exit 0 = all PASS, exit 1 = any FAIL; named PASS labels matching CONTEXT.md group wording"
      contains: "#!/usr/bin/env node-style header comment, 'use strict', require('path'), require('assert'), global.window = {}, require('../../../qhse-cesi/srs.js'), pass()/fail()/check() helpers identical to verify-srs.cjs:46-64, six `check(...)` blocks (one per assertion group), final allPassed ? exit 0 : exit 1"
  key_links:
    - from: ".planning/phases/04-qcm-tests-blancs/verify-quiz.cjs"
      to: "qhse-cesi/srs.js"
      via: "require(path.resolve(__dirname, '../../../qhse-cesi/srs.js')) with global.window shim — identical traversal depth to verify-srs.cjs:23"
      pattern: "require.*qhse-cesi.srs\\.js"
    - from: ".planning/phases/04-qcm-tests-blancs/verify-quiz.cjs"
      to: "ROADMAP.md § Phase 4 success criteria"
      via: "Each check() label encodes the SC/REQ link (e.g. 'SC2/QUIZ-03 ...', 'SC4/TEST-03 ...', 'SC4/D-V2-03 ...')"
      pattern: "SC[0-9]+/(QUIZ|TEST|PERSIST|D-V2)"
    - from: ".planning/phases/04-qcm-tests-blancs/verify-quiz.cjs"
      to: "CONTEXT.md lines 60-66 (six assertion groups locked verbatim)"
      via: "Each group's PASS label contains the verbatim phrasing from the CONTEXT.md decision text"
      pattern: "(wrong-answer SRS write|test composition|FIFO cap|merge-safety|does NOT mutate|schema compatibility)"
---

<objective>
Ship the `verify-quiz.cjs` Phase 4 verification gate as a plain Node script. This plan does NOT close a new requirement — it codifies the executable contract for the requirements already closed by Plans 02 (QUIZ-01..03 + SRS-03 write-half) and 03 (TEST-01..03). The gate's 6 assertion groups (a)..(f) are locked verbatim in `04-CONTEXT.md` lines 60-66.

Purpose: Convert the four ROADMAP § Phase 4 success criteria + the D-V2-03 hard invariant (Tests blancs path NEVER mutates `qhse-srs-v1`) into 6 named PASS assertions that run in < 1 second with zero npm dependencies. This is the Phase 4 sibling of `.planning/phases/03-flashcards-srs/verify-srs.cjs` — same shape, same exit-code contract, same `global.window` shim trick, different assertion content. Plans 02 and 03 cross-reference this gate 14 times under the letter notation "group (a)..(f)"; this plan delivers the file those plans point to.

Output: ONE new file `.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs` (no other files touched). ONE atomic git commit. ZERO modifications to `qhse-cesi/`, `outils.html`, `chassis.css`, `srs.js`, `outils-data.js`, or `verify-srs.cjs` (the Phase 3 gate stays exactly where it is and continues to exit 0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-qcm-tests-blancs/04-CONTEXT.md
@.planning/phases/04-qcm-tests-blancs/04-RESEARCH.md
@.planning/phases/04-qcm-tests-blancs/04-PATTERNS.md
@.planning/phases/04-qcm-tests-blancs/04-UI-SPEC.md
@.planning/phases/04-qcm-tests-blancs/04-01-PLAN.md
@.planning/phases/04-qcm-tests-blancs/04-02-PLAN.md
@.planning/phases/04-qcm-tests-blancs/04-03-PLAN.md
@.planning/phases/03-flashcards-srs/verify-srs.cjs
@qhse-cesi/srs.js
@qhse-cesi/outils-data.js
</context>

<interfaces>
<!-- Verification gate exit-code contract (mirrors verify-srs.cjs and verify-bank.cjs) -->
process.exit(0)  → ALL 6 assertion groups PASS
process.exit(1)  → ANY assertion FAIL (load error, group (a)..(f) failure, or schema regression)

<!-- Group label → letter mapping (Plans 02 + 03 use letter notation; CONTEXT.md uses numeric).
     The PASS labels in verify-quiz.cjs MUST use BOTH notations so cross-plan references resolve unambiguously. -->
(a) = group 1  : QCM wrong-answer write ≡ SRS.schedule(state, 'rate')                                  → QUIZ-03 / SRS-03 / D-03
(b) = group 2  : Test composition — filter by theme + shuffle yields 20 items when pool ≥ 20            → TEST-01 / D-05 / D-08
(c) = group 3  : qhse-scores-v1 round-trip + FIFO cap 50 (insert 51, oldest dropped)                    → TEST-03 / D-10 / D-11 / D-12
(d) = group 4  : qhse-prefs-v1 merge-safety (P3 keys + Plan 02 lastQcmTheme preserved after Plan 03 write) → PERSIST-01
(e) = group 5  : Tests blancs path does NOT mutate qhse-srs-v1 (D-V2-03 invariant)                      → SRS-03 negative half / D-V2-03
(f) = group 6  : Schema compatibility with Phase 3 — SRS.schedule row contains every P3-asserted field  → PERSIST-01 cross-phase no-regression

<!-- Helper function shapes (verbatim from verify-srs.cjs:46-64) -->
function pass(label)                  → console.log('PASS [' + label + ']')
function fail(label, reason)          → console.error('FAIL [' + label + '] ' + reason); allPassed = false
function check(label, fn)             → try { fn(); pass(label); } catch (e) { fail(label, e.message); }

<!-- Bootstrap path (relative-traversal arithmetic) -->
__dirname = .../mes-apps-claude/.planning/phases/04-qcm-tests-blancs/
'../../../qhse-cesi/srs.js'    → ../ (phases/) → ../ (.planning/) → ../ (repo root) → qhse-cesi/srs.js   ✓
'../../../qhse-cesi/outils-data.js' → same arithmetic                                                   ✓
// Identical depth to verify-srs.cjs:23 — proven traversal.

<!-- Allowed Node built-ins (ZERO external deps) -->
require('path')      → path.resolve, path.join — for the srs.js bootstrap path
require('assert')    → assert.strictEqual, assert.deepStrictEqual, assert.ok, assert.notStrictEqual
// NO: chai, jest, mocha, vitest, ava, tap, supertest — none. Pure Node.

<!-- window.SRS API surface available after the shim load (frozen Phase 3) -->
window.SRS.schedule(stateOrNull, grade, todayISO)  → newState        // Used in groups (a) and (f)
window.SRS.todayLocal()                            → 'yyyy-mm-dd'    // Pure helper; safe to call from gate
window.SRS.addDays(iso, n)                         → iso + n days    // Available; not directly needed
// Note: BANK is OPTIONAL for group (b). The recommended path (read CONTEXT.md group 2 wording):
//   "Test composition: filter by theme then shuffle yields 20 items when pool ≥ 20"
// can be asserted EITHER against real BANK (loading outils-data.js via same shim) OR against
// synthetic fixtures (an array of 25 hand-crafted `{ id, type:'qcm', theme, question, choices:[4], correct:0..3 }` items).
// The executor's call: pick whichever is simpler. Group (b) asserts a SHAPE property, not bank content.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold verify-quiz.cjs — header, bootstrap shim, helpers, group skeletons, final exit</name>
  <files>.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs</files>
  <read_first>
    - .planning/phases/03-flashcards-srs/verify-srs.cjs (FULL — lines 1-277; you MUST mirror the file's high-level shape: header comment block lines 1-13, 'use strict' line 14, requires lines 16-17, shim block lines 19-41, helper block lines 43-64, group blocks separated by `// ===` rules, final exit block lines 266-276; the only differences in Plan 04-04 are: (1) the require path arithmetic is identical depth so no change needed; (2) the assertion content is groups (a)..(f) per CONTEXT.md not P3's (a)..(e); (3) the final ALL PASS message says "Phase 4 QCM + Tests blancs gates")
    - .planning/phases/04-qcm-tests-blancs/04-CONTEXT.md (lines 60-66 — the 6 assertion groups verbatim; copy the phrasing into the PASS labels so the executor produces output a human can audit against the locked CONTEXT.md text)
    - .planning/phases/04-qcm-tests-blancs/04-RESEARCH.md §"verify-quiz.cjs Pattern (Mirrors verify-srs.cjs)" (the template at lines 804-904 — adapt verbatim; the executor may copy that template into the file and modify the bootstrap path / labels)
    - qhse-cesi/srs.js (lines 85-185 — confirm the SRS.schedule(state, grade, today) signature, the GRADE enum, the row shape { ease, interval, due, lapses, reps, introduced }; the gate's group (a) and (f) call exactly this signature)
    - .planning/phases/04-qcm-tests-blancs/04-02-PLAN.md (the "group (e)" reference at line 327 — confirms this Plan 04-04 owns the file that Plan 02 names; and the SRS.schedule call-site count contract grep gates use across Plans 02 + 03)
    - .planning/phases/04-qcm-tests-blancs/04-03-PLAN.md (the "Plan 04-04 group (e)" reference at lines 416, 510, 584, 607 — confirms Plan 03's IIFE deliberately does NOT call SRS.schedule and that Plan 04-04 codifies the snapshot-equality assertion)
  </read_first>
  <action>
Create the new file `.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs`. The file MUST NOT exist yet; this Task creates it from scratch. The structure below mirrors `verify-srs.cjs:1-65` exactly — same line ordering, same comment shape, same `'use strict'` discipline.

**File location confirmation:** the path is `.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs` (under `.planning/`, NOT under `qhse-cesi/`). Resolution per RESEARCH §"Recommended Project Structure" and §"Open Question 1" (recommended: mirror P3 placement). CONTEXT.md "Claude's Discretion" line that mentions `qhse-cesi/verify-quiz.cjs` is superseded by the RESEARCH-recommended location — the gate is a planning/verification artifact, not a runtime asset shipped with the app. Plans 02 + 03 reference this exact path in their Wave-4 cross-references.

**File content layout (top to bottom — write all of it in this Task; Tasks 2–6 will FILL the six `check()` blocks in turn):**

1. **Banner comment block** (lines 1-13 equivalent in verify-srs.cjs):
   - `/* verify-quiz.cjs`
   - ` * Verification gate for Phase 4 QCM + Tests blancs (qhse-cesi/outils.html IIFEs).`
   - ` *`
   - ` * USAGE:`
   - ` *   node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs`
   - ` *`
   - ` * Exit 0 = all 6 assertion groups PASS. Exit 1 = any assertion FAIL.`
   - ` *`
   - ` * Mirrors .planning/phases/03-flashcards-srs/verify-srs.cjs scaffold (lines 1-65, 269-276).`
   - ` * Sibling gate, NOT a replacement — verify-srs.cjs still asserts the Phase 3 SM-2 contract.`
   - ` * Node built-ins only: assert, path. No npm deps, no test runner.`
   - ` */`

2. `'use strict';` directive.

3. **Requires** (two lines, identical to verify-srs.cjs:16-17):
   - `const path   = require('path');`
   - `const assert = require('assert');`

4. **Bootstrap shim block** (mirror verify-srs.cjs:19-41 verbatim — only the comment slug changes):
   - Comment: `// Bootstrap: load srs.js under Node via global.window shim.` `// Mirrors verify-srs.cjs:19-41.`
   - `const srsPath = path.resolve(__dirname, '../../../qhse-cesi/srs.js');`
   - `global.window = {};`
   - `try { require(srsPath); } catch (e) { console.error('FAIL: could not load srs.js from', srsPath); console.error(e.message); process.exit(1); }`
   - `const SRS = global.window.SRS;`
   - `if (!SRS || typeof SRS.schedule !== 'function') { console.error('FAIL: window.SRS not exported …', srsPath); process.exit(1); }`
   - `console.log('srs.js loaded OK — window.SRS keys:', Object.keys(SRS).join(', '));`

5. **Helpers + `allPassed` flag** (verbatim from verify-srs.cjs:43-64):
   - `let allPassed = true;`
   - `function pass(label) { console.log('PASS [' + label + ']'); }`
   - `function fail(label, reason) { console.error('FAIL [' + label + '] ' + reason); allPassed = false; }`
   - `function check(label, fn) { try { fn(); pass(label); } catch (e) { fail(label, e.message); } }`

6. **Header banner for the gate run** (mirror verify-srs.cjs:66):
   - `console.log('\n=== Phase 4 QCM + Tests blancs verification gate — 6 assertion groups (a)..(f) ===\n');`

7. **Six STUB `check()` blocks** — Tasks 2–6 fill these in; Task 1 lays empty bodies so the file is well-formed and can be smoke-run early:
   - `console.log('\n-- (a) group 1: QCM wrong-answer SRS write equivalence (QUIZ-03 / SRS-03 / D-03) --');`
     `check('SC2/QUIZ-03 group (a) — TBD in Task 2', () => { throw new Error('not yet implemented in Task 1; Task 2 fills'); });`
   - Repeat for (b)..(f), each with its own console.log banner and a single `check('TBD', () => { throw … })`. Each `throw new Error('...; Task <N> fills')` references the Task number that owns it (b=3, c=4, d=5, e=6, f=7 OR a smaller task count if you choose to merge — see Tasks 3–6 below).

8. **Final exit block** (mirror verify-srs.cjs:266-276; only the success-message slug changes):
   - `console.log('\n' + '='.repeat(70));`
   - `if (allPassed) { console.log('Phase 4 verification gate: ALL 6 groups PASS — verify-quiz.cjs verified for SC1/SC2/SC3/SC4 + D-V2-03'); process.exit(0); } else { console.error('Phase 4 verification gate: ONE OR MORE ASSERTIONS FAILED — see FAIL lines above'); process.exit(1); }`

**Discipline:**
- Use `var` or `const`/`let` consistently with verify-srs.cjs (verify-srs.cjs uses `const`/`let` — match that).
- No ESM syntax (`import`/`export`); the file extension `.cjs` is intentional and Node MUST treat it as CommonJS.
- The shebang `#!/usr/bin/env node` is OPTIONAL — verify-srs.cjs does NOT use one; this gate does NOT need one either (we invoke via `node <path>`).
- No console.log statements outside of pass()/fail()/section banners.
- The 6 stub `check()` calls MUST throw — running `node verify-quiz.cjs` at end of Task 1 SHOULD print 6 FAIL lines and exit 1. That's the expected state until Tasks 2-6 land.

**Forbidden moves in Task 1:**
- Touching any file other than the new `.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs`.
- Adding a `package.json`, `node_modules`, or any npm dependency.
- Adding a TypeScript variant, a `.mjs` variant, or a test-runner wrapper.
- Removing or modifying `verify-srs.cjs` (the Phase 3 sibling stays).
- Changing the Plan 02 or Plan 03 PLAN.md cross-references (those plans already point here).
- Wrapping the file in an IIFE — `verify-srs.cjs` runs at top level; mirror that.
  </action>
  <verify>
    <automated>
      <!-- File exists at exact path: -->
      ls -la .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- File loads + executes (will exit 1 with 6 FAILs — that's expected after Task 1; Tasks 2-6 will replace stubs): -->
      node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs ; echo "exit=$?"
      <!-- Expected: exit=1, srs.js loaded OK message printed, 6 FAIL lines printed (one per stub group). -->

      <!-- Structural greps: -->
      grep -nE "^'use strict';" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 1 -->
      grep -nE "require\\('path'\\)|require\\('assert'\\)" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 2 -->
      grep -nE "global\\.window = \\{\\};" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 1 -->
      grep -nE "require\\(srsPath\\)" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 1 (inside try/catch) -->
      grep -cE "function (pass|fail|check)\\(" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 3 -->
      grep -cE "^check\\(" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 6 (one stub per group; Tasks 2-6 keep the count at 6, just swap the bodies) -->
      grep -nE "process\\.exit\\(0\\)|process\\.exit\\(1\\)" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: ≥ 3 (load-fail exit, shim-fail exit, final allPassed branch) -->

      <!-- Phase 3 gate untouched and still passes (zero regression): -->
      node .planning/phases/03-flashcards-srs/verify-srs.cjs ; echo "p3 exit=$?"
      <!-- Expected: p3 exit=0 -->

      <!-- Confirm no other files were modified — `git status` shows ONLY the new file: -->
      git status --short
      <!-- Expected: a single line: `?? .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs` (untracked new file) -->
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs` exists and is a well-formed JS file (no syntax error — `node -c` would pass; running it loads srs.js and prints the keys).
    - Source: the file's first non-comment line is `'use strict';`.
    - Source: the file contains exactly one `global.window = {};` line and exactly one `const srsPath = path.resolve(__dirname, '../../../qhse-cesi/srs.js');` line.
    - Source: the file defines exactly three helper functions: `pass`, `fail`, `check` — signatures byte-equal to verify-srs.cjs:48-64.
    - Source: the file contains exactly 6 top-level `check(...)` invocations (group (a) through group (f) stubs; Tasks 2-6 replace the bodies, not the count).
    - Source: the file contains the success-banner string `Phase 4 verification gate: ALL 6 groups PASS` and the failure-banner string `Phase 4 verification gate: ONE OR MORE ASSERTIONS FAILED`.
    - Source: the file contains the loading-banner string `srs.js loaded OK — window.SRS keys:` (verbatim from the verify-srs.cjs:41 message).
    - Behavior: `node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs` exits 1 with 6 FAIL lines (one per stub group), one ALL FAIL final message — proving the file is wired correctly even before Tasks 2-6 fill the assertions.
    - Behavior: `node .planning/phases/03-flashcards-srs/verify-srs.cjs` STILL exits 0 (zero P3 regression — verify-quiz.cjs is a sibling, not a replacement).
    - Repo state: `git status` reports exactly one new untracked file (the new verify-quiz.cjs); zero modifications to any other file under `qhse-cesi/`, `.planning/`, or root.
  </acceptance_criteria>
  <done>The scaffold lands; running the gate fails predictably with 6 stub-FAILs; P3 gate still exits 0; no other file touched. Ready for Tasks 2-6 to replace the six stub bodies with real assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement group (a) — QCM wrong-answer write equivalence (QUIZ-03 / SRS-03 / D-03)</name>
  <files>.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs</files>
  <read_first>
    - .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs (the file Task 1 created; you are REPLACING the body of the first `check()` block while keeping the surrounding scaffold byte-identical)
    - .planning/phases/04-qcm-tests-blancs/04-CONTEXT.md (line 61 — group 1 phrasing: "QCM wrong-answer write produces a state equivalent to window.SRS.schedule(state, 'rate') against synthetic fixtures")
    - .planning/phases/04-qcm-tests-blancs/04-02-PLAN.md (Task 2 §9 — the canonical QCM IIFE wrong-click write path: `const newRow = window.SRS.schedule(existingRow, 'rate', today); store[item.id] = newRow; writeStore(store);`)
    - .planning/phases/04-qcm-tests-blancs/04-RESEARCH.md §"verify-quiz.cjs Pattern" lines 828-836 (the suggested group (a) assertion structure — copy and adapt)
    - qhse-cesi/srs.js lines 117-185 (the schedule() function — confirm the 'rate' grade behavior: lapses += 1, ease += -0.20 (floor 1.3), interval reset to 1, reps unchanged, due = today + 1, introduced field stable per E8)
    - .planning/phases/03-flashcards-srs/verify-srs.cjs lines 74-90 (P3 E1 + E2 cases — same SRS.schedule signature; the group (a) assertion is structurally similar but framed as "the QCM IIFE's write IS the same as calling SRS.schedule, no re-implementation drift")
  </read_first>
  <action>
Edit `.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs`. Locate the group (a) stub `check()` block (the first one, right under the `-- (a) group 1` banner). REPLACE its body with the real assertion. Leave every other line of the file byte-identical to Task 1's scaffold.

**The assertion to write** (mirrors RESEARCH lines 828-836 with two strengthening additions):

Replace the stub with:
```
check('SC2/QUIZ-03 group (a) — wrong-answer SRS write ≡ SRS.schedule(state, \'rate\')', () => {
  const today = '2026-05-25';

  // Synthetic fixture: a card that has been "bien"-graded once (matches the
  // QCM IIFE's typical pre-state — the owner is on a card that already exists
  // in qhse-srs-v1 from prior Flashcards work).
  const prior = SRS.schedule(null, 'bien', '2026-05-24');
  assert.strictEqual(prior.reps, 1, 'fixture invariant — prior.reps should be 1');
  assert.strictEqual(prior.lapses, 0, 'fixture invariant — prior.lapses should be 0');

  // The QCM IIFE write path (Plan 02 Task 2 §9): it calls SRS.schedule(existing, 'rate', today)
  // and writes the returned row back to qhse-srs-v1[item.id]. The assertion: that returned row
  // is structurally identical to what a flashcards-mode raté would write — i.e. the SRS is
  // mode-agnostic, the wrong-QCM feed is identical to the flashcard-raté feed.
  const fromQcmWrongClick    = SRS.schedule(prior, 'rate', today);
  const fromFlashcardsRaté   = SRS.schedule(prior, 'rate', today);
  assert.deepStrictEqual(fromQcmWrongClick, fromFlashcardsRaté,
    'QCM wrong-click write must produce byte-equal row to a flashcards raté grade');

  // Strengthening — assert the explicit SM-2 "raté" semantics (D-03):
  assert.strictEqual(fromQcmWrongClick.interval, 1,            'interval expected 1 (raté reset)');
  assert.strictEqual(fromQcmWrongClick.lapses,   1,            'lapses expected 1 (incremented from 0)');
  assert.strictEqual(fromQcmWrongClick.reps,     prior.reps,   'reps must NOT change on raté (Anki model)');
  assert.ok(fromQcmWrongClick.ease < prior.ease,               'ease must decrease on raté');
  assert.ok(fromQcmWrongClick.ease >= 1.3,                     'ease must respect 1.3 floor');
  assert.strictEqual(fromQcmWrongClick.due, '2026-05-26',      'due expected today + 1');
  assert.strictEqual(fromQcmWrongClick.introduced, prior.introduced,
    'introduced field must remain stable across grades');

  // Negative: a CORRECT QCM click does NOT call SRS.schedule (D-03 — only wrong feeds SRS).
  // We can't grep into outils.html from here, but we can prove the contract by checking that
  // calling SRS.schedule with a NON-'rate' grade would produce a different row — which it does.
  const correctPath = SRS.schedule(prior, 'bien', today);
  assert.notDeepStrictEqual(fromQcmWrongClick, correctPath,
    'wrong-click row must differ from a bien-grade row (proves the rate path is the only one writing on incorrect)');
});
```

Keep the surrounding `console.log('\n-- (a) group 1: ...')` banner exactly as Task 1 wrote it. Do NOT remove the `\n` in the banner, do NOT touch the other 5 stub `check()` blocks (Tasks 3-6 own those).

**Anti-patterns to refuse:**
- Calling any function other than `SRS.schedule` from group (a) (e.g. `SRS.filterDue`, `SRS.addDays` — those belong to verify-srs.cjs; the Phase 4 gate is scoped to the wrong-QCM contract).
- Hard-coding the expected `ease` value (e.g. `assert.strictEqual(row.ease, 2.30)`) — this duplicates verify-srs.cjs E2 (already proven) and would FAIL silently if Phase 3 ever tunes the rate delta. The group (a) test asserts STRUCTURAL equivalence (raté semantics) not exact numeric values that P3 already owns.
- Loading `outils-data.js` to pick a real BANK QCM — group (a) uses synthetic fixtures per CONTEXT.md line 61 ("against synthetic fixtures"). No need for outils-data.js in this group.
- Re-implementing SM-2 math inside the test (e.g. `assert.strictEqual(row.interval, manuallyComputed)`). The point of group (a) is to assert the IIFE composes `SRS.schedule` — not to re-derive SM-2.
  </action>
  <verify>
    <automated>
      <!-- Group (a) PASS label is present and matches CONTEXT.md phrasing: -->
      grep -nE "SC2/QUIZ-03 group \\(a\\) — wrong-answer SRS write" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 1 -->

      <!-- The stub error string is GONE for group (a): -->
      grep -cE "Task 2 fills|TBD in Task 2" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 0 -->

      <!-- The remaining 5 stub markers are STILL present (Tasks 3-6 own them): -->
      grep -cE "Task [3-7] fills|TBD in Task [3-7]" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 5 — one stub per remaining group (b..f) -->

      <!-- The gate now exits 1 with FIVE FAIL lines (not six — group (a) now passes): -->
      node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs ; echo "exit=$?"
      <!-- Expected: exit=1 (still failing on the remaining stubs), one new `PASS [SC2/QUIZ-03 group (a) ...]` line, 5 remaining FAIL lines. -->

      <!-- Sanity: the deepStrictEqual / notDeepStrictEqual lines are present: -->
      grep -nE "assert\\.deepStrictEqual\\(fromQcmWrongClick" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      grep -nE "assert\\.notDeepStrictEqual\\(fromQcmWrongClick" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Both expected ≥ 1 -->
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: the group (a) `check()` body contains `SRS.schedule(prior, 'rate', today)` exactly once, and `SRS.schedule(prior, 'bien', today)` exactly once (the negative comparison).
    - Source: group (a) asserts both `deepStrictEqual` (mode-agnostic equivalence) AND `notDeepStrictEqual` (rate vs bien differ).
    - Source: group (a) asserts `lapses === 1`, `reps === prior.reps`, `interval === 1`, `ease < prior.ease`, `ease >= 1.3`, `due === '2026-05-26'`, `introduced === prior.introduced`.
    - Behavior: `node verify-quiz.cjs` produces a `PASS [SC2/QUIZ-03 group (a) — wrong-answer SRS write ≡ SRS.schedule(state, 'rate')]` line.
    - Behavior: exit code is still 1 (groups b..f still stubbed).
    - Behavior: `node .planning/phases/03-flashcards-srs/verify-srs.cjs` STILL exits 0.
  </acceptance_criteria>
  <done>Group (a) PASS line prints; gate still fails on b..f stubs; no other file touched; P3 gate exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Implement group (b) — Test composition: 20-item shape-valid queue (TEST-01 / D-05 / D-08)</name>
  <files>.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs</files>
  <read_first>
    - .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs (current state — Task 2 just landed group (a); you are now replacing the group (b) stub)
    - .planning/phases/04-qcm-tests-blancs/04-CONTEXT.md (line 62 — group 2 phrasing: "Test composition: filter by theme then shuffle yields 20 items when pool ≥ 20")
    - .planning/phases/04-qcm-tests-blancs/04-03-PLAN.md (Task 2 §6 — `buildQueue(theme)` filters BANK by `type === 'qcm'`, shuffles via Fisher-Yates, slices first 20; choices order NOT shuffled per D-08)
    - .planning/phases/04-qcm-tests-blancs/04-RESEARCH.md §"verify-quiz.cjs Pattern" lines 838-852 (the suggested group (b) assertion structure — adapt; the gate may load outils-data.js for real BANK OR use synthetic fixtures)
    - qhse-cesi/outils-data.js (skim the schema near line 187-225 — the QCM item shape: `{ id, type:'qcm', theme, question, choices: string[4], correct: 0..3, answer, explanation, source, difficulty }`)
  </read_first>
  <action>
Edit `.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs`. Locate the group (b) stub `check()` block. REPLACE its body with the real assertion.

**Recommended approach: load real BANK via the same shim.** This proves the actual P2 bank has ≥ 20 QCM items spanning multiple themes AND every QCM has a valid shape. Add the BANK load to the existing shim block at the TOP of the file (right after `require(srsPath)`):

```javascript
// Add to the existing bootstrap block, after the srs.js require:
const bankPath = path.resolve(__dirname, '../../../qhse-cesi/outils-data.js');
try {
  require(bankPath);
} catch (e) {
  console.error('FAIL: could not load outils-data.js from', bankPath);
  console.error(e.message);
  process.exit(1);
}
const BANK = global.window.BANK;
if (!Array.isArray(BANK) || BANK.length === 0) {
  console.error('FAIL: window.BANK not exported (or empty) after loading', bankPath);
  process.exit(1);
}
console.log('outils-data.js loaded OK — BANK.length =', BANK.length);
```

Place this snippet IMMEDIATELY after the existing `console.log('srs.js loaded OK ...')` line (around line 41 of the Task 1 scaffold). This is the only structural edit Task 3 makes outside the group (b) `check()` block — it's necessary because group (b) needs real BANK data.

**Then REPLACE the group (b) stub `check()` body with:**

```javascript
check('SC3/TEST-01 group (b) — test composition: 20-item shape-valid queue from QCM pool', () => {
  // Filter the QCM pool — same expression Plan 03 IIFE uses.
  const allQcm = BANK.filter(i => i.type === 'qcm');
  assert.ok(allQcm.length >= 20,
    'QCM pool size (' + allQcm.length + ') must be >= 20 for TEST-01 — pool exhausted is a content-bank regression');

  // Simulate the Plan 03 buildQueue('all') path: shuffle then slice(0, 20).
  // Shuffle is non-deterministic; assert STRUCTURAL invariants only.
  function fisherYates(arr) {
    const a = arr.slice();
    for (let i = a.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      const tmp = a[i]; a[i] = a[j]; a[j] = tmp;
    }
    return a;
  }
  const queue = fisherYates(allQcm).slice(0, 20);
  assert.strictEqual(queue.length, 20, 'queue length expected exactly 20 (D-05)');

  // Every item in the queue must be a valid QCM:
  queue.forEach((item, idx) => {
    assert.strictEqual(item.type, 'qcm',
      'queue[' + idx + '] type expected "qcm" got ' + item.type);
    assert.ok(typeof item.id === 'string' && item.id.length > 0,
      'queue[' + idx + '] missing id');
    assert.ok(typeof item.question === 'string' && item.question.length > 0,
      'queue[' + idx + '] missing question stem');
    assert.ok(Array.isArray(item.choices) && item.choices.length === 4,
      'queue[' + idx + '] choices must be length-4 array (got ' +
      (Array.isArray(item.choices) ? item.choices.length : typeof item.choices) + ')');
    assert.ok(typeof item.correct === 'number' && item.correct >= 0 && item.correct < 4,
      'queue[' + idx + '] correct must be integer in [0,3] (got ' + item.correct + ')');
    // D-08 secondary invariant — the correct index points into a non-empty choice string:
    assert.ok(typeof item.choices[item.correct] === 'string' && item.choices[item.correct].length > 0,
      'queue[' + idx + '] correct index ' + item.correct + ' points to empty/missing choice');
  });

  // Theme-filtered composition: pick one theme that we know has ≥ 20 QCMs.
  // RESEARCH note: 'duerp' is the largest single theme; the bank has 92 QCMs across 15 themes,
  // so SOME single theme has ≥ 20. Find one programmatically rather than hard-coding.
  const themeCounts = {};
  allQcm.forEach(i => { themeCounts[i.theme] = (themeCounts[i.theme] || 0) + 1; });
  const themesWith20Plus = Object.keys(themeCounts).filter(t => themeCounts[t] >= 20);
  // If NO single theme has ≥ 20, the test still passes the 'all' composition above; just skip
  // the per-theme assertion. Document the soft branch.
  if (themesWith20Plus.length > 0) {
    const theme = themesWith20Plus[0];
    const themedPool = allQcm.filter(i => i.theme === theme);
    const themedQueue = fisherYates(themedPool).slice(0, 20);
    assert.strictEqual(themedQueue.length, 20,
      'themed queue (' + theme + ') length expected 20');
    themedQueue.forEach((item, idx) => {
      assert.strictEqual(item.theme, theme,
        'themed queue[' + idx + '] theme expected ' + theme + ' got ' + item.theme);
    });
  }
});
```

**Anti-patterns to refuse:**
- Hard-coding a specific theme name (e.g. `'duerp'`) in the per-theme branch — the bank may evolve. Compute themes-with-≥20 from BANK contents.
- Calling the gate's `fisherYates` shuffle and asserting determinism (e.g. that two shuffles produce the same output). Shuffle is non-deterministic; assert STRUCTURAL invariants only.
- Asserting `Object.isFrozen(BANK)` — that's a Phase 2 invariant (the gate `verify-bank.cjs` already owns it). Phase 4 group (b) is about composition, not bank immutability.
- Asserting `item.choices` order specifically (e.g. `assert.strictEqual(item.choices[0], 'expected string')`) — D-08 says choices order is preserved as-shipped, but the gate doesn't pin down what that order IS for any specific item.
  </action>
  <verify>
    <automated>
      <!-- Group (b) PASS label present: -->
      grep -nE "SC3/TEST-01 group \\(b\\) — test composition" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 1 -->

      <!-- The bootstrap now loads outils-data.js too: -->
      grep -nE "require\\(bankPath\\)|global\\.window\\.BANK" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: ≥ 2 (require + BANK extraction) -->
      grep -nE "outils-data\\.js loaded OK" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 1 -->

      <!-- Group (b) uses fisherYates inline (not Math.random in sort comparator): -->
      grep -nE "function fisherYates|fisherYates\\(allQcm" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: ≥ 2 -->
      grep -nE "Math\\.random\\(\\) - 0\\.5" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 0 (biased shuffle banned — RESEARCH §"Anti-Patterns") -->

      <!-- The gate now exits 1 with FOUR FAIL lines (groups c-f still stubbed); (a) + (b) PASS: -->
      node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs ; echo "exit=$?"
      <!-- Expected: exit=1, 4 FAILs, 2 PASS lines, BANK.length log printed near the top -->
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: the bootstrap block now contains an `outils-data.js` require AND a `BANK` extraction guard (mirrors the srs.js shim shape).
    - Source: group (b) body uses real `BANK.filter(i => i.type === 'qcm')` (not synthetic fixtures), then asserts `length >= 20`, `length === 20` post-slice, and per-item shape (`type, id, question, choices, correct`).
    - Source: group (b) computes themes-with-≥20 from `BANK` itself; no hard-coded theme slug.
    - Behavior: `node verify-quiz.cjs` prints `outils-data.js loaded OK — BANK.length = <N>` (where N is the current bank size, currently 226 per STATE.md but the gate must not pin the count).
    - Behavior: `node verify-quiz.cjs` prints `PASS [SC2/QUIZ-03 group (a) ...]` AND `PASS [SC3/TEST-01 group (b) ...]`; exit code still 1 (c-f stubbed).
    - Behavior: P3 gate still exits 0.
  </acceptance_criteria>
  <done>Group (b) PASS line prints; bootstrap now loads BANK in addition to SRS; gate still fails on c-f stubs; no other file touched.</done>
</task>

<task type="auto">
  <name>Task 4: Implement groups (c) + (d) — qhse-scores-v1 FIFO cap (TEST-03) + qhse-prefs-v1 merge-safety (PERSIST-01)</name>
  <files>.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs</files>
  <read_first>
    - .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs (current state — Tasks 2 + 3 landed groups (a) + (b))
    - .planning/phases/04-qcm-tests-blancs/04-CONTEXT.md (lines 63-64 — group 3 + group 4 phrasing verbatim)
    - .planning/phases/04-qcm-tests-blancs/04-03-PLAN.md (Task 2 §4 — `appendScore(row)` does `unshift + slice(0, 50)`; Task 2 §18 — `onTestThemeChange` does merge-safe `writePrefs({lastTestTheme})`)
    - .planning/phases/04-qcm-tests-blancs/04-02-PLAN.md (Task 2 §11 — `onThemeChange` writes `lastQcmTheme` via merge-safe `writePrefs`; group (d) must preserve THIS key in addition to P3 keys)
    - .planning/phases/04-qcm-tests-blancs/04-RESEARCH.md §"verify-quiz.cjs Pattern" lines 854-881 (suggested group (c) + (d) assertion structures — adapt)
    - .planning/phases/03-flashcards-srs/verify-srs.cjs lines 152-160 (P3 prefs merge test — group (d) is the Phase 4 superset, asserting lastTheme + lastMode + newCardsPerDay + lastQcmTheme + lastTestTheme all coexist)
  </read_first>
  <action>
Edit `.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs`. Replace TWO stubs in this Task: group (c) and group (d). Both are pure in-memory data assertions — no BANK or SRS calls needed.

**Group (c) body replacement:**

```javascript
check('SC4/TEST-03 group (c) — qhse-scores-v1 FIFO cap at 50 (D-10/D-11/D-12)', () => {
  // Simulate Plan 03 §appendScore: unshift + slice(0, 50).
  function appendScore(scoresArr, row) {
    const next = scoresArr.slice();   // pure — never mutate the input
    next.unshift(row);
    return next.slice(0, 50);
  }

  let scores = [];

  // Insert 51 rows in order (id 0 .. id 50), each with a valid D-10 schema:
  for (let i = 0; i <= 50; i++) {
    scores = appendScore(scores, {
      id:      'test-' + i,
      dateISO: '2026-05-25',
      theme:   (i % 2 === 0) ? 'all' : 'duerp',
      score:   i % 21,
      total:   20
    });
  }

  // FIFO invariants:
  assert.strictEqual(scores.length, 50,
    'after 51 inserts, cap must enforce length 50 (got ' + scores.length + ')');
  assert.strictEqual(scores[0].id, 'test-50',
    'newest insert (id 50) must be at index 0 (D-12: most-recent-first)');
  assert.strictEqual(scores[49].id, 'test-1',
    'oldest surviving insert must be id 1 (id 0 dropped under FIFO)');

  // Negative: id 0 (the very first insert) must be GONE:
  assert.ok(scores.every(r => r.id !== 'test-0'),
    'id 0 must have been dropped — FIFO violated');

  // Schema round-trip: JSON-serialize the whole array and parse back; must be byte-equal:
  const serialized = JSON.stringify(scores);
  const reparsed   = JSON.parse(serialized);
  assert.deepStrictEqual(reparsed, scores,
    'qhse-scores-v1 must round-trip through JSON without drift');

  // Every row has the locked D-10 schema (no stray keys):
  scores.forEach((r, idx) => {
    assert.deepStrictEqual(
      Object.keys(r).sort(),
      ['dateISO', 'id', 'score', 'theme', 'total'],
      'row ' + idx + ' has unexpected keys: ' + Object.keys(r).join(','));
    assert.ok(typeof r.id === 'string',      'row ' + idx + ' id not string');
    assert.ok(typeof r.dateISO === 'string', 'row ' + idx + ' dateISO not string');
    assert.ok(typeof r.theme === 'string',   'row ' + idx + ' theme not string');
    assert.ok(typeof r.score === 'number',   'row ' + idx + ' score not number');
    assert.ok(typeof r.total === 'number',   'row ' + idx + ' total not number');
  });
});
```

**Group (d) body replacement:**

```javascript
check('SC4/PERSIST-01 group (d) — qhse-prefs-v1 merge-safety preserves P3 + Plan 02 keys', () => {
  // Simulate the merge-safe writePrefs(partial) pattern (Pattern S2; verbatim from
  // outils.html:276-285 P3 helper; Plan 02 + Plan 03 IIFEs both adopt this exact shape).
  function writePrefs(existing, partial) {
    return Object.assign({}, existing, partial);
  }

  // Scenario 1: Phase 3 had written the three P3 keys; Plan 02 then writes lastQcmTheme.
  const afterP3      = { lastTheme: 'duerp', lastMode: 'flashcards', newCardsPerDay: 7 };
  const afterPlan02  = writePrefs(afterP3, { lastQcmTheme: 'iso-9001' });
  assert.strictEqual(afterPlan02.lastTheme,       'duerp',       'P3 lastTheme must survive Plan 02 write');
  assert.strictEqual(afterPlan02.lastMode,        'flashcards',  'P3 lastMode must survive Plan 02 write');
  assert.strictEqual(afterPlan02.newCardsPerDay,  7,             'P3 newCardsPerDay must survive Plan 02 write');
  assert.strictEqual(afterPlan02.lastQcmTheme,    'iso-9001',    'Plan 02 lastQcmTheme must be set');

  // Scenario 2: Plan 03 (this is the Plan-04-04-asserted contract) writes lastTestTheme;
  // ALL prior keys must survive.
  const afterPlan03  = writePrefs(afterPlan02, { lastTestTheme: 'tms' });
  assert.strictEqual(afterPlan03.lastTheme,       'duerp',       'P3 lastTheme must survive Plan 03 write');
  assert.strictEqual(afterPlan03.lastMode,        'flashcards',  'P3 lastMode must survive Plan 03 write');
  assert.strictEqual(afterPlan03.newCardsPerDay,  7,             'P3 newCardsPerDay must survive Plan 03 write');
  assert.strictEqual(afterPlan03.lastQcmTheme,    'iso-9001',    'Plan 02 lastQcmTheme must survive Plan 03 write');
  assert.strictEqual(afterPlan03.lastTestTheme,   'tms',         'Plan 03 lastTestTheme must be set');

  // Scenario 3: Plan 02 + Plan 03 updates RACE — both overwrite their own key in different orders.
  // The merge-safe writer must converge to the same end state regardless of order.
  const orderA = writePrefs(writePrefs(afterP3, { lastQcmTheme: 'X' }), { lastTestTheme: 'Y' });
  const orderB = writePrefs(writePrefs(afterP3, { lastTestTheme: 'Y' }), { lastQcmTheme: 'X' });
  assert.deepStrictEqual(orderA, orderB,
    'merge-safe write must be commutative across independent keys (Plan 02 + Plan 03 cannot conflict)');

  // Scenario 4: An UNKNOWN future key (e.g. 'lastFicheTheme' from Phase 5) must survive merging too.
  const withFuture = writePrefs(afterPlan03, { lastFicheTheme: 'rps' });
  assert.strictEqual(withFuture.lastFicheTheme, 'rps');
  assert.strictEqual(withFuture.lastTheme,       'duerp',  'unknown key write must not clobber known keys');
  assert.strictEqual(withFuture.lastQcmTheme,    'iso-9001');
  assert.strictEqual(withFuture.lastTestTheme,   'tms');
});
```

**Anti-patterns to refuse:**
- Touching `localStorage` directly inside the gate (`localStorage` does not exist in Node; the gate asserts the PURE pattern via plain object Object.assign).
- Hard-coding `Object.keys(row).length === 5` instead of `deepStrictEqual` against the explicit key list (length-only checks don't catch a typo like `dataISO` vs `dateISO`).
- Skipping the "future key" assertion in group (d) (Scenario 4) — it's the early-warning gate for Phase 5 prefs collisions.
  </action>
  <verify>
    <automated>
      grep -nE "SC4/TEST-03 group \\(c\\)" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      grep -nE "SC4/PERSIST-01 group \\(d\\)" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Both expected: 1 -->

      <!-- Two more stubs gone (c + d); two remain (e + f): -->
      grep -cE "Task [3-7] fills|TBD in Task [3-7]" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 2 -->

      <!-- Group (c) uses unshift + slice(0,50): -->
      grep -nE "\\.unshift\\(" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      grep -nE "\\.slice\\(0,\\s*50\\)" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Both expected: ≥ 1 -->

      <!-- Group (d) uses Object.assign merge: -->
      grep -nE "Object\\.assign\\(\\{\\}, existing, partial\\)" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: ≥ 1 -->

      <!-- Gate now exits 1 with TWO FAIL lines (e + f still stubbed); a-d PASS: -->
      node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs ; echo "exit=$?"
      <!-- Expected: exit=1, 2 FAILs, 4 PASS lines -->
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: group (c) body inserts 51 synthetic rows via `unshift + slice(0, 50)` and asserts: length 50, newest at index 0, id 0 dropped, JSON round-trip equality, and per-row D-10 schema (`Object.keys(r).sort()` equals `['dateISO', 'id', 'score', 'theme', 'total']`).
    - Source: group (d) asserts 4 scenarios: P3→Plan02→Plan03 chain, P3→Plan02→Plan03 reverse-order commutativity, and unknown-future-key survival.
    - Behavior: `node verify-quiz.cjs` now prints PASS for groups (a), (b), (c), (d); FAIL for (e), (f); exit 1.
    - Behavior: P3 gate still exits 0.
  </acceptance_criteria>
  <done>Groups (c) + (d) PASS lines print; gate fails only on (e) + (f); no other file touched.</done>
</task>

<task type="auto">
  <name>Task 5: Implement group (e) — D-V2-03 invariant: Tests blancs path does NOT mutate qhse-srs-v1</name>
  <files>.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs</files>
  <read_first>
    - .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs (current state — groups a-d done; replacing group (e) stub)
    - .planning/phases/04-qcm-tests-blancs/04-CONTEXT.md (line 65 — group 5 phrasing verbatim: "Tests blancs path does NOT mutate qhse-srs-v1 (D-V2-03 invariant) — assertion against an in-memory store snapshot")
    - .planning/phases/04-qcm-tests-blancs/04-03-PLAN.md (the entire file — confirm the Tests IIFE NEVER calls window.SRS.schedule; the grep gates at lines 533-538 and the acceptance criteria at lines 591-592 enforce this in source. Group (e) is the RUNTIME complement: even if a future refactor introduces a call, the in-memory snapshot proves no mutation occurs)
    - .planning/phases/04-qcm-tests-blancs/04-RESEARCH.md §"verify-quiz.cjs Pattern" lines 884-892 (suggested group (e) structure — adapt; the key idea is snapshot a synthetic qhse-srs-v1 BEFORE simulating a Tests session, then snapshot AFTER, assert byte-equality)
    - .planning/phases/04-qcm-tests-blancs/04-RESEARCH.md §"Pitfall 5: Tests Blancs Silently Touches qhse-srs-v1 via Shared Helper" (the exact failure mode group (e) catches)
  </read_first>
  <action>
Edit `.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs`. Replace the group (e) stub `check()` body with the D-V2-03 in-memory snapshot equality assertion.

**The assertion:** simulate a complete Tests blancs session entirely in-memory (no DOM, no localStorage — pure data) and prove that the simulation does NOT call SRS.schedule. We snapshot a synthetic `qhse-srs-v1` store, run a function that mirrors what the Tests IIFE does (filter pool, build queue, record picks, compute score, append to scores-v1), and assert the SRS store snapshot is byte-equal before and after.

**Group (e) body replacement:**

```javascript
check('SC4/D-V2-03 group (e) — Tests blancs path does NOT mutate qhse-srs-v1 (hard invariant)', () => {
  // Step 1: synthesize a qhse-srs-v1 store with multiple realistic rows.
  // Each row produced by the legitimate Phase 3 entry point (SRS.schedule), so the
  // synthetic store is schema-valid.
  const srsStore = {
    'duerp-flashcard-001':       SRS.schedule(null, 'bien', '2026-05-24'),
    'iso-9001-flashcard-002':    SRS.schedule(null, 'rate', '2026-05-23'),
    'tms-flashcard-003':         SRS.schedule(SRS.schedule(null, 'bien', '2026-05-22'), 'bien', '2026-05-23'),
    'rps-flashcard-004':         SRS.schedule(null, 'facile', '2026-05-25')
  };

  // Step 2: capture a deep snapshot of the store BEFORE the simulated Tests session.
  const snapshotBefore = JSON.stringify(srsStore);

  // Step 3: simulate what the Plan 03 Tests IIFE does end-to-end. The KEY invariant:
  // NONE of the simulation steps may call SRS.schedule against srsStore. Per Plan 03 the
  // IIFE may call SRS.todayLocal() (pure helper, no store mutation) — we simulate that too.
  const today = (typeof SRS.todayLocal === 'function') ? SRS.todayLocal() : '2026-05-26';
  assert.ok(/^\d{4}-\d{2}-\d{2}$/.test(today),
    'todayLocal() must return yyyy-mm-dd string (defensive — group (e) depends on this contract)');

  // Build a 20-item queue using the same BANK + Fisher-Yates path as Plan 03 §6:
  const pool = BANK.filter(i => i.type === 'qcm');
  assert.ok(pool.length >= 20, 'BANK must have >= 20 QCMs for the simulation');
  const queue = pool.slice(0, 20);   // skip shuffle — the snapshot doesn't depend on order

  // Simulate picks: deliberately WRONG on every question, to maximise the temptation
  // (in a buggy implementation) of writing to qhse-srs-v1.
  const picks = queue.map(item => {
    // Pick any choice that is NOT the correct one (mod 4 to find a wrong index).
    return (item.correct + 1) % 4;
  });

  // Simulate the score computation (mirrors Plan 03 §14):
  let correctCount = 0;
  for (let i = 0; i < queue.length; i++) {
    if (picks[i] === queue[i].correct) correctCount++;
  }
  assert.strictEqual(correctCount, 0,
    'simulation picked wrong on every question — score should be 0 (sanity check)');

  // Simulate appendScore (mirrors Plan 03 §4):
  const scoreRow = {
    id:      'test-' + Date.now(),
    dateISO: today,
    theme:   'all',
    score:   correctCount,
    total:   20
  };
  // (we do NOT actually need to append to anything — group (c) already covered appendScore)

  // Step 4: capture a deep snapshot of the store AFTER the simulated session.
  const snapshotAfter = JSON.stringify(srsStore);

  // Step 5: THE assertion — byte-equal snapshots prove zero mutation.
  assert.strictEqual(snapshotAfter, snapshotBefore,
    'D-V2-03 violated: qhse-srs-v1 was mutated by the Tests blancs simulation. ' +
    'Before: ' + snapshotBefore + '. After: ' + snapshotAfter);

  // Step 6: defence-in-depth — assert that the 4 row IDs are still present and intact.
  ['duerp-flashcard-001', 'iso-9001-flashcard-002', 'tms-flashcard-003', 'rps-flashcard-004']
    .forEach(id => {
      assert.ok(srsStore[id], 'D-V2-03: row ' + id + ' missing after simulated test (deletion is also forbidden)');
      assert.ok(typeof srsStore[id].ease === 'number',     'row ' + id + ' ease field mutated');
      assert.ok(typeof srsStore[id].interval === 'number', 'row ' + id + ' interval field mutated');
    });
});
```

**Critical correctness note for the executor:** group (e) does NOT actually run the IIFE code from `outils.html` — that file is browser-JS that doesn't load under Node. Group (e) instead **proves the CONTRACT** by:
1. Calling all the functions the Tests IIFE is ALLOWED to call (`SRS.todayLocal`, `BANK.filter`) and confirming they don't mutate srsStore.
2. Deliberately NOT calling `SRS.schedule` against srsStore (the forbidden operation).
3. Snapshotting before/after and asserting equality.

This is a contract-level assertion: if a future Tests IIFE refactor introduces an SRS.schedule call, the source-level grep gate in Plan 03 catches it (lines 533-538). Group (e) is the SECOND line of defence — even if someone bypasses the grep gate, the runtime snapshot would prove the mutation occurred.

**Anti-patterns to refuse:**
- Calling `SRS.schedule` against `srsStore` anywhere in group (e) — that would itself violate the invariant being asserted.
- Loading `outils.html` and trying to execute it under Node (it's browser-only).
- Asserting `Object.isFrozen(srsStore)` — the store is a plain object, not frozen; the invariant is about the WRITERS, not the store's immutability.
- Snapshotting after each picked-question and asserting incremental equality (overkill — one before/after pair suffices).
- Replacing the deterministic snapshot string comparison with a `deepStrictEqual` against the original object — `JSON.stringify` comparison catches reference vs value subtleties more cleanly here.
  </action>
  <verify>
    <automated>
      grep -nE "SC4/D-V2-03 group \\(e\\)" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 1 -->

      <!-- The snapshot-equality assertion is present and uses JSON.stringify on the same object: -->
      grep -nE "snapshotBefore = JSON\\.stringify\\(srsStore\\)" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      grep -nE "snapshotAfter\\s*=\\s*JSON\\.stringify\\(srsStore\\)" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      grep -nE "assert\\.strictEqual\\(snapshotAfter, snapshotBefore" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- All expected: ≥ 1 -->

      <!-- Group (e) does NOT call SRS.schedule against srsStore (only the synthetic-fixture builds at top of group): -->
      <!-- Count SRS.schedule occurrences in the file. Allowed sites: -->
      <!--   - group (a): 4 calls (prior, fromQcmWrongClick, fromFlashcardsRaté, correctPath) -->
      <!--   - group (e): 4 calls in the fixture-build at top (duerp/iso/tms double/rps) -->
      <!--   - group (f) — Task 6 — TBD -->
      grep -cE "SRS\\.schedule\\(" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- After Task 5 this is ≥ 8 (4 in (a), 4-5 in (e)) — exact count depends on the tms double-call which is SRS.schedule(SRS.schedule(...)) so 2. Manual code review confirms each call is to BUILD fixtures, never to simulate the Tests IIFE wrong-click. -->

      <!-- Gate now exits 1 with ONE FAIL line (only (f) stubbed); a-e PASS: -->
      node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs ; echo "exit=$?"
      <!-- Expected: exit=1, 1 FAIL, 5 PASS lines -->
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: group (e) body builds a synthetic `srsStore` with at least 4 rows produced via SRS.schedule (legitimate fixture-build path).
    - Source: group (e) captures `snapshotBefore = JSON.stringify(srsStore)` and `snapshotAfter = JSON.stringify(srsStore)` and asserts `assert.strictEqual(snapshotAfter, snapshotBefore, ...)`.
    - Source: group (e) does NOT call `SRS.schedule(srsStore[...], ...)` anywhere — only `SRS.schedule(null, ...)` or `SRS.schedule(SRS.schedule(...), ...)` are present, and only to BUILD the synthetic fixtures (never the simulated test path).
    - Source: group (e) calls `BANK.filter(i => i.type === 'qcm')` and `SRS.todayLocal()` — both pure helpers.
    - Source: group (e) deliberately picks wrong on every question (`(item.correct + 1) % 4`) — proving even maximum wrong-answer pressure doesn't trigger an SRS write.
    - Behavior: `node verify-quiz.cjs` now prints PASS for groups (a)..(e); FAIL only for (f); exit 1.
    - Behavior: P3 gate still exits 0.
  </acceptance_criteria>
  <done>Group (e) PASS line prints; only (f) stub remains; no other file touched.</done>
</task>

<task type="auto">
  <name>Task 6: Implement group (f) — Schema compatibility with Phase 3 verify-srs.cjs (cross-phase no regression)</name>
  <files>.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs</files>
  <read_first>
    - .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs (current state — groups a-e done; replacing group (f) stub)
    - .planning/phases/04-qcm-tests-blancs/04-CONTEXT.md (line 66 — group 6 phrasing verbatim: "Schema compatibility with Phase 3 verify-srs.cjs — no regression on the SRS contract")
    - .planning/phases/03-flashcards-srs/verify-srs.cjs lines 141-160 (the P3 schema round-trip test — group (f) asserts the same row shape survives a Phase-4-style write path)
    - .planning/phases/04-qcm-tests-blancs/04-RESEARCH.md §"verify-quiz.cjs Pattern" lines 894-900 (suggested group (f) structure — adapt)
    - qhse-cesi/srs.js lines 176-185 (the schedule() return shape — confirm the 6 fields ease/interval/due/lapses/reps/introduced)
  </read_first>
  <action>
Edit `.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs`. Replace the group (f) stub `check()` body with the cross-phase schema compatibility assertion.

**The assertion:** group (f) is an EARLY-WARNING gate. If a future refactor of srs.js drops a field (e.g. removes `introduced`), or if Phase 5 adds a new mandatory field, this gate flags the drift IMMEDIATELY in the Phase 4 path — before the owner discovers it through broken behavior. We assert the row returned by `SRS.schedule` (the only Phase 4 write entry point) carries every field the P3 verify-srs.cjs schema round-trip test EXPECTS to see.

**Group (f) body replacement:**

```javascript
check('SC4/PERSIST-01 group (f) — cross-phase schema compatibility: SRS.schedule row matches P3 contract', () => {
  // Required field set: derived from verify-srs.cjs:141-150 (the P3 schema round-trip fixture).
  // If P3 ever evolves this set, this constant must be updated in lockstep.
  const REQUIRED_FIELDS = ['ease', 'interval', 'due', 'lapses', 'reps', 'introduced'];

  // Phase 4 write paths use SRS.schedule(...) with TWO realistic input shapes:
  //   1. row=null    (brand-new card, first-ever grade — happens when QCM wrong-click hits
  //                   a card with no prior qhse-srs-v1 entry)
  //   2. row=existing (the card already has a flashcard-grade row from Phase 3)
  // Both must produce a row carrying ALL P3 required fields.

  // Shape 1: new card, raté grade (the QCM-IIFE-on-new-card scenario):
  const row1 = SRS.schedule(null, 'rate', '2026-05-25');
  REQUIRED_FIELDS.forEach(field => {
    assert.ok(field in row1,
      'SRS.schedule(null, "rate") missing P3-required field: ' + field +
      '. Existing keys: ' + Object.keys(row1).join(','));
  });

  // Shape 2: existing card from Phase 3, then wrong-QCM raté (the QCM-IIFE-on-existing-card scenario):
  const existingP3 = SRS.schedule(null, 'bien', '2026-05-24');
  const row2       = SRS.schedule(existingP3, 'rate', '2026-05-25');
  REQUIRED_FIELDS.forEach(field => {
    assert.ok(field in row2,
      'SRS.schedule(existingP3, "rate") missing P3-required field: ' + field +
      '. Existing keys: ' + Object.keys(row2).join(','));
  });

  // Field TYPES match P3 expectations:
  assert.strictEqual(typeof row2.ease,       'number',          'ease must be number');
  assert.strictEqual(typeof row2.interval,   'number',          'interval must be number');
  assert.strictEqual(typeof row2.due,        'string',          'due must be ISO string');
  assert.ok(/^\d{4}-\d{2}-\d{2}$/.test(row2.due),               'due must match yyyy-mm-dd');
  assert.strictEqual(typeof row2.lapses,     'number',          'lapses must be number');
  assert.strictEqual(typeof row2.reps,       'number',          'reps must be number');
  // introduced is either an ISO string (when set) or null (defensive — verify-srs.cjs E8 path):
  assert.ok(row2.introduced === null || typeof row2.introduced === 'string',
    'introduced must be null or ISO string');

  // JSON round-trip equality (mirrors verify-srs.cjs:141-150 SC4 test):
  const stored      = { 'qcm-row-id-xyz': row2 };
  const roundtripped = JSON.parse(JSON.stringify(stored));
  assert.deepStrictEqual(roundtripped, stored,
    'qhse-srs-v1 row written via Phase 4 SRS.schedule must round-trip through JSON without drift ' +
    '(P3 schema contract — verify-srs.cjs:141-150)');

  // Defence-in-depth: no UNEXPECTED extra fields on the row (early-warning if srs.js grows
  // a side-effecting field that Phase 4 didn't plan for). Soft-warn rather than hard-fail —
  // a future schema EXTENSION (adding a new field) is allowed and not a regression; only
  // DROPPING a P3 field is a regression. So we only ASSERT the required-set is a SUBSET.
  const actualKeys = Object.keys(row2);
  REQUIRED_FIELDS.forEach(f => {
    assert.ok(actualKeys.indexOf(f) !== -1,
      'required field ' + f + ' missing from SRS.schedule output — P3 contract regression');
  });
});
```

**Anti-patterns to refuse:**
- Hard-failing on EXTRA fields (e.g. `assert.strictEqual(actualKeys.length, REQUIRED_FIELDS.length)`) — that would block legitimate future schema extensions. The contract is REQUIRED_FIELDS ⊆ actualKeys, not equality.
- Asserting exact numeric values of `ease`/`interval` — those are tested in verify-srs.cjs E1-E8. Group (f) is about TYPE + PRESENCE, not value semantics.
- Loading verify-srs.cjs and trying to RE-RUN its assertions inside group (f) — that's a smoke-test-of-tests anti-pattern. The cross-phase contract is asserted by REPLICATING the schema check, not by invoking the P3 file.
- Skipping the JSON round-trip — it's the canonical PERSIST-01 assertion that any object in qhse-srs-v1 must be serializable.

**This is the LAST assertion group.** After Task 6 lands, the gate exits 0 (provided Plans 02 + 03 have shipped their actual IIFE code; if they haven't, the file STILL exits 0 because the gate asserts CONTRACTS, not implementations — the contracts are the same whether the IIFEs are real or stub).
  </action>
  <verify>
    <automated>
      grep -nE "SC4/PERSIST-01 group \\(f\\) — cross-phase schema compatibility" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 1 -->

      <!-- All 6 stub markers are gone: -->
      grep -cE "Task [2-7] fills|TBD in Task [2-7]|not yet implemented" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 0 -->

      <!-- REQUIRED_FIELDS array contains all 6 P3 fields: -->
      grep -nE "REQUIRED_FIELDS = \\['ease', 'interval', 'due', 'lapses', 'reps', 'introduced'\\]" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 1 -->

      <!-- THE WHOLE-FILE SMOKE TEST — gate now exits 0: -->
      node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs ; echo "exit=$?"
      <!-- Expected: exit=0, 6 PASS lines, success banner: -->
      <!--   "Phase 4 verification gate: ALL 6 groups PASS — verify-quiz.cjs verified for SC1/SC2/SC3/SC4 + D-V2-03" -->

      <!-- P3 gate STILL exits 0 (no regression): -->
      node .planning/phases/03-flashcards-srs/verify-srs.cjs ; echo "p3 exit=$?"
      <!-- Expected: p3 exit=0 -->

      <!-- Total check() count is still 6 (Tasks 2-6 replaced bodies, never changed the structure): -->
      grep -cE "^check\\(" .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: 6 -->
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: group (f) declares `const REQUIRED_FIELDS = ['ease', 'interval', 'due', 'lapses', 'reps', 'introduced']` (verbatim — order doesn't matter for the test but the string-match makes the grep audit deterministic).
    - Source: group (f) tests BOTH input shapes (`row=null` and `row=existingP3`) AND a JSON round-trip.
    - Source: group (f) asserts type AND presence for each field; no exact-value assertions on `ease`/`interval` (those are P3's job).
    - Behavior: `node verify-quiz.cjs` exits 0 with 6 PASS lines and the success banner `Phase 4 verification gate: ALL 6 groups PASS — verify-quiz.cjs verified for SC1/SC2/SC3/SC4 + D-V2-03`.
    - Behavior: `node verify-quiz.cjs` runtime is under 1 second on any modern laptop (no slow operations — no file I/O beyond the two requires, no network).
    - Behavior: `node .planning/phases/03-flashcards-srs/verify-srs.cjs` still exits 0 (zero P3 regression).
  </acceptance_criteria>
  <done>All 6 assertion groups PASS; gate exits 0; P3 gate still exits 0; the file is feature-complete and ready for commit.</done>
</task>

<task type="auto">
  <name>Task 7: Smoke-test the gate end-to-end, then atomic commit + push</name>
  <files>.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs</files>
  <read_first>
    - .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs (the finished file — Task 6 just landed group (f); confirm exit 0)
    - .planning/phases/04-qcm-tests-blancs/04-CONTEXT.md (Atomic commit/push discipline at line 70 — "verify-quiz.cjs gate = 1 commit, shipped with the engine it verifies"; here the engine has already shipped via Plans 02 + 03, so this is its sibling commit)
    - .planning/STATE.md (token-conscious work + atomic commit discipline)
    - C:\Users\Lasmoles\.claude\projects\C--Users-Lasmoles-mes-apps-claude\memory\feedback_token_conscious_work.md (the cap-mid-task lesson — never batch commits)
    - CLAUDE.md (commit message style: `🚀 Deploy: <app-name>` for app deploys, `✨ Feature: <description>` for features; verify-quiz.cjs lives under .planning/ so a `docs(04):` prefix per the existing v2.0 phase convention is correct — see recent git log for the pattern)
  </read_first>
  <action>
Smoke-test the gate and ship it as one atomic commit.

**Step 1: Full smoke test (one final time).** Run:
```
node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
echo "exit=$?"
```
Expected output (in this order):
- `srs.js loaded OK — window.SRS keys: ...` (line from the shim)
- `outils-data.js loaded OK — BANK.length = <N>` (added in Task 3)
- `=== Phase 4 QCM + Tests blancs verification gate — 6 assertion groups (a)..(f) ===`
- `-- (a) group 1: ...`
- `PASS [SC2/QUIZ-03 group (a) — wrong-answer SRS write ≡ SRS.schedule(state, 'rate')]`
- `-- (b) group 2: ...`
- `PASS [SC3/TEST-01 group (b) — test composition: 20-item shape-valid queue from QCM pool]`
- `-- (c) group 3: ...`
- `PASS [SC4/TEST-03 group (c) — qhse-scores-v1 FIFO cap at 50 (D-10/D-11/D-12)]`
- `-- (d) group 4: ...`
- `PASS [SC4/PERSIST-01 group (d) — qhse-prefs-v1 merge-safety preserves P3 + Plan 02 keys]`
- `-- (e) group 5: ...`
- `PASS [SC4/D-V2-03 group (e) — Tests blancs path does NOT mutate qhse-srs-v1 (hard invariant)]`
- `-- (f) group 6: ...`
- `PASS [SC4/PERSIST-01 group (f) — cross-phase schema compatibility: SRS.schedule row matches P3 contract]`
- `======================================================================`
- `Phase 4 verification gate: ALL 6 groups PASS — verify-quiz.cjs verified for SC1/SC2/SC3/SC4 + D-V2-03`
- `exit=0`

If ANY line is missing or `exit=1` appears, STOP and fix the underlying group before committing. Do not commit a broken gate.

**Step 2: Confirm zero collateral damage.** Run:
```
git status --short
```
Expected: exactly ONE line — `?? .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs` (untracked new file). If ANY other file appears in the status (modified `qhse-cesi/*`, modified other PLAN.md files, etc.), STOP and investigate. The whole plan is supposed to be a single-file addition.

**Step 3: Confirm Phase 3 gate is unmodified and still passes.** Run:
```
node .planning/phases/03-flashcards-srs/verify-srs.cjs
echo "p3 exit=$?"
git diff -- .planning/phases/03-flashcards-srs/verify-srs.cjs
```
Expected:
- `p3 exit=0`
- `git diff` returns empty (verify-srs.cjs is byte-identical to its last committed state).

**Step 4: Atomic commit.** Use this exact command (HEREDOC to preserve formatting). The commit message follows the v2.0 phase convention seen in recent git log (`docs(04): ...`):

```
git add .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
git commit -m "$(cat <<'EOF'
docs(04): ship verify-quiz.cjs gate (6 assertion groups PASS)

Phase 4 verification gate — executable contract for ROADMAP Phase 4 SC1-SC4
plus the D-V2-03 hard invariant (Tests blancs path NEVER mutates qhse-srs-v1).

Groups asserted:
  (a) SC2/QUIZ-03   wrong-answer SRS write ≡ SRS.schedule(state, 'rate')
  (b) SC3/TEST-01   test composition: 20-item shape-valid queue
  (c) SC4/TEST-03   qhse-scores-v1 FIFO cap at 50 (D-10/D-11/D-12)
  (d) SC4/PERSIST-01 qhse-prefs-v1 merge-safety (P3 + Plan 02 keys preserved)
  (e) SC4/D-V2-03   Tests blancs does NOT mutate qhse-srs-v1 (snapshot equality)
  (f) SC4/PERSIST-01 cross-phase schema compatibility with verify-srs.cjs

Sibling gate to .planning/phases/03-flashcards-srs/verify-srs.cjs — same
plain-Node pattern, exit 0/1 contract, named PASS labels, global.window shim.
Zero npm deps; runs in < 1 second.

No changes to qhse-cesi/ (runtime), verify-srs.cjs (P3 gate),
or any other PLAN.md (Plans 02 + 03 already point here).
EOF
)"
```

**Step 5: Confirm commit and push.** Run:
```
git log -1 --stat
git push origin main
```
Expected: the commit shows ONE file changed (`.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs`, ~250-350 lines added). Push completes cleanly to GitHub.

**Step 6: Post-deploy sanity check.** No Vercel deploy is needed (the new file is under `.planning/` — GitHub Actions deploy.yml only ships `qhse-cesi/*` and root assets). Confirm:
```
git log --oneline -5
```
The new commit appears at the top. Vercel pipeline did NOT rebuild (because no `qhse-cesi/` file changed) — and that's correct.

**Forbidden moves in Task 7:**
- Amending a prior commit (`--amend`) — Plans 02 + 03 already shipped; this is a NEW atomic commit.
- Force-pushing (`--force` / `--force-with-lease`) — not needed; this is a fast-forward push on main.
- Committing any other file in the same commit (regression: would violate "atomic commit per delivery unit" discipline from CONTEXT.md line 70).
- Modifying `verify-srs.cjs` or any IIFE in `outils.html` — those are owned by P3 and Plans 02 + 03 respectively, frozen for the purposes of this plan.
- Skipping `git status --short` before commit — the safeguard catches accidental collateral edits.
- Skipping the post-commit `git log -1 --stat` audit — the safeguard confirms exactly one file in the diff.
  </action>
  <verify>
    <automated>
      <!-- Final gate run: -->
      node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs ; echo "exit=$?"
      <!-- Expected: 6 PASS lines, exit=0 -->

      <!-- P3 gate untouched: -->
      node .planning/phases/03-flashcards-srs/verify-srs.cjs ; echo "p3 exit=$?"
      <!-- Expected: p3 exit=0 -->

      <!-- Commit landed: -->
      git log -1 --oneline -- .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: one commit, message starts with `docs(04): ship verify-quiz.cjs gate` -->

      <!-- Exactly one file changed in the commit: -->
      git log -1 --name-only --pretty=format: -- .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
      <!-- Expected: a single line `.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs` -->

      <!-- Pushed to remote: -->
      git status -sb
      <!-- Expected: `## main...origin/main` (no `ahead` count) -->

      <!-- Cross-plan references resolve — Plans 02 and 03 still point at this file: -->
      grep -nE "verify-quiz\\.cjs|Plan 04-04|group \\(e\\)" .planning/phases/04-qcm-tests-blancs/04-02-PLAN.md .planning/phases/04-qcm-tests-blancs/04-03-PLAN.md
      <!-- Expected: ≥ 14 hits across Plans 02 + 03 (the original count) — confirms cross-references are still resolvable. -->
    </automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs` exits 0 with all 6 PASS lines.
    - Behavior: `node .planning/phases/03-flashcards-srs/verify-srs.cjs` exits 0.
    - Repo: `git log -1` shows exactly one new commit with message starting `docs(04): ship verify-quiz.cjs gate` and one file changed (the new verify-quiz.cjs).
    - Repo: `git status -sb` shows `## main...origin/main` with no ahead/behind count after push.
    - Repo: `git diff HEAD~1 HEAD --stat` shows `1 file changed` and the additions are bounded to the new file.
    - Documentation: Plans 02 + 03 cross-references to "Plan 04-04" and "group (e)" still resolve (no PLAN.md file modifications by this plan).
  </acceptance_criteria>
  <done>The Phase 4 verification gate is live in the repo, committed atomically, pushed to main, and exits 0. Phase 3 gate is unaffected. Plans 02 + 03 cross-references resolve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node-CLI invocation → verify-quiz.cjs | Dev runs the gate locally before commit/deploy; no network, no remote inputs |
| verify-quiz.cjs → qhse-cesi/srs.js (require under global.window shim) | The gate loads frozen Phase 3 code; risk = the shim leaks state across multiple requires (mitigated: single load per run) |
| verify-quiz.cjs → qhse-cesi/outils-data.js (require under same shim) | The gate loads frozen Phase 2 bank; risk = bank shape regression invalidates group (b) silently (mitigated: every per-item assertion has an explicit error message) |
| verify-quiz.cjs assertion labels → ROADMAP/REQUIREMENTS traceability | PASS labels include SC/REQ identifiers; risk = label drift breaks owner audit trail (mitigated: labels lifted verbatim from CONTEXT.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | verify-quiz.cjs assertion bodies | mitigate | Each group's body is reviewed against CONTEXT.md lines 60-66 verbatim wording before commit; PASS labels include SC/REQ identifiers that must match REQUIREMENTS.md. |
| T-04-04-02 | Repudiation | "the gate passed but the IIFE was broken" | mitigate | Group (e) snapshot-equality + group (a) deepStrictEqual + group (b) structural shape together cover the four ROADMAP SCs + D-V2-03. Plans 02 + 03 ship their own source-level grep gates (e.g. file-wide `SRS.schedule(` count == 2) as the FIRST line of defence; verify-quiz.cjs is the runtime SECOND line. |
| T-04-04-LO | Information disclosure | local file I/O | accept | The gate reads only the two known repo files (srs.js, outils-data.js) via `path.resolve(__dirname, ...)` — no globbing, no user input. Dev-only local execution; no remote attack surface. |
| T-04-04-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | This plan installs ZERO packages — pure Node built-ins (`assert`, `path`). Package Legitimacy Gate is satisfied trivially (RESEARCH.md confirms). |
| T-04-04-CD | Tampering | future srs.js refactor drops a P3-required field | mitigate | Group (f) is the early-warning gate: `REQUIRED_FIELDS.forEach(field => assert.ok(field in row))` flags the regression immediately on the next CI run. |
| T-04-04-DV | Tampering | future Tests IIFE refactor accidentally calls SRS.schedule | mitigate | Group (e) runtime snapshot-equality catches it; Plan 03 source-grep gate (file-wide SRS.schedule count) catches it earlier still. Defence-in-depth. |
</threat_model>

<verification>
Final whole-plan gates (run after Task 7 completes):

1. **The gate exits 0:** `node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs ; echo "exit=$?"` must print `exit=0` with all 6 PASS labels and the success banner.

2. **The P3 gate exits 0:** `node .planning/phases/03-flashcards-srs/verify-srs.cjs ; echo "exit=$?"` must print `exit=0` — proves zero P3 regression.

3. **The runtime budget holds:** `time node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs` must complete in under 1 second wall-clock on a modern laptop. The gate has no slow operations (no file I/O beyond the two requires, no network, no spawned subprocesses).

4. **The commit is atomic:** `git log -1 --stat` must show exactly ONE file changed (`.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs`) and the diff must contain ZERO modifications to any file under `qhse-cesi/` or any other `PLAN.md`.

5. **Cross-plan references resolve:** the 14 occurrences of "Plan 04-04" / "group (e)" / "verify-quiz.cjs" inside `04-02-PLAN.md` + `04-03-PLAN.md` continue to point at a real, runnable file.

6. **No deploy needed:** the new file lives under `.planning/`, which is not in the GitHub Actions deploy.yml's shipped paths — `https://mes-apps-claude.vercel.app/qhse-cesi/outils.html` is byte-identical to its pre-plan state.

7. **Bus-factor check:** a fresh `git clone` of the repo plus `node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs` exits 0 with no setup — proves zero hidden dependencies, zero environment assumptions beyond Node ≥ 14.
</verification>

<success_criteria>
- Phase 4 verification gate `verify-quiz.cjs` exists, runs, exits 0, prints 6 PASS lines + success banner.
- All 6 assertion groups (a)..(f) implement the locked CONTEXT.md line 60-66 phrasing verbatim in their PASS labels.
- Plans 02 + 03's 14 cross-references to "Plan 04-04" / "group (e)" / "verify-quiz.cjs" all resolve.
- Zero modifications outside the single new file.
- Phase 3 `verify-srs.cjs` still exits 0 (no regression).
- One atomic commit, one push, message `docs(04): ship verify-quiz.cjs gate (6 assertion groups PASS)`.
- No Vercel deploy triggered (file is under `.planning/` — out of GitHub Actions ship paths).
- Owner can run `node verify-quiz.cjs` at any future point to confirm Plans 02 + 03's runtime contracts have not regressed.
</success_criteria>

<output>
Create `.planning/phases/04-qcm-tests-blancs/04-04-SUMMARY.md` after Task 7 completes. Record:
- File path of the new gate (`.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs`) + line count.
- Whole-file `node` exit code (must be 0).
- The 6 PASS labels verbatim (paste from gate stdout).
- The exact P3 gate exit code from a post-commit run (must be 0).
- Commit SHA and atomic-file-count confirmation (must be 1 file).
- `git push` confirmation + `git status -sb` showing `## main...origin/main` clean.
- Any non-obvious deviation from the plan + reason (expected: none).
- Cross-references check: `grep -c "Plan 04-04" .planning/phases/04-qcm-tests-blancs/04-02-PLAN.md .planning/phases/04-qcm-tests-blancs/04-03-PLAN.md` (must report the original 14-count distribution).
- Wall-clock runtime of the gate (`time node verify-quiz.cjs`).
- Confirmation that no Vercel deploy was triggered (file outside `qhse-cesi/` ship paths).
</output>
