---
phase: 04-qcm-tests-blancs
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - qhse-cesi/outils.html
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
requirements_addressed: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "Owner opens the Tests blancs tab, picks a theme (or 'Tous les thèmes'), clicks Démarrer un test, and a timed 20-question exam begins (TEST-01 / D-05 / D-06)"
    - "A MM:SS countdown ticks down from 20:00; at 5 minutes remaining the timer goes red; at 00:00 a tolerant banner appears but the test continues (D-13)"
    - "Owner can move Précédent / Suivant between any of the 20 questions and change answers freely until final submission (D-07 / D-08)"
    - "On the last question, Suivant becomes Terminer le test; clicking it shows the final score (N/20) and per-question corrections with sources (TEST-02)"
    - "A new row {id, dateISO, theme, score, total} is appended to localStorage qhse-scores-v1; cap 50 FIFO; sorted newest-first (TEST-03 / D-10 / D-11)"
    - "qhse-srs-v1 is NEVER touched by the Tests blancs path — verifiable via in-memory snapshot equality before/after a session (D-V2-03 hard invariant)"
    - "Abandon button → native confirm() with French copy; on confirm → return to start screen, no qhse-scores-v1 write (D-15)"
    - "F5 / tab close / panel switch = test lost silently; no beforeunload, no auto-save (D-14 / D-16)"
    - "Score history table at bottom of #panel-tests renders all qhse-scores-v1 rows (date, theme, score N/20), newest first; visible in STATE A + C, hidden during STATE B (D-09 — persistent Historique section below the start/running/results UI, single-panel UX, no modal, no sub-tab)"
    - "qhse-prefs-v1 gains lastTestTheme via merge-safe writer that preserves Phase 3 keys AND the Phase 4 lastQcmTheme from Plan 02"
  artifacts:
    - path: "qhse-cesi/outils.html"
      provides: "Replacement DOM scaffold for #panel-tests (replaces placeholder) + new Tests blancs engine IIFE appended after the Plan 02 QCM IIFE"
      contains: "<h2 class=\"sr-only\">Tests blancs chronométrés</h2>, .qz-start, .qz-running[hidden], .qz-results[hidden], .qz-history-section, [data-qz-start], [data-qz-timer], [data-qz-progress], .qz-timer-banner[hidden], [data-qz-prev], [data-qz-next], [data-qz-abandon], [data-qz-score], .qz-corrections, .qz-history <table>, IIFE with window.__qzTestsBooted guard"
  key_links:
    - from: "qhse-cesi/outils.html #panel-tests IIFE"
      to: "window.BANK (outils-data.js)"
      via: "BANK.filter(i => i.type === 'qcm' && (theme === 'all' || i.theme === theme)) → shuffle → slice(0,20)"
      pattern: "BANK\\.filter.*type.*qcm"
    - from: "qhse-cesi/outils.html #panel-tests IIFE"
      to: "localStorage qhse-scores-v1"
      via: "appendScore({id, dateISO, theme, score, total}) with unshift + slice(0,50) FIFO"
      pattern: "qhse-scores-v1"
    - from: "qhse-cesi/outils.html #panel-tests IIFE"
      to: "Date.now() wall-clock"
      via: "setInterval(tickTimer, 1000) computing remaining = DURATION_MS - (Date.now() - startedAt)"
      pattern: "Date\\.now\\(\\)"
    - from: "qhse-cesi/outils.html #panel-tests IIFE"
      to: "qhse-srs-v1"
      via: "NONE — D-V2-03 invariant; window.SRS.schedule MUST NOT be called from this IIFE"
      pattern: "(absence of SRS.schedule inside the Tests IIFE block)"
---

<objective>
Ship the Tests blancs chronométrés mode inside `#panel-tests` of `outils.html`. This plan closes TEST-01 (20-question timed exam), TEST-02 (final score + per-question corrections with sources), TEST-03 (qhse-scores-v1 persistence + qhse-srs-v1 isolation).

Purpose: Give the owner an exam-realistic timed run he can take repeatedly, with a score history he can scroll to track progress per theme — without polluting the SRS queue with test attempts. Tests blancs is pure simulation; the SRS queue is only fed by daily flashcard grading + wrong-QCM révision rapide clicks (Plan 02).

Output: One commit modifying `outils.html` — (a) the `#panel-tests` placeholder is replaced with a tri-state scaffold (Start / Running / Results) + persistent history section; (b) a new IIFE is appended after the Plan 02 QCM IIFE, implementing the state machine, drift-resistant timer, free-nav answer map, abandon flow, results render, and score history table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-qcm-tests-blancs/04-CONTEXT.md
@.planning/phases/04-qcm-tests-blancs/04-UI-SPEC.md
@.planning/phases/04-qcm-tests-blancs/04-PATTERNS.md
@.planning/phases/04-qcm-tests-blancs/04-RESEARCH.md
@.planning/phases/04-qcm-tests-blancs/04-01-SUMMARY.md
@.planning/phases/04-qcm-tests-blancs/04-02-SUMMARY.md
@qhse-cesi/outils.html
@qhse-cesi/outils-data.js
@qhse-cesi/srs.js
</context>

<interfaces>
<!-- localStorage qhse-scores-v1 row schema (D-10 — locked) -->
{
  id: string,         // 'test-<Date.now()>' — timestamp-based stable id
  dateISO: string,    // 'yyyy-mm-dd' local civil day via window.SRS.todayLocal()
  theme: string,      // theme slug ('all' or one of the 15) — same vocabulary as picker
  score: number,      // count of correct answers (0..20)
  total: number       // always 20 under D-05; explicit for future flexibility
}
// qhse-scores-v1 itself is an ARRAY (not an object map), sorted most-recent-first.

<!-- Allowed window.SRS calls in this IIFE (D-V2-03 boundary): -->
window.SRS.todayLocal()  → 'yyyy-mm-dd'   // PURE function; does NOT mutate qhse-srs-v1; safe to call for dateISO
// FORBIDDEN: window.SRS.schedule(...)    // would mutate qhse-srs-v1 — D-V2-03 hard invariant; verify-quiz.cjs asserts.

<!-- State machine (Tests blancs only) -->
STATE.START   ('A')  → .qz-start visible, .qz-running hidden, .qz-results hidden, .qz-history-section visible
STATE.RUNNING ('B')  → .qz-start hidden, .qz-running visible, .qz-results hidden, .qz-history-section hidden
STATE.RESULTS ('C')  → .qz-start hidden, .qz-running hidden, .qz-results visible, .qz-history-section visible

<!-- Timer constants -->
DURATION_MS         = 20 * 60 * 1000   // 20 minutes (D-06)
ALERT_THRESHOLD_MS  = 5  * 60 * 1000   // last 5 minutes — timer turns red (UI-SPEC)
TICK_MS             = 1000              // setInterval cadence

<!-- Test composition (D-05 / D-08) -->
TEST_SIZE = 20
pool = BANK.filter(i => i.type === 'qcm' && (theme === 'all' || i.theme === theme))
if (pool.length < TEST_SIZE) → render "Pool insuffisant" inline error, disable Démarrer button
else → queue = fisherYates(pool).slice(0, TEST_SIZE)   // question order shuffled, choices[] NOT shuffled (D-08)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace #panel-tests placeholder with the tri-state Tests blancs scaffold + history section</name>
  <files>qhse-cesi/outils.html</files>
  <read_first>
    - qhse-cesi/outils.html (lines 138-141 — current placeholder; the surrounding tabpanel block; verify Plan 02 landed the QCM scaffold cleanly before this Task)
    - .planning/phases/04-qcm-tests-blancs/04-UI-SPEC.md §Composition: #panel-tests (the full tri-state ASCII diagram), §Component Inventory (Tests panel container + every #panel-tests row in the table — lines 541-579), §Copywriting Contract — Tests blancs (every visible French string + abandon confirm() copy + 4-tier score heading template)
    - .planning/phases/04-qcm-tests-blancs/04-UI-SPEC.md §History table visual treatment + §Responsive layout (Test footer controls row order on mobile vs desktop) + §ARIA contract (role="timer", role="status" + aria-live="polite" on banner, role="group" on choices, `<time datetime>` in date cells)
    - .planning/phases/04-qcm-tests-blancs/04-CONTEXT.md `<decisions>` D-05 (20Q), D-06 (20min), D-07 (free nav), D-09 (history below tests UI), D-10 (row schema), D-12 (table compact), D-13 (banner not autosubmit)
    - .planning/phases/04-qcm-tests-blancs/04-PATTERNS.md §Surface 1 (placeholder-replacement pattern from Plan 02 — same approach for #panel-tests)
  </read_first>
  <action>
Edit `qhse-cesi/outils.html` to replace the inner content of the `<div role="tabpanel" id="panel-tests" aria-labelledby="tab-tests" tabindex="0" hidden>` block (currently containing the single `<p class="placeholder">` at line 140). The tabpanel wrapper attributes MUST NOT change.

**Children to add inside `#panel-tests` (in DOM source order — top to bottom):**

1. `<h2 class="sr-only">Tests blancs chronométrés</h2>` — first child; screen-reader landmark.

2. **STATE A — Start screen** (visible by default; no `hidden` attribute):
   ```
   <section class="qz-start" data-qz-start-screen>
     <h3>Démarrer un test blanc</h3>
     <p>20 questions tirées au hasard · 20 minutes · navigation libre. Le test ne modifie pas tes flashcards.</p>
     <nav class="qz-theme" aria-label="Choisir un thème">
       <label for="qz-test-theme-select">Thème</label>
       <select id="qz-test-theme-select" data-qz-test-theme>
         <!-- 16 options — same slugs and order as #qz-qcm-theme-select; see Plan 02 -->
       </select>
     </nav>
     <p class="qz-pool-small" data-qz-pool-warning hidden></p>
     <button type="button" data-qz-start>Démarrer un test</button>
   </section>
   ```
   The 16 `<option>` elements are identical to the QCM picker (Plan 02 §"Theme picker block") — copy the same 16 lines verbatim. `data-qz-pool-warning` is `hidden` by default; IIFE un-hides + fills the text when the chosen theme pool < 20 (UI-SPEC pool-too-small copy).

3. **STATE B — Test running container** (`hidden` by default; IIFE un-hides on Démarrer):
   ```
   <section class="qz-running" data-qz-running-screen hidden>
     <p class="qz-timer-banner" data-qz-timer-banner role="status" aria-live="polite" hidden>Temps écoulé — tu peux continuer.</p>
     <div class="qz-timer-row">
       <span class="sr-only">Temps restant</span>
       <span data-qz-timer role="timer" aria-live="off">20:00</span>
       <span data-qz-progress aria-label="Question 1 sur 20">1/20</span>
     </div>
     <article class="qz-card" data-qz-test-card>
       <h3 id="qz-test-question" data-qz-question></h3>
       <div class="qz-choices" role="group" aria-labelledby="qz-test-question" data-qz-test-choices>
         <!-- 4 <button data-qz-choice data-qz-choice-idx="0..3"> injected by IIFE -->
       </div>
     </article>
     <div class="qz-test-controls">
       <button type="button" data-qz-prev>Précédent</button>
       <button type="button" data-qz-abandon>Abandonner</button>
       <button type="button" data-qz-next>Suivant</button>
     </div>
   </section>
   ```
   Notes:
   - `[data-qz-timer]` text init `20:00`; IIFE replaces on start.
   - `[data-qz-progress]` text init `1/20`.
   - `[data-qz-prev]` is `disabled` at question 1 (IIFE manages); pure native `disabled` attribute.
   - `[data-qz-next]` label morphs to `Terminer le test` on the last question (label swap, same node — IIFE writes `textContent`).
   - The choices container has its OWN class `data-qz-test-choices` (distinct from QCM's `data-qz-choices`) so the Tests IIFE binds to the right surface and Plan 02's delegate does NOT receive Tests clicks.
   - No `[data-qz-reveal]` here — Tests blancs does NOT reveal per-question during the run (D-01 + D-07 mean answers stay selected without immediate feedback; corrections come at STATE C).

4. **STATE C — Results screen** (`hidden` by default):
   ```
   <section class="qz-results" data-qz-results-screen hidden>
     <div class="qz-results__hero">
       <p class="qz-results__heading"><span data-qz-results-prefix></span> — <strong data-qz-score data-qz-score-tier="mid">0/20</strong>.</p>
       <p class="qz-results__subheading">Thème : <span data-qz-results-theme></span></p>
     </div>
     <h3>Corrections</h3>
     <ol class="qz-corrections" data-qz-corrections>
       <!-- 20 <li> children injected by IIFE; see Task 2 render -->
     </ol>
     <button type="button" data-qz-restart>Nouveau test</button>
   </section>
   ```
   Notes:
   - `[data-qz-results-prefix]` holds the 4-tier heading prefix per UI-SPEC Copywriting (`Sans faute`, `Bravo`, `À retravailler`, `Beaucoup à revoir`) — IIFE fills it.
   - `[data-qz-score]` is the large Fraunces score; `data-qz-score-tier` attribute toggles between `high|mid|low` per Plan 01 CSS rule.

5. **HISTORY section** (always exists in DOM; visibility toggled by IIFE per state — visible in A + C, hidden during B):
   ```
   <section class="qz-history-section" data-qz-history-section>
     <h3>Historique des tests</h3>
     <table class="qz-history" data-qz-history>
       <caption class="sr-only">Historique des tests</caption>
       <thead>
         <tr><th scope="col">Date</th><th scope="col">Thème</th><th scope="col">Score</th></tr>
       </thead>
       <tbody data-qz-history-body>
         <!-- <tr> rows injected by IIFE on boot + after each appendScore -->
       </tbody>
     </table>
     <p class="qz-history__empty" data-qz-history-empty hidden>Aucun test terminé pour le moment. Lance ton premier test ci-dessus.</p>
     <p class="qz-history__announce sr-only" data-qz-history-announce aria-live="polite"></p>
   </section>
   ```
   - The `<caption class="sr-only">` carries the accessible name; the visible `<h3>` is the human-facing heading (per UI-SPEC `<caption>` vs `<h3>` planner-choice — pick BOTH, with caption sr-only so screen readers don't hear the heading twice).
   - The `<tbody>` and `.qz-history__empty` are mutually exclusive — IIFE toggles `<table>.hidden` and `.qz-history__empty.hidden` based on whether `qhse-scores-v1` is empty.

6. Comment marker after the scaffold (still inside `#panel-tests`):
   `<!-- Phase 4 Tests blancs engine — IIFE appended after QCM IIFE; see outils.html#tests-iife -->`

**Forbidden moves:**
- Modifying `<div role="tabpanel" id="panel-tests" …>` wrapper attributes.
- Touching `#panel-fiches`, `#panel-flashcards`, `#panel-qcm` (Plan 02 owns the latter; do not re-edit), tablist nodes, or any line outside the `#panel-tests` block.
- Inline `style="…"`, inline `onclick="…"`, or any inline JS.
- A `[data-qz-reveal]` inside `.qz-running` (Tests blancs has no per-question reveal during the run).
- A `<button data-qz-next>` outside `.qz-test-controls` (only one Tests Suivant; Plan 02 owns the QCM Suivant which is `#panel-qcm [data-qz-next]`).
  </action>
  <verify>
    <automated>
      grep -nE "<h2 class=\"sr-only\">Tests blancs chronométrés</h2>" qhse-cesi/outils.html
      grep -nE "id=\"qz-test-theme-select\"" qhse-cesi/outils.html
      grep -nE "data-qz-start-screen|data-qz-running-screen|data-qz-results-screen|data-qz-history-section" qhse-cesi/outils.html
      grep -nE "data-qz-timer\\b|data-qz-progress|data-qz-timer-banner" qhse-cesi/outils.html
      grep -nE "data-qz-prev|data-qz-abandon|data-qz-restart" qhse-cesi/outils.html
      grep -nE "data-qz-score|data-qz-score-tier|data-qz-results-prefix|data-qz-results-theme" qhse-cesi/outils.html
      grep -nE "data-qz-history\\b|data-qz-history-body|data-qz-history-empty|data-qz-history-announce" qhse-cesi/outils.html
      <!-- Tests panel placeholder removed: -->
      grep -c "Ce mode arrive en Phase 4 — tests blancs chronométrés" qhse-cesi/outils.html
      <!-- Expected: 0 (Plan 02 already removed the QCM placeholder; this removes the Tests placeholder) -->
      <!-- 16 theme options in the Tests picker — total in file now ≥ 48 (3 pickers × 16). -->
      grep -cE "value=\"(all|duerp|principes-generaux|iso-9001|iso-14001|iso-45001|tms|risque-routier|risque-chimique|rps|espaces-confines|acronymes|metiers|calendrier|icpe-seveso|rncp)\"" qhse-cesi/outils.html
      <!-- Expected: ≥ 48 -->
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `outils.html` no longer contains the string `Ce mode arrive en Phase 4 — tests blancs chronométrés`.
    - Source: `outils.html` contains exactly one `<h2 class="sr-only">Tests blancs chronométrés</h2>`.
    - Source: `outils.html` contains exactly one of each: `data-qz-start-screen`, `data-qz-running-screen`, `data-qz-results-screen`, `data-qz-history-section`, `data-qz-history-body`, `data-qz-history-empty`, `data-qz-history-announce`, `data-qz-timer-banner`, `data-qz-start`, `data-qz-prev`, `data-qz-abandon`, `data-qz-restart`, `data-qz-score`, `data-qz-results-prefix`, `data-qz-results-theme`, `data-qz-corrections`.
    - Source: `[data-qz-running-screen]`, `[data-qz-results-screen]`, `[data-qz-timer-banner]`, `[data-qz-history-empty]` ALL carry the bare `hidden` attribute in the initial HTML.
    - Source: `[data-qz-start-screen]` and `[data-qz-history-section]` do NOT carry `hidden` in the initial HTML.
    - Source: the Tests Suivant button (`[data-qz-next]`) inside `[data-qz-running-screen] .qz-test-controls` is the only Tests-panel Suivant; the QCM Suivant (Plan 02) remains the only `#panel-qcm [data-qz-next]`.
    - Source: `[data-qz-timer]` has both `role="timer"` and `aria-live="off"` attributes.
    - Source: `[data-qz-timer-banner]` has both `role="status"` and `aria-live="polite"` attributes.
    - Source: `#panel-fiches`, `#panel-flashcards`, `#panel-qcm` are byte-identical to pre-Task-1 (`git diff` confirms changes only inside the `#panel-tests` block).
    - Behavior: browser load shows STATE A (Démarrer screen + history section). Clicking Démarrer does nothing yet (Task 2 wires it). No console errors.
  </acceptance_criteria>
  <done>Tests scaffold lands; grep gates pass; `git diff` is bounded to the `#panel-tests` block.</done>
</task>

<task type="auto">
  <name>Task 2: Append the Tests blancs engine IIFE (state machine, drift-resistant timer, free-nav, score history)</name>
  <files>qhse-cesi/outils.html</files>
  <read_first>
    - qhse-cesi/outils.html (the entire file — confirm Plan 02 QCM IIFE shape near the end; confirm Task 1 scaffold is in place; the new IIFE goes after the QCM IIFE's closing `</script>` and before `</body>`)
    - .planning/phases/04-qcm-tests-blancs/04-PATTERNS.md §Surface 3 (every excerpt — IIFE shell, tri-state machine, drift-resistant timer, free-nav with answer map, qhse-scores-v1 append-only FIFO, abandon flow, final score row) and §Shared Patterns S1-S8
    - .planning/phases/04-qcm-tests-blancs/04-UI-SPEC.md §Layout & Interaction Contracts §Timer / Progress / Reveal / Results / History; §Keyboard contract #panel-tests rows; §ARIA contract; §Score color tiers (the 3-tier ratio table)
    - .planning/phases/04-qcm-tests-blancs/04-CONTEXT.md `<decisions>` ALL D-05..D-16 + Claude's Discretion §Module layout + §Verification gate (group e assertion target informs how this IIFE must NOT call SRS.schedule)
    - .planning/phases/04-qcm-tests-blancs/04-RESEARCH.md §Pattern 3 (drift-resistant timer with backgrounded-tab tolerance), §Pattern 6 (qhse-scores-v1 append-only FIFO), §Pattern 7 (tri-state machine), §Pattern 8 (free-nav answer map), §Pitfall 2 (setInterval throttling), §Pitfall 5 (cross-IIFE SRS-write contamination)
    - qhse-cesi/srs.js — confirm `window.SRS.todayLocal()` is exposed and is a pure function (no qhse-srs-v1 mutation); confirm `window.SRS.schedule` is NOT what this IIFE will call.
  </read_first>
  <action>
Append a new inline `<script>` IIFE to `outils.html` AFTER the closing `</script>` of the Plan 02 QCM IIFE, BEFORE `</body>`. Same indentation/formatting as previous IIFEs.

**IIFE shell + boot (Pattern S1):**
- Banner: `/* ============ IIFE: Tests blancs chronométrés — tri-state, 20Q/20min, free-nav, score history ============ */`
- `'use strict';`
- Double-load guard: `if (window.__qzTestsBooted) return; window.__qzTestsBooted = true;` (unique name).
- `function boot() { ... }` dispatched via the DCL pattern (identical to QCM IIFE).

**Module-top constants (inside IIFE, outside `boot`):**
```
const TEST_SIZE          = 20;
const DURATION_MS        = 20 * 60 * 1000;
const ALERT_THRESHOLD_MS = 5  * 60 * 1000;
const TICK_MS            = 1000;
const STATE              = { START: 'A', RUNNING: 'B', RESULTS: 'C' };
const SCORES_KEY         = 'qhse-scores-v1';
const PREFS_KEY          = 'qhse-prefs-v1';
const DEFAULT_PREFS      = { lastTestTheme: 'all' };
```

**Inside `boot()`:**

1. **Resolve mount points + pre-flight BANK guard.** The Tests IIFE does NOT need `window.SRS.schedule`, but it DOES use `window.SRS.todayLocal()`. Pre-flight: `if (!window.BANK || !Array.isArray(window.BANK)) { render inline qz-error in start screen; return. }`. If `window.SRS` or `todayLocal` is missing, fall back to a local `todayLocal()` defined inside the IIFE (`return new Date().toLocaleDateString('sv-SE');`) and continue — the test should still work without Phase 3 loaded (graceful degradation).

2. **Cache DOM handles** — gather all `[data-qz-*]` and `.qz-*` nodes inside `#panel-tests` into a single `els` object. Distinct from Plan 02's QCM cache; never share state across IIFEs.

3. **In-memory state** (resets on each test start; the IIFE persists nothing about an in-progress test — D-14, D-16):
   ```
   const sessionState = {
     mode: STATE.START,
     theme: 'all',
     queue: [],              // 20 BANK items
     currentIdx: 0,
     picks: [],              // length 20, picks[i] ∈ {null, 0, 1, 2, 3}
     timer: { startedAt: 0, intervalId: null, bannerShown: false }
   };
   ```

4. **localStorage helpers (Pattern S5 — try/catch around every {get,set}Item):**
   - `readPrefs()` and `writePrefs(partial)` — copy verbatim from QCM IIFE (Pattern S2 merge-safe writer); the only key this IIFE writes is `lastTestTheme`. **CRITICAL:** the merge MUST preserve `lastQcmTheme` (just added in Plan 02) AND the Phase 3 keys (`lastTheme`, `lastMode`, `newCardsPerDay`). The `Object.assign({}, existing, partial)` pattern handles this automatically.
   - `readScores()` and `appendScore(row)` — per PATTERNS §Surface 3 Net-new pattern 4:
     ```
     function readScores() {
       try {
         const raw = localStorage.getItem(SCORES_KEY);
         if (!raw) return [];
         const parsed = JSON.parse(raw);
         return Array.isArray(parsed) ? parsed : [];
       } catch (e) {
         console.warn('[Tests] readScores failed (corrupt — starting fresh):', e.message);
         return [];
       }
     }
     function appendScore(row) {
       try {
         const scores = readScores();
         scores.unshift(row);                    // newest-first — D-12
         const capped = scores.slice(0, 50);     // FIFO cap — D-11
         localStorage.setItem(SCORES_KEY, JSON.stringify(capped));
         return capped;
       } catch (e) {
         console.warn('[Tests] appendScore failed:', e.message);
         return readScores();
       }
     }
     ```
   - **DO NOT** add a `qhse-srs-v1` reader/writer in this IIFE. The verify gate (Plan 04-04) will grep for `qhse-srs-v1` inside the Tests block — zero matches expected.

5. **State machine `transitionTo(target)`** (PATTERNS Surface 3 net-new pattern 1):
   - Toggle `[hidden]` on `.qz-start`, `.qz-running`, `.qz-results` so exactly one is visible.
   - Toggle `[hidden]` on `.qz-history-section` (hidden during RUNNING; visible in START and RESULTS).
   - Focus moves:
     - START → `els.themeSelect.focus()`.
     - RUNNING → first choice button of the current question (re-render fires first; focus after).
     - RESULTS → `els.restartBtn.focus()`.
   - Side effects:
     - On entering anything other than RUNNING → `stopTimer()`.
   - Update `sessionState.mode = target;`.

6. **`buildQueue(theme)`** (D-05 + D-08):
   - `pool = window.BANK.filter(i => i.type === 'qcm' && (theme === 'all' || i.theme === theme))`.
   - If `pool.length < TEST_SIZE` → return `null` (caller renders the "Pool insuffisant" message + disables Démarrer).
   - Fisher-Yates shuffle of the pool (copy from outils.html:344-351 in Plan 02 if not already in scope — each IIFE owns its own copy is acceptable per CONTEXT.md Claude's Discretion §Module layout). Slice the first `TEST_SIZE` items.
   - Choices order inside each item stays untouched (D-08 — `item.correct` index validity).

7. **`updatePoolWarning()`** — fired on theme change at STATE A:
   - Compute `pool.length` for the picked theme.
   - If `< TEST_SIZE`: un-hide `[data-qz-pool-warning]`, set text via Copywriting template `Pool insuffisant pour ce thème (${N} QCM disponibles, 20 requis). Choisis "Tous les thèmes" ou un autre thème.`, set `els.startBtn.disabled = true`.
   - Else: hide warning, `els.startBtn.disabled = false`.

8. **`onStartClick()`** (STATE A → B):
   - `sessionState.theme = els.themeSelect.value;`.
   - `writePrefs({ lastTestTheme: sessionState.theme });` — the only `writePrefs` call in this IIFE.
   - `const queue = buildQueue(sessionState.theme); if (queue === null) return;` (defensive — the Démarrer button shouldn't even be enabled in this case).
   - `sessionState.queue = queue; sessionState.currentIdx = 0; sessionState.picks = new Array(TEST_SIZE).fill(null); sessionState.timer.bannerShown = false;`.
   - Hide banner: `els.banner.hidden = true;`.
   - `startTimer();`.
   - `transitionTo(STATE.RUNNING);`.
   - `renderTestQuestion(0);`.

9. **`startTimer()` / `tickTimer()` / `stopTimer()`** (PATTERNS Surface 3 net-new pattern 2 — Pitfall 2 defense):
   ```
   function startTimer() {
     sessionState.timer.startedAt = Date.now();
     tickTimer();   // immediate first paint
     sessionState.timer.intervalId = setInterval(tickTimer, TICK_MS);
   }
   function tickTimer() {
     const elapsed   = Date.now() - sessionState.timer.startedAt;
     const remaining = DURATION_MS - elapsed;
     if (remaining <= 0) {
       els.timer.textContent = '00:00';
       els.timer.dataset.qzTimerState = 'expired';
       if (!sessionState.timer.bannerShown) {
         els.banner.hidden = false;
         sessionState.timer.bannerShown = true;
       }
       return;        // tick stays cheap; do NOT clear interval — keeps display stable + idempotent
     }
     const totalSec = Math.floor(remaining / 1000);
     const mm = String(Math.floor(totalSec / 60)).padStart(2, '0');
     const ss = String(totalSec % 60).padStart(2, '0');
     els.timer.textContent = mm + ':' + ss;
     els.timer.dataset.qzTimerState = (remaining <= ALERT_THRESHOLD_MS) ? 'alert' : 'normal';
   }
   function stopTimer() {
     if (sessionState.timer.intervalId !== null) {
       clearInterval(sessionState.timer.intervalId);
       sessionState.timer.intervalId = null;
     }
   }
   ```
   Comment: `// Drift-resistant: wall-clock (Date.now() delta) is source of truth; setInterval is paint-only. Survives mobile Safari tab-background throttling per Pitfall 2.`

10. **`renderTestQuestion(idx)`** (free-nav — PATTERNS Surface 3 net-new pattern 3):
    - Bounds: `if (idx < 0 || idx >= sessionState.queue.length) return;`.
    - `sessionState.currentIdx = idx;`.
    - `const item = sessionState.queue[idx];`.
    - Stem: `els.question.textContent = item.question;` (Pattern S3 XSS-safe).
    - Empty + rebuild 4 choice buttons identical to Plan 02 structure (mono letter span + choice text + aria-label `'Choix ' + letter + ' : ' + item.choices[i]`), defaulting `data-qz-choice-state="idle"`. If `sessionState.picks[idx] !== null`, set the corresponding button `data-qz-choice-state="selected"`.
    - Update progress: `els.progress.textContent = (idx + 1) + '/' + sessionState.queue.length; els.progress.setAttribute('aria-label', 'Question ' + (idx + 1) + ' sur ' + sessionState.queue.length);`.
    - Prev button disabled when `idx === 0`.
    - Suivant label morph: `els.nextBtn.textContent = (idx === sessionState.queue.length - 1) ? 'Terminer le test' : 'Suivant';`. (D-07 + UI-SPEC line 234.)
    - Focus first choice button (only if focus is currently inside `.qz-running`, to avoid stealing focus from out-of-panel users).

11. **`onTestChoiceClick(event)`** — event delegate on `[data-qz-test-choices]`:
    - Resolve button via `event.target.closest('[data-qz-choice]')`.
    - `const idx = Number(button.dataset.qzChoiceIdx);`.
    - **Toggle behavior** (UI-SPEC keyboard contract line 470 "toggle if same choice, replace if different"):
      - If `sessionState.picks[sessionState.currentIdx] === idx` → set to `null` (deselect on second click; button returns to idle).
      - Else → set to `idx` (replace previous pick if any).
    - Re-render the current question (cheap; updates `data-qz-choice-state` on the four buttons in place).
    - **NO SRS WRITE.** **NO `window.SRS.schedule` call.** This is the D-V2-03 hard invariant; verify-quiz.cjs Plan 04-04 group (e) asserts.

12. **`onPrevClick()` / `onNextClick()`:**
    - `onPrevClick()`: `if (sessionState.currentIdx > 0) renderTestQuestion(sessionState.currentIdx - 1);`.
    - `onNextClick()`: if `sessionState.currentIdx === sessionState.queue.length - 1` → `finishTest();`. Else → `renderTestQuestion(sessionState.currentIdx + 1);`.

13. **`onAbandonClick()`** (D-15):
    ```
    function onAbandonClick() {
      const ok = window.confirm('Es-tu sûr de vouloir abandonner ? Tes réponses seront perdues.');
      if (!ok) return;
      stopTimer();
      transitionTo(STATE.START);
      // NO write to qhse-scores-v1 (D-14 + D-15).
    }
    ```

14. **`finishTest()`** (STATE B → C):
    - `stopTimer();`.
    - Compute `correctCount = sessionState.picks.reduce((acc, pick, i) => acc + ((pick !== null && pick === sessionState.queue[i].correct) ? 1 : 0), 0);`.
    - Build the score row (PATTERNS Surface 3 §Final score row):
      ```
      const today = (window.SRS && typeof window.SRS.todayLocal === 'function')
        ? window.SRS.todayLocal()
        : new Date().toLocaleDateString('sv-SE');
      const row = {
        id:      'test-' + Date.now(),
        dateISO: today,
        theme:   sessionState.theme,
        score:   correctCount,
        total:   TEST_SIZE
      };
      const capped = appendScore(row);
      ```
    - Render results (see Task 2 §15 below).
    - Render the history table with the freshly capped array (`renderHistory(capped)`).
    - Announce: `els.historyAnnounce.textContent = capped.length + ' tests enregistrés.';` (sr-only `aria-live="polite"` — UI-SPEC ARIA contract).
    - `transitionTo(STATE.RESULTS);`.

15. **`renderResults(correctCount, total, themeSlug, queue, picks)`**:
    - Compute ratio + tier:
      - `ratio = correctCount / total;`
      - `tier = (correctCount === total) ? 'high' : (ratio >= 0.75 ? 'high' : (ratio >= 0.50 ? 'mid' : 'low'));`
      - Prefix (UI-SPEC Copywriting):
        - `correctCount === total` → `'Sans faute'`
        - `else ratio >= 0.75` → `'Bravo'`
        - `else ratio >= 0.50` → `'À retravailler'`
        - `else` → `'Beaucoup à revoir'`
    - Populate: `els.resultsPrefix.textContent = prefix; els.scoreEl.textContent = correctCount + '/' + total; els.scoreEl.dataset.qzScoreTier = tier; els.resultsTheme.textContent = themeDisplayName(themeSlug);` where `themeDisplayName` looks up the human-readable French label from the same option list as the picker (extract the `<option>` `textContent` from the `els.themeSelect` for the matching `value`, falling back to the slug if not found).
    - Build `.qz-corrections` `<ol>`: for each `i in 0..total-1`, build a `<li>` with:
      - Badge: `<p class="qz-reveal__badge" data-qz-badge-state="…">…</p>` — `correct` / `incorrect` / `noanswer`:
        - `picks[i] === null` → text `— Non répondu`, class `qz-correction__noanswer`, no `data-qz-badge-state` (UI-SPEC line 244 — no glyph, ink-2).
        - `picks[i] === queue[i].correct` → text `✓ Correct`, `data-qz-badge-state="correct"`.
        - else → text `✗ Incorrect`, `data-qz-badge-state="incorrect"`.
      - `<h4 class="qz-correction__question">` with `textContent = queue[i].question`.
      - 4 inline read-only "choice" pills replicating the QCM picked/correct/distractor visual: build 4 `<div>` (or `<button disabled>`) elements with the same `data-qz-choice-state` semantics from Plan 01 CSS (`picked-correct`, `picked-incorrect`, `unpicked-correct`, `unpicked-distractor`).
      - `[data-qz-answer]`, `[data-qz-explanation]`, `[data-qz-source]` paragraphs populated via the same XSS-safe builder pattern as Plan 02 (createElement for `<code>` + `<a>`).
    - Pure DOM construction; no innerHTML on bank content.

16. **`renderHistory(rows)`**:
    - If `rows.length === 0`: hide `<table>`, show `[data-qz-history-empty]`.
    - Else: hide `[data-qz-history-empty]`, show `<table>`, empty `<tbody>`, append one `<tr>` per row in array order (already newest-first per `appendScore`):
      - `<td class="qz-history__date"><time datetime="<dateISO>"><dateISO></time></td>` (machine-readable + visible; per UI-SPEC ARIA line 500).
      - `<td class="qz-history__theme">` text = `themeDisplayName(row.theme)`.
      - `<td class="qz-history__score">` text = `row.score + '/' + row.total`.
    - Cap visualisation: no pagination (D-11 cap 50 fits in DOM).

17. **`onRestartClick()`** (STATE C → A):
    - Reset `sessionState` (queue, picks, currentIdx, timer flags).
    - `transitionTo(STATE.START);`.
    - Focus `els.themeSelect`.

18. **`onTestThemeChange()`** (STATE A only):
    - `sessionState.theme = els.themeSelect.value;`.
    - `writePrefs({ lastTestTheme: sessionState.theme });` (merge-safe).
    - `updatePoolWarning();`.

19. **Event wiring (panel-scoped — Pattern S4; no document keydown):**
    - `els.themeSelect.addEventListener('change', onTestThemeChange);`
    - `els.startBtn.addEventListener('click', onStartClick);`
    - `els.testChoices.addEventListener('click', onTestChoiceClick);`
    - `els.prevBtn.addEventListener('click', onPrevClick);`
    - `els.nextBtn.addEventListener('click', onNextClick);`
    - `els.abandonBtn.addEventListener('click', onAbandonClick);`
    - `els.restartBtn.addEventListener('click', onRestartClick);`

20. **Boot tail of `boot()`:**
    - `const prefs = readPrefs(); sessionState.theme = (typeof prefs.lastTestTheme === 'string') ? prefs.lastTestTheme : 'all';`.
    - `els.themeSelect.value = sessionState.theme;` (defensive: if value not in the option list, will fall back to first option).
    - `updatePoolWarning();` (sets initial enabled/disabled state of Démarrer).
    - `renderHistory(readScores());` (renders the history table on first paint).
    - State stays in STATE.START (initial HTML default).

**Anti-patterns to refuse (PATTERNS Surface 3 anti-pattern checklist + Pitfalls):**
- `window.SRS.schedule(` anywhere inside this IIFE — D-V2-03 (verify-quiz.cjs group e will fail).
- Decrement counter inside `setInterval` — Pitfall 2 (use `Date.now()` delta).
- `setItem('qhse-prefs-v1', JSON.stringify({lastTestTheme}))` without merging — Pitfall 3.
- `Array.sort(() => Math.random() - 0.5)` — biased; use Fisher-Yates.
- `beforeunload` listener — D-14 explicit rejection.
- Auto-submit at 00:00 — D-13 (banner only).
- `<dialog>` element for abandon — UI-SPEC explicit native `confirm()`.
- `innerHTML` on bank content — Pattern S3 / T-04-03-01.
- `document.addEventListener('keydown', …)` — Pattern S4.
- Persisting in-progress test state to localStorage — D-14, D-16 forbid.
- Shuffling `item.choices[]` — D-08 invalidates `item.correct`.
- Calling `clearInterval` inside `tickTimer` at expiry — leave the interval running so display stays stable at `00:00`.
- `Escape` key abandon — UI-SPEC line 475: Escape is a no-op; only the Abandonner button triggers `confirm()`.
  </action>
  <verify>
    <automated>
      <!-- 1. Guard + DCL pattern: -->
      grep -c "window.__qzTestsBooted" qhse-cesi/outils.html
      <!-- Expected: 2 -->

      grep -nE "document\\.readyState === 'loading'" qhse-cesi/outils.html | wc -l
      <!-- Expected: ≥ 3 (Flashcards + QCM + Tests IIFEs) -->

      <!-- 2. THE CRITICAL D-V2-03 GATE — count of SRS.schedule calls in the entire file: -->
      grep -nE "SRS\\.schedule\\(" qhse-cesi/outils.html
      <!-- Expected: exactly 2 occurrences (Flashcards + QCM). Tests IIFE adds ZERO. -->
      <!-- A common-sense secondary check: -->
      grep -nE "SRS\\.todayLocal\\(\\)" qhse-cesi/outils.html
      <!-- Expected: ≥ 1 (Tests IIFE may call this for dateISO) -->

      <!-- 3. Timer is Date.now() based, not counter-decrement: -->
      grep -nE "Date\\.now\\(\\)" qhse-cesi/outils.html
      <!-- Expected: ≥ 1 inside Tests IIFE -->
      grep -nE "setInterval\\(" qhse-cesi/outils.html
      <!-- Expected: ≥ 1 (Tests IIFE — tickTimer at TICK_MS) -->

      <!-- 4. qhse-scores-v1 helpers + cap + unshift: -->
      grep -nE "qhse-scores-v1" qhse-cesi/outils.html
      grep -nE "scores\\.unshift|\\.slice\\(0,\\s*50\\)" qhse-cesi/outils.html
      <!-- Both expected ≥ 1 -->

      <!-- 5. lastTestTheme written via merge-safe writePrefs: -->
      grep -nE "lastTestTheme" qhse-cesi/outils.html
      <!-- Expected: ≥ 2 (declaration + writePrefs call) -->

      <!-- 6. No beforeunload, no Escape-abandon, no document keydown: -->
      grep -c "beforeunload" qhse-cesi/outils.html
      <!-- Expected: 0 -->
      grep -c "document\\.addEventListener.\\?'keydown'" qhse-cesi/outils.html
      <!-- Expected: 0 -->
      grep -nE "'Escape'|key === 'Escape'" qhse-cesi/outils.html
      <!-- Acceptable: 0 OR explicit no-op comment; no Abandon trigger on Escape. -->

      <!-- 7. No innerHTML on bank content (still 0 after Tests IIFE lands): -->
      grep -nE "\\.innerHTML\\s*=" qhse-cesi/outils.html
      <!-- Expected: 0 OR only static literal strings (no bank fields) -->

      <!-- 8. confirm() exists for abandon (and only there): -->
      grep -nE "window\\.confirm\\(|^\\s*confirm\\(" qhse-cesi/outils.html
      <!-- Expected: 1 — the Abandonner copy -->

      <!-- 9. <time datetime=…> tag for history dates (UI-SPEC ARIA line 500): -->
      grep -nE "createElement\\('time'\\)|<time datetime" qhse-cesi/outils.html
      <!-- Expected: ≥ 1 -->

      <!-- 10. Manual browser smoke (run after Vercel deploy):
            - Tests tab → see Start screen + history (empty initially)
            - Switch theme to one with <20 QCMs (none currently — owner verifies with all 15 + 'all')
            - Click Démarrer → STATE B begins, timer counts down from 19:59 → … → 19:58
            - Pick A on Q1; Suivant; Précédent; confirm Q1 still shows A selected (data-qz-choice-state="selected")
            - Click same A again on Q1 → toggles off (idle)
            - Navigate to Q20 → button label morphs to "Terminer le test"
            - Click Terminer → STATE C; score hero shows N/20 with appropriate tier; corrections list shows 20 entries; the history table now has 1 row
            - Click Nouveau test → STATE A; history table still shows the entry from above
            - Now: in DevTools, BEFORE running another test, snapshot qhse-srs-v1 (JSON.stringify); run a 2nd full test; AFTER finish, snapshot qhse-srs-v1 again → MUST be byte-equal (D-V2-03 manual proof; verify-quiz.cjs Plan 04-04 codifies this)
            - Reload page → STATE A; theme select shows last-picked theme; history table renders both entries
            - Switch to Flashcards tab → P3 still works, last flashcard theme still restored
      -->
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `outils.html` contains exactly one `window.__qzTestsBooted` declaration AND one assignment.
    - Source: across the entire file, `SRS.schedule(` appears exactly 2 times (Flashcards + QCM); ZERO occurrences inside the Tests blancs IIFE block — verifiable by inspecting line numbers returned by `grep -n "SRS.schedule("` and confirming none fall between the Tests IIFE banner comment and its closing `})()`.
    - Source: `outils.html` contains exactly one `window.confirm(` call site, and its argument is the literal `'Es-tu sûr de vouloir abandonner ? Tes réponses seront perdues.'`.
    - Source: `outils.html` contains zero `beforeunload` references.
    - Source: `outils.html` contains zero `document.addEventListener('keydown'` references.
    - Source: the Tests IIFE contains `Date.now()` (timer source-of-truth) and `setInterval(` (paint loop).
    - Source: the Tests IIFE contains `scores.unshift(` and `.slice(0, 50)` (D-11 FIFO + D-12 newest-first).
    - Source: `lastTestTheme` written via `writePrefs({...})` using the merge-safe `Object.assign({}, existing, partial)` pattern (already proven in Plan 02; verify the same helper is reused or duplicated correctly here).
    - Source: zero `.innerHTML =` assignments to dynamic bank content.
    - Behavior: open `outils.html` → Tests tab → see Start screen with Démarrer button enabled (assuming any theme with ≥20 QCMs), history section visible (empty state if first run).
    - Behavior: click Démarrer → STATE B; timer reads `19:59` after 1s and decrements every second.
    - Behavior: pick A on Q1 → choice button gains `data-qz-choice-state="selected"` (accent border + tint per Plan 01 CSS); click same A again → toggles back to idle.
    - Behavior: pick A on Q1, Suivant to Q2, Précédent back to Q1 → Q1's A still selected (`picks[]` persistence across nav).
    - Behavior: on Q20 the Suivant button label reads `Terminer le test`.
    - Behavior: click Terminer → STATE C; the score hero renders `N/20` with the 4-tier prefix sentence; the corrections list has exactly 20 `<li>` children; the history table has gained one new row at the top showing today's date, the chosen theme, and the N/20 score.
    - Behavior: complete a second test → history table has 2 rows, newest first; reload the page → both rows still present.
    - Behavior: at start of a fresh test, snapshot `localStorage.qhse-srs-v1` (or `null` if Phase 3 never ran); complete the test; snapshot again → byte-equal. D-V2-03 invariant manually proven; codified by Plan 04-04 verify-quiz.cjs group (e).
    - Behavior: during a running test, click Abandonner → native confirm appears; click Cancel → stay in STATE B with timer running; click again, OK → return to STATE A, no row added to history.
    - Behavior: reload during a running test → STATE A on reload (no auto-save, no warning dialog — D-14 + D-16).
    - Behavior: let timer reach `00:00` → the banner `Temps écoulé — tu peux continuer.` un-hides, timer color goes red, timer text stays `00:00`, but the test continues; user can still pick + navigate + Terminer.
    - Behavior: `qhse-prefs-v1` after switching the Tests theme contains all of: `lastTestTheme` (new), `lastQcmTheme` (from Plan 02), `lastTheme` + `lastMode` + `newCardsPerDay` (from Phase 3) — none clobbered.
    - Behavior: `qhse-scores-v1` after 51 completed tests has length exactly 50 (FIFO cap; oldest dropped) — owner verifies via DevTools or trusts Plan 04-04 verify-quiz.cjs group (c) assertion.
    - Behavior: Phase 3 Flashcards mode and Plan 02 QCM mode both render and behave identically to before this commit (regression-free).
  </acceptance_criteria>
  <done>Tests engine boots, runs, scores, persists; all grep gates pass; manual browser smoke confirms tri-state machine + timer + free-nav + abandon flow + score history + qhse-srs-v1 isolation + zero P3/P4 regression. Commit message: `🚀 Phase 4 Plan 03 — outils.html: Tests blancs chronométrés (TEST-01/02/03 + qhse-scores-v1)`. Push to main; verify Vercel deploys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outils-data.js (BANK content) → DOM | Bank prose flows into rendered question, choices, corrections; XSS surface if innerHTML used |
| browser tab backgrounding → setInterval | Mobile Safari + Chromium throttle setInterval to 1s minimum on hidden tabs; timer could drift if counter-decrement used |
| user → localStorage qhse-scores-v1 | Final-test write mutates persistent history; risk = unbounded growth, corrupted JSON, or accidentally writing on abandon |
| user → localStorage qhse-prefs-v1 | Theme change writes prefs; risk = clobbering Phase 3 + Plan 02 keys |
| Tests blancs IIFE ↔ QCM IIFE / Flashcards IIFE | Shared window.SRS + qhse-srs-v1; risk = accidental schedule() call leaks SRS mutation into Tests path |
| user keyboard (Escape, F5, tab close) → test state | Out-of-band exits; risk = unwanted auto-save, beforeunload warning, or persisted partial test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | renderResults source line + question stem + explanation (bank fields) | mitigate | XSS-safe DOM construction (Pattern S3): `textContent` + `createElement('code')` + `createElement('a')` with `rel="noopener noreferrer"`. No `innerHTML` on bank content. Verified by grep gate. |
| T-04-03-02 | Tampering | qhse-srs-v1 (D-V2-03 invariant) | mitigate | Tests IIFE NEVER calls `window.SRS.schedule`. Code path verified: only `window.SRS.todayLocal()` (pure helper) is called. Grep gate asserts file-wide `SRS.schedule(` count stays at exactly 2 (Flashcards + QCM). Plan 04-04 group (e) codifies. |
| T-04-03-03 | Tampering | qhse-prefs-v1 Phase 3 + Plan 02 keys | mitigate | Pattern S2 merge-safe `writePrefs({lastTestTheme: …})` — `Object.assign({}, existing, partial)` preserves `lastTheme`, `lastMode`, `newCardsPerDay`, `lastQcmTheme`. Plan 04-04 group (d) asserts. |
| T-04-03-04 | Denial of service | qhse-scores-v1 unbounded growth | mitigate | D-11 cap 50 FIFO enforced by `scores.slice(0, 50)` on every append. At ~80 bytes/row × 50 = ~4KB max — far below 5MB quota. |
| T-04-03-05 | Denial of service | setInterval drift / timer race on backgrounded tab | mitigate | Pattern 3: `Date.now()` delta is source of truth; setInterval is paint-only. When tab returns to foreground, the next tick recomputes elapsed correctly. Tested mentally: 20 min in background → tab return → timer jumps to actual remaining (or 00:00 + banner). |
| T-04-03-06 | Tampering | localStorage corrupt JSON (qhse-scores-v1) | mitigate | `readScores` wraps `JSON.parse` in try/catch; returns `[]` on corrupt. IIFE renders empty-state inline message; user can still start a new test which appends fresh. |
| T-04-03-07 | Information disclosure | history rows expose study patterns | accept | Single-user app; localStorage is owner-only on owner's device. No multi-tenant boundary. |
| T-04-03-08 | Elevation of privilege | malformed BANK entry (e.g. `correct` out of bounds, missing `choices`) | mitigate | Defensive: render falls back gracefully — `item.choices[item.correct]` returning undefined just shows empty string; scoring uses `pick === item.correct` (strict equality) so missing choices count as wrong, not crash. No throws. |
| T-04-03-09 | Tampering | F5 / tab close / panel switch mid-test | mitigate | D-14 + D-16 architecturally accept this — IIFE persists nothing about in-progress test. No `beforeunload` to bypass. No partial test row ever reaches `qhse-scores-v1`. |
| T-04-03-10 | Denial of service | timer running after panel switch (cross-tab pollution) | mitigate | The tablist IIFE in `outils.html` (Phase 1) hides the panel via `[hidden]` toggle but does NOT unmount or fire a custom event. The Tests timer keeps ticking in the background while the panel is hidden — and that is correct behavior (a switched-away test is still "running" until the timer expires or the user returns and abandons). On reload (D-16), the timer is gone with the page. No quota or perf risk. |
| T-04-03-11 | Tampering | UI-cheating: opening DevTools and editing `sessionState.timer.startedAt` | accept | Single-user; if owner cheats their own timer, they cheat their own exam practice. No multi-user trust to defend. |
| T-04-03-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero packages installed. Inline JS edit only. |
</threat_model>

<verification>
- Vercel deploy succeeds (~60s after push); visit `/qhse-cesi/outils.html` Tests tab → no `[Tests]` console errors.
- Phase 3 `verify-srs.cjs` STILL exits 0 (`node .planning/phases/03-flashcards-srs/verify-srs.cjs`).
- Manual eyeball: run one complete 20-question test → see score hero + corrections + history row added → reload → history persists → snapshot `qhse-srs-v1` before/after → confirm unchanged.
- Open Flashcards and QCM tabs → both still work; their schedule calls write to `qhse-srs-v1` as before.
</verification>

<success_criteria>
- TEST-01 closed: timed 20-question exam with theme selection.
- TEST-02 closed: final score + per-question corrections + sources rendered.
- TEST-03 closed: `qhse-scores-v1` row appended per completion; `qhse-srs-v1` untouched by Tests path (D-V2-03 hard invariant; manually verifiable, codified by Plan 04-04).
- Phase 3 Flashcards, Plan 02 QCM, root QHSE Trainer, Hub reading content all render and behave identically.
- One atomic commit; one push.
</success_criteria>

<output>
Create `.planning/phases/04-qcm-tests-blancs/04-03-SUMMARY.md` after completion. Record: IIFE lines added, file-wide `SRS.schedule(` call count (must equal 2), Tests IIFE SRS.schedule call count (must equal 0), qhse-scores-v1 write call site, smoke-test result for full session + abandon + timeout + reload + regression on Flashcards/QCM, commit SHA, deploy URL.
</output>
