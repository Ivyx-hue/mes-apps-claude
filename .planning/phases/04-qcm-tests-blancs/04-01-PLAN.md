---
phase: 04-qcm-tests-blancs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - qhse-cesi/chassis.css
autonomous: true
requirements: []
requirements_addressed: []
# Foundation plan: no requirement closes on CSS alone; CSS underpins QUIZ-01..03 + TEST-01..03 visuals.
# Requirements close in 04-02 / 04-03 / 04-04 where behavior is wired.
must_haves:
  truths:
    - "Every Phase 4 selector is scoped under #panel-qcm or #panel-tests (no bare .qz-* leaks)"
    - "chassis.css declares no new :root custom properties (Phase 4 reuses 100% of existing tokens)"
    - "The .fc-* (Phase 3) block, Phase 1/2 chassis rules, and root QHSE Trainer remain byte-identical (additive-only diff)"
    - "Choice button geometry, picked-state semantics, timer states, results hero, and history table all render with chassis tokens only"
  artifacts:
    - path: "qhse-cesi/chassis.css"
      provides: "Phase 4 .qz-* component CSS appended inside @layer components, after the existing .fc-* block (~line 882, before the closing brace of @layer components on line 883)"
      contains: ".qz-theme, .qz-card, .qz-choices, .qz-reveal, .qz-reveal__badge, .qz-timer-row, .qz-timer-banner, .qz-test-controls, .qz-results__hero, .qz-corrections, .qz-history"
  key_links:
    - from: "qhse-cesi/chassis.css"
      to: "qhse-cesi/outils.html #panel-qcm, #panel-tests"
      via: "parent-selector scoping (#panel-qcm .qz-*, #panel-tests .qz-*)"
      pattern: "(#panel-qcm|#panel-tests)\\s+\\.qz-"
---

<objective>
Ship the entire `.qz-*` CSS namespace inside `chassis.css` `@layer components` so that Wave 2 (QCM IIFE) and Wave 3 (Tests blancs IIFE) can render against a finished visual contract.

Purpose: Decouple visuals from JS so executors in Wave 2/3 think only about behavior and state machines — never about colour, spacing, focus rings, or zebra stripes. The UI-SPEC is exhaustive (647 lines); this plan translates it to CSS once.

Output: A single CSS append (~250–400 new lines) to `chassis.css`, inside the existing `@layer components { ... }` block, after the `.fc-*` Phase 3 region (~line 882) and before the closing brace (line 883). Zero token additions, zero modifications to any prior chassis rule, zero new fonts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-qcm-tests-blancs/04-CONTEXT.md
@.planning/phases/04-qcm-tests-blancs/04-UI-SPEC.md
@.planning/phases/04-qcm-tests-blancs/04-PATTERNS.md
@qhse-cesi/chassis.css
</context>

<interfaces>
<!-- Chassis tokens Phase 4 consumes (all already declared in @layer tokens, chassis.css ~lines 33-89). Phase 4 MUST NOT add to or mutate this set. -->

Spacing tokens: --space-xs, --space-sm, --space-md, --space-lg, --space-xl
Type-scale tokens: --step--1, --step-0, --step-1, --step-2, --step-3, --step-5
Color tokens: --bg-1, --bg-2, --ink-1, --ink-2, --ink-3, --accent, --accent-soft, --success, --warning, --alert, --border-subtle, --focus-ring
Radius tokens: --radius-sm, --radius-lg
Font stacks: --font-sans (Inter), --font-serif (Fraunces), --font-mono (JetBrains Mono)

Existing analog rules to copy/adapt (chassis.css):
- .fc-theme + #fc-theme-select : lines 621-651
- .fc-card : lines 654-671 (note: Phase 4 .qz-card uses min-height: clamp(20rem, 50vh, 32rem) per UI-SPEC §Card visual treatment, vs P3's clamp(16rem, 40vh, 24rem))
- #panel-flashcards [data-fc-question] : lines 679-687
- #panel-flashcards [data-fc-reveal] (primary accent CTA) : lines 688-703
- #panel-flashcards [data-fc-reveal-hint] (desktop keyboard hint) : lines 704-715
- .fc-verso + [data-fc-answer/explanation/source] + source code/a children : lines 718-752
- .fc-grades (choice analog, but Phase 4 stays column on both viewports — D-08): lines 755-804
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append the `.qz-*` namespace to chassis.css (one atomic CSS block)</name>
  <files>qhse-cesi/chassis.css</files>
  <read_first>
    - qhse-cesi/chassis.css (FULL — you MUST see the existing layer structure, the .fc-* block ~547-882, the closing brace of @layer components on line 883, and confirm where to append)
    - .planning/phases/04-qcm-tests-blancs/04-UI-SPEC.md §Design System, §Spacing Scale, §Typography (QCM + Tests panels), §Color (all sub-sections including Score color tiers + Choice button color contract), §Layout & Interaction Contracts (Card visual treatment, Timer visual treatment, Progress indicator, Reveal panel, Results screen, History table, Theme picker, Choice button geometry)
    - .planning/phases/04-qcm-tests-blancs/04-UI-SPEC.md §Component Inventory (lines 521-579 — exhaustive selector map)
    - .planning/phases/04-qcm-tests-blancs/04-PATTERNS.md §Surface 4 (chassis.css analog excerpts — Theme picker, Card, Question stem, Primary CTA, Keyboard hint, Reveal-panel typography, Grade buttons → choice buttons, Net-new CSS Phase 4 needs)
    - .planning/phases/04-qcm-tests-blancs/04-CONTEXT.md `<decisions>` — D-01, D-08, D-12, D-13 (visual implications) and "Claude's Discretion" §CSS namespace (`.qz-*` shared prefix, scoped under #panel-qcm/#panel-tests, NOT `@scope`)
  </read_first>
  <action>
Append a single contiguous CSS block inside `@layer components { ... }` of `chassis.css`, positioned AFTER the existing `.fc-*` Phase 3 region (~line 882) and BEFORE the closing brace of `@layer components` on line 883. Wrap the entire Phase 4 region with a comment banner mirroring Phase 3 style: `/* ============ Phase 4 — QCM + Tests blancs (.qz-*) ============ */`.

**Hard scoping rule (per D-CSS-namespace + UI-SPEC line 31):** Every selector MUST start with `#panel-qcm` or `#panel-tests`. Selectors that apply to both panels use comma-list (e.g., `#panel-qcm .qz-card, #panel-tests .qz-card { ... }`). NO bare `.qz-*` selectors anywhere.

**Token discipline (per UI-SPEC line 33):** Use only the existing tokens listed in `<interfaces>`. Do NOT declare any new `:root` custom properties. Do NOT add any new font-family beyond the three already loaded.

**Phase 4 CSS blocks to author (in this order inside the appended region):**

1. **Theme picker** (shared by both panels) — adapt chassis.css:621-651. Selectors: `.qz-theme` (flex row container), `.qz-theme label`, `#qz-qcm-theme-select, #qz-test-theme-select` (shared rule body), `#qz-qcm-theme-select:focus-visible, #qz-test-theme-select:focus-visible`. Mobile: 100% width; ≥48rem: min-width 16rem / max-width 24rem. Min-height 44px (WCAG 2.5.5 — UI-SPEC Exceptions table).

2. **Card** — adapt chassis.css:654-671. Selectors: `#panel-qcm .qz-card, #panel-tests .qz-card`. `background: var(--bg-2)`, `border: 1px solid var(--border-subtle)`, `border-radius: var(--radius-lg)`, `padding: var(--space-md)` mobile / `var(--space-lg)` ≥48rem, `min-height: clamp(20rem, 50vh, 32rem)` (Phase 4 specific — accommodates 4-choice stack), `display: flex; flex-direction: column; gap: var(--space-md)`, `max-width: 48rem; margin-inline: auto` on ≥48rem.

3. **Question stem** — adapt chassis.css:679-687. Selector: `#panel-qcm [data-qz-question], #panel-tests [data-qz-question]`. Fraunces 600, `--step-3` (clamp 20-24px), color `--ink-1`, `line-height: 1.3`, `text-wrap: balance`, margin 0.

4. **Choices group + choice button** (the focal new component) — synthesised from `.fc-grades` analog (chassis.css:755-804) with D-08 column override:
   - `#panel-qcm .qz-choices, #panel-tests .qz-choices`: `display: flex; flex-direction: column; gap: var(--space-sm)` on BOTH viewports (D-08 — no 2×2 reshuffle, keeps `correct` index unambiguous).
   - `#panel-qcm .qz-choices > button, #panel-tests .qz-choices > button`: `display: block; width: 100%; min-height: 44px; padding: var(--space-sm) var(--space-md); border-radius: var(--radius-sm); background: transparent; color: var(--ink-1); border: 1px solid var(--border-subtle); font: inherit; font-weight: 500; font-size: var(--step-1); line-height: 1.4; text-align: left; cursor: pointer`.
   - Leading letter span inside button (the `A · ` / `B · ` / `C · ` / `D · ` prefix): `#panel-qcm .qz-choices > button > .qz-choice__letter, #panel-tests .qz-choices > button > .qz-choice__letter` — `font-family: var(--font-mono); color: var(--ink-3); font-size: var(--step-0); padding-inline-end: var(--space-sm)`.
   - Hover/focus-visible (pre-interaction): `border-color: var(--accent)` (focus ring inherited from chassis).
   - **Picked-state semantics via `[data-qz-choice-state]` attribute** (UI-SPEC Choice button color contract):
     - `[data-qz-choice-state="picked-correct"]`: `border: 2px solid var(--success); background: color-mix(in oklch, var(--success) 12%, transparent)`
     - `[data-qz-choice-state="picked-incorrect"]`: `border: 2px solid var(--alert); background: color-mix(in oklch, var(--alert) 12%, transparent)`
     - `[data-qz-choice-state="unpicked-correct"]`: `border: 2px dashed var(--success); background: transparent`
     - `[data-qz-choice-state="unpicked-distractor"]`: `border: 1px solid var(--border-subtle); background: transparent; color: var(--ink-2)`
     - `[data-qz-choice-state="selected"]` (Tests blancs running): `border: 2px solid var(--accent); background: color-mix(in oklch, var(--accent) 8%, transparent)`
     - Disabled post-reveal (QCM mode only): `#panel-qcm .qz-choices[data-qz-disabled="true"] > button { pointer-events: none; cursor: not-allowed; }` (Tests blancs choices stay interactive for free nav — D-07).

5. **Reveal panel** (used by QCM post-reveal AND per-question corrections in Results) — replicate chassis.css:718-752 `.fc-verso` + children verbatim:
   - `#panel-qcm .qz-reveal, #panel-tests .qz-reveal`: `border-top: 1px solid var(--border-subtle); padding-top: var(--space-md); margin-top: var(--space-md)`.
   - `.qz-reveal__badge` (inline-flex badge): `display: inline-flex; gap: var(--space-xs); align-items: center; padding: var(--space-xs) var(--space-sm); border-radius: var(--radius-sm); font-size: var(--step-1); font-weight: 600; line-height: 1`.
     - `[data-qz-badge-state="correct"]`: `color: var(--success); border: 1px solid var(--success); background: color-mix(in oklch, var(--success) 14%, transparent)`.
     - `[data-qz-badge-state="incorrect"]`: `color: var(--alert); border: 1px solid var(--alert); background: color-mix(in oklch, var(--alert) 14%, transparent)`.
   - `#panel-qcm [data-qz-answer], #panel-tests [data-qz-answer]`: sans 600, `--step-2` (clamp 17-18px), color `--ink-1`, `line-height: 1.5`.
   - `#panel-qcm [data-qz-explanation], #panel-tests [data-qz-explanation]`: sans 400, `--step-1`, color `--ink-2`, `line-height: 1.6`.
   - `#panel-qcm [data-qz-source], #panel-tests [data-qz-source]`: sans 400, `--step-0`, color `--ink-3`, `line-height: 1.5`.
   - `#panel-qcm [data-qz-source] code, #panel-tests [data-qz-source] code`: `font-family: var(--font-mono); font-size: 0.95em` (mirror of `[data-fc-source] code` rule chassis.css:742-745).
   - `#panel-qcm [data-qz-source] a, #panel-tests [data-qz-source] a`: inherit chassis defaults; if explicit override needed, mirror `[data-fc-source] a` chassis.css:746-752.

6. **QCM "Suivant" / Tests "Démarrer un test" / Results "Nouveau test" primary CTAs** — adapt chassis.css:688-703 `[data-fc-reveal]` rule:
   - `#panel-qcm [data-qz-next], #panel-tests [data-qz-start], #panel-tests [data-qz-restart]`: `min-height: 44px; padding: 0 var(--space-lg); background: var(--accent); color: var(--bg-1); border: 1px solid var(--accent); border-radius: var(--radius-sm); font: inherit; font-weight: 600; font-size: var(--step-1); cursor: pointer; align-self: start`.
   - Hover (same triple): `filter: brightness(1.05)`.
   - Disabled state (Tests start screen when pool < 20 — D-05): `:disabled { opacity: 0.5; cursor: not-allowed; }`.

7. **Keyboard hint (QCM Suivant only)** — mirror chassis.css:704-715:
   - `#panel-qcm [data-qz-next-hint]`: `display: none` mobile.
   - `@media (min-width: 48rem) { #panel-qcm [data-qz-next-hint] { display: inline; color: var(--ink-3); font-family: var(--font-mono); font-size: var(--step--1); margin-inline-start: var(--space-sm); } }`.

8. **Tests blancs — timer row + timer + progress + banner** (net-new, no analog):
   - `#panel-tests .qz-timer-row`: `display: flex; justify-content: space-between; align-items: baseline; gap: var(--space-md); padding-block: var(--space-sm)`.
   - `#panel-tests [data-qz-timer]`: `font-family: var(--font-mono); font-weight: 500; font-size: var(--step-2); color: var(--ink-1); line-height: 1`.
   - `#panel-tests [data-qz-timer][data-qz-timer-state="alert"]`: `color: var(--alert)`.
   - `#panel-tests [data-qz-timer][data-qz-timer-state="expired"]`: `color: var(--alert)`.
   - `#panel-tests [data-qz-progress]`: `font-family: var(--font-mono); font-weight: 500; font-size: var(--step-2); color: var(--ink-1); line-height: 1`.
   - `#panel-tests .qz-timer-banner`: `padding: var(--space-sm) var(--space-md); margin-block-end: var(--space-sm); border-radius: var(--radius-sm); background: color-mix(in oklch, var(--alert) 12%, var(--bg-2)); color: var(--alert); font-weight: 600; font-size: var(--step-1)`. (`[hidden]` toggle handled natively; chassis declares no override of `[hidden]`.)

9. **Tests blancs — Précédent / Abandonner / Suivant footer controls**:
   - `#panel-tests .qz-test-controls`: `display: flex; gap: var(--space-sm); margin-block-start: var(--space-md); flex-wrap: wrap`.
   - Mobile (default): `[data-qz-prev]` + `[data-qz-next]` form a top row 50/50 (`flex: 1 1 calc(50% - var(--space-sm) / 2)`); `[data-qz-abandon]` is full width below (`flex: 1 1 100%`).
   - ≥48rem: `flex-wrap: nowrap; justify-content: space-between`; Précédent left, Abandonner middle (less weight), Suivant right.
   - `#panel-tests [data-qz-prev], #panel-tests [data-qz-next]`: secondary chrome — `min-height: 44px; padding: 0 var(--space-md); background: transparent; color: var(--ink-1); border: 1px solid var(--border-subtle); border-radius: var(--radius-sm); font: inherit; font-weight: 600; font-size: var(--step-1); cursor: pointer`. NOTE: `[data-qz-next]` lives in BOTH `#panel-qcm` (accent — rule 6) and `#panel-tests` (secondary — this rule). Selector scoping disambiguates.
   - `#panel-tests [data-qz-abandon]`: even lighter — `min-height: 44px; padding: 0 var(--space-md); background: transparent; color: var(--ink-2); border: 1px solid var(--border-subtle); border-radius: var(--radius-sm); font: inherit; font-weight: 500; font-size: var(--step-0); cursor: pointer`. Hover `color: var(--alert)` (tolerant destructive signal).
   - `#panel-tests [data-qz-prev]:disabled, #panel-tests [data-qz-next]:disabled`: `opacity: 0.4; cursor: not-allowed`.

10. **Tests blancs — Start screen container** (mostly composed from other rules; this is layout only):
    - `#panel-tests .qz-start`: `display: flex; flex-direction: column; gap: var(--space-md); margin-block: var(--space-lg)`.
    - `#panel-tests .qz-start > h3`: Fraunces 600, `--step-3`, color `--ink-1`, `text-wrap: balance`.
    - `#panel-tests .qz-start > p`: sans 400, `--step-1`, color `--ink-2`, `line-height: 1.6`.
    - `#panel-tests .qz-pool-small`: sans 400, `--step-0`, color `--ink-2`, `margin-block-start: var(--space-sm)`. (Inline error below picker when pool < 20.)

11. **Tests blancs — Results screen** (per UI-SPEC §Results screen visual treatment):
    - `#panel-tests .qz-results__hero`: `text-align: center; padding: var(--space-xl) var(--space-lg); background: var(--bg-2); border: 1px solid var(--border-subtle); border-radius: var(--radius-lg); margin-block: var(--space-lg)`.
    - `#panel-tests .qz-results__heading`: Fraunces 600, `--step-2`, color `--ink-1`, margin 0.
    - `#panel-tests [data-qz-score]`: Fraunces 600, `--step-5` (clamp 36-64px), `line-height: 1.1; display: inline-block`. The color comes via `[data-qz-score-tier]` attribute (set once by JS at render time per UI-SPEC Score color tiers):
      - `[data-qz-score-tier="high"]`: `color: var(--accent)` (≥0.75).
      - `[data-qz-score-tier="mid"]`: `color: var(--warning)` (0.50–0.74).
      - `[data-qz-score-tier="low"]`: `color: var(--alert)` (<0.50).
    - `#panel-tests .qz-results__subheading`: sans 400, `--step-1`, color `--ink-2`, margin 0.
    - `#panel-tests .qz-results > h3`: Fraunces 600, `--step-2`, color `--ink-1`, `margin-block-start: var(--space-xl)` (separation from hero).
    - `#panel-tests .qz-corrections`: `list-style: decimal; padding-inline-start: var(--space-xl); display: flex; flex-direction: column; gap: var(--space-xl)`.
    - `#panel-tests .qz-corrections > li`: no border; the gap carries separation.
    - `#panel-tests .qz-correction__question`: Fraunces 600, `--step-2`, color `--ink-1`, `line-height: 1.3; margin: 0 0 var(--space-sm) 0; position: relative; padding-block-end: var(--space-xs)`. Add 2px accent underline via `::after` (mirror chassis `section h2::after` at ~line 334): `#panel-tests .qz-correction__question::after { content: ''; display: block; width: 3rem; height: 2px; background: var(--accent); margin-block-start: var(--space-xs); }`.
    - `#panel-tests .qz-correction__noanswer`: sans 400, `--step-0`, color `--ink-2`. (For the `— Non répondu` inline label per UI-SPEC.)

12. **Tests blancs — History table** (per UI-SPEC §History table visual treatment):
    - `#panel-tests .qz-history-section`: `margin-block-start: var(--space-xl)`.
    - `#panel-tests .qz-history-section > h3`: Fraunces 600, `--step-2`, color `--ink-1`, `margin-block-end: var(--space-md)`.
    - `#panel-tests .qz-history`: `width: 100%; border-collapse: collapse; font-size: var(--step-0)`.
    - `#panel-tests .qz-history caption`: `text-align: left; padding-block-end: var(--space-sm); color: var(--ink-2); font-weight: 500`.
    - `#panel-tests .qz-history thead th`: `text-align: left; padding: var(--space-sm) var(--space-md); color: var(--ink-2); font-weight: 600; border-bottom: 1px solid var(--border-subtle)`.
    - `#panel-tests .qz-history tbody td`: `padding: var(--space-sm) var(--space-md); color: var(--ink-1); vertical-align: baseline`.
    - `#panel-tests .qz-history tbody tr:nth-child(odd) td`: `background: color-mix(in oklch, var(--bg-2) 50%, transparent)`. (Zebra.)
    - `#panel-tests .qz-history__date`: `font-family: var(--font-mono); color: var(--ink-2); white-space: nowrap`.
    - `#panel-tests .qz-history__theme`: `font-family: var(--font-sans); color: var(--ink-1)`.
    - `#panel-tests .qz-history__score`: `font-family: var(--font-mono); font-weight: 500; color: var(--ink-1)`.
    - `#panel-tests .qz-history__empty`: sans 400, `--step-1`, color `--ink-2`, `line-height: 1.6; padding: var(--space-md) 0`. (Replaces `<table>` when `qhse-scores-v1` is empty.)
    - Mobile scroll: `@media (max-width: 47.999rem) { #panel-tests .qz-history-section { overflow-x: auto; } }` (or wrap `.qz-history` in a `.qz-history-scroll` div — JS will add this wrapper).

13. **Inline error / no-pool messages** (graceful degradation):
    - `#panel-qcm .qz-no-pool, #panel-qcm .qz-error`: sans 400, `--step-1`, color `--ink-2`, `line-height: 1.6; padding: var(--space-md) 0`. No red alert chrome (per UI-SPEC Copywriting Contract — bank-load failure copy stays calm).

**Forbidden moves (anti-pattern checklist — if any of these appear, revert and re-do the task):**
- Any `:root { --… }` addition.
- Any modification to lines 1-882 of chassis.css.
- Any bare `.qz-*` selector (e.g., `.qz-card { ... }` without a `#panel-qcm` or `#panel-tests` prefix).
- Any `@scope` block (CONTEXT.md explicitly rejects `@scope` in favour of parent-selector scoping for grep-friendliness).
- Any animation, transition, transform, or keyframes (UI-SPEC §Motion Contract: zero declared transitions).
- Any new `<link>` to a font, icon library, or external CSS.
- Any `!important`.
- Any `min-height: 44px` exception below the 6 control surfaces listed in UI-SPEC Exceptions table (history rows have NO touch-target floor — passive read-only).
  </action>
  <verify>
    <automated>
      <!-- 1. Phase 4 region exists, sits inside @layer components, after the .fc-* block. -->
      grep -n "Phase 4 — QCM + Tests blancs (.qz-\*)" qhse-cesi/chassis.css

      <!-- 2. No new :root declarations introduced by Phase 4 (count BEFORE region must equal count in full file — fast check: grep counts unchanged). -->
      grep -c "^:root\|^\\s*:root" qhse-cesi/chassis.css
      <!-- Expected: identical to pre-edit count (chassis only declares :root once in @layer tokens). -->

      <!-- 3. EVERY .qz- selector is scoped under #panel-qcm or #panel-tests. The following command must output 0:
           "grep -nE '^[^#@/]*\\.qz-' qhse-cesi/chassis.css | grep -v -E '#panel-qcm|#panel-tests' | wc -l" -->
      <!-- 4. The .fc-* block (lines 547-882) is byte-identical to pre-edit. Verifiable via `git diff qhse-cesi/chassis.css | grep '^-' | grep '\\.fc-'` returning empty. -->

      <!-- 5. No new fonts / @import / @font-face: grep -nE '@import|@font-face' qhse-cesi/chassis.css | wc -l → must equal pre-edit count. -->

      <!-- 6. Visual check (Wave 4 will exercise the real DOM): the human-verify gate happens implicitly through the QCM IIFE landing in Wave 2 (visual smoke test). This task ships CSS only; no JS to run yet, no visible DOM change. -->

      <!-- 7. Required selectors are present (sanity grep — must all return ≥1 match): -->
      grep -nE "#panel-qcm \\.qz-card|#panel-tests \\.qz-card" qhse-cesi/chassis.css
      grep -nE "#panel-qcm \\.qz-choices|#panel-tests \\.qz-choices" qhse-cesi/chassis.css
      grep -nE "data-qz-choice-state=\"picked-correct\"" qhse-cesi/chassis.css
      grep -nE "data-qz-choice-state=\"picked-incorrect\"" qhse-cesi/chassis.css
      grep -nE "data-qz-choice-state=\"unpicked-correct\"" qhse-cesi/chassis.css
      grep -nE "data-qz-choice-state=\"selected\"" qhse-cesi/chassis.css
      grep -nE "data-qz-timer-state=\"alert\"|data-qz-timer-state=\"expired\"" qhse-cesi/chassis.css
      grep -nE "data-qz-score-tier=\"high\"|data-qz-score-tier=\"mid\"|data-qz-score-tier=\"low\"" qhse-cesi/chassis.css
      grep -nE "#panel-tests \\.qz-history__date|#panel-tests \\.qz-history__theme|#panel-tests \\.qz-history__score" qhse-cesi/chassis.css
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `chassis.css` contains the comment banner `/* ============ Phase 4 — QCM + Tests blancs (.qz-*) ============ */` exactly once.
    - Source: every `.qz-*` selector in `chassis.css` is preceded by `#panel-qcm` or `#panel-tests` (no bare-selector leaks; verifiable via the negative grep in the verify block).
    - Source: `chassis.css` declares ZERO new `:root { ... }` blocks — the only `:root` block is the one at the start of `@layer tokens`.
    - Source: the `.fc-*` Phase 3 region (chassis.css ~lines 547-882) and lines 1-546 are byte-identical to the pre-edit file (`git diff qhse-cesi/chassis.css` shows additions only — no `-` lines outside the appended region except possibly the closing `}` of `@layer components` being repositioned).
    - Source: `chassis.css` contains all of: `data-qz-choice-state="picked-correct"`, `"picked-incorrect"`, `"unpicked-correct"`, `"unpicked-distractor"`, `"selected"`; `data-qz-badge-state="correct"`, `"incorrect"`; `data-qz-timer-state="alert"`, `"expired"`; `data-qz-score-tier="high"`, `"mid"`, `"low"`.
    - Source: `chassis.css` contains zero `transition:`, `animation:`, `@keyframes`, or `transform:` declarations inside the new Phase 4 region.
    - Source: `chassis.css` contains the selector `#panel-qcm [data-qz-next], #panel-tests [data-qz-start], #panel-tests [data-qz-restart]` (or comma-equivalent) styled with `background: var(--accent)`.
    - Source: choice button rule sets `flex-direction: column` for both panels with no `@media` override that switches to `row` (D-08).
    - Source: `#panel-tests .qz-history` is a `<table>` selector; zebra rule uses `tbody tr:nth-child(odd)`.
    - Behavior: opening `outils.html` in a browser shows zero visual change to the existing Flashcards panel (regression-free; the new rules target only `#panel-qcm` / `#panel-tests` which still hold their `<p class="placeholder">` text — the panels render unchanged, the new CSS sits inert until Wave 2/3 inject DOM).
    - Behavior: the root QHSE Trainer page at `/` and the Hub at `/qhse-cesi/` render byte-identically to pre-edit (CSS-only change with scoped selectors cannot leak).
  </acceptance_criteria>
  <done>
    All `<read_first>` files consumed; one atomic CSS append landed inside `@layer components` after `.fc-*`; all `acceptance_criteria` grep gates pass; commit message `🚀 Phase 4 Plan 01 — chassis.css: .qz-* CSS namespace (additive, scoped, zero new tokens)` (per CLAUDE.md emoji + format convention). Push to `main` so Vercel deploys the inert CSS ahead of Wave 2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → chassis.css | Single-author CSS edit; risk = scope leak (selector escapes to other pages) or token mutation breaking Phase 1/2/3 visuals |
| chassis.css → browser CSSOM | Native CSS parsing; ill-formed rules silently drop, no script execution surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | chassis.css `:root` tokens | mitigate | Forbid new `:root` declarations; verify via grep gate in `<verify>`. Prevents Phase 4 from silently shifting the v1.0 palette. |
| T-04-01-02 | Tampering | chassis.css Phase 1/2/3 rules (lines 1-882) | mitigate | All Phase 4 rules appended after line 882 inside `@layer components`; `acceptance_criteria` asserts `.fc-*` block byte-identical. Prevents regression on Flashcards / Biblio / Découverte. |
| T-04-01-03 | Information disclosure | none | accept | CSS contains no secrets, no user data; only design tokens already public in the deployed site. |
| T-04-01-04 | Denial of service | localStorage quota (downstream) | accept | This plan ships CSS only; no localStorage writes. The downstream IIFEs (plans 02/03) own their quota mitigation. |
| T-04-01-05 | Elevation of privilege | XSS via CSS injection | accept | Phase 4 adds no `attr(...)` or `url(...)` patterns that consume bank content; all values are static literals. No surface for content-driven CSS injection. |
| T-04-01-06 | Tampering (theme/dark-mode injection) | chassis.css `light-dark()` tokens | accept | Phase 4 reuses chassis tokens which already pipe through `light-dark()`; no theme override surface introduced. |
| T-04-01-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | This plan installs zero packages. Pure CSS edit. No package manager invoked. |
</threat_model>

<verification>
- The closing `}` of `@layer components` remains the same node (one closing brace at the end of the block); no stray braces.
- The Vercel deploy succeeds (~60s after push) — verifiable by visiting `https://mes-apps-claude.vercel.app/qhse-cesi/outils.html` and observing no console errors or 404 on chassis.css.
- The Flashcards panel still renders with identical visuals (manual eyeball gate during Wave 2 execution; this Wave 1 plan is JS-less so the visible site doesn't change yet).
</verification>

<success_criteria>
- `chassis.css` is the only file modified by this plan.
- All Phase 4 selectors live in one contiguous block inside `@layer components`, after `.fc-*`, before the closing brace of the layer.
- Wave 2 + Wave 3 executors can build the QCM / Tests blancs DOM and have it style itself correctly without writing a single new CSS rule.
- `git diff` on chassis.css shows only additions (`+` lines) past line 882.
</success_criteria>

<output>
Create `.planning/phases/04-qcm-tests-blancs/04-01-SUMMARY.md` after completion, listing: lines added, selectors introduced, tokens consumed, regression-verification result (Flashcards still renders), commit SHA, deploy URL.
</output>
