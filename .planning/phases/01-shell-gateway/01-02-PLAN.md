---
phase: 01-shell-gateway
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - qhse-cesi/outils.html
  - qhse-cesi/index.html
autonomous: false
requirements: [SHELL-02, SHELL-03, SHELL-04, SHELL-05, PERSIST-02]
user_setup: []

must_haves:
  truths:
    - "qhse-cesi/outils.html exists, loads chassis.css via <link>, and shows a 4-tab ARIA tablist in the locked order Flashcards · Fiches de révision · QCM · Tests blancs"
    - "Each of the 4 tab panels shows a dated placeholder naming the phase the mode arrives in; the page reads as intentional, not broken"
    - "Tab switching works with mouse, arrow keys (Left/Right/Home/End), aria-selected, [hidden] panel toggle, and location.hash sync/restore"
    - "The Hub #outils nav <li> and #outils section are no longer hidden; the section describes the 4 modes and links to outils.html in the same tab"
    - "The v1.0 Hub reading sections (Accueil, Découverte, Biblio) and the root QHSE Trainer remain unchanged"
  artifacts:
    - path: "qhse-cesi/outils.html"
      provides: "4-tab study shell with chassis identity, ARIA tablist, dated placeholders, IIFE tab JS"
      contains: "role=\"tablist\""
      min_lines: 60
    - path: "qhse-cesi/index.html"
      provides: "Hub with un-hidden #outils nav item + gateway section linking to outils.html"
      contains: "<a href=\"outils.html\""
  key_links:
    - from: "qhse-cesi/index.html #outils section"
      to: "qhse-cesi/outils.html"
      via: "<a href=\"outils.html\"> (same tab, no target=_blank)"
      pattern: "href=.outils\\.html"
    - from: "qhse-cesi/outils.html"
      to: "qhse-cesi/chassis.css"
      via: "<link rel=stylesheet> in <head>"
      pattern: "link rel=.stylesheet. href=.chassis\\.css"
    - from: "outils.html [role=tab] buttons"
      to: "outils.html [role=tabpanel] divs"
      via: "aria-controls + IIFE tab activate() toggling [hidden]"
      pattern: "aria-controls=.panel-"
---

<objective>
Create `qhse-cesi/outils.html` — a deployable 4-tab study shell loading the shared `chassis.css`, with the locked tab order Flashcards · Fiches de révision · QCM · Tests blancs, a proper ARIA tablist (keyboard + hash sync), and a dated placeholder per panel naming the phase that mode arrives in. Then un-hide the reserved `#outils` nav item and `#outils` gateway section in `qhse-cesi/index.html` and rewrite the gateway copy to describe the 4 modes and link to `outils.html`.

Purpose: Deliver the owner-visible walking skeleton (D-04, D-05, D-06, D-07, D-08, D-09; SHELL-02, SHELL-03, SHELL-05) while preserving zero regression on the v1.0 Hub and the frozen root Trainer (SHELL-04).
Output: New `qhse-cesi/outils.html`; modified `qhse-cesi/index.html` (two `hidden` removals + gateway copy rewrite — the only edits, on top of plan 01-01's `<link>` swap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shell-gateway/01-CONTEXT.md
@.planning/phases/01-shell-gateway/01-PATTERNS.md
@.planning/V2-ETUDE-SPEC.md
@qhse-cesi/index.html
@qhse-cesi/chassis.css

<interfaces>
<!-- Contracts the executor needs. From 01-PATTERNS.md (verified line numbers in qhse-cesi/index.html). -->

Head boilerplate to mirror into outils.html (index.html lines 1–14, then a <link rel="stylesheet" href="chassis.css"> instead of the old <style>):
  - <!DOCTYPE html><html lang="fr"> ; meta charset UTF-8 ; meta viewport ; meta color-scheme "dark light"
  - dual <link rel="preconnect"> to fonts.googleapis.com / fonts.gstatic.com (crossorigin)
  - one Google Fonts CSS2 <link> (Fraunces + Inter + JetBrains Mono, display=swap)
  - <link rel="stylesheet" href="chassis.css">  (created by plan 01-01 — already exists)

Skip-link pattern (index.html line 644): <a class="skip-link" href="#main">Aller au contenu principal</a>

Footer pattern (index.html lines ~800–805): <footer role="contentinfo"><div class="footer__inner">… <time datetime="2026-05-16">16 mai 2026</time> … GitHub link with target="_blank" rel="noopener noreferrer"</div></footer>

CSS classes already in chassis.css the shell reuses: .skip-link, .placeholder, .eyebrow, .lead, header/nav classes. Do NOT add new CSS — the chassis already styles these (dead-CSS-on-outils is accepted per D-03).

JS discipline (index.html lines 807–810): all JS inside `(() => { 'use strict'; … })();` — no globals, no inline on* handlers.

index.html edit targets (RE-VERIFY line numbers before editing — read the file fresh):
  - nav <li>: currently line 674 → `<li hidden><a href="#outils" data-target="outils">Outils</a></li>`
  - gateway section: currently lines 793–797 → `<section id="outils" aria-labelledby="h-outils" hidden>` with eyebrow `04 / OUTILS`, h2 `Outils — réservé V2`, `<p class="placeholder">Réservé V2 — flipping the two hidden attributes unlocks this surface.</p>`
  - scrollspy (line ~825) queries `main > section[id]:not([hidden])` — self-heals once `hidden` removed; NO scrollspy JS edit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create qhse-cesi/outils.html — 4-tab ARIA shell with chassis identity and dated placeholders</name>
  <files>qhse-cesi/outils.html</files>
  <read_first>
    - qhse-cesi/index.html (FULL — copy the head boilerplate / skip-link / header / footer patterns; re-verify the exact Google Fonts URL and footer markup rather than assuming)
    - qhse-cesi/chassis.css (confirm it exists from plan 01-01 and that `.placeholder`, `.skip-link`, `.eyebrow`, `.lead`, header/nav classes are present so the shell needs zero new CSS)
    - .planning/phases/01-shell-gateway/01-PATTERNS.md (§ "qhse-cesi/outils.html" — head/skip-link/header/footer/tab-shell reference structure and the tab-JS IIFE pattern)
    - .planning/phases/01-shell-gateway/01-CONTEXT.md (D-04 locked tab order; D-05 ARIA pattern; D-06 dated placeholders; D-09 same-tab internal links)
  </read_first>
  <action>
    Create `qhse-cesi/outils.html` as a standalone zero-build HTML page. Mirror the `<head>` boilerplate from `index.html` lines 1–14 verbatim EXCEPT: set `<title>` to `Outils d'étude — QHSE CESI Hub`, set `<meta name="description">` to an outils-specific French sentence, and use `<link rel="stylesheet" href="chassis.css">` (the file plan 01-01 created) in place of any inline `<style>`. Keep `<meta name="color-scheme" content="dark light">` and the identical Google Fonts preconnect + CSS2 `<link>` (Fraunces + Inter + JetBrains Mono) so the visual identity matches the Hub (SHELL-02, SHELL-05 dark default).

    Add the skip-link `<a class="skip-link" href="#main">Aller au contenu principal</a>` as the first focusable element in `<body>`. Add a minimal header with the brand and a back-link to the Hub: a `<nav aria-label="Navigation principale">` whose only item is `<a href="index.html">← Hub</a>` (internal link, NO target="_blank", per D-09). Do NOT include the CSS-only burger checkbox/label (outils.html is a single-purpose page, not a multi-section hub) — omitting it is fine since chassis.css styles degrade gracefully.

    Add `<main id="main">` containing a tab shell. Implement the ARIA tablist per D-05 exactly:
      - A `<ul role="tablist" aria-label="Modes d'étude">` containing 4 `<li role="presentation">` each wrapping a `<button role="tab">`.
      - The 4 tabs in this LOCKED order with these exact visible labels: 1) `Flashcards`, 2) `Fiches de révision`, 3) `QCM`, 4) `Tests blancs`.
      - Tab ids: `tab-flashcards`, `tab-fiches`, `tab-qcm`, `tab-tests`. Each `aria-controls` its matching panel id: `panel-flashcards`, `panel-fiches`, `panel-qcm`, `panel-tests`.
      - First tab: `aria-selected="true"` and no/zero tabindex; the other three: `aria-selected="false" tabindex="-1"` (roving tabindex).
      - 4 `<div role="tabpanel">` with ids `panel-flashcards` / `panel-fiches` / `panel-qcm` / `panel-tests`, each `aria-labelledby` its tab id. Only `panel-flashcards` visible; the other three carry the `hidden` attribute.
      - Each panel's only content is `<p class="placeholder">…</p>` with a dated/phase-accurate message (D-06, French, Claude's-discretion wording): Flashcards → "arrive en Phase 3", QCM → "arrive en Phase 4", Tests blancs → "arrive en Phase 4", Fiches de révision → "arrive en Phase 5". Confirm those phase numbers against ROADMAP.md (P3 Flashcards+SRS, P4 QCM+Tests blancs, P5 Fiches). The copy must read as intentional ("Ce mode arrive en Phase N — …"), not like a broken page.

    Add the footer by copying the `index.html` footer block verbatim (`<footer role="contentinfo">` with `.footer__inner`, the `<time datetime>` and the GitHub link which DOES keep `target="_blank" rel="noopener noreferrer"` because it is external); set the `<time datetime>` and visible date to 2026-05-16.

    Add ONE inline `<script>` at the end of `<body>` wrapped in `(() => { 'use strict'; … })();` (no globals, no inline `on*` handlers — D-05, project convention). It must: collect `[role="tab"]` and `[role="tabpanel"]`; an `activate(tab)` function that sets `aria-selected` true/false + roving `tabIndex` (0 on active, -1 others), toggles each panel's `hidden` so only the controlled panel shows, sets `location.hash` to the active tab id, and focuses the tab; wire `click` on each tab → `activate`; wire `keydown` so ArrowRight/ArrowLeft cycle (wrapping) and Home/End jump to first/last (all with `preventDefault`); on load, read `location.hash.slice(1)` and activate the matching tab or fall back to the first. Do NOT load `outils-data.js` or any study logic — that is Phases 2–5; `outils-data.js` is reserved for the Phase 2 content bank and MUST NOT be created or referenced here. Do NOT add any external script/CDN. Do NOT touch `qhse-cesi/index.html` in this task.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('qhse-cesi/outils.html','utf8');const need=['<link rel=\"stylesheet\" href=\"chassis.css\">','role=\"tablist\"','id=\"tab-flashcards\"','id=\"tab-fiches\"','id=\"tab-qcm\"','id=\"tab-tests\"','id=\"panel-flashcards\"','id=\"panel-fiches\"','id=\"panel-qcm\"','id=\"panel-tests\"','aria-selected','aria-controls','class=\"placeholder\"','use strict','href=\"index.html\"'];for(const s of need){if(!h.includes(s)){throw new Error('missing: '+s)}}if(/<style[\s>]/.test(h)){throw new Error('outils.html must not have an inline <style>')}if(/outils-data\.js/.test(h)){throw new Error('outils-data.js must NOT be referenced in Phase 1')}if(/<button[^>]*target=._blank/.test(h)||/href=\"outils\.html\"[^>]*target=._blank/.test(h)){throw new Error('internal nav must not open new tab')}const order=['Flashcards','Fiches de révision','QCM','Tests blancs'].map(x=>h.indexOf(x));if(!(order[0]<order[1]&&order[1]<order[2]&&order[2]<order[3])){throw new Error('tab labels not in locked order')}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `qhse-cesi/outils.html` exists, ≥60 lines, has `<!DOCTYPE html><html lang="fr">`, `<meta name="color-scheme" content="dark light">`, the identical Google Fonts CSS2 `<link>` as `index.html`, and exactly one `<link rel="stylesheet" href="chassis.css">`; zero `<style` substrings.
    - Contains a `role="tablist"` with `aria-label`, 4 `role="tab"` buttons with ids `tab-flashcards/tab-fiches/tab-qcm/tab-tests` and matching `aria-controls`, and 4 `role="tabpanel"` divs with ids `panel-flashcards/panel-fiches/panel-qcm/panel-tests`.
    - Visible tab labels appear in the locked source order: `Flashcards` then `Fiches de révision` then `QCM` then `Tests blancs`.
    - First tab has `aria-selected="true"`; the other three have `aria-selected="false"` and `tabindex="-1"`; three of four panels carry `hidden`.
    - Each panel contains a `<p class="placeholder">` whose text names the correct arriving phase (Flashcards=3, QCM=4, Tests blancs=4, Fiches=5) and reads as intentional French copy.
    - One inline `<script>` containing `'use strict'`, an `activate` function, `location.hash` read+write, and ArrowLeft/ArrowRight/Home/End handling; no inline `on*` attributes; no external script/CDN; no `outils-data.js` reference.
    - The Hub back-link is `href="index.html"` with no `target="_blank"`.
    - `git status --porcelain` shows `qhse-cesi/outils.html` added and NO change to `qhse-cesi/index.html`, repo-root `index.html`, or `.github/workflows/deploy.yml` from this task.
  </acceptance_criteria>
  <done>outils.html exists with chassis identity, the 4 locked-order ARIA tabs, dated phase-accurate placeholders, and a strict-mode IIFE tab script with keyboard + hash sync; no inline style, no external deps, no outils-data.js, no index.html change.</done>
</task>

<task type="auto">
  <name>Task 2: Un-hide the Hub #outils nav item + gateway section and rewrite the gateway copy</name>
  <files>qhse-cesi/index.html</files>
  <read_first>
    - qhse-cesi/index.html (FULL — RE-VERIFY current line numbers before editing; plan 01-01 already removed the inline `<style>` so line numbers shifted ~625 lines up. Locate the nav `<li hidden>` for `#outils` and the `<section id="outils" … hidden>` by their content, not by the pre-shift numbers 674/793.)
    - .planning/phases/01-shell-gateway/01-PATTERNS.md (§ "qhse-cesi/index.html — Edit B and Edit C" — exact target markup and replacement structure)
    - .planning/phases/01-shell-gateway/01-CONTEXT.md (D-07 un-hide; D-08 gateway copy; D-09 same-tab link)
  </read_first>
  <action>
    Make exactly two edits to `qhse-cesi/index.html`, located by content (line numbers shifted after plan 01-01's `<style>`→`<link>` swap — do NOT trust 674/793; find the markup):

    Edit B — the nav list item. Find `<li hidden><a href="#outils" data-target="outils">Outils</a></li>`. Remove the `hidden` attribute. To stay consistent with siblings (`<li id="nav-accueil">`, `<li id="nav-decouverte">`, `<li id="nav-biblio">`), give it `id="nav-outils"`. Result: `<li id="nav-outils"><a href="#outils" data-target="outils">Outils</a></li>`. Do NOT change the `href="#outils"` (in-page anchor, consistent with all other Hub nav — D-09 / Claude's-discretion navigation model) or the `data-target`.

    Edit C — the gateway section. Find `<section id="outils" aria-labelledby="h-outils" hidden>` and its current body (eyebrow `04 / OUTILS`, h2 `Outils — réservé V2`, the single `<p class="placeholder">Réservé V2 — flipping the two hidden attributes unlocks this surface.</p>`). Remove the `hidden` attribute from the `<section>`. Keep the `<p class="eyebrow">04 / OUTILS</p>` line unchanged and keep `aria-labelledby="h-outils"`. Change the h2 (id stays `h-outils`) to `Outils d'étude`. Replace the placeholder `<p>` with: a `<p class="lead">` editorial paragraph (French, Découverte/Accueil declarative voice, no "coming soon" marketing filler — introduce that the study tools live on a dedicated page and set expectation that modes arrive across the next phases); then a `<ul>` with four `<li>` each `<strong>`-naming a mode with a one-line French description and the arriving phase — `Flashcards` (Phase 3), `Fiches de révision` (Phase 5), `QCM` (Phase 4), `Tests blancs` (Phase 4); then a final `<p>` containing `<a href="outils.html">Ouvrir les outils d'étude</a>` — an internal same-tab link, NO `target="_blank"` (D-09). Exact prose wording is Claude's discretion within this structure/tone.

    These two edits are the ONLY changes to `index.html` in this task. Do NOT touch the scrollspy `IntersectionObserver` (it queries `:not([hidden])` and self-heals — confirmed in PATTERNS.md / CONTEXT.md), the CSS-only burger, the Biblio data, the `<link rel="stylesheet" href="chassis.css">` from plan 01-01, the repo-root `/index.html`, or `.github/workflows/deploy.yml`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('qhse-cesi/index.html','utf8');if(/<li hidden><a href=\"#outils\"/.test(h)){throw new Error('nav <li> still hidden')}if(!/<li id=\"nav-outils\"><a href=\"#outils\" data-target=\"outils\">Outils<\/a><\/li>/.test(h)){throw new Error('nav-outils li not in expected un-hidden form')}if(/<section id=\"outils\"[^>]*\bhidden\b/.test(h)){throw new Error('#outils section still hidden')}if(!/<section id=\"outils\" aria-labelledby=\"h-outils\">/.test(h)){throw new Error('#outils section header not in expected form')}if(!/<a href=\"outils\.html\"/.test(h)){throw new Error('gateway missing link to outils.html')}if(/<a href=\"outils\.html\"[^>]*target=._blank/.test(h)){throw new Error('outils.html link must be same-tab (no target=_blank)')}if(/Réservé V2/.test(h)){throw new Error('old placeholder copy still present')}if(!/<link rel=\"stylesheet\" href=\"chassis\.css\">/.test(h)){throw new Error('chassis.css link from plan 01-01 was lost')}if(/<style[\s>]/.test(h)){throw new Error('inline <style> reintroduced')}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `qhse-cesi/index.html` no longer contains `<li hidden><a href="#outils"`; it contains exactly `<li id="nav-outils"><a href="#outils" data-target="outils">Outils</a></li>`.
    - `qhse-cesi/index.html` no longer contains a `hidden` attribute on the `<section id="outils">`; it is `<section id="outils" aria-labelledby="h-outils">`.
    - The `<p class="eyebrow">04 / OUTILS</p>` line is preserved; the h2 is `Outils d'étude` with `id="h-outils"`.
    - The old string `Réservé V2 — flipping the two hidden attributes unlocks this surface.` no longer appears anywhere in the file.
    - The section contains a `<p class="lead">` paragraph, a `<ul>` whose four `<li>` `<strong>`-name Flashcards / Fiches de révision / QCM / Tests blancs with their arriving phase, and a `<a href="outils.html">` link with NO `target="_blank"`.
    - The `<link rel="stylesheet" href="chassis.css">` from plan 01-01 is still present; zero `<style` substrings (no regression of plan 01-01).
    - `git status --porcelain` shows only `qhse-cesi/index.html` modified (plus `qhse-cesi/outils.html` from Task 1) — repo-root `index.html` and `.github/workflows/deploy.yml` untouched.
  </acceptance_criteria>
  <done>The #outils nav item and section are un-hidden, the gateway copy describes the 4 modes and links to outils.html in the same tab, plan 01-01's chassis link is intact, and no out-of-scope file changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner verifies the deployed walking skeleton against ROADMAP Phase 1 success criteria</name>
  <files>qhse-cesi/outils.html, qhse-cesi/index.html, qhse-cesi/chassis.css</files>
  <action>This is a human-verification checkpoint — no code is written. Before pausing, the executor performs the deploy: `git pull` (per CLAUDE.md), then commit the v2.0 Phase 1 changes (`qhse-cesi/chassis.css`, `qhse-cesi/index.html`, `qhse-cesi/outils.html`) with a `✨ Feature:`-style message, then `git push origin main` so the existing GitHub Actions pipeline deploys (~60s). Do NOT modify `.github/workflows/deploy.yml` or the repo-root `index.html`. Then present the verification steps below to the owner and wait.</action>
  <what-built>
    `outils.html` (4-tab ARIA study shell, chassis identity, dated placeholders), and the Hub `#outils` nav item + gateway section un-hidden with copy describing the 4 modes and a same-tab link to `outils.html`. The executor commits + pushes the three `qhse-cesi/` files so the GitHub Actions pipeline deploys (~60s). The repo-root QHSE Trainer and `.github/workflows/deploy.yml` are untouched.
  </what-built>
  <how-to-verify>
    1. Wait ~60s after push, then open https://mes-apps-claude.vercel.app/qhse-cesi/ — confirm a new "Outils" item appears in the nav; click it and confirm it smooth-scrolls to an "Outils d'étude" section that describes the 4 modes and has an "Ouvrir les outils d'étude" link.
    2. Click that link — it should open `https://mes-apps-claude.vercel.app/qhse-cesi/outils.html` in the SAME tab. Confirm the same dark editorial identity (Fraunces headings, Inter body, warm-dark background) and a 4-tab bar: Flashcards · Fiches de révision · QCM · Tests blancs.
    3. Click each tab and confirm the panel changes and shows an intentional dated placeholder ("arrive en Phase 3/4/5"). Use Left/Right arrow keys on the tabs — focus should move and the panel should follow. Note the URL hash changes (e.g. `#tab-qcm`); reload on a non-first tab and confirm that tab is restored.
    4. Repeat steps on a phone-width viewport — confirm the shell is usable on mobile, dark by default.
    5. Back on the Hub, scroll through Accueil / Découverte / Biblio and confirm they look exactly like v1.0 (zero regression). Confirm the sticky-header scrollspy now also highlights "Outils" when that section is in view.
    6. Open https://mes-apps-claude.vercel.app/ (root QHSE Trainer) and confirm it is completely unchanged.
  </how-to-verify>
  <read_first>
    - qhse-cesi/outils.html
    - qhse-cesi/index.html
    - .planning/ROADMAP.md (Phase 1 § "Success Criteria" — the 5 owner-verifiable points)
  </read_first>
  <verify>Owner walks the 5 ROADMAP Phase 1 success criteria + tab keyboard nav + hash restore on the live URL and confirms all hold.</verify>
  <acceptance_criteria>
    - Owner confirms ROADMAP Phase 1 success criterion 1: "Outils" nav item + gateway section visible, smooth-scrolls, working link to `outils.html`.
    - Owner confirms criterion 2: `outils.html` shows the 4-tab shell with v1.0 dark editorial identity on mobile + desktop.
    - Owner confirms criterion 3: Accueil/Découverte/Biblio render identically to v1.0 (zero regression).
    - Owner confirms criterion 4: root QHSE Trainer unchanged.
    - Owner confirms criterion 5: `chassis.css` is loaded by both pages, no duplicated inline `<style>`.
    - Tab keyboard navigation and `location.hash` restore work as described.
  </acceptance_criteria>
  <done>Owner has confirmed all 5 ROADMAP Phase 1 success criteria, tab keyboard navigation, and hash restore hold on the live deployed URL.</done>
  <resume-signal>Type "approuvé" if all 5 ROADMAP Phase 1 success criteria hold on the live URL, or list the criteria that failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → static files | Only boundary. No server, no input fields, no auth, no DB. `outils.html`, `chassis.css`, `index.html` are static assets on Vercel edge. |
| User click → in-page navigation | The `#outils` anchor and the `outils.html` link are navigation targets — relevant only for open-redirect / tabnabbing classes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering / reverse tabnabbing | Gateway link to `outils.html`; outils.html back-link | mitigate | D-09: internal links are same-tab and relative (`outils.html`, `index.html`) with NO `target="_blank"` — no `window.opener` exposure. Acceptance criteria assert no `target="_blank"` on these internal links. External GitHub footer link keeps `rel="noopener noreferrer"` (copied verbatim from v1.0). |
| T-01-06 | Tampering (XSS) | outils.html inline tab `<script>` | mitigate | Script reads only `location.hash` and compares it (strict `===`) to a fixed allowlist of 4 hardcoded tab ids; the value is never written to `innerHTML`/`document.write`/`eval`. No user-supplied string is interpolated into the DOM. No inline `on*` handlers (acceptance-asserted). |
| T-01-07 | Information disclosure | New external script/CDN/dependency | accept | Zero external scripts; only the same Google Fonts CSS2 `<link>` already trusted by v1.0 is reused. Acceptance criteria assert no `outils-data.js` and no external script is added in Phase 1. |
| T-01-08 | Denial of service | Broken `chassis.css` relative path on `outils.html` → unstyled page | mitigate | `outils.html` sits in the same `qhse-cesi/` dir as `chassis.css`; relative `href="chassis.css"` resolves. Owner live verification (Task 3) confirms styled render before acceptance. |
| T-01-09 | Tampering | Regression of plan 01-01 (chassis link lost / `<style>` reintroduced) | mitigate | Task 2 acceptance criteria explicitly assert the `chassis.css` `<link>` is still present and zero `<style` substrings remain. |
| T-01-10 | Elevation of privilege | n/a — no auth/roles | accept | Static single-user site; no privilege model exists. |

No HIGH-severity threats: no backend, no user input, no DB, no external executable dependency. The only executable code is a ~25-line strict-mode IIFE that switches tabs from a hardcoded id allowlist and never writes untrusted data to the DOM. Reverse-tabnabbing is structurally excluded by the same-tab internal-link decision (D-09).
</threat_model>

<verification>
- `node` assertions: outils.html has chassis link, 4 ARIA tabs in locked order, dated placeholders, strict-mode IIFE with hash + arrow-key handling, no inline `<style>`, no `outils-data.js`, no external script, internal links same-tab.
- `node` assertions: index.html nav `<li>` and `#outils` section no longer `hidden`; gateway links to `outils.html` same-tab; old "Réservé V2" copy gone; plan 01-01's chassis `<link>` intact; no `<style>` reintroduced.
- `git status` shows only the three intended `qhse-cesi/` files across both plans; repo-root `index.html` and workflow file untouched.
- Owner live-URL verification of all 5 ROADMAP Phase 1 success criteria, keyboard tab nav, and hash restore.
</verification>

<success_criteria>
- SHELL-02: `outils.html` exists with the v1.0 dark editorial identity and the 4-tab nav shell in locked order.
- SHELL-03: `#outils` nav item and section un-hidden; section describes the 4 modes and links to `outils.html`.
- SHELL-04: v1.0 Hub reading sections and root QHSE Trainer unchanged; plan 01-01 chassis link preserved.
- SHELL-05: `outils.html` is usable on mobile + desktop, dark default, zero build step (relative `<link>`/`<script>` only; no bundler).
- PERSIST-02: no backend/accounts/sync/runtime generation; structure only — hand-authored, static.
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-gateway/01-02-SUMMARY.md`.
</output>
