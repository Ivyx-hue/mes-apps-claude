---
phase: 01-shell-gateway
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - qhse-cesi/chassis.css
  - qhse-cesi/index.html
autonomous: false
requirements: [SHELL-01, SHELL-04, PERSIST-02]
user_setup: []

must_haves:
  truths:
    - "D-01: qhse-cesi/chassis.css is a byte-identical verbatim extraction of the index.html inline <style> body (lines 15–640); no reorganization, rule cleanup, or renaming (non-mutation diff passes)"
    - "D-02: qhse-cesi/index.html has no inline <style> block and instead references chassis.css via a single <link rel=\"stylesheet\" href=\"chassis.css\">; no build step or preprocessor"
    - "D-03: page-specific rules (Biblio cards, Découverte prose) stay inside chassis.css as accepted harmless dead CSS; no shared-vs-specific split is attempted"
    - "The v1.0 Hub (Accueil, Découverte, Biblio) renders byte-for-byte identical CSS rules to before the extraction"
    - "No build step, npm dependency, or preprocessor is introduced"
    - "The root QHSE Trainer (/index.html at repo root) is not touched"
  artifacts:
    - path: "qhse-cesi/chassis.css"
      provides: "Shared chassis stylesheet — verbatim extraction of the v1.0 inline <style> body"
      contains: "@layer reset, tokens, base, components, utilities;"
      min_lines: 600
    - path: "qhse-cesi/index.html"
      provides: "v1.0 Hub now linking chassis.css instead of inlining it"
      contains: "<link rel=\"stylesheet\" href=\"chassis.css\">"
  key_links:
    - from: "qhse-cesi/index.html"
      to: "qhse-cesi/chassis.css"
      via: "<link rel=stylesheet> in <head>"
      pattern: "link rel=.stylesheet. href=.chassis\\.css"
---

<objective>
Extract the inline `<style>` block from `qhse-cesi/index.html` (lines 15–640, the CSS body between the `<style>` and `</style>` tags) verbatim into a new `qhse-cesi/chassis.css`, then replace that inline block in `index.html` with a single `<link rel="stylesheet" href="chassis.css">`.

Purpose: Establish the shared chassis stylesheet that both `index.html` and the future `outils.html` will load — the architectural foundation for v2.0 (D-01, D-02, SHELL-01). The extraction must be provably non-mutating so the v1.0 Hub has zero visual/functional regression (D-01, SHELL-04 hard constraint).
Output: New file `qhse-cesi/chassis.css`; modified `qhse-cesi/index.html` (single edit: `<style>`-block → `<link>`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shell-gateway/01-CONTEXT.md
@.planning/phases/01-shell-gateway/01-PATTERNS.md
@.planning/V2-ETUDE-SPEC.md
@qhse-cesi/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract chassis.css verbatim from index.html and swap the inline block for a link</name>
  <files>qhse-cesi/chassis.css, qhse-cesi/index.html</files>
  <read_first>
    - qhse-cesi/index.html (FULL file — never assume line numbers; re-verify that `<style>` is the opening tag and `</style>` the closing tag of the inline block before extracting)
    - .planning/phases/01-shell-gateway/01-PATTERNS.md (§ "qhse-cesi/chassis.css (stylesheet, verbatim extraction)" and § "qhse-cesi/index.html — Edit A" — exact boundaries and the byte-identical constraint)
    - .planning/phases/01-shell-gateway/01-CONTEXT.md (D-01, D-02, D-03 — locked verbatim-extraction decisions; do not re-litigate)
  </read_first>
  <action>
    Create `qhse-cesi/chassis.css`. Its content is the EXACT body of the inline `<style>` block in `qhse-cesi/index.html` — every character between (but NOT including) the `<style>` open tag and the `</style>` close tag. Currently the `<style>` open tag is at line 15 and the `</style>` close tag is at line 640; re-confirm these by reading the file before extracting. Copy lines 16 through 639 inclusive verbatim into `chassis.css`. Do NOT reorder rules, rename selectors, clean up "dead" CSS, reformat, change whitespace semantics, or alter `@layer` ordering — this is a byte-level move per D-01/SHELL-04. Leading per-line indentation MAY be left as-is (do not bother stripping it) — the only requirement is the CSS rule bytes are unchanged. The file must begin with the comment `/* CSS layer order — declared once. Reset always loses to component styles. */` followed by `@layer reset, tokens, base, components, utilities;` and end with the closing of the print `@media` block (`section { break-inside: avoid-page; }` then `}` then `}`).

    Then edit `qhse-cesi/index.html`: delete the entire inline block — the `<style>` tag line, all CSS body lines, and the `</style>` tag line (currently lines 15–640 inclusive) — and replace it with the single line `  <link rel="stylesheet" href="chassis.css">` placed immediately after the Google Fonts `<link href="https://fonts.googleapis.com/css2?...">` line and immediately before `</head>`. This is the ONLY edit to `index.html` in this plan. Do NOT touch the nav `<li hidden>` (line 674) or the `<section id="outils" … hidden>` (line 793) — those belong to plan 01-02. Do NOT touch the repo-root `/index.html` (frozen QHSE Trainer). Do NOT modify `.github/workflows/deploy.yml`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('qhse-cesi/index.html','utf8');const c=fs.readFileSync('qhse-cesi/chassis.css','utf8');if(/<style[\s>]/.test(h)){throw new Error('index.html still has a <style> block')}if(!/<link rel=\"stylesheet\" href=\"chassis\.css\">/.test(h)){throw new Error('index.html missing chassis.css link')}if(!/@layer reset, tokens, base, components, utilities;/.test(c)){throw new Error('chassis.css missing layer declaration')}if(c.split(/\n/).length<600){throw new Error('chassis.css suspiciously short — extraction likely truncated')}console.log('OK lines='+c.split(/\n/).length)"</automated>
  </verify>
  <acceptance_criteria>
    - `qhse-cesi/chassis.css` exists and is at least 600 lines.
    - `qhse-cesi/chassis.css` first non-blank content is `/* CSS layer order — declared once. ... */` then `@layer reset, tokens, base, components, utilities;` (same as the former inline block start).
    - `qhse-cesi/chassis.css` ends with the print `@media` block closing braces (last meaningful rule `section { break-inside: avoid-page; }`).
    - Non-mutation proof: extract the CSS body that was removed from `index.html` (git: `git show HEAD:qhse-cesi/index.html` → lines between `<style>` and `</style>`) and assert it equals `chassis.css` byte-for-byte modulo only leading indentation — run `node -e` diff: strip leading whitespace from each line of both the original inline body and chassis.css, join with `\n`, assert strict string equality; print `IDENTICAL` on success.
    - `qhse-cesi/index.html` contains zero `<style` substrings.
    - `qhse-cesi/index.html` contains exactly one `<link rel="stylesheet" href="chassis.css">`, located after the Google Fonts stylesheet `<link>` and before `</head>`.
    - `git status --porcelain` shows only `qhse-cesi/chassis.css` (new) and `qhse-cesi/index.html` (modified) — no change to repo-root `index.html` or `.github/workflows/deploy.yml`.
  </acceptance_criteria>
  <done>chassis.css exists as a verbatim (modulo indentation) extraction; index.html loads it via a single &lt;link&gt; and has no inline &lt;style&gt;; non-mutation diff prints IDENTICAL; no other files changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner confirms zero visual regression on the v1.0 Hub after chassis extraction</name>
  <files>qhse-cesi/index.html, qhse-cesi/chassis.css</files>
  <action>This is a human-verification checkpoint — no code is written. The executor pauses and presents the verification steps below to the owner. The executor must NOT push to git in this plan; the commit/push happens at the end of plan 01-02 so the deployed surface ships coherently (token-conscious atomic ship).</action>
  <what-built>
    `qhse-cesi/chassis.css` was created as a byte-identical (modulo indentation) extraction of the former inline `<style>` block; `qhse-cesi/index.html` now loads it via `<link rel="stylesheet" href="chassis.css">`. Local automated checks already proved the CSS body is unchanged and the link is wired. Owner verifies locally by opening the file.
  </what-built>
  <how-to-verify>
    1. Open `qhse-cesi/index.html` in a browser (double-click the local file, or serve the `qhse-cesi/` folder with any static server so `chassis.css` resolves via the relative path).
    2. Confirm the page looks identical to the live v1.0 Hub at https://mes-apps-claude.vercel.app/qhse-cesi/ — same warm-dark background, Fraunces headings, Inter body, the Accueil / Découverte / Biblio sections, the sticky header, the burger menu on mobile width.
    3. Resize to a narrow (mobile) width and confirm the burger menu still opens/closes and layout reflows as before.
    4. Confirm the `#outils` section is NOT yet visible / nav still has no "Outils" item (that is plan 01-02's job — its absence here is expected and correct).
    5. Optionally open the root QHSE Trainer file at the repo root `index.html` and confirm it is untouched and still works.
  </how-to-verify>
  <read_first>
    - qhse-cesi/index.html
    - qhse-cesi/chassis.css
  </read_first>
  <verify>Owner visually compares the locally-opened Hub against the live v1.0 URL and confirms no difference.</verify>
  <acceptance_criteria>
    - Owner confirms the Hub renders with no visible difference from the live v1.0 (colors, fonts, spacing, all three reading sections, sticky header, mobile burger).
    - Owner confirms the "Outils" nav item / `#outils` section is still absent (deferred to plan 01-02 — expected).
  </acceptance_criteria>
  <done>Owner has visually confirmed zero regression on the v1.0 Hub and acknowledged the Outils surface is intentionally still absent.</done>
  <resume-signal>Type "approuvé" if the Hub is visually/functionally identical to v1.0, or describe any visual difference observed.</resume-signal>
</task>

</tasks>

<decision_traceability>
## Decision Traceability

Locked decisions from `.planning/phases/01-shell-gateway/01-CONTEXT.md` implemented by this plan, mapped to the task that delivers each one (citations only — no task logic was changed by this mapping):

- D-01 → Task 1: chassis.css is a byte-identical verbatim extraction of index.html lines 15–640 (no reorder/cleanup/rename); non-mutation diff asserts byte equality modulo leading indentation.
- D-02 → Task 1: index.html references chassis.css via a single `<link rel="stylesheet" href="chassis.css">`, inline `<style>` removed, no build step or preprocessor. (D-02 is also implemented for outils.html in plan 01-02.)
- D-03 → Task 1: page-specific rules (Biblio cards, Découverte prose) remain inside chassis.css as accepted harmless dead CSS; no shared-vs-specific split is attempted (verbatim extraction inherently preserves this).
</decision_traceability>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → static files | The only boundary. No server, no user input, no auth, no DB. `chassis.css` is a static asset served by Vercel edge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `chassis.css` extraction altering v1.0 rendering | mitigate | Byte-level non-mutation diff in acceptance criteria (stripped-indentation strict-equality assertion) + owner visual confirmation. |
| T-01-02 | Information disclosure | New external resource pulled in by the refactor | accept | No new external script/CDN/font is added; `chassis.css` is same-origin relative. Verified by the "zero `<style`, exactly one local `<link href="chassis.css">`" assertion — no `http`-scheme asset introduced. |
| T-01-03 | Denial of service | Broken relative path → unstyled page deployed | mitigate | Relative `href="chassis.css"` resolves alongside `index.html` in `qhse-cesi/`; owner local-open verification (Task 2) confirms styles load before any push. |
| T-01-04 | Elevation of privilege | n/a — no auth/roles exist | accept | Static single-user reading site; no privilege model exists to elevate. |

No HIGH-severity threats: this plan adds zero backend, zero input handling, zero external dependency, and zero executable code (CSS only). The single realistic risk (a non-faithful extraction) is fully mitigated by the byte-level diff acceptance criterion.
</threat_model>

<verification>
- `node` diff proves the extracted CSS body equals the removed inline body (modulo leading indentation).
- `index.html` has zero `<style` and exactly one `<link href="chassis.css">`.
- `git status` shows only the two intended files changed; repo-root `index.html` and the workflow file untouched.
- Owner visually confirms zero regression on the v1.0 Hub (Accueil/Découverte/Biblio, header, mobile burger).
</verification>

<success_criteria>
- SHELL-01 (partial): `chassis.css` exists as a verbatim extraction, loaded by `index.html` via `<link>`, no build step.
- SHELL-04 (partial): v1.0 Hub renders identically; repo-root QHSE Trainer untouched.
- PERSIST-02 (partial): no backend, no npm, no preprocessor introduced — pure file split.
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-gateway/01-01-SUMMARY.md`.
</output>
