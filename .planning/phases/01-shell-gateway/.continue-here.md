---
context: phase
phase: 01-shell-gateway
task: 0
total_tasks: 0
status: paused
last_updated: 2026-05-16T15:10:26.527Z
---

# Phase 1 "Shell & Gateway" — Paused at discuss→plan boundary

_No blocking constraints. This session was a clean discuss-phase completion — nothing was discovered through failure._

<current_state>
v2.0 "Étude" milestone, Phase 1 "Shell & Gateway". The **discuss-phase workflow is fully
complete and committed**. `01-CONTEXT.md` (scope boundary + 9 locked decisions D-01..D-09 +
canonical refs + code context) and `01-DISCUSSION-LOG.md` (alternatives + rationale audit
trail) exist in this directory. Both committed (10549f9, 9677f1f) and pushed to origin/main.

**No PLAN.md yet. No code touched.** Working tree clean (untracked `.claude/` is
GSD/superpowers config, unrelated to phase work). This is a pristine handoff — the next
step is planning, nothing is half-done.
</current_state>

<completed_work>

- Task 1: Phase 1 discuss-phase — scope boundary + 9 locked decisions (D-01..D-09) — Done (10549f9)
- Task 2: `01-CONTEXT.md` + `01-DISCUSSION-LOG.md` authored — Done (10549f9)
- Task 3: Project state/memory recorded, pushed to origin/main — Done (9677f1f)
</completed_work>

<remaining_work>

- Task 4: Run `/gsd-plan-phase 1` → produce `PLAN.md` (task decomposition) — Not started
- Task 5: Execute Phase 1 (chassis.css extraction, outils.html tab shell, #outils gateway un-hide) — Not started
</remaining_work>

<decisions_made>
The 9 locked decisions live in `01-CONTEXT.md` (D-01..D-09). The load-bearing ones:

- **D-01:** Extract `index.html` `<style>` (l.15–640, ~626 lines) **verbatim** into
  `chassis.css` — byte-identical, no reorg/cleanup/rename. SHELL-04 (zero v1.0 regression)
  is the governing hard constraint; a non-mutating move is the only provably safe approach.
- **D-03:** Page-specific CSS (Biblio/Découverte) stays in `chassis.css` as harmless dead
  CSS on `outils.html`. Splitting shared-vs-specific was rejected — classification risk →
  v1.0 regression.
- **D-05:** `outils.html` tab switching = minimal vanilla JS + proper ARIA tab pattern
  (NOT a CSS-only radio hack). Deliberate scoped departure from v1.0's CSS-only philosophy
  because `outils.html` is inherently a JS app (P3–P5). Hub's CSS-only burger/scrollspy
  stay untouched.
- **D-07/D-08:** Un-hide reserved `hidden` attrs at `index.html:674` (nav `<li>`) and
  `:793` (section); rewrite gateway copy to editorial paragraph + 4-mode mini-list +
  primary link to `outils.html`. Only structural edits to frozen `index.html` besides the
  `<link>` swap.
- **D-09:** `outils.html` link opens same tab; `#outils` nav item stays `href="#outils"`
  in-page anchor (consistent with all other Hub nav).
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/V2-ETUDE-SPEC.md` — owner-approved milestone design, locked D-V2-01..06; Architecture section specifies the chassis.css / outils.html / outils-data.js split, zero build, the Hub-gateway un-hide path. **Do not re-litigate its locked decisions.**
2. `.planning/phases/01-shell-gateway/01-CONTEXT.md` — this phase's boundary + D-01..D-09 + canonical refs + code landmarks (line numbers in `index.html`).
3. `.planning/phases/01-shell-gateway/01-DISCUSSION-LOG.md` — why each decision beat its alternatives (avoid re-deriving).
4. `.planning/ROADMAP.md` — Phase 1 goal + 5 owner-verifiable success criteria.
5. `.planning/REQUIREMENTS.md` — SHELL-01..05, PERSIST-02 full text + traceability.

## Critical Anti-Patterns (do NOT repeat these)
- None discovered this session. Forward-looking guardrail (from the locked context, not a past failure): the `chassis.css` extraction must be **verbatim** — a rendered diff of v1.0 Hub pages before/after must be empty. Any "while I'm in here" CSS cleanup violates SHELL-04 and is out of scope.

## Infrastructure State
- Git: branch `main`, clean working tree, in sync with origin/main (HEAD 9677f1f). Untracked `.claude/` only.
- No running servers/watchers. Deploy pipeline unchanged (push `main` → GitHub Actions → Vercel, ~60s).
- No code changes pending. v1.0 Hub live and frozen at `…/qhse-cesi/`.

<context>
This is the cleanest possible pause: a planning boundary. Phase 1 is fully discussed and
locked; the mental model is "the structure is decided, now decompose it into executable
tasks." Phase 1 is pure scaffolding (extract chassis CSS, scaffold empty 4-tab
`outils.html`, un-hide the `#outils` gateway) — NO study-mode logic, NO content bank, NO
SM-2 (those are Phases 2–5). The single governing constraint across the whole phase is
SHELL-04: zero visual/functional regression on the v1.0 Hub and the frozen root QHSE
Trainer.

User's stated planning preferences for resumption: standard planning flow with mini-research
on CSS extraction is reasonable; `--skip-research` flag available; `/gsd-ui-phase 1`
available but low value since tabs/identity reuse the v1.0 chassis. Optionally re-read or
edit `01-CONTEXT.md` before planning if anything needs adjustment.
</context>

<next_action>
Start with: `/gsd-plan-phase 1`. Before it decomposes tasks, ensure the planner reads
`.planning/V2-ETUDE-SPEC.md` and `01-CONTEXT.md` (canonical refs). Standard flow with
mini-research on the CSS extraction approach is appropriate; pass `--skip-research` if
the extraction approach feels already settled by D-01..D-03.
</next_action>
