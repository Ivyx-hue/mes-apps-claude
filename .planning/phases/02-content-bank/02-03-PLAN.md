---
phase: 02-content-bank
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - qhse-cesi/outils-data.js
autonomous: true
requirements: [BANK-01, BANK-02, BANK-03, BANK-04, BANK-05]
user_setup: []

must_haves:
  truths:
    - "Batch C (tms 14 + risque-routier 12 + rps 14 = 40 items) is present, schema-compliant"
    - "Every Batch C source.url is a content-verified pédagogique INRS / ameli risques-pro page (D-05) — never a search/index/soft-404 page (D-09)"
    - "tms, risque-routier, rps are each a separate D-01 theme slug; no value outside the 15-slug set"
    - "Every Batch C item carries the full source object (authority, ref, url, verified); regulatory ref in source.ref, Légifrance only in explanation (D-07)"
    - "Exam-critical stats are exactly right: TMS ~88% maladies pro, risque routier ~30% AT mortels, RPS 3 composantes (D-10/D-11/D-12)"
  artifacts:
    - path: "qhse-cesi/outils-data.js"
      provides: "40 items appended (tms 14, risque-routier 12, rps 14); running total >= 122"
      contains: "risque-routier"
  key_links:
    - from: "tms / risque-routier / rps items"
      to: "INRS /risques/<slug>/ce-qu-il-faut-retenir.html + ameli risques-pro pages"
      via: "content-verified source.url (D-05 pédagogique-first)"
      pattern: "(tms|risque-routier|rps)"
---

<objective>
Author Batch C — three INRS `/risques/` themes: `tms` (14), `risque-routier` (12), `rps` (14) = 40 items. All three have a pre-vetted `[CV]` INRS "ce qu'il faut retenir" page (and TMS additionally a `[CV]` ameli risques-pro page), so this is a clean-backbone batch — the dominant risk is statistical/regulatory accuracy, not URL discovery.

Purpose: TMS (88% des maladies pro), risque routier (~30% des AT mortels) and RPS (stress + violences internes + violences externes) are high-frequency exam content with precise statistics that must be exact — a wrong percentage becomes false revision material for a real diploma.
Output: 40 schema-compliant, source-verified items appended to `window.BANK`; one atomic commit pushed to `main`. Fully autonomous (all backbone URLs already `[CV]` in RESEARCH).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/V2-ETUDE-SPEC.md
@.planning/phases/02-content-bank/02-CONTEXT.md
@.planning/phases/02-content-bank/02-RESEARCH.md
@.planning/phases/02-content-bank/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Batch C — tms (14) + risque-routier (12) + rps (14), content-verify every source.url, commit atomically</name>
  <files>qhse-cesi/outils-data.js</files>
  <read_first>
    - .planning/phases/02-content-bank/02-RESEARCH.md (Theme: tms lines 309-324 — [CV] INRS + ameli URLs + accuracy anchors 88% MP / 3 catégories facteurs / TMS-Pros 4 étapes / PRAP; Theme: risque-routier lines 327-342 — [CV] INRS URL + ~30% AT mortels + mission vs trajet + 3 axes prévention; Theme: rps lines 365-380 — [CV] INRS URL + stress/violences internes/violences externes + burnout + L1152-1 harcèlement moral + RPS-DU/Sumer; Content-Verification Protocol lines 510-547; Domain Accuracy Anchors lines 605-648; Common Pitfalls lines 652-702)
    - .planning/phases/02-content-bank/02-CONTEXT.md (D-05 pédagogique-first, D-07 Légifrance in explanation only, D-08 authority map row tms/risque-routier/rps = INRS + ameli risques pro, D-09 surface gaps, D-10 answer/explanation split, D-11 QCM answer/explanation, D-12 plausible distractors, D-13 difficulty rubric)
    - C:\Users\Lasmoles\.claude\projects\C--Users-Lasmoles-mes-apps-claude\memory\feedback_verify_links_before_ship.md (content-verified URL discipline — real title + topic match + soft-404 grep; HTTP-status-only audits BANNED)
    - qhse-cesi/outils-data.js (the file being appended to — confirm window.BANK is the global, append before the closing `]; // end window.BANK`)
  </read_first>
  <action>Append 40 items to `window.BANK`: 14 `theme:'tms'`, 12 `theme:'risque-routier'`, 14 `theme:'rps'`. Per-theme mix ≈ flashcard:qcm from RESEARCH §Volume Distribution (tms 8f/6q, risque-routier 7f/5q, rps 8f/6q). `id` = `tms-flashcard-001`, `risque-routier-qcm-001`, `rps-flashcard-001`, etc. (zero-padded NNN, sequential per theme+type). Primary `source.url` MUST be a pédagogique INRS/ameli page from RESEARCH §tms / §risque-routier / §rps: `https://www.inrs.fr/risques/tms-troubles-musculosquelettiques/ce-qu-il-faut-retenir.html`, `https://www.ameli.fr/entreprise/sante-travail/risques/troubles-musculosquelettiques-tms/pourquoi-comment-agir`, `https://www.inrs.fr/risques/routiers/ce-qu-il-faut-retenir.html`, `https://www.inrs.fr/risques/psychosociaux/ce-qu-il-faut-retenir.html` (and the `/prevention.html` / `/demarche-prevention.html` sub-pages listed in RESEARCH where a prevention-specific item needs them). `source.authority` = `INRS` or `Assurance Maladie - ameli` per the page used; `source.ref` = exact Code du travail article or statistical authority where applicable (e.g. `Art. L1152-1 Code du travail` for harcèlement moral, `INRS - statistiques TMS` / `INRS - statistiques accidents routiers du travail` for stat-only items). Légifrance deep-links (e.g. harcèlement moral L1152-1) go in `explanation` prose parentheticals ONLY, never in `source.url` (D-07). Get exam-critical facts EXACTLY right from the RESEARCH accuracy anchors: TMS = ~88% (ameli 2024) / >80% (INRS) des maladies professionnelles reconnues; 3 catégories de facteurs (biomécaniques, psychosociaux, environnementaux); TMS-Pros = programme Assurance Maladie en 4 étapes; PRAP = Prévention des Risques liés à l'Activité Physique. Risque routier: mission (cadre d'une mission pro) vs trajet (domicile↔travail); ~30% des AT mortels; 3 axes de prévention (organisation des déplacements, ressources, compétences); intégration obligatoire au DUERP. RPS: 3 composantes = stress + violences internes (harcèlement moral/sexuel) + violences externes (incivilités/agressions); burnout = épuisement consécutif à un stress chronique au travail; harcèlement moral = Art. L1152-1 (NOT L1153-1 which is harcèlement sexuel — common trap); prévention RPS = démarche collective centrée sur le travail/organisation, pas analyse psychologique individuelle; questionnaires INRS RPS-DU et Sumer. QCM distractors per D-12 = real-domain confusions (88% vs 80% vs 50%; L1152-1 vs L1153-1; mission vs trajet swapped; "analyse individuelle" as a false prevention approach; OHSAS-style false-friends). D-10/D-11: `answer` terse recall-grade, `explanation` carries the articulation/mnemonic and (for QCM) why each distractor is wrong. Then run the RESEARCH §Content-Verification curl snippet against EVERY distinct source.url used in this batch; set each item's `source.verified` to today's actual content-verification date. If any URL fails (soft-404, generic title, Cloudflare, wrong-doc), per D-09 surface the gap in the SUMMARY and either substitute a verified RESEARCH-listed alternative (e.g. the `/prevention.html` sibling) or drop the item — never ship a status-only/unverified URL. Update the `// Total items:` trailer. Then create ONE atomic commit `feat(bank): add tms/risque-routier/rps batch (40 items)` and push to `main` (token-conscious: never leave the batch uncommitted).</action>
  <verify>
    <automated>node "C:/Users/Lasmoles/mes-apps-claude/.planning/phases/02-content-bank/verify-bank.cjs" tms:14 risque-routier:12 rps:14 iso-45001:18 iso-9001:14 iso-14001:14 duerp:18 principes-generaux:18</automated>
  </verify>
  <acceptance_criteria>
    - `BANK.filter(i=>i.theme==='tms').length >= 14` AND `risque-routier >= 12` AND `rps >= 14` (or fewer with a documented D-09 gap in SUMMARY)
    - verify-bank.cjs exits 0 (all schema fields present, every theme within the 15-slug set, all QCMs have a `choices` array + integer `correct` with `0 <= correct < choices.length`, zero `source.url` contains `legifrance`)
    - Every Batch C item has non-empty `id,type,theme,question,answer,explanation` + complete `source{authority,ref,url,verified}`; every `difficulty` in {1,2,3}
    - Every distinct Batch C `source.url` was run through the RESEARCH §Content-Verification curl snippet: HTTP 200 + non-generic `<title>` matching the item topic + no soft-404 grep hit (status-only checks are a FAIL); each `source.verified` = the real verification date
    - Stat/article accuracy cross-checked against RESEARCH anchors: TMS ≈88% MP, risque routier ≈30% AT mortels, harcèlement moral = L1152-1 (not L1153-1) — reflected in answer/explanation text
    - Any URL that failed verification is recorded as a surfaced gap in the SUMMARY (D-09) — no unverified item shipped
    - One atomic commit pushed to `main` (`git status` shows branch up to date)
  </acceptance_criteria>
  <done>40 schema-compliant, source-verified items (tms 14 + risque-routier 12 + rps 14) appended on content-verified INRS/ameli pédagogique pages; exam-critical stats and article numbers cross-checked; committed atomically and pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content → owner's exam revision | A wrong statistic (TMS %, AT-mortels %) or article (L1152-1 vs L1153-1) becomes false study material for a real diploma |
| external source URL → bank `source.url` | A wrong/search/soft-404 INRS or ameli URL is shipped as an authoritative citation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering (content integrity) | tms/risque-routier/rps item `answer`/`ref` | mitigate | Task 1 cross-checks every stat/article against RESEARCH §Domain Accuracy Anchors (TMS ≈88% MP; risque routier ≈30% AT mortels; L1152-1 vs L1153-1; 3 composantes RPS); D-09 surface-gap rule — no unsourced claim ships |
| T-02-08 | Spoofing (hostile/wrong link) | `source.url` on every Batch C item | mitigate | Task 1 runs the RESEARCH §Content-Verification curl snippet (real `<title>` + topic match + soft-404 grep) on every URL; status-only checks are a FAIL; `verified` set to the real date |
| T-02-09 | Information disclosure / LEGAL scope | linked INRS/ameli pages | mitigate | D-06 free-authoritative-page-only + LEGAL.md link-only; no `.pdf` hosted under `/qhse-cesi/`; Batch C uses only free INRS/ameli pédagogique pages |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase; reuses Plan 01 stdlib `verify-bank.cjs` — no supply-chain surface |
</threat_model>

<verification>
- `node verify-bank.cjs tms:14 risque-routier:12 rps:14 iso-45001:18 iso-9001:14 iso-14001:14 duerp:18 principes-generaux:18` exits 0
- Every distinct Batch C `source.url` content-verified per the RESEARCH protocol; gaps (if any) surfaced in SUMMARY (D-09)
- Exam-critical stats/articles cross-checked against RESEARCH §Domain Accuracy Anchors
- One atomic commit pushed to `main`
</verification>

<success_criteria>
- 40 items added (tms 14 + risque-routier 12 + rps 14); `BANK.length >= 122` after this plan
- All three themes filterable as separate D-01 slugs
- All Batch C source URLs content-verified; gaps (if any) surfaced per D-09
</success_criteria>

<output>
Create `.planning/phases/02-content-bank/02-03-SUMMARY.md` — record the Batch C verified URL list, any surfaced source gaps (D-09), the per-theme verified counts, and the running `BANK.length`.
</output>
</content>
