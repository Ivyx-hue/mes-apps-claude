---
phase: 02-content-bank
plan: 06
type: execute
wave: 6
depends_on: [02-05]
files_modified:
  - qhse-cesi/outils-data.js
autonomous: false
requirements: [BANK-01, BANK-02, BANK-03, BANK-04, BANK-05]
user_setup: []

must_haves:
  truths:
    - "Batch F (metiers 12 + rncp 13 = 25 items, plus the SPA-deferred acronymes from Batch E) is present, schema-compliant"
    - "Every MétierScope and France compétences source.url was human-eyeballed in a real browser before items shipped (RESEARCH Open Question 2 + SPA checkpoint rule) — curl-200 is explicitly NOT sufficient for these domains"
    - "metiers and rncp are each a separate D-01 theme slug; rncp is the meta-theme for BC01–BC04 diploma-structure items (D-03) with no bc01..bc04 themes and no bloc schema field"
    - "RNCP41446 facts exactly right: BC01–BC04 titles, Niveau 6 (Bac+3), expiration 27-10-2030; ROME H1302/H1502/H1523/H1303 mapped (D-12 distractors)"
    - "Every Batch F item carries the full source object; SPA verification is the recorded human-eyeball date"
  artifacts:
    - path: "qhse-cesi/outils-data.js"
      provides: "25 items appended (metiers 12, rncp 13) + any SPA-deferred acronymes from Batch E; running total >= 220"
      contains: "rncp"
  key_links:
    - from: "metiers / rncp items"
      to: "francetravail MétierScope fiche-metier + francecompetences RNCP41446 (SPA — human-eyeballed)"
      via: "human-verified source.url (curl insufficient — blocking checkpoint)"
      pattern: "(metiers|rncp)"
---

<objective>
Author Batch F — `metiers` (12, ROME codes) + `rncp` (13, RNCP41446 / BC01–BC04 diploma structure per D-03) = 25 items, plus the SPA-only acronymes deferred from Batch E. Both source domains are JS SPAs: France Travail MétierScope (`candidat.francetravail.fr/metierscope/fiche-metier/...`) returns a ~5KB "Fiche Métier" skeleton to curl, and France compétences (`francecompetences.fr/recherche/rncp/41446/`) returns a generic "Rechercher une certification" title even though the RNCP content is embedded. Per the RESEARCH §Content-Verification SPA checkpoint rule and Open Question 2/4, curl-200 is explicitly NOT sufficient for these URLs — a blocking human browser eyeball is mandatory before any item cites them. This is why the plan is `autonomous: false`.

Purpose: Métiers/ROME and the RNCP41446 bloc structure are the diploma-context layer of the bank (D-03 meta-theme `rncp`). They are also the highest source-fragility surface in the phase — the SPA checkpoint is the controlled mitigation.
Output: 25 (+ deferred-acronyme) schema-compliant, human-source-verified items appended to `window.BANK`; one atomic commit pushed to `main`. Contains a blocking human-verify checkpoint for the MétierScope + France compétences SPA URLs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/V2-ETUDE-SPEC.md
@.planning/phases/02-content-bank/02-CONTEXT.md
@.planning/phases/02-content-bank/02-RESEARCH.md
@.planning/phases/02-content-bank/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Curl-pre-screen and stage the MétierScope + France compétences SPA URLs and the Batch-E-deferred acronyme list</name>
  <files>qhse-cesi/outils-data.js</files>
  <read_first>
    - .planning/phases/02-content-bank/02-RESEARCH.md (Theme: metiers lines 418-434 — [SPA] MétierScope H1302/H1502/H1523/H1303 URLs + ROME accuracy anchors + SPA mitigation strategy; Theme: rncp lines 479-494 — [SPA] francecompetences RNCP41446 URL + BC01–BC04 titles + Niveau 6 + expiration 27-10-2030 + ROME mapping; Open Questions 2 & 4 lines 730-733 + 763-764; Content-Verification Protocol lines 510-547 incl. the SPA checkpoint rule line 547; Pitfall 4 lines 684-692)
    - .planning/phases/02-content-bank/02-CONTEXT.md (D-03 rncp meta-theme, NO bc01..bc04 themes, NO bloc schema field; D-04 metiers first-class theme; D-08 authority map rows metiers=francetravail/Apec, rncp=France compétences; D-09 surface gaps)
    - .planning/phases/02-content-bank/02-05-SUMMARY.md (the list of SPA-only acronymes (RNCP/CFA/VAE/BC01–BC04) deferred from Batch E that this plan must absorb)
  </read_first>
  <action>Stage, for the checkpoint: (a) the four MétierScope URLs from RESEARCH §metiers (`https://candidat.francetravail.fr/metierscope/fiche-metier/H1302`, `.../H1502/responsable-qualite-en-industrie`, `.../H1523`, `.../H1303`); (b) the France compétences URL `https://www.francecompetences.fr/recherche/rncp/41446/`. Run the RESEARCH §Content-Verification curl snippet against each as a PRE-screen (expect SPA skeletons — record the curl title + body size as evidence the eyeball is necessary, NOT as a pass). Then read 02-05-SUMMARY.md and compile the explicit list of acronymes deferred from Batch E whose only source is the France compétences SPA (RNCP, CFA, VAE, BC01–BC04 expansions) — these will be authored in Task 3 under `theme:'acronymes'` once the SPA is human-confirmed. If MétierScope renders unreliably, note the RESEARCH fallback (`https://www.francetravail.fr/employeur/vos-recrutements/le-rome-et-les-fiches-metiers.html` + static fiche) for the checkpoint. Produce a short scouting note (the 5 SPA URLs + curl evidence + the deferred-acronyme list) for the checkpoint.</action>
  <verify>
    <automated>node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 SPA URLs (4 MétierScope + 1 France compétences) staged, each curl-pre-screened with the RESEARCH protocol; curl title + body size recorded as evidence (not as a pass)
    - The Batch-E-deferred acronyme list (SPA-only: RNCP/CFA/VAE/BC01–BC04 …) is compiled from 02-05-SUMMARY.md and explicitly listed for Task 3
    - The MétierScope static fallback (`francetravail.fr/.../le-rome-et-les-fiches-metiers.html`) is noted for the checkpoint in case rendering is unreliable
  </acceptance_criteria>
  <done>5 SPA source URLs + curl evidence + the Batch-E-deferred acronyme list staged in a scouting note for the blocking human-verify checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Five SPA source URLs (4 France Travail MétierScope fiche-métier pages for ROME H1302/H1502/H1523/H1303, + the France compétences RNCP41446 fiche), curl-pre-screened — all return SPA skeletons ("Fiche Métier" ~5KB / "Rechercher une certification" generic title) so curl is explicitly NOT sufficient (RESEARCH §Content-Verification SPA checkpoint rule + Open Question 2/4 + Pitfall 4). 25 metiers/rncp items plus the Batch-E-deferred SPA-only acronymes will cite these — they cannot ship until a human confirms each renders the real content in a real browser.</what-built>
  <how-to-verify>
    1. Open each of the 5 SPA URLs (provided in the Task 1 scouting note) in a real browser (Chrome AND Firefox if possible — RESEARCH Open Question 2 recommends both).
    2. For each MétierScope URL: confirm the page renders the actual fiche métier (missions, ROME code H13xx, salaires/conditions) — NOT a spinner, NOT a "Fiche Métier" empty skeleton, NOT a 404/redirect.
    3. For the France compétences RNCP41446 URL: confirm the page renders the actual certification fiche — BC01–BC04 bloc titles, Niveau 6, certificateur CESI, expiration 27-10-2030 — NOT a generic search page or spinner.
    4. For any URL that does NOT render reliably: it is a `GAP` — those items must use the RESEARCH fallback (`francetravail.fr/.../le-rome-et-les-fiches-metiers.html` + static fiche) or be dropped per D-09.
    5. Reply with one of: `approved: metiers=[<urls that render>] rncp=<url> fallback=<any>` (confirm or correct each), or describe what is broken / which are GAP.
  </how-to-verify>
  <resume-signal>Type `approved` with the confirmed/corrected SPA URLs (and any GAP/fallback), or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author Batch F — metiers (12) + rncp (13) + Batch-E-deferred acronymes, verify and commit atomically</name>
  <files>qhse-cesi/outils-data.js</files>
  <read_first>
    - .planning/phases/02-content-bank/02-RESEARCH.md (Theme metiers accuracy anchors lines 430-434 — H1302 mgmt/ingénierie HSE Bac+5, H1502 qualité, H1523 QSE Bac+3, H1303 technicien HSE Bac+2; Theme rncp accuracy anchors lines 486-494 — BC01 Construire / BC02 Améliorer / BC03 Manager les risques / BC04 Accompagner RSE-DD le SMQSE, Niveau 6 Bac+3, certificateur CESI, expiration 27-10-2030, ROME H1302/H1502/H1523; Domain Accuracy Anchors RNCP41446 table lines 640-648; Common Pitfalls 652-702)
    - .planning/phases/02-content-bank/02-CONTEXT.md (D-03 rncp meta-theme — topical items keep topical theme, NO bc01..bc04 themes, NO bloc schema field; D-04; D-08 rows; D-09; D-10; D-11; D-12; D-13)
    - C:\Users\Lasmoles\.claude\projects\C--Users-Lasmoles-mes-apps-claude\memory\feedback_verify_links_before_ship.md
    - qhse-cesi/outils-data.js (the file being appended to — append before the closing `]; // end window.BANK`)
  </read_first>
  <action>Append to `window.BANK`: 12 `theme:'metiers'`, 13 `theme:'rncp'`, plus the Batch-E-deferred SPA-only acronymes as `theme:'acronymes'` items (now sourceable on the human-confirmed France compétences URL). Per-theme mix from RESEARCH §Volume Distribution (metiers 7f/5q, rncp 8f/5q). `id` = `metiers-flashcard-001`, `rncp-qcm-001`, `acronymes-flashcard-0NN` (continue the acronymes numbering after Batch E — do NOT collide ids). Use the human-CONFIRMED-at-checkpoint URLs as `source.url` (or the confirmed fallback for any GAP). `source.authority` = `France Travail - MétierScope` / `France compétences` (or `France Travail` for the static fallback); `source.ref` = `code ROME H1302` (etc.) for metiers, `RNCP41446` / `RNCP41446 BC02` (etc.) for rncp — D-03: the bloc mapping stays in `ref`/`explanation`, never a schema field or a `bc0x` theme. Get exam-critical facts EXACTLY right: BC01 = "Construire le système de management QSE", BC02 = "Améliorer le système de management QSE", BC03 = "Manager les risques QSE", BC04 = "Accompagner l'organisme dans ses démarches RSE et de développement durable"; Niveau 6 (Bac+3); certificateur CESI; expiration de la fiche 27-10-2030; ROME associés H1302/H1502/H1523. Métiers: H1302 = management et ingénierie HSE industriels (typiquement Bac+5), H1502 = management et ingénierie qualité industrielle, H1523 = responsable QSE (Bac+3 — niveau du RNCP41446), H1303 = technicien HSE (Bac+2). QCM distractors per D-12 = real-domain confusions (BC01↔BC02 titles swapped; "Manager les risques" attributed to BC04; Niveau 5 vs 6 vs 7; H1302 vs H1303 vs H1523 swapped; OHSAS/false-friend). D-10/D-11: `answer` terse recall-grade, `explanation` carries the articulation and (for QCM) why each distractor is wrong; D-07 Légifrance only in explanation (rare here). For SPA sources, `source.verified` = the human-eyeball date from the checkpoint (NOT a curl date — Pitfall 4: curl-200 is insufficient). Any URL flagged GAP at the checkpoint: use the confirmed fallback or drop those items and record the gap in the SUMMARY (D-09). Update the `// Total items:` trailer. Then create ONE atomic commit `feat(bank): add metiers/rncp batch + deferred SPA acronymes (25+ items)` and push to `main`.</action>
  <verify>
    <automated>node "C:/Users/Lasmoles/mes-apps-claude/.planning/phases/02-content-bank/verify-bank.cjs" metiers:12 rncp:13 acronymes:20 icpe-seveso:12 calendrier:11 risque-chimique:18 espaces-confines:12 tms:14 risque-routier:12 rps:14 iso-45001:18 iso-9001:14 iso-14001:14 duerp:18 principes-generaux:18</automated>
  </verify>
  <acceptance_criteria>
    - `BANK.filter(i=>i.theme==='metiers').length >= 12` AND `rncp >= 13`; `acronymes` count grew by the absorbed Batch-E-deferred set (verifier `acronymes:20` still passes; SUMMARY states the new acronymes total)
    - verify-bank.cjs exits 0 (all schema fields present, every theme within the 15-slug set, all QCMs valid, zero `source.url` contains `legifrance`)
    - No `bc01`/`bc02`/`bc03`/`bc04` theme value exists and no `bloc` field was added (D-03 — bloc mapping stays in `ref`/`explanation`)
    - Every Batch F `source.url` is a human-confirmed-at-checkpoint URL (or confirmed fallback); `source.verified` = the human-eyeball date, NOT a curl-only date (Pitfall 4)
    - RNCP facts cross-checked against RESEARCH §Domain Accuracy Anchors: BC01–BC04 titles exact, Niveau 6, expiration 27-10-2030, ROME H1302/H1502/H1523 — reflected in answer/explanation text
    - Any GAP URL + any dropped/fallback item recorded in the SUMMARY (D-09) — no unverified item shipped
    - One atomic commit pushed to `main` (`git status` shows branch up to date)
  </acceptance_criteria>
  <done>25+ schema-compliant items (metiers 12 + rncp 13 + Batch-E-deferred acronymes) appended on human-confirmed SPA sources; D-03 honored (rncp meta-theme, no bc0x theme, no bloc field); committed atomically and pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content → owner's exam revision | A wrong BC0x title, wrong Niveau, or wrong ROME code becomes false diploma-structure study material |
| SPA source page → `source.url` | A MétierScope/France compétences page that curls 200 but renders empty/spinner is shipped as a verified citation (Pitfall 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Tampering (content integrity) | metiers/rncp item `answer`/`ref` | mitigate | Task 2 cross-checks against RESEARCH §Domain Accuracy Anchors (BC01–BC04 titles; Niveau 6; expiration 27-10-2030; ROME H1302/H1502/H1523/H1303); D-09 surface-gap rule |
| T-02-18 | Spoofing (SPA skeleton shipped as verified) | metiers/rncp/deferred-acronyme `source.url` | mitigate | Blocking human-verify checkpoint — browser eyeball each SPA URL renders the real fiche before items ship; `source.verified` = human-eyeball date, curl-200 explicitly insufficient (Pitfall 4 / SPA checkpoint rule) |
| T-02-19 | Information disclosure / LEGAL scope | francetravail / francecompetences pages | mitigate | D-06 free-authoritative-page-only + LEGAL.md link-only; no `.pdf` hosted under `/qhse-cesi/`; only free public pages used |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase; reuses Plan 01 stdlib `verify-bank.cjs` — no supply-chain surface |
</threat_model>

<verification>
- Blocking human-verify checkpoint passed (4 MétierScope + 1 France compétences SPA URLs confirmed to render the real fiche in a real browser)
- `node verify-bank.cjs metiers:12 rncp:13 acronymes:20 icpe-seveso:12 calendrier:11 risque-chimique:18 espaces-confines:12 tms:14 risque-routier:12 rps:14 iso-45001:18 iso-9001:14 iso-14001:14 duerp:18 principes-generaux:18` exits 0
- No `bc0x` theme / no `bloc` field (D-03); `source.verified` on SPA items = the human-eyeball date
- RNCP/ROME facts cross-checked against RESEARCH §Domain Accuracy Anchors
- One atomic commit pushed to `main`
</verification>

<success_criteria>
- 25+ items added (metiers 12 + rncp 13 + Batch-E-deferred acronymes); `BANK.length >= 220` after this plan
- metiers + rncp filterable as separate D-01 slugs; rncp is the BC01–BC04 meta-theme (D-03)
- All SPA source URLs human-confirmed; gaps (if any) surfaced per D-09
</success_criteria>

<output>
Create `.planning/phases/02-content-bank/02-06-SUMMARY.md` — record the human-confirmed SPA source URLs (+ human-eyeball dates), any GAP/fallback URLs, the absorbed Batch-E-deferred acronyme list + new acronymes total, the per-theme verified counts, and the running `BANK.length` (expect >= 220).
</output>
</content>
