---
phase: 02-content-bank
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - qhse-cesi/outils-data.js
autonomous: false
requirements: [BANK-01, BANK-02, BANK-03, BANK-04, BANK-05]
user_setup: []

must_haves:
  truths:
    - "Batch B (iso-45001 18 + iso-9001 14 + iso-14001 14 = 46 items) is present, schema-compliant"
    - "Each ISO theme is a separate filterable theme slug (iso-9001, iso-14001, iso-45001) — no single iso theme, no norm sub-field (D-02)"
    - "Every ISO source.url is a FREE authoritative page that actually covers the cited point — never a paywall/shop/iso.org-403 page (D-06)"
    - "The ISO-9001/14001/45001 free-source URLs were human-eyeballed before items shipped (RESEARCH open question 1)"
    - "Every Batch B item carries the full source object; ref = norm number + clause (e.g. ISO 45001:2018 §6.1.2)"
  artifacts:
    - path: "qhse-cesi/outils-data.js"
      provides: "46 ISO items appended (iso-45001 18, iso-9001 14, iso-14001 14)"
      contains: "iso-45001"
  key_links:
    - from: "iso-* items"
      to: "free authoritative source pages"
      via: "human-verified source.url (not iso.org/standard paywall)"
      pattern: "iso-(9001|14001|45001)"
---

<objective>
Author Batch B — the three ISO themes (`iso-45001` 18, `iso-9001` 14, `iso-14001` 14 = 46 items). This is the hardest batch: `iso.org` returns HTTP 403 to curl and the natural INRS ISO-45001 source uses the banned `media.html?refINRS=` redirect, so a free authoritative source per ISO theme must be human-eyeballed before items ship (RESEARCH Open Question 1).

Purpose: ISO norms are exam-heavy and exam-trap-rich (45001 replaces OHSAS 18001 not "ISO 18001"; 7 vs 8 quality principles; HLS §4–§10). The three separate theme slugs (D-02) enable per-norm Phase-5 fiches.
Output: 46 ISO items appended to `window.BANK`, one atomic commit. Contains a blocking human-verify checkpoint for the ISO free-source URLs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/V2-ETUDE-SPEC.md
@.planning/phases/02-content-bank/02-CONTEXT.md
@.planning/phases/02-content-bank/02-RESEARCH.md
@.planning/phases/02-content-bank/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scout and curl-pre-screen free authoritative ISO source pages</name>
  <files>qhse-cesi/outils-data.js</files>
  <read_first>
    - .planning/phases/02-content-bank/02-RESEARCH.md (Theme: iso-9001 lines 241-264, iso-14001 266-286, iso-45001 288-307 — candidate free pages + ISO accuracy anchors + the [HTTP403]/[AVOID media.html] flags; Open Questions line 725-733; Pitfall 5 lines 694-702 — never link paywall/shop; Content-Verification Protocol 510-547)
    - .planning/phases/02-content-bank/02-CONTEXT.md (D-02 three separate ISO themes no norm field; D-06 free authoritative page that actually covers the point; D-08 authority map ISO row; D-09 surface gaps)
  </read_first>
  <action>For each of `iso-9001`, `iso-14001`, `iso-45001`, identify a FREE authoritative French page that actually contains the cited clauses (D-06). Start from the RESEARCH candidates: for 9001/14001 try `qualiteperformance.org` ISO summary pages and `fr.wikipedia.org/wiki/ISO_9001` / `ISO_14001`; for 45001 try `preventionbtp.fr` ISO 45001 focus or an INRS `/risques/` or `/demarche/` static page (NOT `media.html?refINRS=` — banned redirect). Run the RESEARCH §Content-Verification curl snippet against each candidate. NEVER select `iso.org/standard/*` (HTTP 403 / paywall), `boutique.afnor.org`, or `shop.bsigroup.com` (Pitfall 5). Write the scouting result to `.planning/phases/02-content-bank/iso-scouting-note.txt` — one line per ISO theme recording the chosen URL + curl title + body size, or an explicit `GAP` marker for any theme where no free page passes curl (D-09). This note file is the artifact the human-verify checkpoint and Task 2 both read; it must not be empty.</action>
  <verify>
    <automated>node -e "require('fs').statSync('.planning/phases/02-content-bank/iso-scouting-note.txt')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-content-bank/iso-scouting-note.txt` exists and contains one line per ISO theme (iso-9001, iso-14001, iso-45001) — chosen free source URL + curl title + body size, or an explicit `GAP` marker
    - One candidate free source URL identified per ISO theme, each curl-screened with the RESEARCH protocol; title + body size recorded in the note file
    - Zero candidate is an `iso.org/standard`, `boutique.afnor.org`, or `shop.bsigroup.com` URL
    - Zero candidate uses the INRS `media.html?refINRS=` redirect pattern
    - Any ISO theme with no passing free source is explicitly written as `GAP` in the note file for the checkpoint (D-09 — surface, do not ship unverified)
  </acceptance_criteria>
  <done>`.planning/phases/02-content-bank/iso-scouting-note.txt` written with one curl-pre-screened free authoritative source URL (or an explicit GAP flag) per ISO theme, ready for the human-verify checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Three candidate free authoritative ISO source URLs (one each for iso-9001, iso-14001, iso-45001), curl-pre-screened and recorded in `.planning/phases/02-content-bank/iso-scouting-note.txt`. These are SPA/Cloudflare-prone domains and `iso.org` is paywalled — RESEARCH Open Question 1 requires a human eyeball that each chosen page actually renders the cited ISO clauses in a real browser before 46 items cite them.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/02-content-bank/iso-scouting-note.txt` and read the chosen URL (or `GAP` marker) for each of the three ISO themes.
    2. Open each chosen ISO source URL in a real browser (Chrome or Firefox).
    3. For each: confirm the page actually displays ISO clause/principle content (not a spinner, not a 403, not a purchase page, not a generic landing/search page).
    4. Confirm the page is free (no paywall, no "acheter la norme" gate blocking the content).
    5. For any URL flagged `GAP` in the note: confirm no acceptable free page exists — those items will be dropped or re-sourced per D-09.
    6. Reply with one of: `approved: iso-9001=<url> iso-14001=<url> iso-45001=<url>` (confirm or correct each URL), or describe what is broken.
  </how-to-verify>
  <resume-signal>Type `approved` with the confirmed/corrected per-theme URLs, or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author Batch B — iso-45001 (18) + iso-9001 (14) + iso-14001 (14), verify and commit</name>
  <files>qhse-cesi/outils-data.js</files>
  <read_first>
    - .planning/phases/02-content-bank/02-RESEARCH.md (Theme iso-9001/14001/45001 accuracy anchors lines 258-306; Domain Accuracy Anchors ISO table lines 621-628; State of the Art lines 706-719 — OHSAS 18001 withdrawn, ISO 9001:2015 7 principles, CLP; Common Pitfalls 652-702)
    - .planning/phases/02-content-bank/02-CONTEXT.md (D-02, D-06, D-08 ISO row, D-09, D-10, D-11, D-12, D-13)
    - .planning/phases/02-content-bank/iso-scouting-note.txt (the per-theme free-source URLs confirmed/corrected at the checkpoint)
    - C:\Users\Lasmoles\.claude\projects\C--Users-Lasmoles-mes-apps-claude\memory\feedback_verify_links_before_ship.md
    - qhse-cesi/outils-data.js (the file being appended to)
  </read_first>
  <action>Append 46 items: 18 `theme:'iso-45001'`, 14 `theme:'iso-9001'`, 14 `theme:'iso-14001'` (per-theme ~ flashcard:qcm = research §Volume Distribution). `id` = `iso-45001-flashcard-001`, `iso-9001-qcm-001`, etc. Use the human-CONFIRMED free-source URL per theme (from the checkpoint reply, cross-checked against `.planning/phases/02-content-bank/iso-scouting-note.txt`) as `source.url`. `source.authority` = the page's authority (e.g. `INRS`, `Qualité Performance`, `AFNOR`, `Wikipédia` — whatever the confirmed page is); `source.ref` = norm number + clause, e.g. `ISO 45001:2018 §6.1.2`, `ISO 9001:2015 §8`, `ISO 14001:2015 §6.1.2`. Get exam-critical ISO facts EXACTLY right from RESEARCH: ISO 45001:2018 replaces OHSAS 18001 (NOT "ISO 18001"), published March 2018, §5.4 worker participation, §6.1.2 hazard identification; ISO 9001:2015 has 7 quality management principles (not 8 — the 2000 version had 8), HLS §4–§10; ISO 14001:2015 SME, aspects/impacts environnementaux, same HLS. QCM distractors per D-12 = real-domain confusions (OHSAS 18001 vs ISO 45001, 7 vs 8 principles, swapped clause numbers, neighbouring norm). Per D-02 do NOT add any `norm` sub-field — theme slug carries it. Run the RESEARCH §Content-Verification curl snippet again on the final URLs; set each `source.verified` to today. Any theme that was flagged GAP at the checkpoint: drop or re-source those items per D-09 and record the gap in the SUMMARY. Update the `// Total items:` trailer. Then create ONE atomic commit `feat(bank): add ISO batch iso-45001/iso-9001/iso-14001 (46 items)` and push to main.</action>
  <verify>
    <automated>node "C:/Users/Lasmoles/mes-apps-claude/.planning/phases/02-content-bank/verify-bank.cjs" iso-45001:18 iso-9001:14 iso-14001:14 duerp:18 principes-generaux:18</automated>
  </verify>
  <acceptance_criteria>
    - `BANK.filter(i=>i.theme==='iso-45001').length >= 18`, `iso-9001 >= 14`, `iso-14001 >= 14` (or fewer with a documented D-09 gap in SUMMARY)
    - verify-bank.cjs exits 0 (all schema fields present, themes within the 15-slug set, all QCMs valid, zero `source.url` contains `legifrance`)
    - No `source.url` contains `iso.org/standard`, `boutique.afnor.org`, or `shop.bsigroup.com`; no `media.html?refINRS=` (Pitfall 5 / RESEARCH anti-pattern)
    - Every Batch B `source.url` is the human-confirmed-at-checkpoint URL and was re-run through the content-verification curl snippet; `source.verified` = the real date
    - Item facts cross-checked: ISO 45001 replaces OHSAS 18001 (not "ISO 18001"), ISO 9001:2015 = 7 principles (not 8) — at least the answer/explanation text reflects these
    - One atomic commit pushed to `main`
  </acceptance_criteria>
  <done>46 schema-compliant ISO items appended on human-confirmed free sources; D-02 honored (3 separate themes, no norm field); committed atomically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored ISO content → owner exam revision | A wrong norm/clause/replacement fact becomes false study material |
| ISO source page → `source.url` | `iso.org` paywall / shop / SPA / Cloudflare page shipped as an authoritative free citation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering (content integrity) | iso-* item `answer`/`ref` | mitigate | Task 2 cross-checks against RESEARCH ISO accuracy anchors (OHSAS 18001 not "ISO 18001"; 7 not 8 principles; §5.4/§6.1.2; HLS §4–§10); D-09 gap surfacing |
| T-02-05 | Spoofing (hostile/wrong link) | iso-* `source.url` | mitigate | Blocking human-verify checkpoint (browser eyeball each free source renders the cited clauses) + curl re-verification in Task 2; status-only checks BANNED |
| T-02-06 | Information disclosure / LEGAL | iso.org / AFNOR boutique | mitigate | D-06 free-authoritative-only; Pitfall 5 hard ban on `iso.org/standard`, `boutique.afnor.org`, `shop.bsigroup.com`; LEGAL.md link-only, no `.pdf` hosted |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed; reuses Plan 01 stdlib verifier — no supply-chain surface |
</threat_model>

<verification>
- Blocking human-verify checkpoint passed (three ISO free-source URLs confirmed to render in a real browser)
- `node verify-bank.cjs iso-45001:18 iso-9001:14 iso-14001:14 duerp:18 principes-generaux:18` exits 0
- No paywall/shop/iso.org-403/media.html URL in any Batch B `source.url`
- One atomic commit pushed to `main`
</verification>

<success_criteria>
- 46 ISO items added; `BANK.length >= 82` after this plan
- Three separate ISO theme slugs filterable (D-02); no norm sub-field
- All ISO source URLs human-confirmed free + content-verified; gaps (if any) surfaced per D-09
</success_criteria>

<output>
Create `.planning/phases/02-content-bank/02-02-SUMMARY.md` — record the human-confirmed per-theme ISO source URLs, any GAP themes (D-09), and the verified count.
</output>
</content>
