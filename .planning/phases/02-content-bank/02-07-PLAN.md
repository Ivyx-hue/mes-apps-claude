---
phase: 02-content-bank
plan: 07
type: execute
wave: 7
depends_on: [02-06]
files_modified:
  - qhse-cesi/outils-data.js
  - .planning/phases/02-content-bank/verify-bank.cjs
autonomous: true
requirements: [BANK-01, BANK-02, BANK-03, BANK-04, BANK-05, SHELL-05]
user_setup: []

must_haves:
  truths:
    - "ROADMAP SC1 mechanically holds: window.BANK is a console-evaluable global with BANK.length >= 200 (target >= 210), spanning all 15 D-01 themes (BANK-01)"
    - "ROADMAP SC2 mechanically holds: a 10-item random sample has every required field (id,type,theme,question,answer,explanation,source{authority,ref,url,verified},difficulty; +choices/correct for qcm) (BANK-02)"
    - "ROADMAP SC3 mechanically holds: every distinct source.url was content-verified (real <title> + topic match + soft-404 grep); status-only checks are a FAIL; zero broken links at ship time (BANK-04)"
    - "ROADMAP SC4 mechanically holds: BANK.filter(i=>i.theme===X) is non-empty for every one of the 15 themes (>= 6 required, all 15 must pass) (BANK-05)"
    - "SHELL-05 closes here: outils.html loads outils-data.js via <script src> and the deferred clause is asserted closed; D-01..D-13 each verifiably honored across the shipped bank"
  artifacts:
    - path: "qhse-cesi/outils-data.js"
      provides: "final integrated bank — all 6 batches, BANK.length >= 200 (target >= 210), every source.url content-verified"
      contains: "window.BANK = ["
      min_lines: 200
    - path: ".planning/phases/02-content-bank/verify-bank.cjs"
      provides: "extended with a --final mode asserting all 4 ROADMAP success criteria + D-01..D-13 + SHELL-05 closure"
      contains: "--final"
  key_links:
    - from: "qhse-cesi/outils.html"
      to: "qhse-cesi/outils-data.js"
      via: "script src tag (SHELL-05 deferred clause — asserted closed here)"
      pattern: "<script src=\"outils-data\\.js\"></script>"
    - from: ".planning/phases/02-content-bank/verify-bank.cjs --final"
      to: "ROADMAP Phase 2 success criteria 1-4"
      via: "mechanical assertions (count, sample, url-verify ledger, theme filter)"
      pattern: "--final"
---

<objective>
Final integration & verification. No new content is authored here — this plan proves the assembled bank mechanically satisfies every ROADMAP Phase 2 success criterion, that SHELL-05's deferred `<script src="outils-data.js">` clause is closed, and that every locked decision D-01..D-13 is verifiably honored across the shipped bank. It extends the reusable `verify-bank.cjs` with a `--final` gate that asserts all four ROADMAP success criteria as code, and confirms a content-verification ledger exists for every distinct `source.url` (status-only checks are a FAIL).

Purpose: This is the goal-backward closure of the phase — the bank only "exists" if a fresh Claude/owner can run one command and see all four ROADMAP truths hold and SHELL-05 closed. It is the single mechanical gate the phase-verifier and owner-verify will trust.
Output: extended `verify-bank.cjs` with `--final` mode, a recorded full source-URL verification ledger, and one atomic commit. No content edits unless `--final` surfaces a defect (then the smallest possible fix + a documented D-09 note).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/V2-ETUDE-SPEC.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-content-bank/02-CONTEXT.md
@.planning/phases/02-content-bank/02-RESEARCH.md
@.planning/phases/02-content-bank/02-01-SUMMARY.md
@.planning/phases/02-content-bank/02-02-SUMMARY.md
@.planning/phases/02-content-bank/02-03-SUMMARY.md
@.planning/phases/02-content-bank/02-04-SUMMARY.md
@.planning/phases/02-content-bank/02-05-SUMMARY.md
@.planning/phases/02-content-bank/02-06-SUMMARY.md

<interfaces>
<!-- ROADMAP Phase 2 success criteria (the four truths --final must mechanically assert): -->
SC1: BANK.length >= 200 ; items span all declared themes
SC2: 10 random items each carry id,type,theme,question,answer,explanation,source{authority,ref,url,verified},difficulty (+choices,correct for qcm)
SC3: every source.url content-verified (real <title> + topic match + soft-404 grep; no search/index pages; HTTP-status-only BANNED)
SC4: BANK.filter(i=>i.theme==='duerp') non-empty AND same for >= 5 other themes (this plan requires ALL 15)
SHELL-05 (REQUIREMENTS.md PARTIAL): outils.html loads the bank via <script src="outils-data.js"> — the deferred clause closes this phase

<!-- 15 locked D-01 theme slugs (verifier's allowed set): -->
duerp · principes-generaux · iso-9001 · iso-14001 · iso-45001 · tms · risque-routier · risque-chimique · rps · espaces-confines · acronymes · metiers · calendrier · icpe-seveso · rncp
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend verify-bank.cjs with a --final gate asserting all 4 ROADMAP success criteria + SHELL-05 closure + D-01..D-13</name>
  <files>.planning/phases/02-content-bank/verify-bank.cjs</files>
  <read_first>
    - .planning/phases/02-content-bank/verify-bank.cjs (the existing reusable verifier from Plan 01 — extend it, do not rewrite the existing per-theme assertion behavior; it is still called by the regression command below with theme:count args)
    - .planning/ROADMAP.md (Phase 2 "Content Bank" — the four "Success Criteria (what must be TRUE)" lines 58-62; the exact theme list in SC1)
    - .planning/REQUIREMENTS.md (BANK-01..05 full text + the SHELL-05 PARTIAL note — confirm the SHELL-05 re-map "closes this phase")
    - .planning/phases/02-content-bank/02-CONTEXT.md (D-01..D-13 — the decisions the --final gate must each verifiably check)
    - .planning/V2-ETUDE-SPEC.md (Content bank schema — the field set SC2 samples; correct = 0-based index into choices; difficulty in {1,2,3})
  </read_first>
  <action>Add a `--final` mode to `.planning/phases/02-content-bank/verify-bank.cjs` (keep all existing `theme:count` arg behavior intact — it is still the per-batch gate). When invoked as `node verify-bank.cjs --final`, load `global.window={};require('../../../qhse-cesi/outils-data.js')` then assert and print PASS/FAIL for each, exiting 1 on the first FAIL:
  (SC1 / BANK-01) `Array.isArray(window.BANK)` AND `window.BANK.length >= 200` (warn-but-pass-note if `< 210` since RESEARCH target is 210; hard-FAIL only `< 200`); print the count.
  (SC1 themes / BANK-05 / D-01) every item's `theme` ∈ the 15 locked D-01 slugs and the set of themes present === all 15 (no theme empty; no theme outside the set; no `divers`); print any missing/extra theme.
  (SC4 / BANK-05) for each of the 15 themes, `BANK.filter(i=>i.theme===t).length >= 1` (ROADMAP requires ≥6; this phase asserts all 15 non-empty); print the per-theme histogram.
  (SC2 / BANK-02) draw 10 random items (seedless `Math.random` ok) and assert each has non-empty `id,type,theme,question,answer,explanation` + a complete `source{authority,ref,url,verified}` + `difficulty ∈ {1,2,3}`; and EVERY `type==='qcm'` item (not just the sample) has an array `choices` and an integer `correct` with `0 <= correct < choices.length`; print the sampled ids.
  (SC3 / BANK-04 / D-09) assert a content-verification ledger exists (Task 2 produces it) — read `.planning/phases/02-content-bank/url-verification-ledger.txt`; assert every DISTINCT `source.url` in `window.BANK` appears in the ledger with a `PASS` verdict line (title + topic-match + soft-404 columns present — a status-only/empty line is a FAIL); print any URL missing from the ledger or marked FAIL.
  (D-02) assert no item has a `norm` field and the three ISO themes exist as separate slugs `iso-9001`/`iso-14001`/`iso-45001` (no bare `iso` theme).
  (D-03) assert no theme value matches `/^bc0[1-4]$/` and no item has a `bloc` field (rncp meta-theme only).
  (D-07) assert zero `source.url` contains the substring `legifrance` (Légifrance is explanation-only).
  (D-10/D-11/D-13) assert `answer` is non-empty on every item and `difficulty` is strictly one of 1|2|3 (rubric integrity); print a difficulty histogram.
  (SHELL-05) read `qhse-cesi/outils.html` and assert it contains the literal `<script src="outils-data.js"></script>`; print `SHELL-05 deferred clause: CLOSED`.
  End with a single summary line `--final: ALL ROADMAP SC1-4 + SHELL-05 + D-01..D-13 PASS — BANK.length=<N>` and exit 0 only if every assertion passed.</action>
  <verify>
    <automated>node "C:/Users/Lasmoles/mes-apps-claude/.planning/phases/02-content-bank/verify-bank.cjs" duerp:18 principes-generaux:18 iso-45001:18 iso-9001:14 iso-14001:14 tms:14 risque-routier:12 rps:14 risque-chimique:18 espaces-confines:12 icpe-seveso:12 calendrier:11 acronymes:20 metiers:12 rncp:13</automated>
  </verify>
  <acceptance_criteria>
    - `verify-bank.cjs` still passes the full per-theme regression invocation above (existing `theme:count` behavior unbroken)
    - `node verify-bank.cjs --final` exists, runs, and prints an explicit PASS/FAIL line for each of SC1, SC1-themes, SC4, SC2, SC3, D-02, D-03, D-07, D-10/D-11/D-13, SHELL-05
    - The `--final` mode reads the url-verification ledger and FAILs if any distinct `source.url` is absent or marked FAIL (status-only ledger lines are a FAIL — BANK-04/D-09)
    - The `--final` mode asserts `qhse-cesi/outils.html` contains `<script src="outils-data.js"></script>` and prints `SHELL-05 deferred clause: CLOSED`
  </acceptance_criteria>
  <done>verify-bank.cjs gains a `--final` gate that mechanically asserts all four ROADMAP success criteria, SHELL-05 closure, and D-01..D-13; existing per-batch behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Build the full source-URL content-verification ledger, run --final, fix any defect minimally, commit atomically</name>
  <files>qhse-cesi/outils-data.js, .planning/phases/02-content-bank/verify-bank.cjs</files>
  <read_first>
    - .planning/phases/02-content-bank/02-RESEARCH.md (Content-Verification Protocol lines 510-547 — the exact curl + title + soft-404 snippet; FAIL patterns table + remedies; the SPA checkpoint rule for metiers/rncp)
    - .planning/phases/02-content-bank/02-06-SUMMARY.md (the human-eyeball dates for the MétierScope/France compétences SPA URLs — these are PASS-by-human, recorded as `human-eyeball <date>` ledger verdicts, not curl)
    - C:\Users\Lasmoles\.claude\projects\C--Users-Lasmoles-mes-apps-claude\memory\feedback_verify_links_before_ship.md (HTTP-status-only audits BANNED — the ledger must record title + topic-match + soft-404 per URL)
    - .planning/phases/02-content-bank/02-01-SUMMARY.md … 02-05-SUMMARY.md (the per-batch verified URL lists + any surfaced D-09 gaps to reconcile)
  </read_first>
  <action>Enumerate every DISTINCT `source.url` across the assembled `window.BANK`. For each non-SPA URL, run the RESEARCH §Content-Verification curl snippet (realistic browser UA, follow redirects, extract `<title>`, grep soft-404 markers) and append a ledger line to `.planning/phases/02-content-bank/url-verification-ledger.txt` in the form `PASS|FAIL <url> | title="<extracted title>" | topic-match=<yes/no> | soft404=<no/yes> | verified=<date>`. For the MétierScope + France compétences SPA URLs, do NOT use curl as the verdict — copy the human-eyeball verdict + date from 02-06-SUMMARY.md as `PASS <url> | human-eyeball | verified=<date>` (Pitfall 4: status/curl-only is insufficient for SPAs). A URL with HTTP 200 but a generic/empty/wrong title, a soft-404 grep hit, or a topic mismatch is a `FAIL`. Then run `node verify-bank.cjs --final`. If `--final` reports any FAIL (a missing field, a theme outside the 15-slug set, a `legifrance` source.url, a FAIL/absent ledger URL, an empty theme, SHELL-05 not closed, etc.): make the SMALLEST possible corrective edit to `qhse-cesi/outils-data.js` — substitute a RESEARCH-listed verified alternative URL, fix the offending field, or per D-09 drop the single offending item (only if no verified source exists) while keeping `BANK.length >= 200` (target >= 210; if a drop would breach 200, instead author a replacement item on an already-verified backbone URL for that theme). Re-run `--final` until it exits 0. Record every defect found + fix applied + any D-09 gap in the SUMMARY. Then stage `qhse-cesi/outils-data.js`, `.planning/phases/02-content-bank/verify-bank.cjs`, `.planning/phases/02-content-bank/url-verification-ledger.txt` and create ONE atomic commit `chore(bank): final integration — assert ROADMAP SC1-4 + SHELL-05 closure (BANK >= 200)` and push to `main`.</action>
  <verify>
    <automated>node "C:/Users/Lasmoles/mes-apps-claude/.planning/phases/02-content-bank/verify-bank.cjs" --final</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-content-bank/url-verification-ledger.txt` exists with one `PASS|FAIL` line per DISTINCT `source.url` in the bank; every line carries title + topic-match + soft-404 columns (or `human-eyeball` for the 5 SPA URLs) — zero status-only lines
    - `node verify-bank.cjs --final` exits 0 and prints `--final: ALL ROADMAP SC1-4 + SHELL-05 + D-01..D-13 PASS — BANK.length=<N>` with `N >= 200` (target >= 210)
    - Every distinct `source.url` appears in the ledger with a `PASS` verdict; any URL that could not pass was reconciled (alternative substituted, item replaced on a verified backbone, or D-09 drop documented) — no FAIL/absent URL remains in the shipped bank
    - SHELL-05: `--final` confirms `qhse-cesi/outils.html` contains `<script src="outils-data.js"></script>` and prints `SHELL-05 deferred clause: CLOSED`
    - Every defect `--final` surfaced is recorded in the SUMMARY with the minimal fix applied (D-09 for any drop)
    - One atomic commit pushed to `main` (`git status` shows branch up to date)
  </acceptance_criteria>
  <done>Full content-verification ledger built (curl for static URLs, human-eyeball verdicts for the 5 SPA URLs); `--final` green asserting all 4 ROADMAP SC + SHELL-05 closure + D-01..D-13; defects fixed minimally and documented; committed atomically and pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assembled bank → owner exam revision + 3 downstream phases | A field/theme/URL defect that slipped a per-batch gate ships as the P3/P4/P5 contract and as false revision material |
| `--final` gate verdict → phase-verify / owner-verify trust | A weak gate (status-only URL check, missing SC assertion) would pass a defective bank |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Tampering (escaped defect) | assembled `window.BANK` | mitigate | Task 1 `--final` re-asserts SC1-4 + D-01..D-13 across the WHOLE bank (not per-batch); Task 2 reconciles any FAIL with the smallest D-09-compliant fix |
| T-02-21 | Spoofing (unverified URL shipped) | every distinct `source.url` | mitigate | Task 2 builds a full ledger: curl title+topic+soft-404 per static URL, human-eyeball verdict for the 5 SPA URLs; `--final` FAILs on any absent/FAIL/status-only ledger line (BANK-04 / feedback_verify_links_before_ship hard gate) |
| T-02-22 | Repudiation (SHELL-05 not actually closed) | `qhse-cesi/outils.html` | mitigate | `--final` mechanically asserts the `<script src="outils-data.js">` tag is present and prints the closure line — SHELL-05 closure is evidenced, not assumed |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase; `verify-bank.cjs` is plain Node stdlib + `curl` (system) — no supply-chain surface |
</threat_model>

<verification>
- `node verify-bank.cjs --final` exits 0 and prints PASS for SC1, SC1-themes, SC4, SC2, SC3, D-02, D-03, D-07, D-10/D-11/D-13, SHELL-05
- `node verify-bank.cjs <all 15 theme:count args>` still passes (per-batch regression intact)
- `url-verification-ledger.txt` has a PASS line (with title+topic+soft404, or human-eyeball) for every distinct `source.url`; zero status-only lines
- `BANK.length >= 200` (target >= 210); all 15 themes non-empty; SHELL-05 closure printed
- One atomic commit pushed to `main`
</verification>

<success_criteria>
- All four ROADMAP Phase 2 success criteria mechanically asserted PASS by `verify-bank.cjs --final`
- SHELL-05 deferred `<script src="outils-data.js">` clause asserted CLOSED here
- D-01..D-13 each verifiably honored across the shipped bank; `BANK.length >= 200` (target >= 210)
- Full content-verification ledger committed; no unverified `source.url` in the shipped bank
</success_criteria>

<output>
Create `.planning/phases/02-content-bank/02-07-SUMMARY.md` — record the final `BANK.length`, the per-theme histogram, the `--final` PASS output, every defect found+fixed (with D-09 notes for any drop), the ledger location, and an explicit "SHELL-05 deferred clause CLOSED" confirmation.
</output>
</content>
