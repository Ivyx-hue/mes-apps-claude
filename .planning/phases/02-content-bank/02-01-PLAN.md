---
phase: 02-content-bank
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - qhse-cesi/outils-data.js
  - qhse-cesi/outils.html
autonomous: true
requirements: [BANK-01, BANK-02, BANK-03, BANK-04, BANK-05]
user_setup: []

must_haves:
  truths:
    - "outils-data.js exists and, when loaded, exposes window.BANK as a true global array (browser-console evaluable, no IIFE)"
    - "outils.html loads outils-data.js via <script src> — closing the deferred SHELL-05 clause"
    - "Batch A (duerp + principes-generaux, 36 items) is present, schema-compliant, every source.url content-verified"
    - "Every Batch A item carries the full source object (authority, ref, url, verified) — no unsourced regulatory claim; duerp/principes-generaux sourced to INRS + ameli risques-pro with Code du travail ref per D-08 authority map"
    - "theme is one of the 15 locked D-01 slugs; no value outside the set; no divers theme"
  artifacts:
    - path: "qhse-cesi/outils-data.js"
      provides: "window.BANK array literal, file header, Batch A items (duerp 18 + principes-generaux 18)"
      contains: "window.BANK = ["
      min_lines: 200
    - path: "qhse-cesi/outils.html"
      provides: "script src outils-data.js tag after the chassis.css link"
      contains: "outils-data.js"
  key_links:
    - from: "qhse-cesi/outils.html"
      to: "qhse-cesi/outils-data.js"
      via: "script src tag in head after chassis.css link"
      pattern: "<script src=\"outils-data\\.js\"></script>"
    - from: "qhse-cesi/outils-data.js"
      to: "window.BANK"
      via: "top-level global assignment (not IIFE)"
      pattern: "window\\.BANK\\s*=\\s*\\["
---

<objective>
Establish the content-bank file architecture and author Batch A. This is the interface-defining plan: it creates `qhse-cesi/outils-data.js` with the locked schema shape and the `window.BANK` global contract that P3/P4/P5 depend on, wires it into `outils.html` (closing SHELL-05's deferred clause), and authors the first two themes (`duerp` 18 + `principes-generaux` 18 = 36 items) fully source-verified.

Purpose: The file shape and `<script src>` wiring are a hard contract for three downstream phases — defining them first, with a real first batch proving the discipline, de-risks all subsequent batches.
Output: `qhse-cesi/outils-data.js` (header + window.BANK + 36 verified items), `qhse-cesi/outils.html` (one `<script src>` line added). One atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/V2-ETUDE-SPEC.md
@.planning/phases/02-content-bank/02-CONTEXT.md
@.planning/phases/02-content-bank/02-RESEARCH.md
@.planning/phases/02-content-bank/02-PATTERNS.md

<interfaces>
<!-- LOCKED item schema (V2-ETUDE-SPEC.md). Executor uses this directly — no codebase exploration needed. -->

Each item object fields:
- id          : string  — `<theme>-<type>-NNN` kebab-case (e.g. `duerp-flashcard-001`)
- type        : 'flashcard' | 'qcm'
- theme       : one of the 15 locked D-01 slugs (closed set, no `divers`)
- question    : string  — recto for flashcard, stem for qcm
- answer      : string  — verso / canonical answer (D-10: short recall-grade, 1-3 sentences or short list)
- choices     : string[] — QCM ONLY (4 options recommended)
- correct     : number  — QCM ONLY — 0-based index into choices
- explanation : string  — the "why" (D-10: context, articulation, mnemonic, pitfall; Légifrance deep-link goes HERE per D-07, never in source.url)
- source      : { authority: string, ref: string, url: string, verified: 'YYYY-MM-DD' }
- difficulty  : 1 | 2 | 3  (D-13: 1=restitution, 2=comprehension/application, 3=analysis/articulation)

15 locked theme slugs (D-01): duerp · principes-generaux · iso-9001 · iso-14001 · iso-45001 · tms · risque-routier · risque-chimique · rps · espaces-confines · acronymes · metiers · calendrier · icpe-seveso · rncp

File shape (PATTERNS.md — greenfield, NO IIFE): a header comment block, then `window.BANK = [` at top level, items, then `]; // end window.BANK` and a `// Total items: <N> — verified <date>` trailer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create outils-data.js with the window.BANK contract and wire it into outils.html</name>
  <files>qhse-cesi/outils-data.js, qhse-cesi/outils.html</files>
  <read_first>
    - .planning/phases/02-content-bank/02-PATTERNS.md (file header pattern lines 38-47; outils.html exact insertion point lines 145-178 — insert AFTER line 15 the chassis.css link, before line 16 the head close; codebase note: zero existing script-src tags, no defer/async, synchronous load is intentional)
    - .planning/V2-ETUDE-SPEC.md (Content bank section — locked schema; Architecture section — zero build, plain array literal)
    - qhse-cesi/outils.html (the file being modified — confirm line 15 is the chassis.css link and line 16 is the head close)
  </read_first>
  <action>Create `qhse-cesi/outils-data.js` with the exact file header comment block from PATTERNS.md lines 38-47, then `window.BANK = [` as a TOP-LEVEL global assignment (NOT wrapped in an IIFE — PATTERNS.md finding 4: it must be a true browser-console-evaluable global so `BANK.filter(...)` works in devtools), the array body, closing `]; // end window.BANK` plus a `// Total items: <N> — verified <date>` trailer line. Use plain ES5-safe syntax: no `export`, no `import`, no `require`, no `type="module"`. Then edit `qhse-cesi/outils.html`: insert exactly the line `  <script src="outils-data.js"></script>` (two-space indent, no defer/async/integrity) on its own line immediately AFTER the existing `  <link rel="stylesheet" href="chassis.css">` line and BEFORE the `</head>` line. This is the ONLY change to outils.html — do not touch the tab IIFE or any markup. This closes SHELL-05's deferred `<script src="outils-data.js">` clause.</action>
  <verify>
    <automated>node -e "global.window={};require('C:/Users/Lasmoles/mes-apps-claude/qhse-cesi/outils-data.js');process.exit(Array.isArray(window.BANK)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "global.window={};require('./qhse-cesi/outils-data.js');process.exit(Array.isArray(window.BANK)?0:1)"` exits 0
    - File contains literal `window.BANK = [` and is NOT wrapped in an IIFE (no `(function` enclosing `window.BANK`)
    - `qhse-cesi/outils.html` contains the literal `<script src="outils-data.js"></script>` on the line immediately after the `<link rel="stylesheet" href="chassis.css">` line
    - `git diff qhse-cesi/outils.html` shows exactly ONE added line (the script tag) and zero other changes
  </acceptance_criteria>
  <done>outils-data.js exists with the window.BANK global contract; outils.html loads it via a single added script-src line; SHELL-05 deferred clause structurally closed.</done>
</task>

<task type="auto">
  <name>Task 2: Author Batch A — duerp (18) + principes-generaux (18), content-verify every source.url</name>
  <files>qhse-cesi/outils-data.js</files>
  <read_first>
    - .planning/phases/02-content-bank/02-RESEARCH.md (Theme: duerp lines 199-216 — pre-vetted [CV] INRS URLs + accuracy anchors R4121-1/L4121-3/L4121-3-1/40-ans/mise-à-jour; Theme: principes-generaux lines 219-237 — [CV] URL + exact L4121-2 order of the 9 principes; Content-Verification Protocol lines 510-547 — the exact curl snippet; Domain Accuracy Anchors lines 605-648; Common Pitfalls lines 652-702)
    - .planning/phases/02-content-bank/02-CONTEXT.md (D-05 pédagogique-first, D-07 Légifrance in explanation only, D-08 authority map row for duerp/principes-generaux = INRS, D-09 surface gaps, D-10 answer/explanation split, D-11 QCM answer/explanation, D-12 plausible distractors, D-13 difficulty rubric)
    - C:\Users\Lasmoles\.claude\projects\C--Users-Lasmoles-mes-apps-claude\memory\feedback_verify_links_before_ship.md (content-verified URL discipline — real title + topic match + soft-404 grep; HTTP-status-only audits BANNED)
    - qhse-cesi/outils-data.js (the file being appended to)
  </read_first>
  <action>Append 36 items to `window.BANK`: 18 with `theme:'duerp'` and 18 with `theme:'principes-generaux'`. Per-theme mix ~10 flashcard + ~8 qcm. `id` = `duerp-flashcard-001`, `duerp-qcm-001`, `principes-generaux-flashcard-001`, etc. (zero-padded NNN, sequential per theme+type). Primary `source.url` MUST be a pédagogique INRS page from RESEARCH §duerp / §principes-generaux (`https://www.inrs.fr/demarche/document-unique/ce-qu-il-faut-retenir.html`, `https://www.inrs.fr/demarche/evaluation-risques-professionnels/ce-qu-il-faut-retenir.html`, `https://www.inrs.fr/demarche/principes-generaux/Principes-generaux-prevention.html`); `source.authority='INRS'`; `source.ref` = exact Code du travail article (`Art. R4121-1 Code du travail`, `Art. L4121-2 Code du travail`, `Art. L4121-3 Code du travail`). Légifrance deep-links (LEGIARTI000023795562 for R4121-1, LEGIARTI000033019913 for L4121-2) go in `explanation` prose parentheticals ONLY, never in `source.url` (D-07). Get exam-critical facts EXACTLY right from the RESEARCH accuracy anchors: DUERP conservation = 40 ans (not 5/10), DUERP obligation = R4121-1 (not L4121-1/L4121-3), 9 principes = L4121-2 (not L4121-1), exact order 1-Éviter 2-Évaluer 3-Combattre-à-la-source 4-Adapter-le-travail-à-l-homme 5-Technique 6-Remplacer 7-Planifier 8-Protection-collective-prioritaire 9-Instructions. QCM distractors per D-12 = real-domain confusions (neighbouring article number, swapped principle order) — never throwaway-wrong. Then run the Content-Verification Protocol curl snippet against EVERY distinct source.url used in this batch; set each item's `source.verified` to today's date (the actual content-verification date). If any URL fails (soft-404, generic title, Cloudflare block, wrong-doc), per D-09 surface the gap in the SUMMARY and either substitute a verified RESEARCH-listed alternative or drop the item — never ship a status-only/unverified URL. Update the `// Total items:` trailer.</action>
  <verify>
    <automated>node "C:/Users/Lasmoles/mes-apps-claude/.planning/phases/02-content-bank/verify-bank.cjs" duerp:18 principes-generaux:18</automated>
  </verify>
  <acceptance_criteria>
    - `BANK.filter(i=>i.theme==='duerp').length >= 18` AND `BANK.filter(i=>i.theme==='principes-generaux').length >= 18`
    - Every Batch A item has non-empty `id,type,theme,question,answer,explanation` and a complete `source{authority,ref,url,verified}`; every `difficulty` is in {1,2,3}
    - Every `type:'qcm'` item has a `choices` array and a `correct` integer with `0 <= correct < choices.length`
    - Zero Batch A `source.url` contains `legifrance` (D-07: Légifrance only in explanation prose)
    - Every distinct `source.url` in this batch was run through the RESEARCH §Content-Verification curl snippet: HTTP 200 + non-generic `<title>` matching the item topic + no soft-404 grep hit (status-only checks are a FAIL); each `source.verified` = the real verification date
    - Any URL that failed verification is recorded as a surfaced gap in the SUMMARY (D-09) — no unverified item shipped
  </acceptance_criteria>
  <done>36 schema-compliant, source-verified items (duerp 18 + principes-generaux 18) appended; all source.urls content-verified; exam-critical article numbers cross-checked against the accuracy anchors.</done>
</task>

<task type="auto">
  <name>Task 3: Create the reusable batch verifier and commit Batch A atomically</name>
  <files>.planning/phases/02-content-bank/verify-bank.cjs</files>
  <read_first>
    - .planning/phases/02-content-bank/02-RESEARCH.md (Per-Batch Self-Check lines 593-601 — the three console assertions this script automates)
    - .planning/V2-ETUDE-SPEC.md (Content bank — schema fields the validator enforces)
  </read_first>
  <action>Create `.planning/phases/02-content-bank/verify-bank.cjs`: a Node CommonJS script that does `global.window={};require('../../../qhse-cesi/outils-data.js')` (resolve the path relative to the script), then for each CLI arg of the form `theme:count` asserts `window.BANK.filter(i=>i.theme===theme).length >= count`. It must also assert globally: every item's `theme` is one of the 15 locked D-01 slugs; every item has non-empty `id,type,question,answer,explanation` and a complete `source{authority,ref,url,verified}`; `difficulty` in {1,2,3}; every `type==='qcm'` item has an array `choices` and integer `correct` with `0<=correct<choices.length`; zero `source.url` contains the substring `legifrance`; and `BANK.length` is printed. Exit 0 only if all assertions pass, else print the first failure and exit 1. This script is the shared gate reused by every subsequent batch plan (02-02..02-06) and the final integration plan (02-07). Then stage `qhse-cesi/outils-data.js`, `qhse-cesi/outils.html`, and `.planning/phases/02-content-bank/verify-bank.cjs` and create ONE atomic commit `feat(bank): scaffold outils-data.js + wire outils.html + Batch A duerp/principes-generaux (36 items)` and push to main (token-conscious: never leave the batch uncommitted).</action>
  <verify>
    <automated>node "C:/Users/Lasmoles/mes-apps-claude/.planning/phases/02-content-bank/verify-bank.cjs" duerp:18 principes-generaux:18</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-content-bank/verify-bank.cjs` exists and exits 0 for `duerp:18 principes-generaux:18`
    - The script exits 1 with a descriptive message if any schema field is missing, any theme is outside the 15-slug set, or any `source.url` contains `legifrance`
    - `git log -1 --name-only` shows a single commit touching `qhse-cesi/outils-data.js`, `qhse-cesi/outils.html`, `.planning/phases/02-content-bank/verify-bank.cjs` (atomic — RESEARCH anti-pattern: never one giant uncommitted file)
    - The commit has been pushed to `main` (`git status` shows branch up to date)
  </acceptance_criteria>
  <done>Reusable verify-bank.cjs gate created and green for Batch A; Batch A + wiring committed atomically and pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content → owner's exam revision | An inaccurate regulatory claim becomes false study material for a real diploma |
| external source URL → bank `source.url` | A wrong/search/soft-404 URL is shipped as an authoritative citation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (content integrity) | duerp/principes-generaux item `answer`/`ref` | mitigate | Task 2 cross-checks every article number against RESEARCH §Domain Accuracy Anchors (R4121-1 vs L4121-1/L4121-3; L4121-2 vs L4121-1; 40 ans); D-09 surface-gap rule — no unsourced claim ships |
| T-02-02 | Spoofing (hostile/wrong link) | `source.url` on every Batch A item | mitigate | Task 2 runs the RESEARCH §Content-Verification curl snippet (real `<title>` + topic match + soft-404 grep) on every URL; status-only checks are a FAIL; `verified` set to the real date |
| T-02-03 | Information disclosure / LEGAL scope | linked source pages | mitigate | D-06 free-authoritative-page-only + LEGAL.md link-only; no `.pdf` hosted under `/qhse-cesi/`; Batch A uses only free INRS pédagogique pages |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (RESEARCH: zero external deps); verifier is plain Node stdlib — no supply-chain surface |
</threat_model>

<verification>
- `node verify-bank.cjs duerp:18 principes-generaux:18` exits 0
- `outils.html` diff is exactly one added `<script src="outils-data.js"></script>` line
- Every distinct Batch A `source.url` content-verified per the RESEARCH protocol; gaps (if any) surfaced in SUMMARY
- One atomic commit pushed to `main`
</verification>

<success_criteria>
- `window.BANK` is a console-evaluable global; `BANK.length >= 36` after this plan
- 18 duerp + 18 principes-generaux items, all schema-compliant, all source-verified
- SHELL-05 deferred clause closed (`<script src="outils-data.js">` present in outils.html)
- Reusable `verify-bank.cjs` gate exists for downstream batch plans
</success_criteria>

<output>
Create `.planning/phases/02-content-bank/02-01-SUMMARY.md` when done — record the Batch A verified URL list, any surfaced source gaps (D-09), and confirm SHELL-05 clause closed.
</output>
