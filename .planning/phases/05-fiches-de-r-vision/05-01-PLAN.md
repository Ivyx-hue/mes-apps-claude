---
phase: 05-fiches-de-r-vision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - qhse-cesi/chassis.css
  - qhse-cesi/outils.html
  - qhse-cesi/fiches-data.js
autonomous: true
requirements: [FICHE-01, FICHE-02]
tags: [phase-5, fiches, plumbing, css-namespace, iife, scaffold]

must_haves:
  truths:
    - "Loading outils.html and clicking the Fiches tab renders the empty fiche scaffold (theme picker visible with 15 options, no 'all') without console errors."
    - "window.FICHES exists, is an Array, and FICHES.length === 0 after fiches-data.js loads (skeleton has empty FICHES array — content arrives in Waves 2-5)."
    - "Browser console emits no '[Fiches] BANK or FICHES missing' error on page load."
    - "Switching tabs (Flashcards / QCM / Tests / Fiches) leaves Phase 3 + Phase 4 IIFEs functionally intact (no regression in flashcard grading, QCM reveal, or Tests blancs timer)."
    - "Printing (Ctrl+P) from the Fiches panel hides the other 3 panels and the tablist; printing from any other panel still shows that panel only — no Phase 5 print rules leak."
    - "localStorage key qhse-prefs-v1 still contains P3 + P4 keys (lastTheme, lastMode, newCardsPerDay, lastQcmTheme, lastTestTheme) after the Fiches IIFE runs writePrefs({lastFicheTheme:'duerp'}) on first boot."
  artifacts:
    - path: "qhse-cesi/chassis.css"
      provides: ".fi-* CSS namespace block inside @layer components + 8 new @media print rules + 4 print typography overrides + conditional @page"
      contains: "Phase 5 — Fiches de révision (.fi-*)"
    - path: "qhse-cesi/outils.html"
      provides: "<script src=\"fiches-data.js\" defer> in head + #panel-fiches DOM scaffold + Fiches IIFE shell (__fiBooted)"
      contains: "__fiBooted"
    - path: "qhse-cesi/fiches-data.js"
      provides: "window.FICHES = [] skeleton with WR-04 double-load guard"
      contains: "window.FICHES"
  key_links:
    - from: "qhse-cesi/outils.html (head)"
      to: "qhse-cesi/fiches-data.js"
      via: "<script src=\"fiches-data.js\" defer></script>"
      pattern: "script src=\"fiches-data.js\""
    - from: "qhse-cesi/outils.html (Fiches IIFE boot)"
      to: "window.BANK + window.FICHES"
      via: "DCL-deferred pre-flight check"
      pattern: "if \\(!window\\.BANK \\|\\| !Array\\.isArray\\(window\\.BANK\\)"
    - from: "qhse-cesi/outils.html (Fiches IIFE writePrefs)"
      to: "localStorage qhse-prefs-v1"
      via: "merge-safe Object.assign({}, existing, partial)"
      pattern: "Object\\.assign\\(\\{\\}, existing, partial\\)"
---

<objective>
Ship the **Phase 5 plumbing** — the static structural foundation that all 5 content-authoring waves will fill in. Three artifacts land in one atomic commit: (1) the `.fi-*` CSS namespace block + 8 new print rules + 4 print typography overrides + conditional `@page` appended to `chassis.css`, (2) the `#panel-fiches` DOM scaffold + Fiches IIFE shell + `<script src="fiches-data.js" defer>` wiring inside `outils.html`, (3) the `qhse-cesi/fiches-data.js` skeleton with `window.FICHES = []` + WR-04 double-load guard.

After this plan ships, the Fiches tab renders the empty-state scaffold (theme picker with 15 options, no fiche article yet because FICHES is empty), the IIFE boots cleanly with no console errors, and the `@media print` extensions are dormant but ready. **No fiche content is authored in this plan.** Content arrives in Plans 02-05.

Purpose: Decouple mechanical plumbing (CSS / DOM / IIFE / wiring — verifiable by grep gates) from prose authoring (verifiable only by link-content-verification and human review). Mixing them in one plan would force the executor to context-switch between two completely different verification surfaces.

Output: Three modified files committed atomically. The phase becomes "wired but empty" — exactly the state Phases 1-4 used at their respective Wave-1 boundaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-fiches-de-r-vision/05-CONTEXT.md
@.planning/phases/05-fiches-de-r-vision/05-RESEARCH.md
@.planning/phases/05-fiches-de-r-vision/05-PATTERNS.md
@.planning/phases/05-fiches-de-r-vision/05-UI-SPEC.md

<interfaces>
<!-- Existing globals the Fiches IIFE will read (do NOT call SRS.schedule per DEC-09). -->

From qhse-cesi/outils-data.js (loaded via <script src defer> at outils.html:16):
- `window.BANK: Array<{id, type, theme, question, answer, choices?, correct?, explanation, source:{authority, ref, url, verified}, difficulty}>` (226 items, 15 theme slugs, frozen since Phase 2).

From qhse-cesi/srs.js (loaded via <script src defer> at outils.html:17):
- `window.SRS` — frozen pure-math module. **Phase 5 reads NOTHING from this** (DEC-09).

From qhse-cesi/chassis.css tokens (lines 32-87, frozen since Phase 1):
- Spaces: `--space-xs|sm|md|lg|xl|2xl|3xl`
- Type: `--step--1|0|1|2|3|4|5` (Phase 5 uses only `--step-0`, `--step-1`, `--step-3`, `--step-4` per UI-SPEC line 70)
- Colors: `--bg-1`, `--bg-2`, `--ink-1`, `--ink-2`, `--ink-3`, `--accent`, `--accent-soft`, `--border-subtle`, `--focus-ring`, `--header-h`
- Radius: `--radius-sm|md|lg`
- Fonts: `--font-serif` (Fraunces), `--font-sans` (Inter), `--font-mono` (JetBrains Mono)
- Layout: `--measure` (68ch)

New globals Phase 5 creates:
- `window.FICHES: Array<{slug, title, tldr, definitions[], cadreLegal, demarche, selectedIds[], pieges[], sources[]}>` — populated by Plans 05-02..05-05. Wave 1 ships `window.FICHES = []` (empty array).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append `.fi-*` CSS namespace block + 8 print rules + 4 print typography overrides + conditional `@page` to chassis.css</name>
  <files>qhse-cesi/chassis.css</files>
  <read_first>
    - qhse-cesi/chassis.css (full file, ~1489 lines) — read once. Extract: token list (lines 32-87), `.qz-*` block (lines 883-1411) as the verbatim analog, `@media print` block (lines 1460-1489). **Confirm there is NO existing `@page` rule** (revision baseline verified: `grep -c "@page" chassis.css` returns 0).
    - .planning/phases/05-fiches-de-r-vision/05-UI-SPEC.md (lines 40-160 Spacing/Typography/Color contracts — note §Print typography lines 99-108; lines 210-380 Layout/Composition/Responsive/Print sections; lines 384-460 Print Contract with 8 numbered rules + the §A4 page margin conditional at line 457)
    - .planning/phases/05-fiches-de-r-vision/05-PATTERNS.md (lines 27-200 — `.fi-*` block patterns + print rules pattern, parent-selector scoping discipline, token inventory, anti-patterns list)
  </read_first>
  <action>
    Append a single new region to `chassis.css` immediately before line 1411 (the closing `}` of `@layer components`), and append the new print rules immediately before line 1489 (the closing `}` of `@media print`).

    **Region 1 — `.fi-*` component block (inside `@layer components { }`):**
    - Banner comment exactly: `/* ============ Phase 5 — Fiches de révision (.fi-*) ============ */`
    - 4-space indent matching the `.qz-*` block.
    - Every selector parent-scoped with `#panel-fiches` prefix (no bare `.fi-*` allowed per UI-SPEC §Scoping line 33 / DEC-06).
    - Zero new `:root` custom properties; only reuse the tokens listed in `<interfaces>` above.
    - Zero `!important` outside the print block. Zero animations. Zero transitions.

    Required selectors (derived from UI-SPEC §Component Inventory lines 463-489):
    - `#panel-fiches .fi-theme` + `#panel-fiches .fi-theme label` + `#fi-theme-select` (theme picker — mirror `.qz-theme` shape from chassis lines 886-921, drop `all` semantics, set `min-height: 44px` per WCAG 2.5.5 + UI-SPEC line 59)
    - `#panel-fiches nav.fi-toc` + `#panel-fiches nav.fi-toc ol` + `#panel-fiches nav.fi-toc li` + `#panel-fiches nav.fi-toc a` + `#panel-fiches nav.fi-toc a:hover` + `#panel-fiches nav.fi-toc a:focus-visible` (in-fiche ToC — UI-SPEC §In-fiche ToC lines 301-310; mobile inline flex/wrap, desktop `position: sticky; top: var(--header-h)` at ≥48rem)
    - `#panel-fiches article.fi-fiche` (card surface — mirror `.qz-card` lines 923-943; `background: var(--bg-2)`, `border: 1px solid var(--border-subtle)`, `border-radius: var(--radius-lg)`, `padding: var(--space-md)` mobile / `var(--space-lg)` desktop, `max-width: 48rem`, `margin-inline: auto` desktop. **DROP `min-height`** per PATTERNS line 110 — fiches are variable-length.)
    - `#panel-fiches .fi-fiche > header.fi-fiche-header` + `#panel-fiches .fi-title` (h2, `--step-4` Fraunces 600, `text-wrap: balance`, `tabindex="-1"` focusable) + `#panel-fiches .fi-meta` (`--step-0` mono, `color: var(--ink-3)`)
    - `#panel-fiches .fi-section` + `#panel-fiches .fi-section > h3` + `#panel-fiches .fi-section > h3::after` (2px solid var(--accent) underline rhythm, UI-SPEC line 329) + `#panel-fiches .fi-section > h4` (`--step-1` Fraunces 600) + `#panel-fiches .fi-section p` (`--step-1` sans 400, `line-height: 1.6`, `max-width: var(--measure)`) + `#panel-fiches .fi-section dl/dt/dd` + `#panel-fiches .fi-section ul/li`
    - `#panel-fiches .fi-section + .fi-section` (`border-top: 1px solid var(--border-subtle); padding-block-start: var(--space-2xl); margin-block-start: 0`) per UI-SPEC §Section separator lines 140-145
    - `#panel-fiches span.fi-cite` (inline citation wrapper — `--step-0`, `color: var(--ink-2)`, `font-family: var(--font-sans)`; nested `code` inherits chassis base)
    - `#panel-fiches .fi-qa-list` + `#panel-fiches details.fi-qa` + `#panel-fiches details.fi-qa > summary` (`min-height: 44px`, weight 600, `cursor: pointer`, prepend `▸` via `::before { content: "\25B8 " }`) + `#panel-fiches details.fi-qa[open]` (border `1px solid var(--accent)`, background `color-mix(in oklch, var(--accent-soft) 60%, transparent)`) + `#panel-fiches details.fi-qa > summary:hover` (color `var(--accent)`) + `#panel-fiches .fi-qa-answer/.fi-qa-explanation/.fi-qa-source` (typography per UI-SPEC lines 90-93)
    - `#panel-fiches .fi-sources-list` + `#panel-fiches .fi-sources-list li` (bibliography — `--step-0` sans 400)
    - `#panel-fiches .fi-empty` + `#panel-fiches .fi-error` (`--step-1` sans 400, `color: var(--ink-2)`, padding `var(--space-md) 0` — mirror `.qz-no-pool/.qz-error` chassis lines 1403-1410)

    **Region 2 — print rules (inside `@media print { }`):**
    Append before line 1489. Begin with a banner comment `/* ---- Phase 5 print extensions ---- */`. 6-space indent matching existing print block. This region contains THREE groups, all from the LOCKED UI-SPEC Print Contract:

    **Group 2a — the 8 numbered print rules** (UI-SPEC §Print Contract lines 404-455, verbatim):
    1. Hide sibling panels + tablist: `#panel-flashcards, #panel-qcm, #panel-tests, [role="tablist"] { display: none !important; }`
    2. Force-show Fiches panel: `#panel-fiches { display: block !important; }`
    3. Per-fiche `page-break-before: always`
    4. Hide `.fi-theme` + `nav.fi-toc`
    5. Force-open `<details>` + remove triangle (3 selectors: `details.fi-qa`, `details.fi-qa > summary`, `details.fi-qa > summary::before`)
    6. Section heading underline restore (`border-bottom: 1px solid oklch(30% 0 0); padding-block-end: 0.2em; margin-block-end: 0.5em`)
    7. Sources section `page-break-before: always` (selector `#panel-fiches [data-fi-section="sources"]`)
    8. Flat-white fiche card (background `#fff`, border none, radius 0, padding 0, max-width none, margin 0)

    **Group 2b — the 4 print typography overrides (LOCKED — UI-SPEC §Print typography lines 99-108, NOT optional):**
    - `#panel-fiches { font-size: 11pt; }` — body density for A4
    - `#panel-fiches .fi-title { font-size: 1.5rem; }` — fiche title scale-down
    - `#panel-fiches .fi-section h3 { font-size: 1.15rem; }` — section heading point-size override
    - `#panel-fiches .fi-cite { font-size: 0.8em; }` — inline citation shrink
    These four print-only font-size overrides are part of the locked Print Contract. Place them inside the same `@media print` Phase 5 region (they may share a small sub-banner comment `/* Phase 5 print typography (UI-SPEC §Print typography) */`).

    **Group 2c — conditional `@page` (LOCKED — UI-SPEC line 457 + §Spacing line 62):**
    The UI-SPEC conditional at line 457 reads: "Check chassis for existing `@page`; if absent, add `@page { margin: 1cm; size: A4 portrait; }` inside `@media print`." Revision baseline confirms `chassis.css` has NO existing `@page` rule (`grep -c "@page" chassis.css` → 0), so the conditional resolves to **ADD it**. Add `@page { margin: 1cm; size: A4 portrait; }` inside the `@media print` block (the 1cm A4 print margin is also specified in UI-SPEC §Spacing line 62). This does NOT conflict with any existing rule.

    **NOTE for reviewers (INFO):** RESEARCH.md references "7 print rules"; the LOCKED UI-SPEC and this plan use the 8 numbered block rules (Group 2a) plus the typography overrides (2b) and `@page` (2c). The locked UI-SPEC is the source of truth — follow it, not the RESEARCH.md count.

    Atomicity: one append region per `@layer components` close, one append region per `@media print` close. Diff must show **zero deletions** (purely additive).
  </action>
  <verify>
    <automated>cd qhse-cesi &amp;&amp; grep -c "Phase 5 — Fiches de révision (.fi-\*)" chassis.css | grep -q "^1$" &amp;&amp; grep -c "#panel-fiches \.fi-" chassis.css | (read n; [ "$n" -ge 20 ]) &amp;&amp; grep -c "^\s*\.fi-" chassis.css | grep -q "^0$" &amp;&amp; grep -cE "animation:|transition:" chassis.css | (read n; [ "$n" -le 0 ]) &amp;&amp; grep -c "--bg-2" chassis.css | (read n; [ "$n" -ge 5 ]) &amp;&amp; grep -c "min-height: 44px" chassis.css | (read n; [ "$n" -ge 4 ]) &amp;&amp; grep -c "page-break-before: always" chassis.css | (read n; [ "$n" -ge 2 ]) &amp;&amp; grep -c "font-size: 11pt" chassis.css | grep -q "^1$" &amp;&amp; grep -c "#panel-fiches \.fi-title { font-size: 1.5rem" chassis.css | grep -q "^1$" &amp;&amp; grep -c "#panel-fiches \.fi-section h3 { font-size: 1.15rem" chassis.css | grep -q "^1$" &amp;&amp; grep -c "#panel-fiches \.fi-cite { font-size: 0.8em" chassis.css | grep -q "^1$" &amp;&amp; grep -c "@page" chassis.css | grep -q "^1$" &amp;&amp; grep -c "size: A4 portrait" chassis.css | grep -q "^1$" &amp;&amp; echo "PASS chassis.css gates"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 5 — Fiches de révision (.fi-\*)" qhse-cesi/chassis.css` returns exactly `1`.
    - `grep -cE "^\s*\.fi-[a-z]" qhse-cesi/chassis.css` returns `0` (no bare `.fi-*` selector — every one is prefixed with `#panel-fiches`).
    - `grep -cE "#panel-fiches[^{]*\.fi-" qhse-cesi/chassis.css` returns ≥ 20 (Phase 5 has ≥ 20 distinct scoped selectors).
    - `grep -cE "^\s*:root\s*\{" qhse-cesi/chassis.css` is unchanged from pre-edit count (no new `:root` declarations introduced by this task).
    - `grep -cE "animation:|transition:" qhse-cesi/chassis.css` returns the same count as pre-edit (Phase 5 introduces zero motion).
    - `grep -c "page-break-before: always" qhse-cesi/chassis.css` returns ≥ 2 (per-fiche + sources section page-break rules).
    - `grep -c "display: none !important" qhse-cesi/chassis.css` returns ≥ 2 (panel-isolation + theme/toc hide rules in print).
    - `grep -c "details.fi-qa" qhse-cesi/chassis.css` returns ≥ 4 (collapsed state, `[open]` state, hover, print force-open).
    - **Print typography overrides (LOCKED — UI-SPEC §Print typography lines 99-108):**
      - `grep -c "font-size: 11pt" qhse-cesi/chassis.css` returns `1` (`#panel-fiches { font-size: 11pt; }`).
      - `grep -c "#panel-fiches .fi-title { font-size: 1.5rem" qhse-cesi/chassis.css` returns `1`.
      - `grep -c "#panel-fiches .fi-section h3 { font-size: 1.15rem" qhse-cesi/chassis.css` returns `1`.
      - `grep -c "#panel-fiches .fi-cite { font-size: 0.8em" qhse-cesi/chassis.css` returns `1`.
    - **Conditional `@page` (LOCKED — UI-SPEC line 457 + §Spacing line 62):**
      - `grep -c "@page" qhse-cesi/chassis.css` returns `1` (was `0` pre-edit; the UI-SPEC conditional resolves to "add it" because no `@page` existed).
      - `grep -c "size: A4 portrait" qhse-cesi/chassis.css` returns `1` and the `@page` rule includes `margin: 1cm`.
    - Diff inspection: file is purely additive (0 deletions).
    - The closing `}` of `@layer components` and `@media print` are both still present (file still parses as valid CSS).
  </acceptance_criteria>
  <done>
    chassis.css contains the `.fi-*` block inside `@layer components`, the 8 new `@media print` rules, the 4 locked print typography overrides, and the conditional `@page { margin: 1cm; size: A4 portrait; }`; all grep gates pass; existing Phase 1-4 rules byte-identical (no regression in `.fc-*` or `.qz-*` blocks).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire `fiches-data.js` skeleton + replace `#panel-fiches` placeholder + append `__fiBooted` IIFE shell in outils.html</name>
  <files>qhse-cesi/outils.html, qhse-cesi/fiches-data.js</files>
  <read_first>
    - qhse-cesi/outils.html (read sections):
      - Lines 1-50 (head, existing `<script src defer>` for outils-data.js + srs.js at lines 16-17)
      - Lines 128-131 (the existing `#panel-fiches` placeholder to replace)
      - Lines 133-173 (QCM `#panel-qcm` scaffold — closest analog)
      - Lines 911-947 (QCM IIFE boot + double-load guard + DCL pattern)
      - Lines 969-996 (QCM IIFE readPrefs/writePrefs merge-safe pattern)
      - Lines 1226-1248 (QCM IIFE source-line builder — Pattern S3)
      - Lines 1337-1358 (QCM IIFE event wiring — panel-scoped)
      - Lines 1949-1955 (end of Tests IIFE — append point for the new Fiches IIFE)
    - qhse-cesi/outils-data.js (lines 1-19 — WR-04 idempotent double-load guard pattern for fiches-data.js skeleton)
    - .planning/phases/05-fiches-de-r-vision/05-UI-SPEC.md (lines 210-380 Composition diagram + lines 462-490 Component Inventory + lines 520-533 IIFE Boot Contract)
    - .planning/phases/05-fiches-de-r-vision/05-PATTERNS.md (lines 200-450 — outils.html scaffold + IIFE patterns + script-src wiring + verbatim ports)
  </read_first>
  <action>
    Make three coordinated edits in **one commit**:

    **Edit 1 — qhse-cesi/outils.html line 18 (head, after the `srs.js` line at line 17):**
    Insert a single line: `  <script src="fiches-data.js" defer></script>` (2-space indent matching lines 16-17, no `./` prefix to match `outils-data.js` style per PATTERNS line 449). Load order MUST be: outils-data.js → srs.js → fiches-data.js.

    **Edit 2 — qhse-cesi/outils.html lines 128-131 (replace placeholder):**
    Replace the existing 4-line `#panel-fiches` placeholder with a ~35-line scaffold preserving the outer `<div role="tabpanel" id="panel-fiches" aria-labelledby="tab-fiches" tabindex="0" hidden>` wrapper exactly as-is (chassis-managed; do NOT change those attributes). Inside, mirror the QCM scaffold shape at lines 133-173 with these children (UI-SPEC §Composition lines 217-296 + §Component Inventory lines 463-489):
    - `<h2 class="sr-only">Fiches de révision</h2>` (sr-only landmark)
    - `<nav class="fi-theme" aria-label="Choisir un thème">` containing `<label for="fi-theme-select">Thème</label>` + `<select id="fi-theme-select" data-fi-theme>` with **15 `<option>` entries, NO `<option value="all">` entry** per DEC-01 / UI-SPEC line 168. Use the same value/label pairs as Phase 3/4 pickers (slugs: `duerp · principes-generaux · iso-9001 · iso-14001 · iso-45001 · tms · risque-routier · risque-chimique · rps · espaces-confines · acronymes · metiers · calendrier · icpe-seveso · rncp`; labels: French display names per UI-SPEC line 168). Default `<option selected>` is `duerp`.
    - `<nav class="fi-toc" aria-label="Sommaire de la fiche" data-fi-toc>` containing `<h3 class="sr-only">Dans cette fiche</h3>` + an empty `<ol></ol>` placeholder (the IIFE re-renders the 6 anchor links per fiche).
    - `<article class="fi-fiche" data-fi-active-fiche aria-live="polite" aria-atomic="false"></article>` — empty placeholder; the IIFE injects the header + 6 sections.
    - Trailing comment `<!-- Phase 5 Fiches engine — IIFE appended after Tests blancs IIFE; see outils.html#fiches-iife -->`

    **Edit 3 — qhse-cesi/outils.html append after line 1955 (end of Tests IIFE `})();` line):**
    Append a new `<script>` block containing the Fiches IIFE shell. Mirror the Phase 4 QCM IIFE shell verbatim (lines 911-1375) with these adaptations:
    - Banner comment: `/* ============ IIFE: Fiches de révision — read-only fiche reader ============ */` followed by `/* No globals, no inline on* handlers, no document-level keydown. Phase 5 / Plan 05-01 (plumbing). */`
    - Double-load guard: `if (window.__fiBooted) return; window.__fiBooted = true;` (distinct from `__fcViewBooted` / `__qzQcmBooted` / `__qzTestsBooted`).
    - DCL gate at the bottom mirroring lines 1370-1374 (Pitfall 1 hotfix from commit 0553899 — mandatory).
    - Pre-flight check inside `boot()`: get `panel = document.getElementById('panel-fiches')`; if null, `console.warn('[Fiches] #panel-fiches not found — cannot boot view.')` and return. Then check `!window.BANK || !Array.isArray(window.BANK) || !window.FICHES || !Array.isArray(window.FICHES)` — if true, append a `<p class="fi-error">` with French copy `'Impossible de charger les données. Recharge la page ou vérifie que outils-data.js et fiches-data.js sont bien servis.'` to the panel and `console.error('[Fiches] BANK or FICHES missing — cannot boot view.', { BANK: !!window.BANK, FICHES: !!window.FICHES })`, then return.
    - `DEFAULT_PREFS = { lastFicheTheme: 'duerp' }` (NOT `'all'`).
    - `readPrefs()` returns `{ lastFicheTheme: (typeof parsed.lastFicheTheme === 'string') ? parsed.lastFicheTheme : DEFAULT_PREFS.lastFicheTheme }` with try/catch fallback. Log prefix `[Fiches]`.
    - `writePrefs(partial)` — byte-identical merge-safe pattern (read existing → `Object.assign({}, existing, partial)` → setItem). Log prefix `[Fiches]`.
    - **Event wiring (Wave 1 stub):** wire `themeSelect.addEventListener('change', onThemeChange)` only. `onThemeChange(event)` reads `event.target.value`, calls `writePrefs({ lastFicheTheme: slug })`, then calls `renderFiche(slug)`.
    - `renderFiche(slug)` (Wave 1 stub): finds the slug via `const fiche = window.FICHES.find(f => f.slug === slug)`. If FICHES is empty (Wave 1 state) or no match, clear the `.fi-fiche` element and append a `<p class="fi-empty">` with French copy `'Aucune fiche disponible pour ce thème. Sélectionne un autre thème dans la liste.'`. **The real per-section rendering arrives in Plan 05-02** — Wave 1 ships the empty-state path only.
    - On boot: read prefs, set `themeSelect.value = prefs.lastFicheTheme || 'duerp'`; if assignment didn't take (unknown slug), fall back to `'duerp'`; then call `renderFiche(themeSelect.value)`. Also call `writePrefs({ lastFicheTheme: themeSelect.value })` once at boot to ensure the new key is materialized in localStorage (this is the merge-safe write that must NOT clobber Phase 3 + Phase 4 keys).
    - ALL listeners panel-scoped — no `document.addEventListener('keydown', ...)`, no document-level listeners.
    - Zero calls to `window.SRS.schedule(...)` anywhere in the IIFE. Zero NEW references to `qhse-srs-v1` or `qhse-scores-v1` storage keys (DEC-09 — the Fiches IIFE must not read or write either store; pre-existing Phase 3/4 references stay untouched).
    - All DOM construction via `createElement` + `textContent` (Pattern S3); zero `.innerHTML =` on BANK or FICHES content.

    **Edit 4 — qhse-cesi/fiches-data.js (NEW FILE at `qhse-cesi/fiches-data.js`):**
    Create the skeleton file mirroring `outils-data.js` lines 1-19 verbatim shape:
    - Top comment block describing the file (per PATTERNS lines 458-499 template): purpose, schema reference, consumer (Phase 5 IIFE), WR-04 guard rationale.
    - Idempotent double-load guard: `if (window.FICHES && window.FICHES.length) { if (typeof console !== 'undefined' && console.warn) { console.warn('fiches-data.js loaded twice — keeping the first FICHES'); } } else { window.FICHES = []; }`
    - **Wave 1 ships `window.FICHES = []` (empty array).** Plans 05-02..05-05 will replace this with the 15-fiche literal.

    Atomicity: all four edits land in one git commit. Diff shows: chassis.css additions (Task 1), outils.html 1 line added (head) + ~31 lines replacing 4 (panel) + ~150-line append (IIFE) + new file fiches-data.js (~20 lines).
  </action>
  <verify>
    <automated>cd qhse-cesi &amp;&amp; grep -c "script src=\"fiches-data.js\" defer" outils.html | grep -q "^1$" &amp;&amp; grep -c "id=\"fi-theme-select\"" outils.html | grep -q "^1$" &amp;&amp; grep -c "data-fi-theme" outils.html | (read n; [ "$n" -ge 1 ]) &amp;&amp; grep -c "data-fi-active-fiche" outils.html | grep -q "^1$" &amp;&amp; grep -c "__fiBooted" outils.html | grep -q "^2$" &amp;&amp; grep -cE "SRS\.schedule\(" outils.html | grep -q "^2$" &amp;&amp; SRS_PRE=$(git show HEAD:qhse-cesi/outils.html | grep -c "qhse-srs-v1"); SRS_NOW=$(grep -c "qhse-srs-v1" outils.html); [ "$SRS_NOW" -le "$SRS_PRE" ] &amp;&amp; SCORES_PRE=$(git show HEAD:qhse-cesi/outils.html | grep -c "qhse-scores-v1"); SCORES_NOW=$(grep -c "qhse-scores-v1" outils.html); [ "$SCORES_NOW" -le "$SCORES_PRE" ] &amp;&amp; grep -c "lastFicheTheme" outils.html | (read n; [ "$n" -ge 4 ]) &amp;&amp; grep -c "DOMContentLoaded" outils.html | (read n; [ "$n" -ge 4 ]) &amp;&amp; grep -c "window.FICHES" fiches-data.js | (read n; [ "$n" -ge 2 ]) &amp;&amp; grep -c "loaded twice" fiches-data.js | grep -q "^1$" &amp;&amp; echo "PASS scaffold + IIFE + data skeleton gates"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "script src=\"fiches-data.js\" defer" qhse-cesi/outils.html` returns exactly `1`.
    - The `<script src>` order in `<head>` is: outils-data.js (line ~16) → srs.js (line ~17) → fiches-data.js (line ~18). Verified by `grep -n "<script src" qhse-cesi/outils.html` showing the three lines in that order.
    - `grep -c "id=\"fi-theme-select\"" qhse-cesi/outils.html` returns exactly `1`.
    - The Fiches panel `<select id="fi-theme-select">` contains exactly 15 `<option>` elements (one per BANK theme slug, no `all`). Verified by counting `<option value=` lines between `<select id="fi-theme-select"` and the next `</select>`.
    - `grep -c "data-fi-active-fiche" qhse-cesi/outils.html` returns exactly `1` (single `<article>` placeholder).
    - `grep -c "__fiBooted" qhse-cesi/outils.html` returns exactly `2` (the check + the set in the IIFE).
    - `grep -cE "SRS\.schedule\(" qhse-cesi/outils.html` returns exactly `2` (Phase 3 flashcards + Phase 4 QCM call sites). The new Fiches IIFE adds zero new occurrences of this string.
    - **DEC-09 store-isolation invariant (robust baseline-delta form — replaces the prior literal `[ n -le 2 ]` gate, which was provably false against the real file where `qhse-srs-v1` already appears 4× and `qhse-scores-v1` 2× via legitimate Phase 3/4 SRS write paths):**
      - `PRE=$(git show HEAD:qhse-cesi/outils.html | grep -c "qhse-srs-v1"); NOW=$(grep -c "qhse-srs-v1" qhse-cesi/outils.html); [ "$NOW" -le "$PRE" ]` MUST hold (the Fiches IIFE adds ZERO new references to the SRS store). Confirmed baseline: PRE = 4.
      - `PRE=$(git show HEAD:qhse-cesi/outils.html | grep -c "qhse-scores-v1"); NOW=$(grep -c "qhse-scores-v1" qhse-cesi/outils.html); [ "$NOW" -le "$PRE" ]` MUST hold (zero new references to the scores store). Confirmed baseline: PRE = 2.
      - Intent (do NOT delete Phase 3/4 code): these gates assert the Fiches IIFE introduces no NEW store references. They must NOT be read as "reduce the count to ≤ 2" — the pre-existing legitimate Phase 3/4 references stay exactly as they are.
    - `grep -c "lastFicheTheme" qhse-cesi/outils.html` returns ≥ 4 (DEFAULT_PREFS + readPrefs + writePrefs call at boot + onThemeChange).
    - `grep -cE "document\.addEventListener\(['\"]keydown" qhse-cesi/outils.html` returns `0` for the Fiches IIFE region (no document-level keydown — Pattern S4).
    - `grep -cE "\.innerHTML\s*=\s*[^';]*window\.(BANK|FICHES)" qhse-cesi/outils.html` returns `0` (no XSS surface — Pattern S3).
    - `qhse-cesi/fiches-data.js` exists.
    - `grep -c "window.FICHES" qhse-cesi/fiches-data.js` returns ≥ 2 (guard check + assignment).
    - `node -e "global.window={}; require('./qhse-cesi/fiches-data.js'); console.log(Array.isArray(global.window.FICHES), global.window.FICHES.length);"` from repo root prints `true 0` (FICHES is an empty Array after Wave 1).
    - Booting `outils.html` in a browser shows no `[Fiches] BANK or FICHES missing` error in the console; clicking the Fiches tab shows the theme picker (15 options) and an empty fiche article + the `'Aucune fiche disponible...'` empty-state message.
    - localStorage `qhse-prefs-v1` after first boot contains the existing P3 + P4 keys unchanged, PLUS a new `lastFicheTheme: "duerp"` key. Verify by clearing localStorage, loading outils.html with a pre-seeded `qhse-prefs-v1` containing `{lastTheme:"x",lastMode:"all",newCardsPerDay:20,lastQcmTheme:"y",lastTestTheme:"z"}`, switching to Fiches tab, then reading the key — all five original keys MUST survive.
    - Phase 4 verify-quiz.cjs still exits 0 (regression check): `node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs` prints all 6 group PASS and exits 0.
    - Phase 3 verify-srs.cjs still exits 0: `node .planning/phases/03-flashcards-srs/verify-srs.cjs` exits 0.
  </acceptance_criteria>
  <done>
    The Fiches tab is wired but empty — `<select>` renders 15 theme options, the IIFE boots without console errors, switching themes persists `lastFicheTheme` to `qhse-prefs-v1` without clobbering sibling keys, the empty-state message renders when FICHES is empty. The Fiches IIFE adds zero new `qhse-srs-v1` / `qhse-scores-v1` references (baseline-delta gates hold). Phase 3 + Phase 4 verify gates still exit 0 (zero regression). The plumbing is complete and the file is ready for Plans 05-02..05-05 to fill in `window.FICHES` content and expand `renderFiche()` per-section logic.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External `<script src>` parse-load | `fiches-data.js` is fetched over HTTPS from the same origin (Vercel static host). Tampering would require Vercel compromise (out of scope). |
| Browser → localStorage (`qhse-prefs-v1`) | Fiches IIFE writes a single key (`lastFicheTheme`) via merge-safe Object.assign. A naive setItem would clobber sibling P3+P4 keys. |
| FICHES content strings → DOM | Wave 1 ships an empty FICHES array, so no content reaches the DOM yet. The IIFE empty-state path only constructs `<p class="fi-empty">` via `createElement` + `textContent` (no injection surface). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | Fiches IIFE writePrefs | mitigate | Merge-safe `Object.assign({}, existing, partial)` pattern (Pattern S2 / P4) — verify-fiches.cjs in Plan 05-06 group (b) will assert P3 + P4 sibling keys survive. |
| T-05-01-02 | Tampering / Elevation | Future FICHES content → `.innerHTML` | mitigate | Wave 1 IIFE never calls `.innerHTML =` on FICHES content (verified by grep). The `<p class="fi-empty">` path uses `createElement` + `textContent`. Plans 05-02..05-05 carry the same discipline; whitelist filter for `cadreLegal`/`demarche` HTML strings is introduced when first content lands. |
| T-05-01-03 | Tampering / Elevation | localStorage manual corruption | mitigate | Try/catch around all `JSON.parse` and `setItem` calls; graceful fallback to `DEFAULT_PREFS`; `verify-fiches.cjs` group (d) (Plan 05-06) will assert round-trip integrity. |
| T-05-01-04 | Tampering | Accidental SRS / scores mutation by Fiches IIFE (DEC-09 violation) | mitigate | Zero references to `window.SRS.schedule(...)` and zero NEW references to `qhse-srs-v1` / `qhse-scores-v1` in the Fiches IIFE — enforced by baseline-delta grep gates (the Fiches IIFE must not increase either store's reference count above the pre-edit baseline: srs-v1 PRE=4, scores-v1 PRE=2). `verify-fiches.cjs` group (e) (Plan 05-06) will replicate the runtime invariant via snapshot equality. |
| T-05-01-05 | Information disclosure / Tampering | `target="_blank"` external links without `rel="noopener noreferrer"` | mitigate | No external `<a>` constructed in Wave 1 (FICHES is empty). When Plans 05-02..05-05 land, the source-line builder (Pattern P3, verbatim port from outils.html lines 1226-1248) always pairs `target="_blank"` with `rel="noopener noreferrer"`. |
| T-05-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 5 installs zero packages (project invariant: no npm deps). No supply-chain surface. |
</threat_model>

<verification>
After Tasks 1 and 2 land, the following must be true:

1. **Grep gates** (automated, runnable from repo root):
   - `cd qhse-cesi && grep -c "Phase 5 — Fiches de révision (.fi-\*)" chassis.css` → `1`
   - `cd qhse-cesi && grep -c "@page" chassis.css` → `1` (added per UI-SPEC line 457 conditional; was 0 pre-edit)
   - `cd qhse-cesi && grep -c "font-size: 11pt" chassis.css` → `1` (locked print typography override)
   - `cd qhse-cesi && grep -c "script src=\"fiches-data.js\" defer" outils.html` → `1`
   - `cd qhse-cesi && grep -c "__fiBooted" outils.html` → `2`
   - `cd qhse-cesi && grep -cE "SRS\.schedule\(" outils.html` → `2` (Phase 3 + Phase 4 only; Fiches adds zero)
   - DEC-09 baseline-delta: `[ $(grep -c "qhse-srs-v1" qhse-cesi/outils.html) -le $(git show HEAD:qhse-cesi/outils.html | grep -c "qhse-srs-v1") ]` (true; PRE=4) AND same for `qhse-scores-v1` (PRE=2)
   - `cd qhse-cesi && grep -cE "^\s*\.fi-[a-z]" chassis.css` → `0` (no bare `.fi-*` selectors)

2. **Skeleton data load** (automated, Node):
   - `node -e "global.window={}; require('./qhse-cesi/fiches-data.js'); console.log(Array.isArray(global.window.FICHES), global.window.FICHES.length);"` → prints `true 0`

3. **Regression gates** (automated):
   - `node .planning/phases/03-flashcards-srs/verify-srs.cjs` exits 0.
   - `node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs` exits 0.

4. **Browser smoke** (manual, owner UAT — deferred to phase-level verify):
   - Open https://mes-apps-claude.vercel.app/qhse-cesi/outils.html — Fiches tab loads scaffold + empty state with zero console errors. Switching tabs back to Flashcards / QCM / Tests works as before.
</verification>

<success_criteria>
- chassis.css contains the `.fi-*` block inside `@layer components` with all selectors `#panel-fiches`-prefixed, zero new tokens, zero animations.
- chassis.css `@media print` contains the 8 new Phase 5 numbered rules + the 4 locked print typography overrides + the conditional `@page { margin: 1cm; size: A4 portrait; }`; the existing chassis §7 rules are byte-identical.
- outils.html head loads fiches-data.js after srs.js via `<script src defer>`.
- outils.html `#panel-fiches` renders the new scaffold (theme picker with 15 options, ToC nav placeholder, fiche article placeholder); the chassis-managed `role="tabpanel"` attributes are intact.
- outils.html contains the new `__fiBooted` IIFE with DCL boot, pre-flight check, merge-safe readPrefs/writePrefs, empty-state renderFiche stub, panel-scoped event wiring.
- The Fiches IIFE adds zero new `qhse-srs-v1` / `qhse-scores-v1` references (DEC-09 baseline-delta gates hold).
- qhse-cesi/fiches-data.js exists with `window.FICHES = []` skeleton + WR-04 guard.
- Phase 3 verify-srs.cjs and Phase 4 verify-quiz.cjs both still exit 0 (zero regression).
- All acceptance-criteria grep gates and the Node FICHES load check pass.
- Single atomic commit on main; pushed to GitHub; Vercel auto-deploys without build errors.
</success_criteria>

<output>
Create `.planning/phases/05-fiches-de-r-vision/05-01-SUMMARY.md` when done. Document: files changed, line counts, grep-gate results, regression check results (verify-srs + verify-quiz exit codes), and the localStorage merge-safety smoke result.
</output>
</content>
</invoke>
