---
phase: 05-fiches-de-r-vision
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - qhse-cesi/fiches-data.js
autonomous: false
requirements: [FICHE-01]
tags: [phase-5, fiches, content-authoring, iso-45001, tms, risque-routier, risque-chimique]

must_haves:
  truths:
    - "Switching the Fiches picker to `iso-45001`, `tms`, `risque-routier`, then `risque-chimique` renders 4 new full revision sheets — each with 6 sections, in-fiche ToC, and 5-10 Questions clés <details> blocks."
    - "window.FICHES.length === 8 after Plan 05-03 ships (Wave 2 four fiches + Wave 3 four fiches)."
    - "Every external URL in the four new fiches has been content-verified before commit (real <title> + topic match + soft-404 grep)."
    - "Every selectedIds[i] in the four new fiches resolves to a BANK item AND that item's theme matches the fiche.slug."
  artifacts:
    - path: "qhse-cesi/fiches-data.js"
      provides: "+4 fiche objects appended to window.FICHES: iso-45001, tms, risque-routier, risque-chimique"
      contains: "slug: 'iso-45001'"
  key_links:
    - from: "fiches-data.js iso-45001 fiche.selectedIds[]"
      to: "window.BANK items with theme === 'iso-45001'"
      via: "BANK.find(b => b.id === id)"
      pattern: "iso-45001-(qcm|flashcard)"
    - from: "fiches-data.js risque-chimique fiche.cadreLegal/demarche citations"
      to: "INRS dossier URLs + Légifrance refs"
      via: "<span class=\"fi-cite\"><a href=\"...\" target=\"_blank\" rel=\"noopener noreferrer\">"
      pattern: "INRS .* Dossier .* chimique"
---

<objective>
Author the **second 4 fiches** (`iso-45001`, `tms`, `risque-routier`, `risque-chimique`) inside `qhse-cesi/fiches-data.js`. Touches only `fiches-data.js` (the renderer was finalized in Plan 05-02 and is byte-stable through the remaining content waves).

**Why these 4 grouped:** ISO 45001 closes the ISO triplet started in Wave 2 (9001/14001/45001), and the three INRS-backbone themes (TMS / risque routier / risque chimique) share the same authority (INRS dossiers) — link verification can be done in one batch session, reducing context-switching.

Output: 8 fiches readable in the browser; 4 new fiches' URLs all content-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-fiches-de-r-vision/05-CONTEXT.md
@.planning/phases/05-fiches-de-r-vision/05-RESEARCH.md
@.planning/phases/05-fiches-de-r-vision/05-UI-SPEC.md
@.planning/phases/05-fiches-de-r-vision/05-02-SUMMARY.md
@qhse-cesi/fiches-data.js
@qhse-cesi/outils-data.js

<interfaces>
Fiche schema (UI-SPEC §Data Contract lines 497-512) — identical to Plan 05-02. See `<interfaces>` block in 05-02-PLAN.md for the full schema.

BANK theme pools available for this plan:
- `iso-45001` (OHSAS 18001 / SST)
- `tms` (Troubles Musculo-Squelettiques)
- `risque-routier`
- `risque-chimique` (CLP / SGH / VLEP)

All four themes have ≥ 5 QCM items in BANK (RESEARCH Assumption A5 verified).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append 4 fiches to fiches-data.js — iso-45001, tms, risque-routier, risque-chimique</name>
  <files>qhse-cesi/fiches-data.js</files>
  <read_first>
    - qhse-cesi/fiches-data.js (current state after Plan 05-02 — 4 fiche objects already present)
    - qhse-cesi/outils-data.js (filter BANK for each of the 4 new themes — verify pool size and pick selectedIds)
    - .planning/phases/05-fiches-de-r-vision/05-02-SUMMARY.md (URL verification format + selectedIds table structure to reuse)
    - .planning/phases/05-fiches-de-r-vision/05-UI-SPEC.md (Copywriting Contract lines 158-196 — French register, `tu` address, sober tone)
    - .planning/STATE.md (Key Decisions table — note "ISO source.url: Wikipedia FR for all three ISO themes" applies to iso-45001 too; "RPS harcèlement moral = L1152-1" precedent for TMS-adjacent law confusion)
  </read_first>
  <action>
    Append 4 new fiche objects to the existing `window.FICHES` array in `qhse-cesi/fiches-data.js`. Maintain the array's structural integrity — the file's WR-04 guard, the closing `];` and `}`, and the existing 4 fiches from Plan 05-02 must remain byte-identical.

    New fiches in this order (appended after `iso-14001`): `iso-45001`, `tms`, `risque-routier`, `risque-chimique`.

    Apply the same authoring rules as Plan 05-02 Task 2 (refer back to that plan's `<action>` block for full specs). Summary:
    - `tldr`: 30-80 French words, plain text.
    - `definitions`: 4-10 `{term, value}` pairs covering the theme's headline acronym + key terms.
    - `cadreLegal`: 2-5 paragraphs of whitelist-HTML with `<span class="fi-cite">` inline citations. Every `target="_blank"` paired with `rel="noopener noreferrer"`.
    - `demarche`: 2-5 paragraphs or 4-8 `<li>` steps, whitelist HTML.
    - `selectedIds`: 5-10 BANK item ids; each id MUST exist in BANK and have `theme === fiche.slug`.
    - `pieges`: 3-8 plain-text strings.
    - `sources`: 3-8 `{authority, ref, url}` objects; all URLs content-verified.

    **Per-fiche editorial guidance:**

    1. **`iso-45001`** (Occupational Health and Safety Management Systems):
       - tldr: SST, OHSAS 18001 ancestry, intégration HSE.
       - definitions: SMS-SST, danger, risque, leadership (lead from the top, ISO 45001 specific), participation des travailleurs, consultation, AT/MP.
       - cadreLegal: Wikipedia FR ISO 45001 (per locked decision); link to ISO 45001:2018 reference; mention OHSAS 18001 retirement (Mars 2021).
       - demarche: PDCA adapted to SST — Plan (politique SST, identification dangers, évaluation risques, exigences légales) → Do (compétences, communication, consultation) → Check (surveillance, AT/MP investigation, audit) → Act (revue, amélioration continue).
       - pieges: confusion 45001/14001/9001; ISO 45001 ≠ DUERP (the SMS is a management framework, the DUERP is the legal evaluation document); confusion participation vs. consultation.
       - sources: Wikipedia FR ISO 45001, INRS page sur les systèmes de management SST.

    2. **`tms`** (Troubles Musculo-Squelettiques):
       - tldr: première cause de MP, démarche de prévention en 4 étapes.
       - definitions: TMS, MP (Maladie Professionnelle), tableau 57 (MP membres supérieurs), tableau 98 (lombalgies), facteurs biomécaniques, facteurs organisationnels, facteurs psychosociaux (the multifactorial framework).
       - cadreLegal: INRS Dossier TMS, articles du Code du travail sur l'évaluation des risques (L4121-1/2/3 referenced in DUERP fiche — same authorities), tableau MP 57/98 (ameli).
       - demarche: démarche INRS en 4 étapes (mobiliser → investiguer → maîtriser → évaluer); ergonomie poste de travail; principes prévention (renvoi à la fiche `principes-generaux`).
       - pieges: TMS ≠ accident du travail (différence MP/AT); confusion biomécanique seule vs. multifactoriel (un piège classique en QCM); reconnaissance MP demande tableau + lien causal.
       - sources: INRS Dossier TMS (https://www.inrs.fr/risques/tms-troubles-musculosquelettiques/...), ameli.fr tableau MP 57, ameli.fr tableau MP 98.

    3. **`risque-routier`**:
       - tldr: première cause d'accidents mortels du travail, mission vs. trajet, plan de prévention.
       - definitions: accident de mission, accident de trajet, PRTM (Plan de Réduction du Trafic Mobilité), véhicule de service vs. personnel, alcoolémie au travail.
       - cadreLegal: Code du travail (mission/trajet jurisprudence), Code de la route applicable au travail, INRS Dossier risque routier, charte route INRS-CNAM.
       - demarche: 4 axes INRS (déplacements / véhicules / communications / compétences); analyse des trajets; sensibilisation + formation.
       - pieges: confusion accident de mission (AT classique) vs. accident de trajet (régime spécifique); les communications téléphoniques en conduite (kit mains-libres ≠ exonération en cas d'accident); responsabilité employeur même pour véhicule personnel utilisé pour le travail.
       - sources: INRS Dossier risque routier, ameli.fr accident de trajet, service-public.fr accident du travail vs. trajet.

    4. **`risque-chimique`** (CLP / SGH / VLEP):
       - tldr: ACD vs. CMR, démarche de substitution prioritaire, étiquetage CLP.
       - definitions: ACD (Agent Chimique Dangereux), CMR (Cancérogène Mutagène Reprotoxique), CLP (Classification Labelling Packaging), SGH (Système Général Harmonisé), VLEP (Valeur Limite d'Exposition Professionnelle), FDS (Fiche de Données de Sécurité), pictogrammes CLP.
       - cadreLegal: Règlement CLP (CE) 1272/2008; INRS Dossier risque chimique; Code du travail R4412-1 et suivants (CMR); FDS obligation R4624-23.
       - demarche: démarche INRS (repérer → évaluer → substituer si possible → protéger collectivement avant individuellement → former); ordre des priorités principes généraux L4121-2 §6 (remplacer ce qui est dangereux).
       - pieges: confusion ACD/CMR (CMR sont un sous-ensemble); pictogramme CLP "tête de mort" ≠ toxicité aiguë seule; substitution est la mesure prioritaire pour CMR, pas l'EPI; FDS doit être en français.
       - sources: INRS Dossier risque chimique, eur-lex.europa.eu CLP 1272/2008, INRS pictogrammes CLP.

    **URL verification gate** (same as Plan 05-02 Task 2 — see that plan for full procedure):
    - Every URL in `cadreLegal` / `demarche` / `sources[].url` must be content-verified before commit.
    - Record in SUMMARY.md URL-verification table: URL · `<title>` fragment · verified-on date · authority.
    - Memory `feedback_verify_links_before_ship.md` policy: real `<title>` + topic match + soft-404 grep. HTTP-status-only checks banned.

    **Diff target:** `fiches-data.js` grows from ~500-700 lines (post 05-02) to ~1000-1300 lines.
  </action>
  <verify>
    <automated>node -e "global.window={}; require('./qhse-cesi/outils-data.js'); require('./qhse-cesi/fiches-data.js'); const F = global.window.FICHES; const B = global.window.BANK; const expectedSlugs = ['duerp','principes-generaux','iso-9001','iso-14001','iso-45001','tms','risque-routier','risque-chimique']; const actualSlugs = F.map(f => f.slug); const slugsOK = JSON.stringify(actualSlugs) === JSON.stringify(expectedSlugs); const lenOK = F.length === 8; const newFiches = F.slice(4); const schemaOK = newFiches.every(f => f.selectedIds.length &gt;= 5 &amp;&amp; f.selectedIds.length &lt;= 10 &amp;&amp; f.definitions.length &gt;= 4 &amp;&amp; f.pieges.length &gt;= 3 &amp;&amp; f.sources.length &gt;= 3 &amp;&amp; f.tldr.length &gt;= 30 &amp;&amp; f.cadreLegal.includes('fi-cite') &amp;&amp; f.sources.every(s => /^https?:\/\//.test(s.url))); const xrefOK = newFiches.every(f => f.selectedIds.every(id => { const it = B.find(b => b.id === id); return it &amp;&amp; it.theme === f.slug; })); const relOK = newFiches.every(f => (f.cadreLegal + f.demarche).split('target=\"_blank\"').length === (f.cadreLegal + f.demarche).split('rel=\"noopener noreferrer\"').length); console.log({lenOK, slugsOK, schemaOK, xrefOK, relOK}); process.exit(lenOK &amp;&amp; slugsOK &amp;&amp; schemaOK &amp;&amp; xrefOK &amp;&amp; relOK ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - `window.FICHES.length === 8` after load.
    - The slug list matches exactly (in order): `['duerp','principes-generaux','iso-9001','iso-14001','iso-45001','tms','risque-routier','risque-chimique']`.
    - All 4 new fiches pass the same schema invariants as Plan 05-02 Task 2 acceptance criteria.
    - Every `selectedIds[i]` in the 4 new fiches resolves to BANK AND `BANK[id].theme === fiche.slug`.
    - Every `<a target="_blank">` in cadreLegal/demarche of the 4 new fiches is paired with `rel="noopener noreferrer"`.
    - URL verification table for the 4 new fiches committed to SUMMARY.md.
    - The first 4 fiches (from Plan 05-02) are byte-identical (no regression — verify by `git diff` showing only appended content).
    - Browser smoke (informal): selecting any of the 4 new themes from the picker renders the fiche without console errors.
    - Phase 3 + Phase 4 verify gates still exit 0.
  </acceptance_criteria>
  <done>
    8 fiches in `fiches-data.js`; 4 new fiches authored and verified; Wave 2 fiches unchanged; URL table committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner UAT — browser + print verification of the 4 new fiches</name>
  <what-built>
    4 additional fiches (iso-45001 · tms · risque-routier · risque-chimique). URL verification table claimed complete by the executor.
  </what-built>
  <how-to-verify>
    1. Wait for Vercel auto-deploy.
    2. Open `https://mes-apps-claude.vercel.app/qhse-cesi/outils.html` and switch to Fiches.
    3. Cycle through the 4 new themes (`ISO 45001`, `TMS`, `Risque routier`, `Risque chimique`) — each renders 6 sections + ToC + Questions clés cleanly. No console errors.
    4. Re-check the 4 Wave 2 fiches (DUERP / Principes généraux / ISO 9001 / ISO 14001) — they still render identically (zero regression).
    5. Ctrl+P from `risque-chimique` (longest content theme typically) — print preview shows clean A4 output: no header, no ToC, no picker, all `<details>` open, external URLs as footnotes, Sources section on its own page.
    6. Spot-check 2 random URLs in the new fiches' content — verify they land on the cited content.

    If any check fails, comment what failed. If all pass, type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New URLs in cadreLegal / demarche / sources → browser | Content verification before commit is the only mitigation. |
| Hand-authored HTML strings → safeSetHTML whitelist | Plan 05-02 Task 1 shipped the filter; this plan adds content that flows through it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering / Elevation | New HTML strings in cadreLegal/demarche | mitigate | safeSetHTML whitelist filter (shipped 05-02) sanitizes; no new code path. |
| T-05-03-02 | Information disclosure | New external URLs (off-topic / 404 / phishing drift) | mitigate | Content-verification per `feedback_verify_links_before_ship.md`; URL table in SUMMARY.md. |
| T-05-03-03 | Tampering | Reverse-tabnabbing | mitigate | rel="noopener noreferrer" forced by safeSetHTML on every external `<a>`; acceptance-criteria count equality check. |
| T-05-03-04 | Tampering | selectedIds[i] cross-reference drift | mitigate | Node check asserts every id resolves AND theme matches. |
| T-05-03-SC | Tampering | Package installs | accept | None. |
</threat_model>

<verification>
1. Node schema check (Task 1 verify).
2. URL verification table in SUMMARY.md.
3. Owner UAT (Task 2): browser + print smoke on 4 new fiches + zero regression on 4 prior fiches.
4. Phase 3 + Phase 4 verify gates still exit 0.
</verification>

<success_criteria>
- 8 fiches in FICHES; 4 new fiches authored end-to-end.
- All URLs verified.
- All selectedIds cross-resolved.
- Owner UAT approves.
- Zero regression on Phase 3/4 gates.
</success_criteria>

<output>
Create `.planning/phases/05-fiches-de-r-vision/05-03-SUMMARY.md` when done. Document diff stats, URL verification table for the 4 new fiches, selectedIds cross-reference table, UAT outcome.
</output>
