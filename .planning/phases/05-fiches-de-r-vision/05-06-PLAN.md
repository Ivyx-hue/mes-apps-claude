---
phase: 05-fiches-de-r-vision
plan: 06
type: execute
wave: 6
depends_on: [05-05]
files_modified:
  - .planning/phases/05-fiches-de-r-vision/verify-fiches.cjs
autonomous: true
requirements: [FICHE-01, FICHE-02]
tags: [phase-5, fiches, verify-gate, dec-09-invariant, schema-validation, regression-guard]

must_haves:
  truths:
    - "Running `node .planning/phases/05-fiches-de-r-vision/verify-fiches.cjs` from repo root exits with code 0 when all assertions PASS, code 1 if any FAIL."
    - "The gate covers 6 assertion groups (a-f) mirroring the Phase 4 verify-quiz.cjs skeleton."
    - "Group (a) asserts FICHES schema: length === 15, every fiche has all 9 required fields with correct types."
    - "Group (b) asserts slug coverage: FICHES slug set === BANK theme set (sorted), no duplicates, no 'all'."
    - "Group (c) asserts selectedIds[] cross-reference: every id resolves in BANK AND BANK[id].theme === fiche.slug, length ∈ [5, 10]."
    - "Group (d) asserts qhse-prefs-v1 merge-safety: writing lastFicheTheme preserves all P3 + P4 keys (lastTheme, lastMode, newCardsPerDay, lastQcmTheme, lastTestTheme)."
    - "Group (e) asserts DEC-09 read-only invariant: simulated Fiches theme change does NOT mutate qhse-srs-v1 OR qhse-scores-v1 snapshots (byte-equality before/after)."
    - "Group (f) asserts sources schema: every sources[i] has {authority, ref, url} with non-empty strings; every url matches /^https?:\\/\\//."
    - "Running the full Phase 3 + Phase 4 + Phase 5 verify chain (verify-srs.cjs && verify-quiz.cjs && verify-fiches.cjs) exits 0 — no regression."
  artifacts:
    - path: ".planning/phases/05-fiches-de-r-vision/verify-fiches.cjs"
      provides: "Node verification gate — 6 assertion groups codifying FICHE-01 + DEC-09 contracts"
      contains: "process.exit(allPassed ? 0 : 1)"
  key_links:
    - from: ".planning/phases/05-fiches-de-r-vision/verify-fiches.cjs"
      to: "qhse-cesi/{srs.js, outils-data.js, fiches-data.js}"
      via: "global.window={} shim + require()"
      pattern: "global\\.window = \\{\\}"
    - from: "verify-fiches.cjs group (e)"
      to: "DEC-09 read-only invariant (qhse-srs-v1 + qhse-scores-v1 untouched)"
      via: "JSON.stringify snapshot equality"
      pattern: "snapshotBefore.*snapshotAfter|snapshotAfter.*snapshotBefore"
---

<objective>
Ship the **Phase 5 verification gate** — a Node CLI script that codifies the FICHE-01 / FICHE-02 / DEC-09 contracts as 6 assertion groups, mirroring the Phase 3 (`verify-srs.cjs`) and Phase 4 (`verify-quiz.cjs`) discipline.

After this plan ships:
- The verify gate is the canonical regression check for Phase 5 — any future edit to `fiches-data.js`, the Fiches IIFE, or chassis.css `.fi-*` block runs through it.
- The full triple-gate chain (`verify-srs && verify-quiz && verify-fiches`) becomes the new phase-quality bar.
- Plan 05-06 owner UAT closes the phase.

**Why this is the final wave:** the gate codifies invariants that depend on the FULL FICHES content being authored (groups a-c-f all enumerate the 15 fiches; groups d-e replicate the IIFE write paths). Shipping it earlier would mean re-running for every content wave; shipping it at the end is one-and-done.

Purpose: Make the DEC-09 read-only invariant (and the FICHES schema + cross-reference + merge-safety contracts) machine-verifiable forever. Catches: cross-ref drift on future BANK edits, accidental SRS mutation in a future IIFE edit, prefs clobber regression.

Output: One Node file under `.planning/phases/05-fiches-de-r-vision/verify-fiches.cjs`. ~250-350 lines. Zero npm deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-fiches-de-r-vision/05-CONTEXT.md
@.planning/phases/05-fiches-de-r-vision/05-RESEARCH.md
@.planning/phases/05-fiches-de-r-vision/05-PATTERNS.md
@.planning/phases/05-fiches-de-r-vision/05-05-SUMMARY.md
@.planning/phases/04-qcm-tests-blancs/verify-quiz.cjs
@.planning/phases/03-flashcards-srs/verify-srs.cjs

<interfaces>
<!-- Module shapes available to the gate (loaded via global.window shim under Node) -->

After bootstrap:
- `global.window.SRS` — frozen pure module from `qhse-cesi/srs.js` (Phase 3). Group (e) uses `SRS.schedule(null, 'bien', '2026-05-24')` etc. to populate the snapshot SRS store, then asserts the simulated Fiches path does not mutate it.
- `global.window.BANK` — array of 226 items from `qhse-cesi/outils-data.js` (Phase 2). Group (c) cross-resolves `selectedIds[i]` against this.
- `global.window.FICHES` — array of 15 items from `qhse-cesi/fiches-data.js` (Phase 5). Subject of groups (a)-(c)-(f).

Required fiche schema fields (group (a)):
- `slug` (string, ∈ 15 BANK theme slugs)
- `title` (string, non-empty)
- `tldr` (string, length ∈ [30, 500])
- `definitions` (array of {term:string, value:string}, length ∈ [4, 10])
- `cadreLegal` (string, contains `fi-cite`)
- `demarche` (string, length ≥ 50)
- `selectedIds` (array of string, length ∈ [5, 10])
- `pieges` (array of string, length ∈ [3, 8])
- `sources` (array of {authority, ref, url}, length ∈ [3, 8])

The 15 BANK theme slugs (frozen Phase 2):
`duerp, principes-generaux, iso-9001, iso-14001, iso-45001, tms, risque-routier, risque-chimique, rps, espaces-confines, acronymes, metiers, calendrier, icpe-seveso, rncp`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify-fiches.cjs with 6 assertion groups + bootstrap + helpers</name>
  <files>.planning/phases/05-fiches-de-r-vision/verify-fiches.cjs</files>
  <read_first>
    - .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs (FULL file, ~399 lines — the verbatim skeleton template per PATTERNS line 552; note especially: bootstrap pattern lines 1-57, helpers pass/fail/check lines 60-80, group (d) merge-safe writePrefs assertion lines 236-272 [which already includes a Phase 5 `lastFicheTheme` scenario at lines 266-271 — reuse the structure], group (e) D-V2-03 snapshot-equality pattern lines 277-335, final exit pattern lines 389-399)
    - .planning/phases/03-flashcards-srs/verify-srs.cjs (lines 1-65 for the bootstrap pattern reference)
    - qhse-cesi/srs.js (just enough to confirm `window.SRS.schedule(state, grade, dateISO)` signature)
    - qhse-cesi/outils-data.js (header for the WR-04 guard pattern)
    - qhse-cesi/fiches-data.js (current final state — 15 fiches)
    - .planning/phases/05-fiches-de-r-vision/05-PATTERNS.md (lines 549-697 — Pattern map for verify-fiches.cjs file structure)
  </read_first>
  <action>
    Create a new Node CLI file at `.planning/phases/05-fiches-de-r-vision/verify-fiches.cjs`. Pure JavaScript using only Node built-ins (`assert`, `path`). Zero npm dependencies (project invariant per CLAUDE.md). Mirror the Phase 4 `verify-quiz.cjs` skeleton verbatim where the pattern applies; adapt assertion bodies for Phase 5 contracts.

    **File header** (~30 lines): banner comment per PATTERNS template — purpose, usage (`node .planning/phases/05-fiches-de-r-vision/verify-fiches.cjs`), exit codes (0 PASS, 1 FAIL), sibling gates declaration (`verify-srs.cjs` + `verify-quiz.cjs`), `'use strict'`, require `path` + `assert`.

    **Bootstrap block** (~40 lines): mirror verify-quiz.cjs lines 22-57 exactly, extended for the third data file. Steps:
    1. `global.window = {}` (shim — both srs.js and the data files write to `window.X`).
    2. `require(path.resolve(__dirname, '../../../qhse-cesi/srs.js'))` inside try/catch — on fail, console.error + `process.exit(1)`. After, capture `const SRS = global.window.SRS` and `assert(SRS && typeof SRS.schedule === 'function')`.
    3. `require(path.resolve(__dirname, '../../../qhse-cesi/outils-data.js'))` inside try/catch. Capture `const BANK = global.window.BANK` and assert `Array.isArray(BANK) && BANK.length >= 200`.
    4. `require(path.resolve(__dirname, '../../../qhse-cesi/fiches-data.js'))` inside try/catch. Capture `const FICHES = global.window.FICHES` and assert `Array.isArray(FICHES) && FICHES.length === 15`. (Group (a) below re-asserts length but the bootstrap should fail fast if zero.)
    5. `console.log('Phase 5 verify gate booted — SRS, BANK (' + BANK.length + ' items), FICHES (' + FICHES.length + ' items) loaded')`.

    **Helpers** (~20 lines): copy lines 60-80 of `verify-quiz.cjs` verbatim — `let allPassed = true;`, `pass(label)`, `fail(label, reason)`, `check(label, fn)`. Identical function bodies.

    **Group (a) — FICHES schema** (~40 lines):
    `check('FICHE-01 group (a) — FICHES schema (15 entries, 9 required fields, type-correct)', () => { ... })`. Inside:
    - `assert.strictEqual(FICHES.length, 15, 'FICHES must have exactly 15 entries (DEC-01 — one per BANK theme)')`.
    - For each fiche, assert presence + types per the `<interfaces>` block:
      - `typeof slug === 'string' && slug.length > 0`
      - `typeof title === 'string' && title.length > 0`
      - `typeof tldr === 'string' && tldr.length >= 30 && tldr.length <= 500`
      - `Array.isArray(definitions) && definitions.length >= 4 && definitions.length <= 10` + every entry has `term` + `value` strings
      - `typeof cadreLegal === 'string' && cadreLegal.length > 0 && cadreLegal.includes('fi-cite')`
      - `typeof demarche === 'string' && demarche.length >= 50`
      - `Array.isArray(selectedIds) && selectedIds.length >= 5 && selectedIds.length <= 10` + every entry is a non-empty string
      - `Array.isArray(pieges) && pieges.length >= 3 && pieges.length <= 8` + every entry is a non-empty string
      - `Array.isArray(sources) && sources.length >= 3 && sources.length <= 8` + every entry has `authority`, `ref`, `url` strings
    - On any assertion failure, the error message must identify the offending fiche by `slug` (e.g. `Fiche 'iso-9001' missing required field 'demarche'`).

    **Group (b) — slug coverage** (~25 lines):
    `check('FICHE-01 group (b) — FICHES slug set === BANK theme set (DEC-01 coverage)', () => { ... })`.
    - Compute `bankThemes = [...new Set(BANK.map(b => b.theme))].sort()`.
    - Compute `fichesSlugs = FICHES.map(f => f.slug).sort()`.
    - `assert.deepStrictEqual(fichesSlugs, bankThemes, 'every BANK theme must have exactly one fiche; no extras, no missing, no "all"')`.
    - Additional: assert no duplicates in FICHES: `assert.strictEqual(new Set(FICHES.map(f => f.slug)).size, 15, 'no duplicate slugs in FICHES')`.
    - Additional: assert `'all'` is NOT a slug (DEC-01 explicit): `assert(!FICHES.some(f => f.slug === 'all'), 'no all slug per DEC-01')`.

    **Group (c) — selectedIds cross-reference** (~30 lines):
    `check('FICHE-01 group (c) — every selectedIds[i] resolves in BANK AND theme matches', () => { ... })`.
    - For each fiche, for each id in `selectedIds`:
      - `const item = BANK.find(b => b.id === id)`.
      - `assert(item, 'fiche "' + fiche.slug + '" selectedIds entry "' + id + '" not found in BANK (Pitfall 7 — selectedIds drift after BANK edit)')`.
      - `assert.strictEqual(item.theme, fiche.slug, 'fiche "' + fiche.slug + '" selectedIds entry "' + id + '" has theme "' + item.theme + '" — must match fiche.slug')`.

    **Group (d) — qhse-prefs-v1 merge-safety** (~50 lines):
    `check('FICHE-01 group (d) — writing lastFicheTheme preserves P3 + P4 sibling keys', () => { ... })`.
    Replicate the Phase 4 `writePrefs` merge logic inline (per PATTERNS lines 635-647):
    - `function writePrefs(existing, partial) { return Object.assign({}, existing, partial); }`.
    - Start from the realistic post-Phase-4 prefs shape:
      `const afterPlan04 = { lastTheme: 'duerp', lastMode: 'all', newCardsPerDay: 20, lastQcmTheme: 'iso-9001', lastTestTheme: 'tms' };`
    - Scenario 1: write `lastFicheTheme: 'rps'`. Assert all 5 prior keys survive AND `lastFicheTheme === 'rps'`.
    - Scenario 2: write again `lastFicheTheme: 'duerp'`. Assert all 5 prior keys still survive AND `lastFicheTheme === 'duerp'`.
    - Scenario 3 (reverse direction): start from `{ lastFicheTheme: 'duerp' }` and write `{ lastQcmTheme: 'rps' }` — assert both survive.
    - Scenario 4: write an unknown future key `{ lastDeepReviewTheme: 'tms' }` — assert P3 + P4 + P5 keys all survive (forward-compat).

    **Group (e) — DEC-09 read-only invariant (`qhse-srs-v1` + `qhse-scores-v1` snapshot equality)** (~70 lines):
    `check('FICHE-01 group (e) — DEC-09: Fiches IIFE path does NOT mutate qhse-srs-v1 OR qhse-scores-v1', () => { ... })`. This is THE critical assertion — adapt the verify-quiz.cjs group (e) pattern (lines 277-335) but assert byte-equality for BOTH stores.
    - Build `srsStore` populated with 3-4 entries computed via `SRS.schedule(null, 'bien', '2026-05-24')`, `SRS.schedule(null, 'rate', '2026-05-25')`, etc. — exact entries borrowed from verify-quiz.cjs group (e) for parity.
    - Build `scoresStore = [{when:'2026-05-26T08:00:00Z', theme:'iso-9001', score:'15/20'}, {when:'2026-05-27T09:00:00Z', theme:'tms', score:'18/20'}]` — realistic FIFO history from Phase 4.
    - Snapshot: `const srsBefore = JSON.stringify(srsStore); const scoresBefore = JSON.stringify(scoresStore);`.
    - Simulate the Fiches IIFE path inline: pick a fiche by slug (`const fiche = FICHES.find(f => f.slug === 'duerp')`), call a stub `renderFiche(fiche)` that mimics what the IIFE does (find BANK items for selectedIds, build pretend DOM strings via JSON.stringify — no actual DOM, just the data accesses). Crucially: **do not call `SRS.schedule(...)` anywhere in this stub** (that's the DEC-09 invariant being asserted). Do not touch `srsStore` or `scoresStore`.
    - Snapshot after: `const srsAfter = JSON.stringify(srsStore); const scoresAfter = JSON.stringify(scoresStore);`.
    - Assert: `assert.strictEqual(srsAfter, srsBefore, 'DEC-09 violated: qhse-srs-v1 mutated by simulated Fiches path. Before: ' + srsBefore + '. After: ' + srsAfter);`.
    - Assert: `assert.strictEqual(scoresAfter, scoresBefore, 'DEC-09 violated: qhse-scores-v1 mutated by simulated Fiches path. Before: ' + scoresBefore + '. After: ' + scoresAfter);`.

    **Group (f) — sources schema + URL format** (~30 lines):
    `check('FICHE-02 group (f) — every sources[i] has {authority, ref, url} with https URL', () => { ... })`.
    - For each fiche, for each source `s` in `sources`:
      - `assert(typeof s.authority === 'string' && s.authority.length > 0, 'fiche ' + fiche.slug + ' source missing authority')`.
      - `assert(typeof s.ref === 'string' && s.ref.length > 0, 'fiche ' + fiche.slug + ' source missing ref')`.
      - `assert(typeof s.url === 'string' && /^https?:\/\//.test(s.url), 'fiche ' + fiche.slug + ' source url must be http(s): got "' + s.url + '"')`.

    **Final exit block** (~15 lines): mirror verify-quiz.cjs lines 389-399:
    ```
    console.log('\n' + '='.repeat(70));
    if (allPassed) {
      console.log('Phase 5 verification gate: ALL 6 groups PASS (a-b-c-d-e-f) — FICHE-01 + FICHE-02 + DEC-09 contracts intact');
      process.exit(0);
    } else {
      console.error('Phase 5 verification gate: ONE OR MORE ASSERTIONS FAILED — see [FAIL] lines above');
      process.exit(1);
    }
    ```

    **Atomicity:** single new file, single commit. No edits to other files.
  </action>
  <verify>
    <automated>node .planning/phases/05-fiches-de-r-vision/verify-fiches.cjs &amp;&amp; node .planning/phases/03-flashcards-srs/verify-srs.cjs &amp;&amp; node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs &amp;&amp; echo "PASS triple-gate"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-fiches-de-r-vision/verify-fiches.cjs` exists.
    - File contains exactly 6 `check(...)` calls (groups a-f). Verify by `grep -c "check(" verify-fiches.cjs` returning ≥ 6.
    - File contains `process.exit(0)` and `process.exit(1)` paths.
    - File uses ONLY Node built-ins: `grep -cE "require\((['\"])(?!path|assert)" verify-fiches.cjs` returns `0` (no third-party requires; only `require('path')` / `require('assert')` and the three `require(path.resolve(__dirname, '...qhse-cesi/X.js'))` loads).
    - `node .planning/phases/05-fiches-de-r-vision/verify-fiches.cjs` exits with code `0` and prints `Phase 5 verification gate: ALL 6 groups PASS` on the last summary line.
    - `node .planning/phases/03-flashcards-srs/verify-srs.cjs` exits 0 (zero regression).
    - `node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs` exits 0 (zero regression).
    - Group (e) assertion bodies contain a `JSON.stringify` snapshot pair AND assert byte-equality (verify by inspection: `grep -c "snapshotBefore\|srsBefore\|scoresBefore" verify-fiches.cjs` returns ≥ 4).
    - Negative-path smoke (manual): mutate `qhse-cesi/fiches-data.js` to drop one fiche (e.g. delete the `rncp` entry). Re-run `verify-fiches.cjs` — it MUST exit 1 with a clear failure message naming the missing slug. Then revert the fiches-data.js change and re-confirm exit 0. (This is a one-off proof; do NOT leave the file mutated.)
    - Negative-path smoke 2 (manual): inject a fake `SRS.schedule(srsStore, 'rate')` call in the group (e) stub — re-run, it MUST exit 1 with the DEC-09 violation message. Revert.
  </acceptance_criteria>
  <done>
    `verify-fiches.cjs` exists; runs exit-0 against the shipped 15-fiche `fiches-data.js`; the triple-gate chain (`verify-srs && verify-quiz && verify-fiches`) all green. Negative-path smokes confirm the gate actually catches its target regressions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner UAT — Phase 5 close-out smoke test</name>
  <what-built>
    Phase 5 is complete. 15 fiches authored, verification gate green, triple-gate chain passes. This is the phase close-out UAT — owner confirms the deliverable meets the ROADMAP SC1+SC2 contract.
  </what-built>
  <how-to-verify>
    1. From repo root, run the triple-gate chain:
       ```
       node .planning/phases/03-flashcards-srs/verify-srs.cjs && node .planning/phases/04-qcm-tests-blancs/verify-quiz.cjs && node .planning/phases/05-fiches-de-r-vision/verify-fiches.cjs && echo "ALL GREEN"
       ```
       Confirm the final line is `ALL GREEN` (exit 0 on all 3 gates).

    2. Open `https://mes-apps-claude.vercel.app/qhse-cesi/outils.html` in a clean browser. Verify the SC1 contract:
       - The Fiches tab opens. Pick any theme — fiche renders with 6 sections, inline source citations in Découverte-style provenance format.
       - Cycle through 4-5 different themes — each fiche renders cleanly.

    3. Verify the SC2 contract: from any fiche, press Ctrl+P. Confirm:
       - Sticky site header is hidden.
       - External link URLs render as footnotes (the `(https://...)` suffix after each `<a>`).
       - No dark-mode ink waste (the body is white, the text near-black).
       - All Questions clés are open (no triangle); they're readable.
       - The fiche is reasonable to print on A4 (1-3 pages depending on theme).

    4. Confirm no regression on the other 3 tabs: switch to Flashcards, grade a card; switch to QCM, answer a question; switch to Tests blancs, start a timed test then abandon it. All Phase 3 + Phase 4 behavior intact.

    5. Confirm localStorage `qhse-prefs-v1` contains all 6 expected keys (one P3, two P3-mode, two P4, one P5): `lastTheme`, `lastMode`, `newCardsPerDay`, `lastQcmTheme`, `lastTestTheme`, `lastFicheTheme`.

    Type "approved" to close Phase 5 (state-saver marks complete + retrospective trigger). Describe issues to trigger revision in the affected upstream plan.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 5 OR describe specific issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node bootstrap (`global.window={}` shim) → require() of qhse-cesi/*.js | The shim is the standard pattern across Phase 3/4 verify gates; risk is project-internal only. |
| Group (e) simulated Fiches path | The stub MUST NOT call `SRS.schedule(...)` — that's the DEC-09 invariant under test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | A future Fiches IIFE edit calls `SRS.schedule(...)` (DEC-09 violation) | mitigate | Group (e) snapshot-equality assertion catches the regression at the next CI run (or local `node verify-fiches.cjs` run). |
| T-05-06-02 | Tampering | A future BANK edit removes an id referenced by `selectedIds[]` | mitigate | Group (c) catches with a named failure pointing to the offending fiche + id. |
| T-05-06-03 | Tampering | A future IIFE edit writes `qhse-prefs-v1` via naive `setItem` (clobber regression) | mitigate | Group (d) covers the 4 merge-safety scenarios; would catch the regression if exercised in a CI environment that integrates the actual IIFE write path. **Limitation:** group (d) replicates the merge logic inline; it does not import the IIFE's writePrefs. Documented as a known coverage boundary in SUMMARY.md. |
| T-05-06-04 | Tampering | A future content edit drops a fiche (FICHES.length !== 15) | mitigate | Bootstrap + group (a) catch with `process.exit(1)`. |
| T-05-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs. |
</threat_model>

<verification>
1. `node verify-fiches.cjs` exits 0.
2. Triple-gate chain (verify-srs + verify-quiz + verify-fiches) all exit 0.
3. Negative-path smokes documented in SUMMARY.md (drop-fiche → exit 1; SRS-mutation-injection → exit 1).
4. Owner UAT confirms SC1 + SC2 ROADMAP contracts met.
</verification>

<success_criteria>
- `verify-fiches.cjs` exists, ~250-350 lines, zero npm deps.
- All 6 assertion groups (a-f) implemented and PASS against the shipped FICHES.
- Triple-gate chain all green.
- Negative-path smokes confirm the gate actually fails on its target regressions.
- Owner UAT closes Phase 5.
- Zero regression on Phase 3 + Phase 4 gates.
</success_criteria>

<output>
Create `.planning/phases/05-fiches-de-r-vision/05-06-SUMMARY.md` when done. Document:
- File path + line count of verify-fiches.cjs.
- Assertion group coverage table (a-f → contract → assertion count).
- Triple-gate chain output (full console transcript).
- Negative-path smoke results (drop-fiche + inject-SRS-mutation).
- Owner UAT outcome.
- Trigger: state-saver should mark Phase 5 complete in STATE.md + ROADMAP.md; retrospective should run at milestone close (next v2.0 phase).
</output>
