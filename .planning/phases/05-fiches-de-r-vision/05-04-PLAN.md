---
phase: 05-fiches-de-r-vision
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - qhse-cesi/fiches-data.js
autonomous: false
requirements: [FICHE-01]
tags: [phase-5, fiches, content-authoring, rps, espaces-confines, acronymes, metiers]

must_haves:
  truths:
    - "Switching the Fiches picker to `rps`, `espaces-confines`, `acronymes`, then `metiers` renders 4 new full revision sheets — each with 6 sections, ToC, and 5-10 Questions clés."
    - "window.FICHES.length === 12 after Plan 05-04 ships."
    - "All URLs in the 4 new fiches are content-verified."
    - "The `acronymes` fiche has a large Définitions section (≥ 15 entries) — exam-relevant acronym dictionary."
    - "The `metiers` fiche uses the SPA-verified francecompetences.fr + MétierScope URLs from the Phase 2 Batch F human-verification record."
  artifacts:
    - path: "qhse-cesi/fiches-data.js"
      provides: "+4 fiche objects: rps, espaces-confines, acronymes, metiers"
      contains: "slug: 'rps'"
  key_links:
    - from: "fiches-data.js rps fiche.pieges"
      to: "L1152-1 vs L1153-1 distinction"
      via: "exam trap encoded from the RPS QCM distractor pattern (per STATE.md Key Decisions)"
      pattern: "L1152-1.*L1153-1|L1153-1.*L1152-1"
    - from: "fiches-data.js metiers fiche.sources"
      to: "francecompetences.fr SPA / MétierScope SPA"
      via: "human-verified URLs from Phase 2 Batch F"
      pattern: "francecompetences\\.fr|metiersco\\.travail-emploi"
---

<objective>
Author the **third 4 fiches** (`rps`, `espaces-confines`, `acronymes`, `metiers`). Touches only `fiches-data.js`.

**Why these 4 grouped:** `rps` + `espaces-confines` close the operational-risk content. `acronymes` is a unique density profile (heavy Définitions, light prose — UI-SPEC line 80-82 acknowledges variable density per theme). `metiers` uses the SPA-verified URLs from Phase 2 Batch F (per STATE.md "Batch F: metiers/rncp + deferred SPA acronymes (25+ items) — blocking human-verify of MétierScope/France compétences SPAs") — link verification can lean on the Phase 2 verified record.

Output: 12 fiches readable; 4 new fiches' URLs verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-fiches-de-r-vision/05-CONTEXT.md
@.planning/phases/05-fiches-de-r-vision/05-RESEARCH.md
@.planning/phases/05-fiches-de-r-vision/05-UI-SPEC.md
@.planning/phases/05-fiches-de-r-vision/05-03-SUMMARY.md
@qhse-cesi/fiches-data.js
@qhse-cesi/outils-data.js
@.planning/STATE.md

<interfaces>
Fiche schema — same as Plan 05-02 (see 05-02-PLAN.md `<interfaces>`).

BANK theme pools available:
- `rps` (Risques PsychoSociaux — L1152-1 harcèlement moral, L1153-1 harcèlement sexuel; burnout)
- `espaces-confines` (O2 thresholds, ATEX, work permit)
- `acronymes` (RNCP, BC01-04, CFA, VAE, AT/MP/IT/CT acronym dictionary)
- `metiers` (ROME codes, METIERS QHSE — alternant-relevant career paths)

Per STATE.md Key Decisions:
- "RPS harcèlement moral = L1152-1 (not L1153-1) — Accuracy anchor embedded in QCM distractor" — bake into `rps` pieges.
- "Burnout not in tableau MP as of 2026" — bake into `rps` pieges or tldr.
- "SPA-only acronymes deferred to Batch F (02-06) — RNCP, CFA, VAE, BC01-BC04 — only authoritative source is francecompetences.fr SPA" — `acronymes` fiche sources must use the human-verified SPA URLs from Phase 2 Batch F (see `.planning/phases/02-content-bank/02-06-SUMMARY.md` for the verification record).
- "Batch F: metiers/rncp + deferred SPA acronymes (25+ items) — blocking human-verify of MétierScope/France compétences SPAs" — `metiers` fiche may reuse those URLs without re-running the verification (they were owner-approved in Batch F).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append 4 fiches to fiches-data.js — rps, espaces-confines, acronymes, metiers</name>
  <files>qhse-cesi/fiches-data.js</files>
  <read_first>
    - qhse-cesi/fiches-data.js (post Plan 05-03 — 8 fiches)
    - qhse-cesi/outils-data.js (filter BANK by `theme === 'rps'`, `'espaces-confines'`, `'acronymes'`, `'metiers'`)
    - .planning/phases/02-content-bank/02-06-SUMMARY.md (if present — for the Batch F SPA verification record; otherwise check git log for `02-06` commits to find the verified URLs)
    - .planning/STATE.md (Key Decisions lines 80-91 — RPS L1152-1/L1153-1 distinction, burnout MP status, SPA verification record)
    - .planning/phases/05-fiches-de-r-vision/05-UI-SPEC.md (lines 158-196 Copywriting Contract; line 81 — `acronymes` has 30+ definitions per CONTEXT — variable density is fine)
  </read_first>
  <action>
    Append 4 fiche objects to `window.FICHES` (after `risque-chimique` from Plan 05-03), in this order: `rps`, `espaces-confines`, `acronymes`, `metiers`. Apply the standard authoring rules (see 05-02-PLAN.md `<action>` for full procedure).

    **Per-fiche editorial guidance:**

    1. **`rps`** (Risques Psychosociaux):
       - tldr: RPS comme stress chronique, harcèlement, burnout-épuisement; impact santé physique et mentale; obligation employeur d'évaluation au titre du DUERP.
       - definitions: RPS, stress, harcèlement moral (L1152-1), harcèlement sexuel (L1153-1), burnout (syndrome d'épuisement professionnel), violence interne, violence externe, droit de retrait (L4131-1).
       - cadreLegal: Code du travail L1152-1 (harcèlement moral — Légifrance), L1153-1 (harcèlement sexuel — Légifrance), L4121-1/2 (obligation générale), INRS Dossier RPS, accord national interprofessionnel sur le stress 2008.
       - demarche: démarche INRS RPS en 5 étapes (préparer → analyser → traiter → suivre → évaluer); place du CSE; rôle du médecin du travail; signalement par le salarié.
       - pieges: **harcèlement moral = L1152-1, PAS L1153-1** (le piège classique encodé dans le BANK rps QCM); **burnout PAS au tableau MP** au 2026 (peut être reconnu en MP "hors tableau" via le CRRMP — rare); confusion stress (état) vs. RPS (catégorie de risques); CSE compétent en santé/sécurité, pas employeur seul.
       - sources: INRS Dossier RPS, Légifrance L1152-1, Légifrance L1153-1, ameli.fr burnout reconnaissance.

    2. **`espaces-confines`**:
       - tldr: travail en espace confiné, risques anoxie + intoxication, autorisation de travail obligatoire, surveillance permanente.
       - definitions: espace confiné, anoxie, atmosphère explosive (ATEX), seuil O2 (19% / 21%), permis de travail (work permit), surveillance permanente, sauveteur extérieur, EPI spécifiques (ARI = appareil respiratoire isolant).
       - cadreLegal: INRS Dossier espaces confinés, R4222-23 (ventilation locaux à pollution spécifique), R4225-1 (espace clos), arrêté ATEX du 8 juillet 2003.
       - demarche: procédure d'entrée en espace confiné — analyse préalable → mesure atmosphère (O2, CO, CO2, ATEX) → ventilation → permis de travail → surveillance permanente → procédure d'évacuation/sauvetage.
       - pieges: confusion espace confiné vs. espace clos (un espace clos n'est pas forcément confiné — la confined-ness vient de la difficulté d'évacuation + atmosphère non respirable); seuil O2 < 19% = atmosphère sous-oxygénée (et > 23% = sur-oxygénée, ATEX accru); l'ARI n'est PAS un masque à cartouche.
       - sources: INRS Dossier espaces confinés (URL Phase 2 Batch D), Légifrance R4222-23, ED 6184 INRS si applicable.

    3. **`acronymes`** (variable-density fiche — heavy Définitions, light prose):
       - tldr: lexique de référence pour la formation Bachelor QHSE; les acronymes les plus testés en exam.
       - definitions: ≥ 15 entries (acronymes peut justifier une section Définitions dense per CONTEXT line 81). Cover at minimum: AT (Accident du Travail), MP (Maladie Professionnelle), IT (Inspection du Travail), CT (Conditions de Travail), CSE (Comité Social et Économique), CHSCT (legacy — Comité d'Hygiène, Sécurité et Conditions de Travail, supprimé en 2017), DUERP, SMQ, SME, SMS-SST, CMR, ACD, CLP, SGH, VLEP, FDS, EPI (Équipement de Protection Individuelle), EPC (Équipement de Protection Collective), RPS, TMS, ICPE, Seveso, RNCP, BC01/02/03/04 (Blocs de Compétences du RNCP41446), CFA, VAE, OPCO.
       - cadreLegal: cite framework — chaque acronyme renvoie à son article CT ou norme; donner 2-3 références au plus pour des acronymes structurants (DUERP → R4121-1; CSE → L2311-1; ICPE → L511-1 Code de l'environnement). Pas besoin d'un cadreLegal lourd pour cette fiche.
       - demarche: paragraphe court expliquant comment utiliser cette fiche (mémoriser par bloc thématique, croiser avec la fiche du thème associé).
       - pieges: CHSCT supprimé en 2017 (devenu CSE); BC01..BC04 sont les blocs **du RNCP41446** spécifiquement (Bachelor QHSE CESI) — pas universels; SMS-SST ≠ DUERP; EPI vs. EPC ordre des principes.
       - sources: francecompetences.fr SPA pour RNCP/BC (URL human-verified Batch F per STATE.md), Légifrance pour les acronymes du Code du travail, ameli.fr pour AT/MP.

    4. **`metiers`** (Métiers ROME / parcours alternance QHSE):
       - tldr: métiers cibles du Bachelor QHSE; codes ROME associés; rôle de l'alternance comme passerelle.
       - definitions: ROME (Répertoire Opérationnel des Métiers et des Emplois), code ROME H1302 (Management et ingénierie HSE), code H1303 (Intervention technique en HSE industriel), responsable QSE, animateur prévention, ingénieur HSE, auditeur QSE.
       - cadreLegal: France Compétences (RNCP enregistrement), Pôle Emploi (référentiel ROME), articles L6313-1 du Code du travail sur la formation pro.
       - demarche: comment lire le RNCP41446 + référentiel ROME — quels métiers cibles, quelles compétences attendues, comment se positionner en alternance (rôle de l'OPCO, du CFA, du tuteur entreprise).
       - pieges: ROME ≠ RNCP (deux référentiels différents — ROME = métiers/Pôle Emploi, RNCP = certifications); le code H1302 couvre management/ingénierie HSE, pas l'opérationnel terrain (H1303); l'animateur prévention n'est pas une certification mais une fonction.
       - sources: MétierScope (URL human-verified Batch F), francecompetences.fr SPA RNCP41446 (URL human-verified Batch F), Pôle Emploi / Travail-Emploi.gouv.fr.

    **URL strategy for SPAs:**
    - `acronymes` and `metiers` use francecompetences.fr / MétierScope URLs that were human-verified in Phase 2 Batch F (`02-06-PLAN.md` / `02-06-SUMMARY.md`). The executor MUST cross-check those URLs against the current Batch F record (read `.planning/phases/02-content-bank/02-06-SUMMARY.md` for the verified URL list — if SUMMARY missing, grep `outils-data.js` for the verified URLs that already shipped under `theme === 'metiers'` or `theme === 'acronymes'` and re-use those).
    - Re-running content verification on SPA URLs is acceptable but not mandatory: the URLs were owner-approved in Batch F.

    **URL verification gate** (same as prior content waves): every NON-SPA URL (Légifrance, INRS, ameli, service-public.fr) must be content-verified before commit; SPA URLs verified against Batch F record.

    **Diff target:** `fiches-data.js` grows from ~1000-1300 lines (post 05-03) to ~1500-1900 lines (the `acronymes` fiche's definitions list alone adds ~50-80 lines).
  </action>
  <verify>
    <automated>node -e "global.window={}; require('./qhse-cesi/outils-data.js'); require('./qhse-cesi/fiches-data.js'); const F = global.window.FICHES; const B = global.window.BANK; const expectedSlugs = ['duerp','principes-generaux','iso-9001','iso-14001','iso-45001','tms','risque-routier','risque-chimique','rps','espaces-confines','acronymes','metiers']; const actualSlugs = F.map(f => f.slug); const slugsOK = JSON.stringify(actualSlugs) === JSON.stringify(expectedSlugs); const lenOK = F.length === 12; const newFiches = F.slice(8); const schemaOK = newFiches.every(f => f.selectedIds.length &gt;= 5 &amp;&amp; f.selectedIds.length &lt;= 10 &amp;&amp; f.pieges.length &gt;= 3 &amp;&amp; f.sources.length &gt;= 3 &amp;&amp; f.tldr.length &gt;= 30 &amp;&amp; f.sources.every(s => /^https?:\/\//.test(s.url))); const xrefOK = newFiches.every(f => f.selectedIds.every(id => { const it = B.find(b => b.id === id); return it &amp;&amp; it.theme === f.slug; })); const acronymes = F.find(f => f.slug === 'acronymes'); const acroOK = acronymes.definitions.length &gt;= 15; const rpsPieges = F.find(f => f.slug === 'rps').pieges.join(' '); const rpsOK = /L1152-1/.test(rpsPieges) &amp;&amp; /L1153-1/.test(rpsPieges); console.log({lenOK, slugsOK, schemaOK, xrefOK, acroOK, rpsOK}); process.exit(lenOK &amp;&amp; slugsOK &amp;&amp; schemaOK &amp;&amp; xrefOK &amp;&amp; acroOK &amp;&amp; rpsOK ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - `window.FICHES.length === 12` with the exact slug list (in order) given in the Node check above.
    - All standard schema invariants pass for the 4 new fiches.
    - `acronymes.definitions.length >= 15` (per CONTEXT line 81 — heavy definitions section).
    - `rps.pieges` joined contains both `L1152-1` AND `L1153-1` strings (the exam-trap distinction is documented).
    - `metiers.sources[]` contains at least 1 URL pointing to `francecompetences.fr` or `metiersco.travail-emploi` (the SPA-verified sources).
    - Every `selectedIds[i]` in the 4 new fiches resolves AND theme-matches.
    - Every `target="_blank"` paired with `rel="noopener noreferrer"`.
    - URL verification table in SUMMARY.md (NON-SPA URLs verified now; SPA URLs annotated as "Verified Batch F 2026-05-XX — see 02-06-SUMMARY").
    - Wave 2 + Wave 3 fiches (slugs 0-7) byte-identical (no regression).
    - Phase 3 + Phase 4 verify gates still exit 0.
  </acceptance_criteria>
  <done>
    12 fiches in `fiches-data.js`; 4 new fiches authored with full URL provenance.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner UAT — browser + print on 4 new fiches</name>
  <what-built>
    4 new fiches: RPS · Espaces confinés · Acronymes · Métiers. URL provenance documented.
  </what-built>
  <how-to-verify>
    1. Wait for Vercel auto-deploy.
    2. Open Fiches tab; cycle through `RPS`, `Espaces confinés`, `Acronymes`, `Métiers`.
    3. Verify the `Acronymes` Définitions section renders ≥ 15 `<dt>/<dd>` pairs (visual scan).
    4. Verify the `RPS` Pièges section explicitly mentions both `L1152-1` and `L1153-1` (the harcèlement distinction).
    5. Spot-check 2 random URLs in `Métiers` and `Acronymes` — they should land on francecompetences.fr / MétierScope SPA content (these were human-verified in Phase 2 Batch F; re-verify the URLs still resolve).
    6. Ctrl+P from `Acronymes` (a heavy-Définitions fiche; print should still be clean).
    7. Re-check the 8 prior fiches — no regression.

    Type "approved" or describe issues.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA URLs (francecompetences.fr / MétierScope) | Stateful single-page apps; URLs may rot if path scheme changes upstream. Phase 2 Batch F record is the authoritative verification timestamp. |
| New HTML strings → safeSetHTML | Same path as prior waves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information disclosure | SPA URL rot (francecompetences.fr / MétierScope) | mitigate | Reuse Batch F human-verification record; re-verify resolve at UAT (Task 2 step 5). |
| T-05-04-02 | Tampering | RPS L1152-1/L1153-1 accuracy anchor regression | mitigate | Acceptance criterion explicitly asserts both strings appear in `rps.pieges`. |
| T-05-04-03 | Tampering / Elevation | New HTML strings in cadreLegal/demarche | mitigate | safeSetHTML whitelist (shipped 05-02). |
| T-05-04-04 | Tampering | selectedIds[i] cross-reference | mitigate | Node check asserts every id resolves AND theme matches. |
| T-05-04-SC | Tampering | Package installs | accept | None. |
</threat_model>

<verification>
1. Node schema + accuracy-anchor check (Task 1 verify).
2. URL verification table in SUMMARY.md.
3. Owner UAT (Task 2).
4. Phase 3 + Phase 4 verify gates still exit 0.
</verification>

<success_criteria>
- 12 fiches in FICHES; 4 new authored.
- Accuracy anchors preserved (RPS L1152-1/L1153-1 distinction, burnout MP status).
- All URLs verified or sourced from Batch F record.
- Owner UAT approves.
- Zero regression.
</success_criteria>

<output>
Create `.planning/phases/05-fiches-de-r-vision/05-04-SUMMARY.md` when done. Document diff stats, URL verification table (with SPA Batch F annotations), selectedIds cross-reference table, accuracy-anchor preservation check, UAT outcome.
</output>
