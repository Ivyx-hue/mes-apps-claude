---
phase: 05-fiches-de-r-vision
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - qhse-cesi/fiches-data.js
autonomous: false
requirements: [FICHE-01]
tags: [phase-5, fiches, content-authoring, calendrier, icpe-seveso, rncp]

must_haves:
  truths:
    - "Switching the Fiches picker to `calendrier`, `icpe-seveso`, then `rncp` renders 3 new full revision sheets — completing the 15-fiche coverage required by DEC-01."
    - "window.FICHES.length === 15 after Plan 05-05 ships; the slug set exactly matches the 15 BANK theme slugs."
    - "The `calendrier` fiche reflects the 2026-05-20 live-refetch of the alternance remuneration percentages (27/43/53/100% 1re année unchanged per STATE.md)."
    - "The `rncp` fiche cites RNCP41446 with BC01-BC04 blocs verified against the francecompetences.fr SPA URL from Phase 2 Batch F."
    - "All URLs content-verified before commit (or annotated as Batch F SPA reuse)."
  artifacts:
    - path: "qhse-cesi/fiches-data.js"
      provides: "+3 fiche objects: calendrier, icpe-seveso, rncp. Total FICHES.length === 15."
      contains: "slug: 'rncp'"
  key_links:
    - from: "fiches-data.js calendrier fiche.cadreLegal"
      to: "service-public.fr calendrier rémunération apprenti URL"
      via: "<span class=\"fi-cite\">"
      pattern: "(27|43|53|100)\\s*%"
    - from: "fiches-data.js rncp fiche.sources"
      to: "francecompetences.fr RNCP41446 SPA URL (Batch F verified)"
      via: "Batch F human-verification record"
      pattern: "RNCP41446|francecompetences\\.fr"
---

<objective>
Author the **final 3 fiches** (`calendrier`, `icpe-seveso`, `rncp`). After this plan ships, `window.FICHES.length === 15` and DEC-01's "1 fiche per BANK theme" contract is fully satisfied. Touches only `fiches-data.js`.

**Why these 3 grouped (and not paired up to 4):** these are the three "specialized reference" themes — `calendrier` is a single table-dominant theme, `icpe-seveso` is regulatory-classification-heavy, `rncp` is meta (the formation's own framework). Each is structurally different from the operational risk themes; grouping them lets the executor finish the phase in one batch and surfaces any structural edge cases (e.g. very thin `calendrier` content per UI-SPEC line 81 "themes with thin material may run 1 page only").

Output: 15 fiches complete; all source URLs verified; ready for Plan 05-06 verification gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-fiches-de-r-vision/05-CONTEXT.md
@.planning/phases/05-fiches-de-r-vision/05-RESEARCH.md
@.planning/phases/05-fiches-de-r-vision/05-UI-SPEC.md
@.planning/phases/05-fiches-de-r-vision/05-04-SUMMARY.md
@qhse-cesi/fiches-data.js
@qhse-cesi/outils-data.js
@.planning/STATE.md

<interfaces>
Fiche schema — same as prior content plans.

BANK theme pools available:
- `calendrier` (alternance rythme + rémunération 27/43/53/100% — verified live-refetch 2026-05-20 per STATE.md)
- `icpe-seveso` (ICPE classification, Seveso seuil haut/bas, dossier d'autorisation)
- `rncp` (RNCP41446 Bachelor QHSE CESI, BC01-BC04 blocs de compétences)

Per STATE.md Key Decisions:
- "Calendrier rémunération % live-re-fetched 2026-05-20 | RESEARCH snapshot confirmed accurate (27/43/53/100% 1re année unchanged); source.verified = 2026-05-20" — bake into `calendrier` fiche with the verified figures.
- "SPA-only acronymes deferred to Batch F (02-06) — RNCP, CFA, VAE, BC01-BC04 — only authoritative source is francecompetences.fr SPA" — the `rncp` fiche reuses the francecompetences.fr URL from Batch F.
- "Batch E: icpe-seveso/calendrier/acronymes (43 items) — calendrier % live-refetch" — `icpe-seveso` BANK items shipped in Batch E with verified URLs (AIDA, F33414, F2918, F15478 per STATE.md line 91).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append final 3 fiches to fiches-data.js — calendrier, icpe-seveso, rncp</name>
  <files>qhse-cesi/fiches-data.js</files>
  <read_first>
    - qhse-cesi/fiches-data.js (post Plan 05-04 — 12 fiches)
    - qhse-cesi/outils-data.js (filter BANK for `theme === 'calendrier'`, `'icpe-seveso'`, `'rncp'`)
    - .planning/STATE.md (Key Decisions — calendrier % verified, ICPE/Seveso Batch E sources, RNCP Batch F SPA verification)
    - .planning/phases/02-content-bank/02-05-SUMMARY.md and 02-06-SUMMARY.md (if present — for Batch E + Batch F verification records)
    - .planning/phases/05-fiches-de-r-vision/05-UI-SPEC.md (line 81 — variable density acceptable; `calendrier` may legitimately run 1 page only)
  </read_first>
  <action>
    Append the final 3 fiche objects to `window.FICHES` (after `metiers` from Plan 05-04), in this order: `calendrier`, `icpe-seveso`, `rncp`. Apply the standard authoring rules.

    **Per-fiche editorial guidance:**

    1. **`calendrier`** (alternance rythme + rémunération):
       - tldr: rythme alternance Bachelor QHSE CESI (rappel — semaines école / semaines entreprise; à confirmer pour la promotion 2026); barème rémunération apprenti selon âge et année de contrat.
       - definitions: contrat d'apprentissage, contrat de professionnalisation, rythme alternance, smic (au moment de la fiche; valeur 2026), CFA, OPCO, maître d'apprentissage.
       - cadreLegal: barème de rémunération apprenti — service-public.fr; Code du travail D6222-26 (rémunération minimale apprenti); accords de branche éventuels.
       - demarche: courte fiche pratique — quoi calculer (% smic × année × âge), où trouver le calculateur (service-public.fr), comment lire son bulletin de paie en alternance.
       - **Live-verified values per STATE.md (1re année apprenti, vérifié 2026-05-20):** moins de 18 ans = 27% SMIC, 18-20 ans = 43%, 21-25 ans = 53%, 26 ans et + = 100% (SMIC entier). 2e année et 3e année: voir tableau service-public.fr (pas besoin de tout dupliquer — référer le lecteur vers le tableau officiel).
       - pieges: confusion contrat apprentissage / contrat professionnalisation (régimes distincts); l'apprenti est rémunéré en % du SMIC OU du salaire minimum conventionnel si plus favorable; la 26+ ans 100% SMIC ≠ SMIC obligatoire (c'est le minimum légal — l'employeur peut payer plus).
       - sources: service-public.fr fiche F2918 (rémunération apprenti — verified Batch E per STATE.md), Légifrance D6222-26.

    2. **`icpe-seveso`**:
       - tldr: ICPE = Installations Classées pour la Protection de l'Environnement; régime de déclaration / enregistrement / autorisation selon nomenclature; Seveso = directive UE pour les sites à risque accident majeur (seuil haut / seuil bas).
       - definitions: ICPE, nomenclature ICPE (rubriques), déclaration / enregistrement / autorisation (3 régimes), Seveso seuil haut, Seveso seuil bas, AIDA (base de données réglementaire ICPE), POI (Plan d'Opération Interne), PPI (Plan Particulier d'Intervention), DREAL (autorité de contrôle), inspecteur ICPE.
       - cadreLegal: Code de l'environnement L511-1 et suivants (ICPE), Directive Seveso III 2012/18/UE, AIDA — base de données ICPE (verified Batch E per STATE.md), fiche service-public.fr F33414 (ICPE entreprise — verified Batch E).
       - demarche: comment classer une activité — consulter la nomenclature ICPE → identifier la rubrique → identifier le régime (D/E/A) → constituer le dossier → DREAL instruit; pour Seveso, calcul de quantité de substance × seuil.
       - pieges: confusion ICPE et Seveso (Seveso est un sous-ensemble — seules certaines ICPE sont Seveso seuil haut/bas); confusion régime déclaration / enregistrement / autorisation (croissance des contraintes); l'inspecteur ICPE relève de la DREAL, pas de l'Inspection du Travail.
       - sources: service-public.fr F33414 (ICPE entreprise — Batch E URL), AIDA INERIS (Batch E URL), Légifrance L511-1, ineris.fr fiche Seveso.

    3. **`rncp`** (RNCP41446 Bachelor QHSE CESI + BC01-BC04):
       - tldr: RNCP41446 = certification professionnelle Bachelor QHSE de CESI; 4 blocs de compétences (BC01-BC04); validité 5 ans renouvelables; reconnu titre RNCP niveau 6 (Bac+3).
       - definitions: RNCP (Répertoire National des Certifications Professionnelles), France Compétences, BC01 (premier bloc — à confirmer libellé exact depuis fiche), BC02, BC03, BC04, niveau de qualification (niveau 6 = Bac+3, cadre européen EQF), bloc de compétences (modalité d'acquisition par capitalisation).
       - cadreLegal: francecompetences.fr fiche RNCP41446 (URL verified Batch F per STATE.md), Code du travail L6113-1 (blocs de compétences), code éducation L335-5 (niveaux de qualification).
       - demarche: comment lire la fiche RNCP — repérer les blocs, repérer les compétences attendues par bloc, identifier les modalités d'évaluation; rôle du jury de certification CESI.
       - pieges: RNCP41446 est CESI-spécifique — d'autres organismes ont leur propre RNCP Bachelor QHSE (vérifier la fiche RNCP pour identifier l'organisme); les BC01-BC04 ne sont pas universels (chaque RNCP a ses propres blocs); niveau 6 RNCP ≠ niveau 6 EQF (compatibilité oui, identité non — l'EQF est européen).
       - sources: francecompetences.fr RNCP41446 SPA (URL verified Batch F — annotate "Verified Batch F 2026-05-XX, source.verified date from outils-data.js metiers/rncp items"), Code du travail L6113-1.

    **URL strategy:**
    - `calendrier`: service-public.fr URLs were verified in Batch E (per STATE.md line 91); re-confirm they still resolve before commit.
    - `icpe-seveso`: AIDA, F33414, F2918, F15478 URLs were Batch E verified per STATE.md line 91; re-confirm.
    - `rncp`: francecompetences.fr SPA URL was Batch F human-verified per STATE.md; re-confirm it still resolves (SPA URLs are most likely to drift).
    - All NEW URLs introduced in this plan (e.g. ineris.fr Seveso page if not already in BANK) must be content-verified per `feedback_verify_links_before_ship.md`.

    **Diff target:** `fiches-data.js` grows from ~1500-1900 lines (post 05-04) to ~1900-2300 lines (these 3 fiches are mid-density).

    **Phase coverage check:** after this commit, run a one-liner to assert the slug set matches the 15 BANK theme slugs exactly (acceptance criteria below).
  </action>
  <verify>
    <automated>node -e "global.window={}; require('./qhse-cesi/outils-data.js'); require('./qhse-cesi/fiches-data.js'); const F = global.window.FICHES; const B = global.window.BANK; const expectedSlugs = ['duerp','principes-generaux','iso-9001','iso-14001','iso-45001','tms','risque-routier','risque-chimique','rps','espaces-confines','acronymes','metiers','calendrier','icpe-seveso','rncp']; const actualSlugs = F.map(f => f.slug); const slugsOK = JSON.stringify(actualSlugs) === JSON.stringify(expectedSlugs); const lenOK = F.length === 15; const newFiches = F.slice(12); const schemaOK = newFiches.every(f => f.selectedIds.length &gt;= 5 &amp;&amp; f.selectedIds.length &lt;= 10 &amp;&amp; f.pieges.length &gt;= 3 &amp;&amp; f.sources.length &gt;= 3 &amp;&amp; f.tldr.length &gt;= 30 &amp;&amp; f.sources.every(s => /^https?:\/\//.test(s.url))); const xrefOK = newFiches.every(f => f.selectedIds.every(id => { const it = B.find(b => b.id === id); return it &amp;&amp; it.theme === f.slug; })); const calendrier = F.find(f => f.slug === 'calendrier'); const calendrierText = calendrier.tldr + ' ' + calendrier.cadreLegal + ' ' + calendrier.demarche + ' ' + calendrier.pieges.join(' '); const calOK = /27\s*%/.test(calendrierText) &amp;&amp; /43\s*%/.test(calendrierText) &amp;&amp; /53\s*%/.test(calendrierText) &amp;&amp; /100\s*%/.test(calendrierText); const rncp = F.find(f => f.slug === 'rncp'); const rncpText = rncp.cadreLegal + ' ' + rncp.sources.map(s => s.url + ' ' + s.ref).join(' '); const rncpOK = /RNCP41446|francecompetences\.fr/.test(rncpText); const bankSlugs = [...new Set(B.map(b => b.theme))].sort(); const fichesSlugsSorted = F.map(f => f.slug).sort(); const coverageOK = JSON.stringify(bankSlugs) === JSON.stringify(fichesSlugsSorted); console.log({lenOK, slugsOK, schemaOK, xrefOK, calOK, rncpOK, coverageOK, bankSlugs, fichesSlugsSorted}); process.exit(lenOK &amp;&amp; slugsOK &amp;&amp; schemaOK &amp;&amp; xrefOK &amp;&amp; calOK &amp;&amp; rncpOK &amp;&amp; coverageOK ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - `window.FICHES.length === 15` with the exact slug list given in the Node check (in order).
    - All 4 new fiches pass schema invariants.
    - `calendrier` text (tldr + cadreLegal + demarche + pieges joined) contains all four percentages `27%`, `43%`, `53%`, `100%` (the 2026-05-20 verified barème).
    - `rncp` content references `RNCP41446` and `francecompetences.fr` at least once.
    - Coverage check: `[...new Set(window.BANK.map(b => b.theme))].sort()` equals `window.FICHES.map(f => f.slug).sort()` (every BANK theme has exactly one fiche).
    - Every `selectedIds[i]` resolves AND theme-matches.
    - Every `target="_blank"` paired with `rel="noopener noreferrer"`.
    - URL verification table in SUMMARY.md (Batch E + Batch F URLs annotated with verification source; any new URLs verified fresh).
    - Wave 2/3/4 fiches (slugs 0-11) byte-identical.
    - Phase 3 + Phase 4 verify gates still exit 0.
  </acceptance_criteria>
  <done>
    All 15 fiches in `fiches-data.js`. DEC-01 coverage contract satisfied. Phase 5 content authoring complete; ready for Plan 05-06 verify gate.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner UAT — final 3 fiches + full-phase smoke test on all 15</name>
  <what-built>
    The final 3 fiches (calendrier · icpe-seveso · rncp). Total: 15 fiches covering every BANK theme. URL provenance documented.
  </what-built>
  <how-to-verify>
    1. Wait for Vercel auto-deploy.
    2. Open Fiches tab; cycle through `Calendrier alternance`, `ICPE / Seveso`, `RNCP`.
    3. Verify `Calendrier` shows the 27/43/53/100% barème (1re année).
    4. Verify `RNCP` cites RNCP41446 + francecompetences.fr.
    5. **Full-phase smoke**: cycle through ALL 15 themes in the picker. Each must render its 6-section fiche without console errors. Watch for: missing fields, broken ToC anchors, unresolved Questions clés.
    6. Ctrl+P from 3 different fiches of different densities (e.g. `acronymes` heavy / `duerp` medium / `calendrier` thin). All should print cleanly.
    7. Spot-check 3 random URLs from the new 3 fiches — they should land on the cited content (no 404 / drift).
    8. Confirm `qhse-prefs-v1` localStorage still contains all P3 + P4 + P5 keys after browsing all 15 fiches.

    Type "approved" or describe issues.
  </how-to-verify>
  <resume-signal>Type "approved" to advance to Plan 05-06 (verify gate). Describe issues to trigger revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Batch E + Batch F URLs (already-verified) | Reuse of prior verification; re-confirmed resolves at commit time. |
| `calendrier` 27/43/53/100% values | Accuracy-anchor: must match STATE.md 2026-05-20 live-refetch record. |
| `rncp` RNCP41446 + BC01-04 references | Bachelor QHSE CESI specificity — accuracy anchor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering / Information disclosure | `calendrier` percentage drift | mitigate | Acceptance criterion asserts all 4 percentages appear in fiche text; STATE.md 2026-05-20 record is the anchor. |
| T-05-05-02 | Information disclosure | `rncp` SPA URL rot | mitigate | Re-verify at commit; Batch F record is the prior anchor. |
| T-05-05-03 | Tampering / Elevation | New HTML strings | mitigate | safeSetHTML whitelist (shipped 05-02). |
| T-05-05-04 | Tampering | selectedIds cross-reference | mitigate | Node check. |
| T-05-05-05 | Tampering | Coverage drift (FICHES slug set ≠ BANK theme set) | mitigate | Coverage acceptance criterion asserts the two sorted lists are equal — Plan 05-06 verify-fiches.cjs codifies. |
| T-05-05-SC | Tampering | Package installs | accept | None. |
</threat_model>

<verification>
1. Node schema + accuracy-anchor + coverage check (Task 1 verify).
2. URL verification table in SUMMARY.md.
3. Owner UAT (Task 2) — final 3 fiches + full-phase smoke on all 15.
4. Phase 3 + Phase 4 verify gates still exit 0.
</verification>

<success_criteria>
- 15 fiches in FICHES with the exact BANK theme slug coverage.
- All accuracy anchors preserved.
- All URLs verified.
- Owner UAT approves both the 3 new fiches AND the full-phase smoke.
- Zero regression on Phase 3/4 gates.
</success_criteria>

<output>
Create `.planning/phases/05-fiches-de-r-vision/05-05-SUMMARY.md` when done. Document: diff stats, URL verification table (with Batch E/F annotations), selectedIds cross-reference table, accuracy-anchor preservation check, full-phase smoke outcome (all 15 fiches), UAT result.
</output>
