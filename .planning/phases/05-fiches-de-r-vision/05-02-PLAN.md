---
phase: 05-fiches-de-r-vision
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - qhse-cesi/fiches-data.js
  - qhse-cesi/outils.html
autonomous: false
requirements: [FICHE-01]
tags: [phase-5, fiches, content-authoring, duerp, principes-generaux, iso-9001, iso-14001]

must_haves:
  truths:
    - "Opening the Fiches tab, selecting `duerp` from the picker renders a full revision sheet: title 'DUERP', meta line, 6 sections (TL;DR, Définitions, Cadre légal, Démarche [+ Questions clés], Pièges fréquents, Sources) in that exact order, with inline citations and 5-10 Questions clés <details> blocks."
    - "Switching the picker to `principes-generaux`, `iso-9001`, then `iso-14001` re-renders the corresponding fiches with the same 6-section structure — no console errors."
    - "Each rendered fiche has an in-fiche ToC with 6 anchor links pointing to the 6 section ids (fi-s-tldr, fi-s-defs, fi-s-cadre, fi-s-demarche, fi-s-pieges, fi-s-sources); clicking each scrolls smoothly to the section."
    - "Every Questions clés <details> resolves to a valid BANK item (no `undefined` displays); each shows answer, explanation, and a source line with authority + <code>ref</code> + <a> URL."
    - "Every external URL inside the four fiches' `sources[]` and inline `cadreLegal/demarche` citations has been content-verified (real <title> + topic match + soft-404 grep) before commit, per feedback_verify_links_before_ship.md."
  artifacts:
    - path: "qhse-cesi/fiches-data.js"
      provides: "4 fiche objects: duerp, principes-generaux, iso-9001, iso-14001"
      contains: "slug: 'duerp'"
    - path: "qhse-cesi/outils.html"
      provides: "Expanded renderFiche() with full 6-section rendering + ToC injection + focus management on theme change"
      contains: "buildFiche"
  key_links:
    - from: "qhse-cesi/outils.html (Fiches IIFE renderFiche)"
      to: "window.FICHES"
      via: "FICHES.find(f => f.slug === slug)"
      pattern: "window\\.FICHES\\.find"
    - from: "qhse-cesi/outils.html (Fiches IIFE buildQuestionsCles)"
      to: "window.BANK"
      via: "BANK.find(b => b.id === id) for each selectedIds[i]"
      pattern: "window\\.BANK\\.find"
---

<objective>
Author the **first 4 fiches** (`duerp`, `principes-generaux`, `iso-9001`, `iso-14001`) inside `qhse-cesi/fiches-data.js` AND expand the Fiches IIFE's `renderFiche()` from the empty-state stub (shipped in Plan 05-01) into the full 6-section renderer + ToC injector. This is the first content wave; the IIFE rendering logic is finalized here once and reused unchanged by Plans 05-03..05-05.

**Why these 4 first:** DUERP is the most-cited theme in the Bachelor QHSE syllabus and the default fiche per UI-SPEC line 169. Principes généraux + ISO 9001 + ISO 14001 form the foundation block. Owner can immediately verify the rendering pipeline on the most exam-relevant content.

**Why bundle IIFE expansion with the first content batch (not a separate plan):** the renderFiche/buildFiche helpers can only be designed once we have real fiche data to test them against. Plumbing-only (Plan 05-01) shipped an empty-state path; this plan ships the populated path. Subsequent content plans (05-03..05-05) touch only `fiches-data.js`.

**Two independently-committable units within this plan:** Task 1 (the mechanical IIFE renderer in `outils.html`) and Task 2 (the 4-fiche prose content in `fiches-data.js`) touch DIFFERENT files and MUST land as **two separate commits** (renderer commit first, then content commit). This isolation means that if a later wave (05-03..05-06) surfaces a renderer bug, the fix re-touches only the `outils.html` renderer commit — it does NOT force re-touching the authored fiches in `fiches-data.js`. The waves and dependency chain are unchanged; only the commit granularity inside this plan is split.

Purpose: Prove the full FICHE-01 render contract (6-section template + ToC + Questions clés + inline citations + sources bibliography) end-to-end on real content, with all source URLs verified.

Output: 4 fiches authored, IIFE renderer complete, owner can read DUERP/Principes généraux/ISO 9001/ISO 14001 fiches in the browser and Ctrl+P prints them cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-fiches-de-r-vision/05-CONTEXT.md
@.planning/phases/05-fiches-de-r-vision/05-RESEARCH.md
@.planning/phases/05-fiches-de-r-vision/05-PATTERNS.md
@.planning/phases/05-fiches-de-r-vision/05-UI-SPEC.md
@.planning/phases/05-fiches-de-r-vision/05-01-SUMMARY.md
@qhse-cesi/outils-data.js
@qhse-cesi/outils.html

<interfaces>
<!-- Schema for each fiche object (UI-SPEC §Data Contract lines 497-512) -->

```
{
  slug: 'duerp',                          // matches BANK theme slug (DEC-01); ∈ {duerp, principes-generaux, iso-9001, iso-14001, iso-45001, tms, risque-routier, risque-chimique, rps, espaces-confines, acronymes, metiers, calendrier, icpe-seveso, rncp}
  title: 'DUERP',                         // French display name (same as theme picker label)
  tldr: '...',                            // 2-3 sentence summary string (plain text, no HTML)
  definitions: [                          // array of {term, value} — rendered as <dl>
    { term: 'DUERP', value: "Document Unique d'Évaluation des Risques Professionnels..." }
  ],
  cadreLegal: '<p>...</p>',               // limited HTML string — whitelist tags only: <p>, <strong>, <em>, <span class="fi-cite">, <a>, <code>, <ul>, <li>
  demarche: '<p>...</p>',                 // limited HTML string — same whitelist
  selectedIds: ['duerp-flashcard-001'],   // 5-10 BANK item IDs — IIFE resolves via window.BANK.find(b => b.id === id)
  pieges: ['L1152-1 ≠ L1153-1', '...'],   // array of plain-text strings — rendered as <ul><li>
  sources: [                              // array of {authority, ref, url} — rendered as <ul class="fi-sources-list">
    { authority: 'INRS', ref: 'Dossier DUERP', url: 'https://www.inrs.fr/...' }
  ]
}
```

Allowed HTML tags inside `cadreLegal` and `demarche` (whitelist, enforced by IIFE setHTML filter):
- `<p>`, `<strong>`, `<em>` — basic typography
- `<span class="fi-cite">` — inline citation wrapper
- `<a href="..." target="_blank" rel="noopener noreferrer">` — external link (rel is mandatory)
- `<code>` — regulatory ref code formatting
- `<ul>`, `<li>` — bullet lists

All other tags (`<script>`, `<iframe>`, `<style>`, `<img>`, `<form>`, etc.) MUST be stripped by the whitelist filter.

BANK theme slugs for cross-reference (frozen Phase 2):
- duerp · principes-generaux · iso-9001 · iso-14001 · iso-45001 · tms · risque-routier · risque-chimique · rps · espaces-confines · acronymes · metiers · calendrier · icpe-seveso · rncp
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand renderFiche() into full 6-section renderer + ToC injection + whitelist HTML filter — COMMIT 1 (renderer, outils.html only)</name>
  <files>qhse-cesi/outils.html</files>
  <read_first>
    - qhse-cesi/outils.html (read the Fiches IIFE region appended in Plan 05-01, around lines ~1957-2110 — the exact start line depends on Plan 05-01 output, check via grep for `__fiBooted`)
    - qhse-cesi/outils.html lines 1100-1250 (QCM IIFE renderQuestion / renderReveal — Pattern S3 source-line builder verbatim at lines 1226-1248)
    - .planning/phases/05-fiches-de-r-vision/05-UI-SPEC.md (lines 210-380 §Composition diagram with the exact DOM tree; lines 333-370 §Focus management + ARIA contract)
    - .planning/phases/05-fiches-de-r-vision/05-PATTERNS.md (lines 313-345 Pattern P3 source-line builder; lines 586-690 buildDefinitionsSection / buildQuestionsCles / onThemeChange code examples)
    - .planning/phases/05-fiches-de-r-vision/05-RESEARCH.md (lines 533-548 Pitfall 6 — `cadreLegal`/`demarche` whitelist filter design)
  </read_first>
  <action>
    **Commit boundary:** This task is COMMIT 1 of two independent commits in Plan 05-02. It touches ONLY `qhse-cesi/outils.html` (the renderer). Commit it on its own (suggested message `✨ Feature: Fiches 6-section renderer + safeSetHTML whitelist`) BEFORE authoring content in Task 2. Rationale: a renderer bug found in a later wave can be fixed against this commit without re-touching the authored fiches.

    Modify the Fiches IIFE inside `qhse-cesi/outils.html` (the region appended by Plan 05-01) to replace the empty-state-only `renderFiche()` stub with a complete renderer. Update the banner comment line to mention Plan 05-02 (`Phase 5 / Plan 05-02 (content + renderer)`). All new code follows Pattern S3 (createElement + textContent) and Pattern S4 (panel-scoped events). Zero new external dependencies. Zero `.innerHTML =` on BANK or FICHES data fields. The IIFE size grows by ~250-400 lines from the Plan 05-01 baseline.

    Add these named helpers inside the IIFE (function declarations in this order):

    1. **`safeSetHTML(el, htmlString)`** — whitelist filter for `cadreLegal` / `demarche`. Parse `htmlString` via `new DOMParser().parseFromString('<div>' + htmlString + '</div>', 'text/html')`, walk the resulting tree, build a sanitized clone where each node is kept only if:
       - Tag name ∈ `{p, strong, em, span, a, code, ul, li, div, text-node}`
       - For `<span>`: only `class="fi-cite"` attribute allowed.
       - For `<a>`: only `href` (must start with `https://` or `http://` for external; otherwise skip), `target` (force `_blank`), `rel` (force `noopener noreferrer`) — all other attributes stripped.
       - For all other allowed tags: zero attributes preserved (strip `id`, `class` except on `span`, `onclick`, `onload`, `style`, etc.).
       - Text nodes preserved as-is.
       - Any non-whitelisted tag → its children are walked but the tag itself dropped (or replaced with a `<span>` if a structural parent is needed — implementer's choice; simpler to drop the tag and inline children).
       Append the sanitized clone's child nodes into `el`. **No raw `.innerHTML = htmlString` anywhere.**

    2. **`appendSourceLine(targetEl, src)`** — verbatim port of the QCM source-line builder at outils.html lines 1226-1248, adapted to *append to* the target element (Pattern P3 in PATTERNS.md lines 313-345). Behavior: append text `src.authority`, if `src.ref` exists append ` — ` + `<code>src.ref</code>`, if `src.url && url.startsWith('http')` append ` ` + `<a href=src.url target="_blank" rel="noopener noreferrer">src.url</a>`. All node insertions via `createElement` + `appendChild`. `textContent` used for prose; `href` set via property assignment.

    3. **`buildToc()`** — returns a static `<ol>` of 6 `<li><a>` items, one per section anchor (UI-SPEC line 226-234). Hrefs: `#fi-s-tldr` · `#fi-s-defs` · `#fi-s-cadre` · `#fi-s-demarche` · `#fi-s-pieges` · `#fi-s-sources`. Link labels: `TL;DR` · `Définitions` · `Cadre légal` · `Démarche` · `Pièges fréquents` · `Sources`. Created once per render — replaces the empty `<ol>` inside `nav.fi-toc` placeholder.

    4. **`buildHeader(fiche)`** — returns the `<header class="fi-fiche-header">` containing `<h2 class="fi-title" tabindex="-1">{fiche.title}</h2>` + `<p class="fi-meta">{fiche.title} · {fiche.selectedIds.length} questions clés</p>`. Both via `createElement` + `textContent`.

    5. **`buildTldrSection(tldr)`** — returns `<section id="fi-s-tldr" data-fi-section="tldr">` containing `<h3>TL;DR</h3>` + `<p>{tldr}</p>` (textContent — no HTML in tldr per schema).

    6. **`buildDefinitionsSection(definitions)`** — returns `<section id="fi-s-defs" data-fi-section="definitions">` containing `<h3>Définitions</h3>` + `<dl>` with `<dt>{term}</dt><dd>{value}</dd>` pairs for each `{term, value}` in `definitions` (all textContent).

    7. **`buildCadreLegalSection(htmlString)`** — returns `<section id="fi-s-cadre" data-fi-section="cadre-legal">` containing `<h3>Cadre légal et normatif</h3>` + a `<div>` whose contents are produced by `safeSetHTML(div, htmlString)`.

    8. **`buildDemarcheSection(htmlString, selectedIds)`** — returns `<section id="fi-s-demarche" data-fi-section="demarche">` containing `<h3>Démarche et méthode</h3>` + a `<div>` populated via `safeSetHTML(div, htmlString)` + `<h4>Questions clés</h4>` + the output of `buildQuestionsCles(selectedIds)`.

    9. **`buildQuestionsCles(selectedIds)`** — returns `<div class="fi-qa-list">` containing one `<details class="fi-qa">` per id where `BANK.find(b => b.id === id)` resolves. Skip + `console.warn('[Fiches] selectedIds entry not in BANK — skipping:', id)` for unresolved ids (Pitfall 7 in RESEARCH.md). For each resolved item: `<summary>{item.question}</summary>` + `<p class="fi-qa-answer">{item.answer}</p>` + `<p class="fi-qa-explanation">{item.explanation}</p>` + `<p class="fi-qa-source">` (populated via `appendSourceLine(p, item.source)`). All prose via textContent.

    10. **`buildPiegesSection(pieges)`** — returns `<section id="fi-s-pieges" data-fi-section="pieges">` containing `<h3>Pièges fréquents</h3>` + `<ul>` with one `<li>{piege}</li>` per string (textContent).

    11. **`buildSourcesSection(sources)`** — returns `<section id="fi-s-sources" data-fi-section="sources">` containing `<h3>Sources</h3>` + `<ul class="fi-sources-list">` with one `<li>` per source, populated via `appendSourceLine(li, src)`.

    12. **`buildFiche(fiche)`** — returns a fully-assembled `<article class="fi-fiche" data-fi-active-fiche aria-live="polite" aria-atomic="false">` containing: header → 6 sections in order (TL;DR · Definitions · Cadre légal · Démarche · Pièges · Sources). Each section appended sequentially.

    13. **Replace `renderFiche(slug)`** body. New behavior:
       - Find `const fiche = window.FICHES.find(f => f.slug === slug)`.
       - Get `const tocNav = panel.querySelector('nav.fi-toc')`.
       - Get `const existing = panel.querySelector('article.fi-fiche')`.
       - If `existing`, remove it (`existing.remove()`).
       - If `!fiche`:
         - Clear `tocNav.querySelector('ol')` contents (replace with empty `<ol>`).
         - Append `<article class="fi-fiche"><p class="fi-empty">Aucune fiche disponible pour ce thème. Sélectionne un autre thème dans la liste.</p></article>` (the article wrapper is necessary so the next render can find/remove it; the empty-state paragraph lives inside).
         - Return.
       - Else: replace the ToC `<ol>` contents with the result of `buildToc()`; build the new article via `buildFiche(fiche)` and append it to the panel (after `nav.fi-toc`).
       - Focus management: after appending, find the `.fi-title` inside the new article and call `.focus()` on it (per UI-SPEC line 356) — but ONLY if the event was a `change` event (not the initial boot render). Pass an optional `{ moveFocus: boolean }` argument to `renderFiche` so the boot path skips the focus move (avoids hijacking focus on page load).

    14. **Update `onThemeChange(event)`** to call `renderFiche(event.target.value, { moveFocus: true })` and `writePrefs({ lastFicheTheme: event.target.value })`.

    15. **Update boot path** to call `renderFiche(themeSelect.value, { moveFocus: false })` after the initial `writePrefs` materialization (the merge-safe write at boot stays unchanged).

    Anti-patterns to enforce (acceptance-criteria gates below assert):
    - No `.innerHTML =` on any FICHES content field except via `safeSetHTML` (which builds a filtered DOM tree, never assigns a raw string to `.innerHTML`).
    - No `window.SRS.schedule(...)` call (DEC-09).
    - No NEW `qhse-srs-v1` / `qhse-scores-v1` references (the renderer must not increase either store's reference count above the Plan 05-01 baseline).
    - No document-level keydown.
    - `safeSetHTML` must use `DOMParser` (not `innerHTML` on a sandboxed element — DOMParser produces a `Document` which is inert until appended).
  </action>
  <verify>
    <automated>cd qhse-cesi &amp;&amp; grep -c "function safeSetHTML" outils.html | grep -q "^1$" &amp;&amp; grep -c "function buildFiche" outils.html | grep -q "^1$" &amp;&amp; grep -c "function buildQuestionsCles" outils.html | grep -q "^1$" &amp;&amp; grep -c "function appendSourceLine" outils.html | grep -q "^1$" &amp;&amp; grep -c "DOMParser" outils.html | (read n; [ "$n" -ge 1 ]) &amp;&amp; grep -cE "SRS\.schedule\(" outils.html | grep -q "^2$" &amp;&amp; grep -c "data-fi-section" outils.html | (read n; [ "$n" -ge 6 ]) &amp;&amp; grep -c "fi-s-tldr\|fi-s-defs\|fi-s-cadre\|fi-s-demarche\|fi-s-pieges\|fi-s-sources" outils.html | (read n; [ "$n" -ge 12 ]) &amp;&amp; echo "PASS renderer expansion gates"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function safeSetHTML" qhse-cesi/outils.html` returns exactly `1`.
    - `grep -c "function buildFiche" qhse-cesi/outils.html` returns exactly `1`.
    - `grep -c "function buildQuestionsCles" qhse-cesi/outils.html` returns exactly `1`.
    - `grep -c "function appendSourceLine" qhse-cesi/outils.html` returns exactly `1`.
    - `grep -c "DOMParser" qhse-cesi/outils.html` returns ≥ 1 (whitelist filter uses parser API, not innerHTML).
    - `grep -cE "SRS\.schedule\(" qhse-cesi/outils.html` returns exactly `2` (Phase 3 + Phase 4 only — Fiches IIFE still adds zero).
    - `grep -cE "(\.innerHTML\s*=\s*[^';]*window\.(BANK|FICHES))|(\.innerHTML\s*=\s*[^';]*fiche\.)" qhse-cesi/outils.html` returns `0` (no raw innerHTML on data fields).
    - DEC-09 baseline-delta (same robust form as Plan 05-01): `[ $(grep -c "qhse-srs-v1" qhse-cesi/outils.html) -le $(git show HEAD:qhse-cesi/outils.html | grep -c "qhse-srs-v1") ]` holds AND same for `qhse-scores-v1` — the renderer adds no NEW store references.
    - All 6 section ids appear in the IIFE source: `grep -cE "fi-s-(tldr|defs|cadre|demarche|pieges|sources)" qhse-cesi/outils.html` returns ≥ 12 (each id appears at least twice — `id=` assignment + `href="#...` in ToC builder).
    - Phase 3 verify-srs.cjs and Phase 4 verify-quiz.cjs both exit 0.
    - **Commit isolation:** this task is committed as its own commit touching only `outils.html`; `git show --stat HEAD` for the renderer commit lists `qhse-cesi/outils.html` and NOT `qhse-cesi/fiches-data.js`.
  </acceptance_criteria>
  <done>
    The Fiches IIFE has a complete 6-section renderer + safeSetHTML whitelist filter + Questions clés resolver, committed as an independent renderer-only commit (outils.html). The empty-state path still works when FICHES is empty (regression with Plan 05-01 boot smoke). All XSS gates pass. Next task fills in 4 fiches in a separate commit.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author 4 fiches in fiches-data.js — duerp, principes-generaux, iso-9001, iso-14001 — COMMIT 2 (content, fiches-data.js only)</name>
  <files>qhse-cesi/fiches-data.js</files>
  <read_first>
    - qhse-cesi/fiches-data.js (current state — Plan 05-01 skeleton with `window.FICHES = []`)
    - qhse-cesi/outils-data.js (search for BANK items with `theme === 'duerp'`, `theme === 'principes-generaux'`, `theme === 'iso-9001'`, `theme === 'iso-14001'`) — these are the pool from which `selectedIds[]` are picked. Each pool has ≥ 5 QCM items per Assumption A5 in RESEARCH.md.
    - qhse-cesi/index.html (Découverte section) — reference for the inline-citation prose style. Authority + ref format: `(INRS — Dossier risque chimique)` per UI-SPEC line 195.
    - .planning/phases/05-fiches-de-r-vision/05-UI-SPEC.md (lines 158-196 Copywriting Contract; lines 492-516 Data Contract schema)
    - .planning/phases/05-fiches-de-r-vision/05-CONTEXT.md (lines 30-95 DEC-01..DEC-09 — especially DEC-02 hybrid content model + DEC-03 fixed 6-section template)
    - feedback_verify_links_before_ship.md (memory) — every URL must be content-verified before commit
  </read_first>
  <action>
    **Commit boundary:** This task is COMMIT 2 of two independent commits in Plan 05-02. It touches ONLY `qhse-cesi/fiches-data.js` (the prose content). Commit it AFTER the Task 1 renderer commit (suggested message `✨ Feature: author 4 fiches — DUERP, principes généraux, ISO 9001/14001`). Keeping content separate from the renderer means a renderer fix in a later wave never re-touches these authored fiches.

    Replace the `window.FICHES = []` line in `qhse-cesi/fiches-data.js` with a populated array containing exactly 4 fiche objects in this order: `duerp`, `principes-generaux`, `iso-9001`, `iso-14001`. Keep the WR-04 guard wrapper unchanged.

    For each of the 4 fiches, author the following fields per the schema in `<interfaces>`:

    **Common rules across all 4 fiches:**
    - `slug` ∈ the 15 BANK theme slugs (exact match: `duerp`, `principes-generaux`, `iso-9001`, `iso-14001`).
    - `title` matches the French display name from the theme picker (per D-04 / UI-SPEC line 168): `DUERP` · `Principes généraux` · `ISO 9001` · `ISO 14001`.
    - `tldr`: 2-3 French sentences (plain text, no HTML), sober register, `tu` address where possible (often impersonal works better for legal/normative content — author's call). Cites the exam-relevance angle explicitly (e.g. "tombe à chaque promotion" or "fondement réglementaire du Bachelor"). Length: 30-80 words.
    - `definitions`: array of `{term, value}` objects. Minimum 4, maximum 10 entries. Each `value` is plain text (no HTML). The first entry is the theme's headline acronym/concept (e.g. for `duerp`: `{ term: 'DUERP', value: 'Document Unique d\'Évaluation des Risques Professionnels...' }`). Cover the acronym + 3-9 other key terms.
    - `cadreLegal`: HTML string with whitelist tags only (`<p>`, `<strong>`, `<em>`, `<span class="fi-cite">`, `<a>`, `<code>`, `<ul>`, `<li>`). Length: 2-5 paragraphs. EVERY regulatory claim cites a `<span class="fi-cite">` block in the format `<span class="fi-cite"><a href="..." target="_blank" rel="noopener noreferrer">{Authority} — <code>{Ref}</code></a></span>` (e.g. `<span class="fi-cite"><a href="https://www.legifrance.gouv.fr/codes/article_lc/LEGIARTI000023795562" target="_blank" rel="noopener noreferrer">Légifrance — <code>Art. R4121-1</code></a></span>`). Authorities used must match those already in BANK (INRS, Légifrance, service-public.fr, ameli, Wikipedia FR for ISO, francecompetences.fr for RNCP) — no new authorities per DEC-02. Every `target="_blank"` is paired with `rel="noopener noreferrer"` (Pattern S6).
    - `demarche`: HTML string with same whitelist tags. Length: 2-5 paragraphs OR `<ul>` of 4-8 `<li>` steps. Describes how to operationalize the theme (e.g. for DUERP: "how to build the document"; for principes généraux: "how to apply the 9 principles in a risk assessment"; for ISO 9001/14001: "how the PDCA cycle structures the audit"). Inline citations as needed.
    - `selectedIds`: array of 5-10 BANK item ids picked from the theme's pool. Editorial selection — prefer high exam-yield items (the QCM/flashcard items that the owner most needs to drill). Every id must exist in `window.BANK` and have `BANK[id].theme === fiche.slug`. **Length ∈ [5, 10]**.
    - `pieges`: array of 3-8 strings, each a single sentence (plain text, no HTML) describing a common exam confusion or trap. Pull from existing BANK explanations + QCM distractors where they encode known traps (e.g. for `rps` the trap "harcèlement moral = L1152-1, pas L1153-1" — for `duerp` analogues: "R4121-1 (DUERP) ≠ L4121-3 (mise à jour) ≠ L4121-1 (obligation générale)").
    - `sources`: array of 3-8 `{authority, ref, url}` objects. EVERY url MUST be content-verified before this commit ships (real `<title>` + topic match + soft-404 grep, per memory `feedback_verify_links_before_ship.md`). At least 1 source per fiche from each of: a regulatory authority (Légifrance / service-public.fr) and an INRS dossier (where the theme is INRS-covered — DUERP/principes/TMS/risque-chimique have INRS; ISO themes use Wikipedia FR per the locked decision in STATE.md "ISO source.url: Wikipedia FR for all three ISO themes"). Deduplicate URLs across the source list within a fiche (no URL appears twice in `sources[]`).

    **Per-fiche editorial guidance:**

    1. **`duerp`** (default fiche per UI-SPEC line 169):
       - tldr emphasizes: obligation employeur, transcription, mise à jour annuelle.
       - definitions cover: DUERP, unité de travail, risque, danger, GERMAN risk eval method (only if cited in BANK), salarié exposé.
       - cadreLegal cites R4121-1 (Légifrance), L4121-3 (mise à jour), L4121-1 (obligation générale principe).
       - demarche enumerates the 5 standard steps: identifier les unités de travail → repérer les dangers → évaluer les risques → hiérarchiser → planifier les actions.
       - pieges include: confusion R4121-1/L4121-3/L4121-1 (the most common exam trap); confusion danger/risque; "le DUERP n'est pas annuel pour les TPE" myth — actual obligation depends on staff threshold.
       - sources: INRS (Dossier DUERP `https://www.inrs.fr/demarche/document-unique/ce-qu-il-faut-retenir.html`), Légifrance R4121-1, possibly service-public.fr employer obligations page.

    2. **`principes-generaux`** (the 9 L4121-2 principles):
       - tldr emphasizes: 9 principes hiérarchisés, fondement de toute démarche prévention.
       - definitions cover: each of the 9 principles by name (éviter les risques, évaluer ceux qui ne peuvent être évités, combattre à la source, adapter le travail à l'homme, tenir compte de l'évolution technique, remplacer ce qui est dangereux, planifier la prévention, donner la priorité à la protection collective, donner les instructions appropriées).
       - cadreLegal cites L4121-2 (Légifrance), L4121-1 (parent obligation).
       - demarche shows how the 9 principles map onto the DUERP workflow + risk hierarchy (STOP method or similar).
       - pieges: confusion ordre des principes (the order matters in some exam questions); confusion principe vs. mesure de prévention.
       - sources: Légifrance L4121-2, INRS principes généraux page.

    3. **`iso-9001`** (Quality Management System):
       - tldr emphasizes: SMQ, exigences clients, amélioration continue, PDCA.
       - definitions cover: SMQ, processus, approche processus, parties intéressées, PDCA, non-conformité, action corrective, audit interne.
       - cadreLegal cites the Wikipedia FR ISO 9001 page (per STATE.md locked decision: "ISO source.url: Wikipedia FR for all three ISO themes"), ISO 9000:2015 vocabulary reference.
       - demarche walks PDCA: Plan (politique qualité, objectifs) → Do (processus, formation) → Check (audit, indicateurs) → Act (revue de direction, amélioration).
       - pieges: confusion 9001/14001/45001 (the family); confusion approche processus vs. approche système; ISO 9001 ≠ certification automatique (organisme accrédité required).
       - sources: Wikipedia FR ISO 9001, ISO official page (organisation page only, not a paywall PDF — verify the URL lands directly on the topic, not a buy-this-standard page).

    4. **`iso-14001`** (Environmental Management System):
       - tldr emphasizes: SME, impacts environnementaux, conformité réglementaire, amélioration continue.
       - definitions cover: SME, aspect environnemental, impact environnemental, partie intéressée, cycle de vie, conformité réglementaire.
       - cadreLegal cites the Wikipedia FR ISO 14001 page; ISO 14001:2015 reference; relevant Code de l'environnement articles if applicable.
       - demarche walks PDCA adapted to environment: Plan (contexte, parties intéressées, aspects significatifs) → Do (opérations, situations d'urgence) → Check (surveillance, conformité, audits) → Act (revue de direction).
       - pieges: confusion aspect vs. impact (a frequent exam trap); confusion ISO 14001 vs. EMAS (the EU eco-management scheme); ISO 14001 ≠ obligation réglementaire (it's voluntary).
       - sources: Wikipedia FR ISO 14001, ADEME or ISO official page.

    **HTML string examples** (the `cadreLegal` / `demarche` fields):
    The IIFE's safeSetHTML filter strips disallowed tags. Author content as escape-safe HTML strings; use template literals or string concatenation in the JS file. Example:
    ```js
    cadreLegal: '<p>L\'obligation est posée par <span class="fi-cite"><a href="https://www.legifrance.gouv.fr/codes/article_lc/LEGIARTI000023795562" target="_blank" rel="noopener noreferrer">Légifrance — <code>Art. R4121-1</code></a></span> du Code du travail. La mise à jour est régie par <span class="fi-cite"><a href="..." target="_blank" rel="noopener noreferrer">Légifrance — <code>L4121-3</code></a></span>.</p>'
    ```

    **selectedIds editorial picks** (the executor must list real ids from BANK):
    - Read `qhse-cesi/outils-data.js`, filter items by `theme === 'duerp'` (etc.). The Phase 2 BANK has ≥ 5 QCM items per theme (verified in RESEARCH Assumption A5).
    - Prefer the QCM items with `difficulty: 2` or `3` for exam-realism; include 1-2 flashcard items per fiche for variety.
    - Verify each picked id by re-grepping `outils-data.js` (`grep -c "id: 'duerp-qcm-001'" qhse-cesi/outils-data.js` should return ≥ 1 per id picked).

    **URL verification gate** (mandatory before commit):
    For EACH URL in any `<a href>` in `cadreLegal`, `demarche`, or `sources[].url` of these 4 fiches:
    1. Fetch the URL.
    2. Confirm HTTP 200 (no 404, no soft-404 like "page not found" body).
    3. Confirm the `<title>` reflects the cited content (e.g. for an INRS DUERP URL, the title should contain "DUERP" or "Document Unique" or "Évaluation des Risques").
    4. Skip search/index pages — link must land on the actual content.
    5. Record verification in the SUMMARY.md (Plan 05-02 produces a URL-verification table: URL · title fragment · verified-on date).
    Per memory `feedback_verify_links_before_ship.md`, HTTP-status-only checks are banned — every URL needs a real content fingerprint.

    No new external files. No new package installs. The diff for this task: `fiches-data.js` grows from ~20 lines (Plan 05-01 skeleton) to ~500-700 lines (4 fiche objects + the wrapper).
  </action>
  <verify>
    <automated>node -e "global.window={}; require('./qhse-cesi/fiches-data.js'); const F = global.window.FICHES; console.log('len:', F.length); const slugs = F.map(f => f.slug).sort(); console.log('slugs:', JSON.stringify(slugs)); const ok = F.length === 4 &amp;&amp; JSON.stringify(slugs) === JSON.stringify(['duerp','iso-14001','iso-9001','principes-generaux']) &amp;&amp; F.every(f => f.selectedIds.length &gt;= 5 &amp;&amp; f.selectedIds.length &lt;= 10) &amp;&amp; F.every(f => f.sources.length &gt;= 3) &amp;&amp; F.every(f => f.definitions.length &gt;= 4) &amp;&amp; F.every(f => f.pieges.length &gt;= 3) &amp;&amp; F.every(f => f.tldr.length &gt;= 30) &amp;&amp; F.every(f => f.cadreLegal.includes('fi-cite')) &amp;&amp; F.every(f => f.sources.every(s => /^https?:\/\//.test(s.url))) &amp;&amp; F.every(f => f.cadreLegal.split('target=\"_blank\"').length - 1 === f.cadreLegal.split('rel=\"noopener noreferrer\"').length - 1); console.log('schema OK:', ok); process.exit(ok ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - `window.FICHES.length === 4` (loaded via Node).
    - The 4 slugs are exactly `duerp`, `principes-generaux`, `iso-9001`, `iso-14001` (no extras, no duplicates).
    - Each fiche has all 9 required keys (slug, title, tldr, definitions, cadreLegal, demarche, selectedIds, pieges, sources).
    - `tldr.length` ∈ [30, 500] characters per fiche.
    - `definitions.length` ∈ [4, 10] per fiche.
    - `cadreLegal` contains at least one `<span class="fi-cite">` per fiche.
    - `demarche` is non-empty (≥ 50 characters) per fiche.
    - `selectedIds.length` ∈ [5, 10] per fiche.
    - Every `selectedIds[i]` resolves to a real BANK item via `window.BANK.find(b => b.id === id)` AND that item's `theme === fiche.slug`. Verify with a one-liner: `node -e "global.window={}; require('./qhse-cesi/outils-data.js'); require('./qhse-cesi/fiches-data.js'); const F = global.window.FICHES; const B = global.window.BANK; const all = F.every(f => f.selectedIds.every(id => { const it = B.find(b => b.id === id); return it && it.theme === f.slug; })); console.log('selectedIds OK:', all); process.exit(all ? 0 : 1);"` exits 0.
    - `pieges.length` ∈ [3, 8] per fiche.
    - `sources.length` ∈ [3, 8] per fiche.
    - Every `sources[i].url` matches `/^https?:\/\//` (no relative URLs, no `mailto:`).
    - Every `<a target="_blank">` in `cadreLegal` / `demarche` is paired with `rel="noopener noreferrer"` (count of `target="_blank"` ≡ count of `rel="noopener noreferrer"`).
    - URL verification table in SUMMARY.md documents EVERY url with: URL · `<title>` fragment captured · verified-on date.
    - Loading `outils.html` in a browser, switching to Fiches tab, and selecting each of the 4 themes from the picker renders the full 6-section fiche with ToC and Questions clés expanding correctly. No console errors.
    - Ctrl+P from any of the 4 fiches produces a clean print preview: no header, no ToC, no theme picker, all `<details>` open, Sources section starts on a new page, every external URL appears as a `(https://...)` footnote.
    - **Commit isolation:** this task is committed as its own commit touching only `fiches-data.js`; `git show --stat HEAD` for the content commit lists `qhse-cesi/fiches-data.js` and NOT `qhse-cesi/outils.html`.
    - Phase 3 + Phase 4 verify gates still exit 0.
  </acceptance_criteria>
  <done>
    4 fully-authored fiches in `fiches-data.js` committed as an independent content-only commit, all URLs content-verified, all `selectedIds[]` cross-reference resolved against BANK, browser smoke-test passes on all 4 fiches, print smoke-test passes on at least 1 fiche. URL verification table committed to SUMMARY.md.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner UAT — browser + print verification of the 4 fiches</name>
  <what-built>
    The Fiches IIFE renders the full 6-section template, and 4 fiches are authored: DUERP, Principes généraux, ISO 9001, ISO 14001. Every URL in the new content has been claimed verified by the executor in the SUMMARY.md verification table.
  </what-built>
  <how-to-verify>
    1. Wait for Vercel auto-deploy (~60s after push to main).
    2. Open `https://mes-apps-claude.vercel.app/qhse-cesi/outils.html` in a clean tab (or hard-reload to bypass cache).
    3. Click the **Fiches** tab. The DUERP fiche should render automatically (default per UI-SPEC line 169).
    4. Verify the 6 sections appear in order: TL;DR · Définitions · Cadre légal · Démarche (+ Questions clés sub-section) · Pièges fréquents · Sources.
    5. Click each of the 6 in-fiche ToC links — they should smooth-scroll to the corresponding section.
    6. Expand 2-3 of the Questions clés `<details>` blocks — each should show answer + explanation + source line.
    7. Switch the picker to **Principes généraux** — fiche re-renders cleanly.
    8. Switch to **ISO 9001** — fiche re-renders cleanly.
    9. Switch to **ISO 14001** — fiche re-renders cleanly.
    10. Open browser DevTools → Console — confirm zero errors and zero warnings (other than expected `[Fiches] selectedIds entry not in BANK — skipping:` warnings IF any selectedIds were intentionally invalid, which they shouldn't be).
    11. Open Application → Local Storage → `qhse-prefs-v1` — verify it contains your existing `lastTheme`, `lastMode`, `newCardsPerDay`, `lastQcmTheme`, `lastTestTheme` AS WELL AS the new `lastFicheTheme: "iso-14001"` (or whichever you ended on).
    12. Ctrl+P from the ISO 14001 fiche. Confirm in print preview:
        - No site header, no skip-link, no nav burger.
        - No theme picker visible.
        - No ToC visible.
        - All Questions clés `<details>` are expanded (no triangle visible).
        - Section headings have a black-underline bar.
        - External URLs appear as `(https://...)` footnotes after each link.
        - Sources section starts on its own page.
        - A4 page margin is 1cm (per the @page rule added in Plan 05-01).
        - Other 3 panels (Flashcards / QCM / Tests) are NOT visible.
    13. Pick 2 URLs at random from the 4 fiches' content and content-verify them: open the URL, confirm the page title and content match the citation in the fiche (no 404, no off-topic landing).

    If any check fails, comment what failed and where. If all pass, type "approved" to advance.
  </how-to-verify>
  <resume-signal>Type "approved" to advance, or describe issues to trigger Plan revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `fiches-data.js` (committed, hand-authored) → DOM | The `cadreLegal` and `demarche` fields are HTML strings; the safeSetHTML whitelist filter is the only legitimate path to the DOM. |
| External URLs in `<a href>` (cadreLegal / demarche / sources[].url) → owner's browser | Every URL points outside the project's control surface; content verification before commit is the only mitigation against off-topic / 404 / phishing drift. |
| Browser localStorage → Fiches IIFE | The merge-safe writePrefs pattern is the only legitimate writer; a naive setItem would clobber P3+P4 keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering / Elevation | safeSetHTML whitelist filter | mitigate | Whitelist enumerates exactly 9 allowed tags + 1 allowed class on `<span>` + forced `rel` on `<a>`. Built via DOMParser (inert until appended), never raw `.innerHTML =`. Plan 05-06 verify-fiches.cjs will smoke-test by injecting a `<script>` in a copy of FICHES and asserting it gets stripped. |
| T-05-02-02 | Tampering | Reverse-tabnabbing via `target="_blank"` without `rel` | mitigate | Whitelist filter forces `rel="noopener noreferrer"` on every `<a target="_blank">`. Acceptance criteria asserts count equality of `target="_blank"` vs `rel="noopener noreferrer"` in cadreLegal/demarche strings. |
| T-05-02-03 | Information disclosure | Off-topic / 404 / phishing URLs in sources | mitigate | Every URL content-verified before commit (real `<title>` + topic match + soft-404 grep) per memory `feedback_verify_links_before_ship.md`. Verification table committed to SUMMARY.md. |
| T-05-02-04 | Tampering | `selectedIds[i]` references stale BANK ids | mitigate | Every `selectedIds[i]` validated against `window.BANK.find(...)` + cross-theme check (`item.theme === fiche.slug`) in acceptance criteria. Plan 05-06 verify-fiches.cjs group (c) codifies this. |
| T-05-02-05 | Tampering | Accidental SRS / scores mutation by Fiches IIFE renderer | mitigate | Renderer touches zero SRS / scores call sites; acceptance-criteria grep gates assert zero `SRS.schedule(` and zero NEW `qhse-srs-v1` / `qhse-scores-v1` references (baseline-delta) in the Fiches IIFE region. |
| T-05-02-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs (project invariant). |
</threat_model>

<verification>
1. Grep gates (Task 1): renderFiche helpers present, no XSS surface, no SRS write.
2. Schema gates (Task 2): FICHES.length === 4, slug set correct, all field constraints, selectedIds cross-resolved, URL format.
3. URL content-verification table in SUMMARY.md (Task 2).
4. Browser + print smoke (Task 3): owner UAT confirms all 4 fiches render and print correctly.
5. Regression: Phase 3 + Phase 4 verify gates still exit 0.
6. Commit isolation: the renderer (Task 1) and content (Task 2) land as two separate commits touching disjoint files.
</verification>

<success_criteria>
- `window.FICHES.length === 4` after load with exact slugs `[duerp, iso-14001, iso-9001, principes-generaux]` (sorted).
- All schema invariants pass.
- All `selectedIds[i]` resolve to BANK items with matching theme.
- All URLs content-verified, verification table in SUMMARY.md.
- The renderer (Task 1, outils.html) and the 4-fiche content (Task 2, fiches-data.js) are committed as two independent commits touching disjoint files.
- Owner UAT approves browser + print rendering.
- Phase 3 + Phase 4 verify gates still exit 0.
- Both commits on main; Vercel deploys cleanly.
</success_criteria>

<output>
Create `.planning/phases/05-fiches-de-r-vision/05-02-SUMMARY.md` when done. Document:
- Diff stats per file (and confirm the two-commit split: renderer commit = outils.html only, content commit = fiches-data.js only)
- URL verification table (every new URL · title fragment · verified-on date · authority)
- selectedIds cross-reference table per fiche
- Owner UAT outcome (approved / issues)
- Regression check results
</output>
</content>
</invoke>
