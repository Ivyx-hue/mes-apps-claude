---
phase: 03-flashcards-srs
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - .planning/phases/03-flashcards-srs/03-SMOKE-TEST.md
autonomous: false
requirements:
  - FLASH-01
  - FLASH-02
  - SRS-01
  - SRS-02
  - SRS-03
  - SRS-04
  - PERSIST-01
tags:
  - integration
  - regression
  - smoke-test
  - human-verify
  - ship

must_haves:
  truths:
    - "Phase 1 ARIA tablist still works: clicking Fiches/QCM/Tests tabs swaps panels; ArrowLeft/ArrowRight navigates between tabs; hash sync via history.replaceState"
    - "Phase 2 bank still renders: `window.BANK.length === 226`; theme picker options align with the 15 locked slugs"
    - "Plan 03-02's `node verify-srs.cjs` exits 0 against the shipped srs.js"
    - "v1.0 Hub pages (index.html Accueil/Découverte/Biblio) render identically — SHELL-04 invariant"
    - "Root QHSE Trainer (/index.html) renders unchanged — frozen sibling"
    - "All 5 ROADMAP success criteria verified by the owner via the smoke-test checklist"
    - "Final commit pushed to main; Vercel deploy live at https://mes-apps-claude.vercel.app/qhse-cesi/outils.html#tab-flashcards"
  artifacts:
    - path: ".planning/phases/03-flashcards-srs/03-SMOKE-TEST.md"
      provides: "Human-verify checklist + regression checks executed before final commit/push"
      contains: "SC1"
  key_links:
    - from: "the shipped Phase 3 surface (outils.html#tab-flashcards)"
      to: "ROADMAP Phase 3 success criteria 1-5"
      via: "owner-executed smoke test"
      pattern: "SC[1-5]"
---

<objective>
Final integration pass: run the Plan 03-02 verification gate, perform a manual regression check on Phase 1 (ARIA tablist) + Phase 2 (bank rendering) + v1.0 Hub (Accueil/Découverte/Biblio) + root QHSE Trainer, walk the owner through all 5 ROADMAP success criteria via a documented smoke-test checklist, and push the final ship commit. This is the "lights-green" gate before phase verification can begin.

Purpose: Plans 03-01/02/03 ship code. Plan 03-04 ships confidence — proves nothing else broke and the five owner-verifiable success criteria all PASS in the live browser. Without this checkpoint, "implementation complete" is a claim, not a fact.

Output: One human-verify checkpoint (the smoke test) executed by the owner; final commit + push; CHECKPOINT.md / SUMMARY.md update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-flashcards-srs/03-CONTEXT.md
@.planning/phases/03-flashcards-srs/03-UI-SPEC.md
@.planning/phases/03-flashcards-srs/03-01-SUMMARY.md
@.planning/phases/03-flashcards-srs/03-02-SUMMARY.md
@.planning/phases/03-flashcards-srs/03-03-SUMMARY.md
@qhse-cesi/srs.js
@qhse-cesi/outils.html
@qhse-cesi/chassis.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the verify-srs.cjs gate + automated regression checks; write 03-SMOKE-TEST.md</name>
  <files>.planning/phases/03-flashcards-srs/03-SMOKE-TEST.md</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 3 (the 5 success criteria are the checklist source)
    - .planning/phases/03-flashcards-srs/03-UI-SPEC.md §Mapping to Owner-Verifiable Success Criteria (the table maps each SC to its UI elements — adapt verbatim)
    - .planning/phases/03-flashcards-srs/03-CONTEXT.md §code_context "No regression on Phase 1/2 deliverables" (the invariants to re-verify)
    - .planning/REQUIREMENTS.md SHELL-04 (v1.0 hub regression invariant)
    - qhse-cesi/outils.html (confirm tablist IIFE untouched + Phase 3 IIFE present)
  </read_first>
  <action>
Execute the following sequence (all commands run via the Bash tool — POSIX semantics):

**Sub-step 1 — Automated gate:**
Run `node .planning/phases/03-flashcards-srs/verify-srs.cjs` from the repo root. Capture the full stdout + exit code. The gate MUST exit 0; if it does not, stop and surface the FAIL line(s) — do not proceed.

**Sub-step 2 — Automated regression sanity checks:**
Run these node one-liners and record each PASS/FAIL:

a) `node -e "global.window={}; require('C:/Users/Lasmoles/mes-apps-claude/qhse-cesi/outils-data.js'); if(window.BANK.length!==226) throw new Error('BANK length drifted from 226: now '+window.BANK.length); console.log('PASS: BANK.length === 226 (Phase 2 invariant)');"`

b) `node -e "global.window={}; require('C:/Users/Lasmoles/mes-apps-claude/qhse-cesi/outils-data.js'); require('C:/Users/Lasmoles/mes-apps-claude/qhse-cesi/srs.js'); const themes=new Set(window.BANK.map(i=>i.theme)); const expected=['duerp','principes-generaux','iso-9001','iso-14001','iso-45001','tms','risque-routier','risque-chimique','rps','espaces-confines','acronymes','metiers','calendrier','icpe-seveso','rncp']; for(const t of expected) if(!themes.has(t)) throw new Error('missing theme: '+t); console.log('PASS: all 15 locked theme slugs present in BANK');"`

c) `node -e "const h=require('fs').readFileSync('C:/Users/Lasmoles/mes-apps-claude/qhse-cesi/outils.html','utf8'); if(!h.includes(\"tab.addEventListener('keydown', e =>\")) throw new Error('Phase 1 tablist keydown handler missing — REGRESSION'); if(!h.includes('aria-selected')) throw new Error('Phase 1 ARIA tablist attributes missing'); console.log('PASS: Phase 1 tablist IIFE intact');"`

d) `node -e "const c=require('fs').readFileSync('C:/Users/Lasmoles/mes-apps-claude/qhse-cesi/chassis.css','utf8'); if(!c.includes('.biblio-card__title')) throw new Error('Phase 1 biblio-* block missing — REGRESSION'); if(!c.includes('.fc-card')) throw new Error('Phase 3 fc-* block missing'); console.log('PASS: chassis.css both biblio (P1) + fc (P3) namespaces present');"`

e) `git -C C:/Users/Lasmoles/mes-apps-claude diff HEAD~3 --stat -- index.html 2>/dev/null && echo "index.html DIFF SHOULD BE EMPTY (SHELL-04)" || echo "git diff check ran"` — confirm `index.html` has NOT been modified by Phase 3 work. (If diff shows changes, abort and revert — SHELL-04 invariant violation.) Note: `2>/dev/null` is POSIX (used by the Bash tool); do NOT substitute `2>nul` (cmd.exe).

**Sub-step 3 — Author 03-SMOKE-TEST.md:**
Create `.planning/phases/03-flashcards-srs/03-SMOKE-TEST.md` with this structure:

```markdown
# Phase 3 — Smoke Test Checklist

**Run:** 2026-05-23 (owner; pre-push)
**Live URL after push:** https://mes-apps-claude.vercel.app/qhse-cesi/outils.html#tab-flashcards

## Automated gates (must all PASS before manual walk-through)

- [ ] `node .planning/phases/03-flashcards-srs/verify-srs.cjs` exits 0
- [ ] BANK.length === 226 (Phase 2 invariant)
- [ ] All 15 locked theme slugs present in BANK
- [ ] Phase 1 tablist IIFE intact (tab.addEventListener keydown handler + aria-selected attrs present in outils.html)
- [ ] chassis.css contains BOTH .biblio-card__title (P1) AND .fc-card (P3)
- [ ] index.html unchanged by Phase 3 (git diff empty for index.html since last commit before P3)

## ROADMAP Success Criteria — owner walk-through (in a real browser)

### SC1 — Recto → Reveal → Verso
- [ ] Open outils.html → Flashcards tab loads automatically (default tab from Phase 1)
- [ ] Bandeau shows two counters (dues/total · new/cap) in JetBrains Mono
- [ ] Theme `<select>` defaults to "Tous les thèmes" (or last-used theme from prefs)
- [ ] A recto question is visible in Fraunces, large size (~20-24px)
- [ ] Click "Révéler" — verso appears below with: canonical answer (sans, 17-18px, weight 600), explanation (sans, 16px, ink-2), source line with authority + ref (in `<code>`) + URL
- [ ] 4 grade buttons appear: `1 · Raté`, `2 · Dur`, `3 · Bien`, `4 · Facile`
- [ ] No console errors in DevTools

### SC2 — Grade buttons advance + persist
- [ ] Click "3 · Bien" — verso hides, next card's recto appears, focus moves to reveal button
- [ ] Open DevTools → Application → Local Storage → `qhse-srs-v1` — a new entry exists with keys `ease`, `interval`, `due`, `lapses`, `reps`, `introduced`
- [ ] The graded card's `introduced` field equals today's local date (yyyy-mm-dd format, your local timezone — NOT UTC)
- [ ] Keyboard: with verso visible, pressing `1`/`2`/`3`/`4` grades the card (no need to click)

### SC3 — "À réviser aujourd'hui" view (bandeau dues counter)
- [ ] After grading a card, bandeau `dues` decrements by 1
- [ ] Bandeau `nouvelles` increments by 1 IF the card was new (had no row before)
- [ ] Grade enough cards (or wait until tomorrow): bandeau `dues` reaches 0 → empty-queue panel appears with "Bravo — file vide pour aujourd'hui." + next-due date

### SC4 — Reload survives
- [ ] Switch theme to e.g. "DUERP" via the picker
- [ ] Set "Réglages" → newCardsPerDay to 5
- [ ] Reload the page (Ctrl-R)
- [ ] Theme picker is still on "DUERP" (restored from `qhse-prefs-v1.lastTheme`)
- [ ] Réglages input shows 5 (restored from `qhse-prefs-v1.newCardsPerDay`)
- [ ] Previously graded cards' SRS rows still in `qhse-srs-v1` (DevTools confirm)

### SC5 — Raté re-queues immediately (cross-phase SRS-03 schema check)
- [ ] Grade a card "1 · Raté"
- [ ] Check `qhse-srs-v1` in DevTools — the card's row has `lapses >= 1`, `interval === 1`, `due === addDays(today, 1)` (i.e. tomorrow's date)
- [ ] Card disappears from today's queue (bandeau decrements; if last card, empty panel appears)
- [ ] Card's `introduced` field set if this was the first time (cross-phase contract for Phase 4 wrong-QCM-feed)

## Regression — Phase 1 + Phase 2 + v1.0 invariants

- [ ] Click "Fiches de révision" tab — Phase 5 placeholder still visible
- [ ] Click "QCM" tab — Phase 4 placeholder still visible
- [ ] Click "Tests blancs" tab — Phase 4 placeholder still visible
- [ ] ArrowLeft / ArrowRight on the tab strip navigates correctly (roving tabindex per WAI-ARIA)
- [ ] Navigate back to Hub (`index.html`) — Accueil / Découverte / Biblio sections render identically to v1.0
- [ ] Navigate to root QHSE Trainer (`/index.html` in the root, NOT `/qhse-cesi/`) — frozen sibling unchanged
- [ ] Hub `#outils` gateway section still visible and links to `outils.html`

## Free-revision mode (D-06 structural guarantee)

- [ ] When dues queue is empty: click "Continuer en révision libre"
- [ ] Banner appears: "Révision libre — la progression SRS n'est pas modifiée."
- [ ] Card area shows random cards with ONLY "Carte suivante" and "Quitter la révision libre" buttons (no grade buttons)
- [ ] Click "Carte suivante" several times — `qhse-srs-v1` in DevTools is UNCHANGED (compare timestamp; key order unchanged)
- [ ] Click "Quitter la révision libre" — empty panel returns; mode flag cleared
- [ ] Reload — free-revision flag NOT persisted (always boots in normal mode)

## Mobile responsive check (Chromium DevTools device emulator)

- [ ] Toggle device mode → iPhone or Pixel preset
- [ ] Bandeau wraps to single line with réglages aligned right
- [ ] Theme picker label stacks above `<select>` (full width)
- [ ] Card is full width, padding `--space-md`
- [ ] Grade buttons stack vertically, each 44px min height
- [ ] Reveal-hint "Espace ou Entrée" is hidden on mobile
- [ ] Tap-on-card-body reveals verso (touch parity)

## A11y spot check

- [ ] Tab order through panel: theme select → réglages summary → reveal button → grade buttons (post-reveal)
- [ ] Each focusable element has a visible `:focus-visible` ring (chassis --focus-ring)
- [ ] Screen reader: visiting the panel announces "Cartes mémoire avec répétition espacée" (sr-only h2)
- [ ] Grade buttons have `aria-label="Noter cette carte : Raté (raccourci : 1)"` etc.
- [ ] Empty-queue date wrapped in `<time datetime="...">`

## Final ship

- [ ] All automated gates PASS
- [ ] All ROADMAP SC1-5 manually verified
- [ ] All regression invariants confirmed
- [ ] Free-revision purity confirmed (qhse-srs-v1 unchanged after "Carte suivante" clicks)
- [ ] Commit + push to main (`🚀 Deploy: Phase 3 Flashcards + SM-2 SRS`)
- [ ] Wait ~60s for Vercel deploy
- [ ] Re-test the live URL https://mes-apps-claude.vercel.app/qhse-cesi/outils.html#tab-flashcards
- [ ] Update STATE.md (mark Phase 3 complete)
- [ ] Update REQUIREMENTS.md (mark FLASH-01, FLASH-02, SRS-01, SRS-02, SRS-04, PERSIST-01 complete; SRS-03 partial — schema half shipped)
- [ ] Update ROADMAP.md (tick Phase 3 checkbox)

## Notes / observations

(Owner records anomalies here; if anything fails, do NOT push — surface the issue and re-plan via /gsd:execute-plan for a hotfix.)
```

Write the file. Stage it. Commit alongside any minor polish fixes (e.g. typos surfaced during the walk-through) under a final commit. If smoke test reveals a real bug, STOP — do not push; surface for hotfix planning.
  </action>
  <verify>
    <automated>cd C:/Users/Lasmoles/mes-apps-claude && node .planning/phases/03-flashcards-srs/verify-srs.cjs && node -e "global.window={}; require('./qhse-cesi/outils-data.js'); require('./qhse-cesi/srs.js'); if(window.BANK.length!==226)throw new Error('BANK regression'); if(typeof window.SRS.schedule!=='function')throw new Error('SRS missing'); console.log('GATES_GREEN');" && node -e "const fs=require('fs'); if(!fs.existsSync('.planning/phases/03-flashcards-srs/03-SMOKE-TEST.md'))throw new Error('smoke test missing'); const s=fs.readFileSync('.planning/phases/03-flashcards-srs/03-SMOKE-TEST.md','utf8'); for(const sc of ['SC1','SC2','SC3','SC4','SC5']) if(!s.includes(sc))throw new Error('smoke test missing '+sc); console.log('SMOKE_TEST_OK');"</automated>
  </verify>
  <done>
    `verify-srs.cjs` exits 0; BANK is 226 items; window.SRS exposes the contract; 03-SMOKE-TEST.md exists with all 5 SC sections + regression block + a11y + mobile check; ready for the owner walk-through (Task 2).
  </done>
  <acceptance_criteria>
    - `node .planning/phases/03-flashcards-srs/verify-srs.cjs` exits 0.
    - `node -e "global.window={}; require('./qhse-cesi/outils-data.js'); console.log(window.BANK.length);"` prints `226`.
    - `node -e "global.window={}; require('./qhse-cesi/srs.js'); console.log(typeof global.window.SRS.schedule);"` prints `function`.
    - `.planning/phases/03-flashcards-srs/03-SMOKE-TEST.md` exists and contains literal substrings `SC1`, `SC2`, `SC3`, `SC4`, `SC5`, `Automated gates`, `Regression`, `Free-revision mode`, `Mobile responsive`, `A11y`, `Final ship`.
    - `git diff --stat HEAD` shows ONLY the smoke-test markdown as the change in this task (no production code modifications).
    - `git log --oneline -5` shows the Plan 03-01, 03-02, 03-03 commits already landed.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner walk-through — execute the smoke test checklist in a real browser; verify all 5 ROADMAP success criteria PASS</name>
  <what-built>
    Phase 3 Flashcards + SRS is fully wired:
    - `qhse-cesi/srs.js` — SM-2 scheduler (Plan 03-01)
    - `.planning/phases/03-flashcards-srs/verify-srs.cjs` — Node gate, exits 0 (Plan 03-02)
    - `qhse-cesi/outils.html` — Flashcards view IIFE + DOM scaffold (Plan 03-03)
    - `qhse-cesi/chassis.css` — `.fc-*` component rules (Plan 03-03)
    - `.planning/phases/03-flashcards-srs/03-SMOKE-TEST.md` — this checklist (Task 1)
    All automated gates green.
  </what-built>
  <how-to-verify>
1. Open `qhse-cesi/outils.html` in your browser locally (or push to a feature branch + Vercel preview, then re-test).
2. Walk through `.planning/phases/03-flashcards-srs/03-SMOKE-TEST.md` from top to bottom, ticking each checkbox in DevTools-supported flow.
3. Specifically confirm the 5 ROADMAP success criteria:
   - **SC1:** Pick a theme → see recto → click Révéler → verso appears with answer + explanation + source.
   - **SC2:** Click `3 · Bien` → next card; in DevTools Application → Local Storage → `qhse-srs-v1` contains the graded card's row.
   - **SC3:** Bandeau `dues` counter reflects only cards where `row.due <= today` after a few grades.
   - **SC4:** Change theme + cap → reload → both restored from `qhse-prefs-v1`; SRS rows still in `qhse-srs-v1`.
   - **SC5:** Grade `Raté` → that card's `due` becomes tomorrow; card leaves today's queue; bandeau decrements.
4. Regression: confirm Phase 1 ARIA tablist still works (click other tabs, ArrowLeft/Right navigation).
5. Regression: navigate to `index.html` — confirm v1.0 Hub still renders identically.
6. Regression: navigate to root `/index.html` (QHSE Trainer, frozen sibling) — confirm unchanged.
7. Mobile responsive check via DevTools device emulator.
8. Free-revision purity: drain queue → click "Continuer en révision libre" → click "Carte suivante" several times → confirm `qhse-srs-v1` unchanged (no new writes).
9. If ANY check fails: type the failure description and do NOT approve. Hotfix planning starts from there.
10. If ALL checks pass: approve, then proceed to Task 3 (final push).
  </how-to-verify>
  <resume-signal>Type "approved" if all smoke-test boxes ticked; otherwise describe the failing check(s).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Final ship — commit smoke test + push to main + update STATE / REQUIREMENTS / ROADMAP</name>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (Current Position section + Progress table — update both)
    - .planning/REQUIREMENTS.md Traceability table (mark FLASH-01, FLASH-02, SRS-01, SRS-02, SRS-04, PERSIST-01 as Complete; SRS-03 stays Pending until Phase 4 — schema half is shipped via P3 but write path is P4)
    - .planning/ROADMAP.md § Phase 3 (tick the `- [ ]` checkbox to `- [x]` + add completion date)
    - .planning/phases/03-flashcards-srs/03-SMOKE-TEST.md (confirm all checkboxes ticked before pushing)
  </read_first>
  <action>
1. Confirm the owner's smoke-test approval from Task 2.
2. Update `.planning/STATE.md`:
   - Bump `completed_phases` from 2 to 3.
   - Bump `completed_plans` accordingly (add 4 for Phase 3's plans).
   - Bump `percent` to 60.
   - Update `Current Position`: Phase 4 next.
   - Update the progress bar: `[██████░░░░] 60% — 3/5 phases complete`.
   - Append a line under "Key Decisions (v2.0)" recording any non-obvious P3 decision surfaced during smoke test (e.g. specific schema field naming).
3. Update `.planning/REQUIREMENTS.md` Traceability table:
   - FLASH-01: Complete
   - FLASH-02: Complete
   - SRS-01: Complete
   - SRS-02: Complete
   - SRS-04: Complete
   - PERSIST-01: Complete
   - SRS-03: Partial (note: "P3 ships schema; P4 writes wrong-QCM feed")
   - Update the top section's checkbox: `- [x] **FLASH-01**: …` etc.
4. Update `.planning/ROADMAP.md` § Phase 3:
   - Change `- [ ] **Phase 3: Flashcards + SRS**` to `- [x] **Phase 3: Flashcards + SRS** — Flashcard mode with SM-2 spaced repetition scheduler and localStorage persistence. (completed 2026-05-XX)` (substitute today's date).
   - Update the Progress table row for Phase 3 (v2.0) — set `Status: Complete`, fill `Completed: 2026-05-XX`.
   - Update the Plans count line + populate the Plans list with the 4 plans + their objectives.
5. Stage all changes (smoke test + STATE + REQUIREMENTS + ROADMAP + any minor polish from Task 2 walk-through).
6. Commit with message: `🚀 Deploy: Phase 3 Flashcards + SM-2 SRS — complete (FLASH-01/02, SRS-01/02/04, PERSIST-01)`
7. Push to main: `git push origin main`
8. Wait ~60s; visit https://mes-apps-claude.vercel.app/qhse-cesi/outils.html#tab-flashcards in the browser; confirm live deploy reflects the Phase 3 view (recto question visible, theme picker populated, no console errors).
9. Surface the live URL + commit SHA to the owner.

CRITICAL: If the smoke test surfaced ANY issue, do NOT execute this task. Instead, plan a hotfix via `/gsd:execute-plan` for a Plan 03-05 patch — keep main green.
  </action>
  <verify>
    <automated>cd C:/Users/Lasmoles/mes-apps-claude && git log --oneline -1 && git rev-parse HEAD && echo "--- post-push verification ---" && node -e "fetch('https://mes-apps-claude.vercel.app/qhse-cesi/outils.html').then(r=>{console.log('HTTP '+r.status);process.exit(r.status===200?0:1)}).catch(e=>{console.error('fetch failed: '+e.message);process.exit(1)})"</automated>
  </verify>
  <done>
    Smoke test marked all-green; ROADMAP/REQUIREMENTS/STATE updated; final commit pushed; Vercel deploy live and returning HTTP 200 at the Flashcards URL; the project state shows Phase 3 complete and Phase 4 as the next focus.
  </done>
  <acceptance_criteria>
    - `git log --oneline -1` shows the Phase 3 final commit (message contains `Phase 3` AND `Flashcards`).
    - `.planning/STATE.md` shows `completed_phases: 3`, `percent: 60`, Current Position references Phase 4.
    - `.planning/REQUIREMENTS.md` Traceability table marks FLASH-01, FLASH-02, SRS-01, SRS-02, SRS-04, PERSIST-01 as `Complete`; SRS-03 marked `Partial` with note.
    - `.planning/ROADMAP.md` Phase 3 line has `- [x]` and a completion date.
    - `node -e "fetch('https://mes-apps-claude.vercel.app/qhse-cesi/outils.html').then(r=>{console.log(r.status);process.exit(r.status===200?0:1)}).catch(e=>{process.exit(1)})"` exits 0 (HTTP 200). Note: this one-liner runs identically on bash and PowerShell (Node 18+ ships fetch natively) — environment-independent, replacing the prior POSIX-only curl invocation.
    - Visiting the live URL in a browser (post-deploy ~60s after push) shows the Flashcards UI with a recto question rendered — NOT the old placeholder text.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → main branch | Owner controls the push; main is the deploy source for Vercel |
| Vercel build → live URL | Existing GitHub Actions pipeline (already configured); no new CI changes in P3 |
| smoke-test sign-off → ship decision | Owner sign-off is the gate; not an automated check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Repudiation | Owner says "I approved" but smoke test never ticked off the regression checks | mitigate | 03-SMOKE-TEST.md is the durable artifact — every checkbox is in the markdown file, committed to the repo. After Task 2, the file shows which boxes were ticked. Auditable. |
| T-03-04-02 | Tampering | A failing smoke-test check leaks past — broken build ships | mitigate | Task 3's action explicitly requires "If smoke test surfaced ANY issue, do NOT execute this task." The action has a guard clause; Task 2's resume-signal requires literal "approved" or failure description. Asymmetric: easier to fail-fast than to ignore. |
| T-03-04-03 | Denial of Service | Vercel deploy fails post-push → main green but live broken | accept | Existing pipeline: if `.github/workflows/deploy.yml` fails, owner sees red X on GitHub; not a code path to mitigate inside Phase 3. The node fetch HTTP-status check in the verify step catches non-200 responses and the owner re-runs deploy. |
| T-03-04-04 | Tampering | Index.html (Hub) inadvertently modified by Phase 3 work | mitigate | Sub-step 2 (e) explicit git-diff check for `index.html` before commit. If `git diff` shows changes, abort and revert. SHELL-04 invariant enforced procedurally. |
| T-03-04-05 | Tampering | Root QHSE Trainer (`/index.html` outside `qhse-cesi/`) inadvertently modified | mitigate | Same — git status pre-commit shows ALL changed files; owner verifies only `qhse-cesi/*` and `.planning/*` paths in the staging area. |

**Severity:** none high. The smoke-test pattern + git-diff inspection makes regression-ship structurally hard, not just culturally discouraged.
</threat_model>

<verification>
- verify-srs.cjs exits 0.
- BANK still 226 items; SRS module loads under Node.
- 03-SMOKE-TEST.md exists with all 5 SC + regression + a11y + mobile + free-revision sections.
- Owner sign-off captured (Task 2 resume-signal).
- Final commit pushed; live URL returns HTTP 200 with the Phase 3 surface.
- STATE/REQUIREMENTS/ROADMAP updated; Phase 3 marked complete.
</verification>

<success_criteria>
- All 5 ROADMAP success criteria PASS in the live browser.
- Zero regression on Phase 1 (ARIA tablist), Phase 2 (BANK rendering), v1.0 Hub (Accueil/Découverte/Biblio), root QHSE Trainer.
- Free-revision mode confirmed read-only (`qhse-srs-v1` unchanged after free-revision navigation).
- Plan 03-02 verify-srs.cjs gate green (executable contract met).
- Project state advances from 40% to 60% complete; Phase 4 (QCM + Tests blancs) becomes the next focus.
</success_criteria>

<output>
Create `.planning/phases/03-flashcards-srs/03-04-SUMMARY.md` when done. Final atomic commit + push covers the smoke test markdown + state/requirements/roadmap updates.
</output>
</output>
