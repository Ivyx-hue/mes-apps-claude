---
phase: 03-flashcards-srs
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - qhse-cesi/outils.html
  - qhse-cesi/chassis.css
autonomous: true
requirements:
  - FLASH-01
  - FLASH-02
  - SRS-02
  - SRS-03
  - SRS-04
  - PERSIST-01
tags:
  - flashcards
  - view
  - dom
  - localstorage
  - keyboard
  - aria

must_haves:
  truths:
    - "D-01: Flashcards mode reads the entire 226-item bank (`window.BANK.filter(...)`), not only `type==='flashcard'`; for QCM items the recto/verso mapping is `question`→recto, `answer`→verso, `choices`/`correct` ignored in P3"
    - "D-02: Verso uniforme — for every card (native flashcard OR QCM-repackaged) the verso renders `answer` + `explanation` + `source` together, no badge distinguishing the two types"
    - "D-03: `newCardsPerDay` default value is 10 on first visit (toutes thèmes confondus); set as DEFAULT_PREFS.newCardsPerDay = 10 in the IIFE constants block"
    - "D-04: The cap is owner-adjustable via a Réglages number input (range 1–50) inside `#panel-flashcards`, persisted to `qhse-prefs-v1.newCardsPerDay` via merge-safe writePrefs; readPrefs clamps to the same range on load"
    - "D-05: Bandeau permanent at top of view in exact format `{N}/{TOTAL} dues · {M}/{CAP} nouvelles`; counters decrement per grade; no separate entry/start screen; owner enters directly into the first card"
    - "D-06: Empty-queue UX shows 'Bravo — file vide pour aujourd'hui' + next due date + (X validées · Y à retravailler) + 'Continuer en révision libre' button; free-revision mode renders random cards from the filtered pool WITHOUT calling writeStore (structurally separate code path — startFreeRevision never references writeStore)"
    - "Owner opens outils.html#tab-flashcards, selects a theme from the picker, sees the recto question (Fraunces, --step-3); clicking Révéler reveals verso with answer + explanation + source (SC1, FLASH-01)"
    - "After reveal, 4 grade buttons (1·Raté, 2·Dur, 3·Bien, 4·Facile) are visible; clicking one advances to the next card AND writes a per-card row to localStorage `qhse-srs-v1` (SC2)"
    - "Bandeau permanent at top shows `{N}/{TOTAL} dues · {M}/{CAP} nouvelles`; counters decrement per grade; `dues` count reflects cards where `row.due <= today` (SC3)"
    - "Reloading the page restores last theme + newCardsPerDay from `qhse-prefs-v1`; the SRS store survives intact in `qhse-srs-v1` (SC4)"
    - "Grading a card 'raté' sets new `row.due = addDays(today, 1)` — card leaves today's queue but appears tomorrow; bandeau decrements; SRS-03 schema (`introduced` field) ready for Phase 4 QCM wrong-answer write path (SC5)"
    - "Keyboard: Space/Enter reveals; 1/2/3/4 grade (verso only); listeners scoped to `#panel-flashcards` (never `document`) so Phase 4/5 panels stay isolated"
    - "`window.SRS` from Plan 03-01 is the ONLY source of SM-2 math; the view never re-implements ease/interval logic (D-07 lives in srs.js)"
  artifacts:
    - path: "qhse-cesi/outils.html"
      provides: "Flashcards view IIFE + DOM scaffold + `<script src=\"srs.js\" defer>` script tag"
      contains: "data-fc-reveal"
      min_lines: 300
    - path: "qhse-cesi/chassis.css"
      provides: "Additive `.fc-*` component rules inside `@layer components` (before line 546 closing brace)"
      contains: ".fc-card"
      min_lines: 600
  key_links:
    - from: "qhse-cesi/outils.html (Flashcards IIFE)"
      to: "window.SRS (from srs.js)"
      via: "global reference; calls SRS.schedule / SRS.isDue / SRS.addDays / SRS.todayLocal / SRS.filterDue / SRS.countNew"
      pattern: "window\\.SRS\\.|SRS\\."
    - from: "qhse-cesi/outils.html (Flashcards IIFE)"
      to: "localStorage `qhse-srs-v1` + `qhse-prefs-v1`"
      via: "thin readStore/writeStore + readPrefs/writePrefs pair — only 4 functions touch localStorage in the entire IIFE"
      pattern: "localStorage\\.(getItem|setItem).*qhse-(srs|prefs)-v1"
    - from: "qhse-cesi/outils.html (Flashcards IIFE)"
      to: "window.BANK (frozen from Phase 2)"
      via: "read-only filter by theme; never mutates"
      pattern: "window\\.BANK\\.filter\\|BANK\\.filter"
    - from: "qhse-cesi/chassis.css `.fc-*` block"
      to: "`#panel-flashcards` DOM injected by the IIFE"
      via: "prefixed selectors inside @layer components"
      pattern: "#panel-flashcards|\\.fc-"
---

<objective>
Replace the `<p class="placeholder">` inside `#panel-flashcards` (outils.html:60-61) with a fully wired Flashcards view: DOM scaffold per UI-SPEC §Component Inventory, an inline `'use strict'` IIFE managing recto/verso state + grading + theme picker + réglages + bandeau counters + empty-queue UX + free-revision mode. Add `<script src="srs.js" defer></script>` to outils.html `<head>` immediately after the existing outils-data.js tag (line 16). Append the `.fc-*` CSS namespace to `chassis.css` immediately before the `@layer components` closing brace at line 546, mirroring the `.biblio-*` pattern.

This plan satisfies ROADMAP success criteria 1, 2, 3, 4, 5 (the entire owner-verifiable surface for Phase 3). It binds Plan 03-01's `window.SRS` to the DOM, persists state to the two locked localStorage keys, and ships the per-card row shape (`introduced` field) that Phase 4 will write into for SRS-03's QCM-wrong-answer half.

Output: outils.html (modified — DOM scaffold inside the existing tabpanel wrapper + new `<script src>` tag + new view IIFE block); chassis.css (modified — additive `.fc-*` rules).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-flashcards-srs/03-CONTEXT.md
@.planning/phases/03-flashcards-srs/03-RESEARCH.md
@.planning/phases/03-flashcards-srs/03-PATTERNS.md
@.planning/phases/03-flashcards-srs/03-UI-SPEC.md
@qhse-cesi/outils.html
@qhse-cesi/outils-data.js
@qhse-cesi/chassis.css
@qhse-cesi/srs.js
</context>

<interfaces>
<!-- The Flashcards IIFE consumes these globals (all defer-loaded, guaranteed present at end-of-body): -->

```js
// From outils-data.js (Phase 2, frozen):
window.BANK: Array<{
  id: string,                  // e.g. 'duerp-flashcard-001'
  type: 'flashcard' | 'qcm',   // D-01: both types render as flashcards in P3
  theme: string,               // one of 15 closed slugs
  question: string,            // recto
  answer: string,              // verso line 1
  choices?: string[],          // QCM only — IGNORED in P3
  correct?: number,            // QCM only — IGNORED in P3
  explanation: string,         // verso line 2
  source: { authority: string, ref: string, url: string, verified: string },
  difficulty: number
}>

// From srs.js (Plan 03-01):
window.SRS: {
  schedule(row|null, grade, today): row,
  isDue(row, today): boolean,
  addDays(yyyymmdd, n): yyyymmdd,
  todayLocal(): yyyymmdd,
  filterDue(cards, store, today): Card[],
  countNew(store, today): number,
  DEFAULTS: {...},
  GRADE: { RATE, DUR, BIEN, FACILE }
}

// Schema written to localStorage by THIS plan:
qhse-srs-v1 = { [itemId]: { ease: number, interval: number, due: 'yyyy-mm-dd', lapses: number, reps: number, introduced: 'yyyy-mm-dd'|null } }
qhse-prefs-v1 = { lastTheme: string, lastMode: 'flashcards', newCardsPerDay: number /* 1..50 */ }
// Both schemas are forward-compatible: writePrefs MUST do `Object.assign({}, existing, partial)` so P4/P5 added keys survive.
```

The 15 theme slugs (source order, locked Phase 2 D-01, must appear in the `<select>` in this exact order per UI-SPEC theme picker rule):
`duerp, principes-generaux, iso-9001, iso-14001, iso-45001, tms, risque-routier, risque-chimique, rps, espaces-confines, acronymes, metiers, calendrier, icpe-seveso, rncp`

French display labels (extract from existing bank `theme` field; UI-SPEC §Copywriting "planner must extract these"):
`duerp → DUERP`, `principes-generaux → Principes généraux de prévention`, `iso-9001 → ISO 9001`, `iso-14001 → ISO 14001`, `iso-45001 → ISO 45001`, `tms → TMS`, `risque-routier → Risque routier`, `risque-chimique → Risque chimique`, `rps → RPS`, `espaces-confines → Espaces confinés`, `acronymes → Acronymes`, `metiers → Métiers`, `calendrier → Calendrier alternance`, `icpe-seveso → ICPE / Seveso`, `rncp → RNCP / Blocs de compétences`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add `.fc-*` CSS namespace to chassis.css (additive only, inside @layer components, before closing brace at line 546)</name>
  <files>qhse-cesi/chassis.css</files>
  <read_first>
    - qhse-cesi/chassis.css lines 159-160 (the `@layer components {` opening — note the indentation style)
    - qhse-cesi/chassis.css lines 361-488 (the `.biblio-*` block — exact analog for `.fc-*` per PATTERNS.md; mirror token-only values, flat-class `__` modifier, focus-visible ring formula)
    - qhse-cesi/chassis.css lines 540-546 (the `.toc` rules + `@layer components` closing brace — insertion point is BEFORE line 546)
    - qhse-cesi/chassis.css lines 43-86 (available tokens — DO NOT add new ones)
    - qhse-cesi/chassis.css line 142 (existing `:focus-visible` global rule — reuse, do not override)
    - .planning/phases/03-flashcards-srs/03-UI-SPEC.md §Layout & Interaction Contracts, §Card visual treatment, §Bandeau visual treatment, §Theme picker, §Réglages, §Empty-queue panel (every selector + property value)
    - .planning/phases/03-flashcards-srs/03-PATTERNS.md §`qhse-cesi/chassis.css` (the full anti-pattern list — NO new :root tokens, NO modification of existing rules, NO transitions/animations, NO @keyframes, NO new fonts)
  </read_first>
  <action>
Locate the closing brace of `@layer components` at line 546 in `chassis.css`. Insert the following block IMMEDIATELY BEFORE that brace, indented to match the existing `.biblio-*` block (4-space indent inside the layer):

A single banner comment `/* === Flashcards (Phase 3) === */` then the `.fc-*` rules. (This exact banner string is the stable delimiter used by the regression-check regex in <verify>; the executor MUST use it verbatim — no variant whitespace, no rephrasing.) Adhere to UI-SPEC contracts verbatim:

**Bandeau** (`.fc-bandeau`): background `var(--bg-2)`, border `1px solid var(--border-subtle)`, border-radius `var(--radius-md)`, padding `var(--space-sm) var(--space-md)`, display flex, gap `var(--space-sm)`, align-items center, flex-wrap wrap, font `var(--step-0)`. `.fc-bandeau__dues` and `.fc-bandeau__new`: font-family `'JetBrains Mono', ui-monospace, monospace`, font-weight 500, font-size `var(--step-1)`. `.fc-bandeau__dues[data-state="has-dues"]`: color `var(--accent)`. `.fc-bandeau__dues[data-state="zero"]`: color `var(--ink-2)`. `.fc-bandeau__new`: always `color: var(--ink-2)`. `.fc-bandeau__sep`: color `var(--ink-3)`, padding-inline `var(--space-xs)`.

**Réglages disclosure** (`.fc-settings`): `margin-inline-start: auto`. `.fc-settings summary`: cursor pointer, color `var(--ink-2)`, font-weight 500, list-style none, padding `var(--space-xs) var(--space-sm)`, border-radius `var(--radius-sm)`. `.fc-settings summary::-webkit-details-marker { display: none }`. `.fc-settings summary::after { content: " ▾"; color: var(--ink-3) }`. `.fc-settings[open] summary::after { content: " ▴" }`. `.fc-settings[open] .fc-settings__panel`: display flex, gap `var(--space-sm)`, align-items center, padding-block `var(--space-sm)`. `.fc-settings input[type="number"]`: min-height 44px (touch target), width 5rem, font: inherit, background `var(--bg-1)`, border `1px solid var(--border-subtle)`, border-radius `var(--radius-sm)`, padding `0 var(--space-sm)`, color `var(--ink-1)`.

**Theme picker** (`.fc-theme`): display flex, gap `var(--space-sm)`, align-items center, margin-block `var(--space-md)`, flex-wrap wrap. `.fc-theme label`: font-weight 500, color `var(--ink-2)`. `#fc-theme-select`: font: inherit, min-height 44px, background `var(--bg-2)`, color `var(--ink-1)`, border `1px solid var(--border-subtle)`, border-radius `var(--radius-sm)`, padding `0 var(--space-md)`, width 100%. `@media (min-width: 48rem)`: `#fc-theme-select { min-width: 16rem; max-width: 24rem; width: auto; }`. `#fc-theme-select:focus-visible { border-color: var(--accent); }`.

**Card** (`.fc-card`): background `var(--bg-2)`, border `1px solid var(--border-subtle)`, border-radius `var(--radius-lg)`, padding `var(--space-md)`, min-height `clamp(16rem, 40vh, 24rem)`, display flex, flex-direction column, gap `var(--space-md)`, margin-block `var(--space-md)`. `@media (min-width: 48rem)`: `.fc-card { padding: var(--space-lg); max-width: 48rem; margin-inline: auto; }`.

**Recto** (`.fc-recto`): display flex, flex-direction column, gap `var(--space-md)`. `[data-fc-question]`: font-family `'Fraunces', Georgia, serif`, font-weight 600, font-size `var(--step-3)`, color `var(--ink-1)`, line-height 1.3, text-wrap balance, margin 0. `[data-fc-reveal]`: min-height 44px, padding `0 var(--space-lg)`, background `var(--accent)`, color `var(--bg-1)`, border `1px solid var(--accent)`, border-radius `var(--radius-sm)`, font: inherit, font-weight 600, font-size `var(--step-1)`, cursor pointer, align-self start. `[data-fc-reveal]:hover`: filter `brightness(1.05)`. `[data-fc-reveal-hint]`: display none. `@media (min-width: 48rem)`: `[data-fc-reveal-hint] { display: inline; color: var(--ink-3); font-family: 'JetBrains Mono', ui-monospace, monospace; font-size: var(--step--1); margin-inline-start: var(--space-sm); }`.

**Verso** (`.fc-verso`): display flex, flex-direction column, gap `var(--space-md)`. (When `hidden` attribute is present the chassis default `[hidden]{display:none}` handles it.) `[data-fc-answer]`: font-weight 600, font-size `var(--step-2)`, color `var(--ink-1)`, line-height 1.5, margin 0. `[data-fc-explanation]`: font-size `var(--step-1)`, color `var(--ink-2)`, line-height 1.6, margin 0. `[data-fc-source]`: font-size `var(--step-0)`, color `var(--ink-3)`, line-height 1.5, margin 0. `[data-fc-source] code`: font-family `'JetBrains Mono', ui-monospace, monospace`, color `var(--ink-2)`. `[data-fc-source] a`: color `var(--ink-2)`, text-decoration underline. `[data-fc-source] a:hover`: color `var(--accent)`.

**Grade buttons** (`.fc-grades`): display flex, flex-direction column, gap `var(--space-sm)`, margin-block-start `var(--space-md)`. `@media (min-width: 48rem)`: `.fc-grades { flex-direction: row; gap: var(--space-md); } .fc-grades > button { flex: 1 1 0; }`. `.fc-grades > button`: min-height 44px, padding `0 var(--space-md)`, background transparent, color `var(--ink-1)`, border `1px solid var(--border-subtle)`, border-radius `var(--radius-sm)`, font: inherit, font-size `var(--step-1)`, cursor pointer. `.fc-grades > button[data-fc-grade="rate"]:hover, .fc-grades > button[data-fc-grade="rate"]:focus-visible`: border-color `var(--alert)`, outline `2px solid var(--alert)`, outline-offset 2px. Repeat the same pattern for `dur` → `var(--warning)`, `bien` → `var(--success)`, `facile` → `var(--accent)`.

**Empty-queue panel** (`.fc-empty`): background `var(--bg-2)`, border-left `4px solid var(--warning)`, border-radius `var(--radius-md)`, padding `var(--space-lg)`, margin-block `var(--space-md)`. `.fc-empty h3`: font-family `'Fraunces', Georgia, serif`, font-weight 600, font-size `var(--step-2)`, color `var(--ink-1)`, margin `0 0 var(--space-sm)`. `.fc-empty p`: color `var(--ink-2)`, margin 0 0 var(--space-md). `[data-fc-free-revision]`: min-height 44px, padding `0 var(--space-md)`, background transparent, color `var(--accent)`, border `1px solid var(--accent)`, border-radius `var(--radius-sm)`, font: inherit, font-weight 600, cursor pointer.

**Free-revision banner** (`.fc-free-banner`): background `transparent`, color `var(--warning)`, font-size `var(--step-0)`, font-weight 500, padding `var(--space-xs) 0`, margin-block-start `var(--space-md)`.

**Free-revision controls** (`.fc-free-controls`): display flex, gap `var(--space-md)`, margin-block-start `var(--space-md)`. `[data-fc-next]`: same shape as a grade button. `[data-fc-quit]`: background transparent, border none, color `var(--ink-2)`, text-decoration underline, cursor pointer, font: inherit, font-size `var(--step-0)`.

**Inline error/no-cards** (`.fc-no-cards`, `.fc-error`): color `var(--ink-2)`, font-size `var(--step-1)`, padding `var(--space-lg) 0`, text-align center.

**Visually-hidden landmark** (reuse chassis `.sr-only` from line 550 — do NOT redeclare).

All selectors above MUST be prefixed by either `#panel-flashcards` parent OR a `.fc-` class. Do NOT introduce any selector that could leak (no bare `button {}`, no `[data-fc-*]` without a `.fc-` or `#panel-flashcards` ancestor).

CRITICAL prohibitions (PATTERNS.md anti-patterns):
- NO new `:root` custom properties.
- NO modification of any existing rule. Existing line 1-545 content unchanged.
- NO `transition`, `@keyframes`, or `animation` declarations (UI-SPEC §Motion contract).
- NO new media queries beyond reusing the `48rem` breakpoint.
- NO new fonts, NO new font-weight values (Inter 400/500/600/700, Fraunces 400/600/700, JetBrains 400/500 already declared).
- NO selectors outside `#panel-flashcards` scope or `.fc-` prefix.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('C:/Users/Lasmoles/mes-apps-claude/qhse-cesi/chassis.css','utf8');if(!c.includes('.fc-card'))throw new Error('.fc-card missing');if(!c.includes('.fc-bandeau'))throw new Error('.fc-bandeau missing');if(!c.includes('.fc-grades'))throw new Error('.fc-grades missing');if(!c.includes('.fc-empty'))throw new Error('.fc-empty missing');if(!c.includes('data-fc-reveal'))throw new Error('data-fc-reveal selector missing');const bannerRe=/\/\* === Flashcards \(Phase 3\) === \*\//g;const bannerHits=(c.match(bannerRe)||[]).length;if(bannerHits!==1)throw new Error('expected exactly 1 Flashcards (Phase 3) banner comment, got '+bannerHits);const m=c.match(/\/\* === Flashcards \(Phase 3\) === \*\/[\\s\\S]*$/);if(!m)throw new Error('banner-to-EOF region not found');const block=m[0].replace(/\/\*[\\s\\S]*?\*\//g,'');if(/(@keyframes|transition:|animation:)/.test(block))throw new Error('forbidden motion (@keyframes/transition:/animation:) inside fc-* block');console.log('chassis.css fc-* block ok; banner=1; no motion rules');"</automated>
  </verify>
  <done>
    chassis.css contains all required `.fc-*` selectors; lines 1-545 unchanged; no `@keyframes`, `transition:`, `animation:`, or new `:root` declarations added; insertion is INSIDE `@layer components` (before the brace that previously closed it at line 546).
  </done>
  <acceptance_criteria>
    - File `qhse-cesi/chassis.css` contains literals: `.fc-bandeau`, `.fc-bandeau__dues`, `.fc-bandeau__new`, `.fc-settings`, `.fc-theme`, `.fc-card`, `.fc-recto`, `.fc-verso`, `.fc-grades`, `.fc-empty`, `.fc-free-banner`, `.fc-free-controls`, `[data-fc-reveal]`, `[data-fc-grade="rate"]`, `[data-fc-grade="dur"]`, `[data-fc-grade="bien"]`, `[data-fc-grade="facile"]`, `[data-fc-quit]`, `[data-fc-free-revision]`.
    - File contains the literal banner comment `/* === Flashcards (Phase 3) === */` exactly once (used as the stable delimiter for the motion-free regression check).
    - File contains the literal `'Fraunces', Georgia, serif` AT LEAST twice (for `[data-fc-question]` and `.fc-empty h3`).
    - File contains the literal `'JetBrains Mono', ui-monospace, monospace` AT LEAST twice (for `.fc-bandeau__dues`/`__new` and `[data-fc-source] code`).
    - File contains `clamp(16rem, 40vh, 24rem)` exactly once (the card min-height).
    - File contains `min-height: 44px` AT LEAST 4 times (reveal, grade buttons, theme select, réglages input — touch-target floor per UI-SPEC).
    - File DOES NOT contain (newly added in the `.fc-*` block): `transition:`, `@keyframes`, `animation:`, `:root{`, `:root {`, `--fc-` (no new tokens prefixed `--fc-`), nor any bare `button {` rule.
    - Non-regression (git-diff authoritative): `git diff --unified=0 HEAD -- qhse-cesi/chassis.css | grep -c '^-[^-]'` returns 0 (zero deleted lines — additions only). Post-commit equivalent: `git diff HEAD~1 HEAD -- qhse-cesi/chassis.css | grep -c '^-[^-]'` returns 0. This is the authoritative non-regression gate; the prior char-count heuristic is removed.
    - `node -e "const c=require('fs').readFileSync('C:/Users/Lasmoles/mes-apps-claude/qhse-cesi/chassis.css','utf8'); const fcMatches=c.match(/\.fc-/g)||[]; if(fcMatches.length<20)throw new Error('expected >= 20 .fc- selector tokens, got '+fcMatches.length); console.log('fc-* selector tokens: '+fcMatches.length);"` succeeds.
    - Opening `outils.html` in a browser shows no console error related to CSS parsing.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Modify outils.html — add `<script src="srs.js" defer>` + replace placeholder with DOM scaffold + append Flashcards view IIFE</name>
  <files>qhse-cesi/outils.html</files>
  <read_first>
    - qhse-cesi/outils.html FULL FILE (lines 1-142 — confirm: line 16 is the existing outils-data.js script tag; lines 59-62 are the `#panel-flashcards` div with the placeholder `<p>`; lines 88-139 are the existing tablist IIFE that MUST stay untouched)
    - qhse-cesi/outils-data.js lines 1-19 (the WR-04 double-load guard pattern)
    - qhse-cesi/srs.js (Plan 03-01 output — the contract this IIFE consumes)
    - .planning/phases/03-flashcards-srs/03-CONTEXT.md §decisions D-01..D-07 (locked behaviors), §Claude's Discretion (theme picker UI, schema, module layout)
    - .planning/phases/03-flashcards-srs/03-RESEARCH.md §3 (keyboard interaction inside ARIA tabpanel — scoped listeners), §4.1-4.4 (merge-safe localStorage writes — readPrefs/writePrefs/readStore/writeStore), §5.2 (free-revision separate code path), §7 (bandeau counter math), §8 (DOM mount lifecycle + suggested DOM structure §8.3)
    - .planning/phases/03-flashcards-srs/03-PATTERNS.md §`qhse-cesi/outils.html` (IIFE shell pattern + panel-scoped listener + hash hygiene + mount preservation + full anti-pattern list)
    - .planning/phases/03-flashcards-srs/03-UI-SPEC.md §Composition diagram, §Copywriting Contract (all French strings — copy verbatim), §Keyboard contract, §ARIA contract
  </read_first>
  <action>
Three modifications to `qhse-cesi/outils.html`. Make them in this order:

**Change 1 — `<head>` script tag (after existing line 16 `<script src="outils-data.js" defer></script>`):**
Add a new line `  <script src="srs.js" defer></script>` immediately after. Both scripts use `defer` and run in document order; `window.BANK` and `window.SRS` are both ready before any inline `<script>` at end-of-body executes. No other changes to `<head>`.

**Change 2 — `#panel-flashcards` DOM (currently lines 59-62):**
Replace the single `<p class="placeholder">` line with the full DOM scaffold per UI-SPEC §Component Inventory + §Composition diagram. Preserve the surrounding `<div role="tabpanel" id="panel-flashcards" aria-labelledby="tab-flashcards" tabindex="0">` wrapper VERBATIM (the existing tablist IIFE at line 95 reads it by id). Only the content INSIDE the div changes.

The injected DOM tree (use 2-space indent matching the surrounding file style; ALL French copy verbatim from UI-SPEC §Copywriting Contract):
- `<h2 class="sr-only">Cartes mémoire avec répétition espacée</h2>`
- `<header class="fc-bandeau" role="status" aria-live="off">` containing two visible spans `<span data-fc-dues>0/0</span>` and `<span data-fc-new>0/10</span>`, a `<span class="fc-bandeau__sep">·</span>` separator, the labels `dues` and `nouvelles`, and a nested `<details class="fc-settings">` with `<summary>Réglages</summary>` containing a `<div class="fc-settings__panel">` with `<label>Cartes nouvelles par jour <input type="number" min="1" max="50" step="1" inputmode="numeric" data-fc-cap></label>` and `<small>Entre 1 et 50. Par défaut : 10.</small>`. Add a `<span class="sr-only" data-fc-aria-live aria-live="polite"></span>` AFTER the bandeau (the sr-only mirror node for polite announcements per UI-SPEC §ARIA).
- `<nav class="fc-theme">` with `<label for="fc-theme-select">Thème</label>` and `<select id="fc-theme-select" data-fc-theme>` containing one `<option value="all">Tous les thèmes</option>` followed by exactly 15 `<option value="{slug}">{label}</option>` in the locked source order from the `<interfaces>` block above (duerp first through rncp last).
- `<article class="fc-card" data-fc-card>` containing:
  - `<section class="fc-recto" data-fc-recto>`:
    - `<h3 data-fc-question></h3>`
    - `<button type="button" data-fc-reveal>Révéler <small data-fc-reveal-hint>Espace ou Entrée</small></button>`
  - `<section class="fc-verso" data-fc-verso hidden>`:
    - `<p><span class="sr-only">Réponse :</span> <span data-fc-answer></span></p>`
    - `<p><span class="sr-only">Explication :</span> <span data-fc-explanation></span></p>`
    - `<p data-fc-source><span class="sr-only">Source :</span> </p>`
    - `<div class="fc-grades">` with 4 buttons: `<button type="button" data-fc-grade="rate" aria-label="Noter cette carte : Raté (raccourci : 1)">1 · Raté</button>`, then `dur`/`Dur`/`2`, `bien`/`Bien`/`3`, `facile`/`Facile`/`4` — each with matching aria-label per UI-SPEC.
- `<div class="fc-empty" data-fc-empty hidden>`:
  - `<h3>Bravo — file vide pour aujourd'hui.</h3>`
  - `<p>Prochaine carte due le <time data-fc-next-due></time>. (<span data-fc-stats-good>0</span> validées · <span data-fc-stats-bad>0</span> à retravailler.)</p>`
  - `<button type="button" data-fc-free-revision>Continuer en révision libre</button>`

**Change 3 — append a new `<script>` block AFTER the existing tablist IIFE script tag (after line 140 `</script>`):**

A new `<script>` opening tag, then a `'use strict'` IIFE wrapped in a double-load guard (mirror outils-data.js:14-18 shape but for a sentinel like `if (window.__fcViewBooted) return;` set at the top of the IIFE). The IIFE must do ALL of the following — code structure outlined, but executor writes the actual JS:

a) **Pre-flight guards:** if `!window.BANK || !window.SRS` → render `.fc-card .fc-error` with the UI-SPEC copy `Impossible de charger la banque de cartes. Recharge la page ou vérifie que outils-data.js est bien servi.` and return early. `console.error` a diagnostic.

b) **Top-of-IIFE constants:** the 15 theme slugs (locked source order from `<interfaces>`), the French label lookup (from `<interfaces>`), DEFAULT_PREFS `{ lastTheme: 'all', lastMode: 'flashcards', newCardsPerDay: 10 }`.

c) **The ONLY 4 localStorage functions** (RESEARCH §4.1 + §4.2 verbatim, with the try/catch around every access — gracefully degrade on QuotaExceededError or corrupt JSON):
- `readPrefs()` — returns merged-with-defaults object; tolerates missing/extra keys; clamps `newCardsPerDay` to [1, 50].
- `writePrefs(partial)` — reads existing, `Object.assign({}, existing, partial)`, writes back (preserves unknown keys from P4/P5).
- `readStore()` — returns the parsed `qhse-srs-v1` object or `{}` on first visit / corrupt JSON.
- `writeStore(store)` — `try { localStorage.setItem('qhse-srs-v1', JSON.stringify(store)); } catch (e) { console.warn(...); }`.

NO other call site in the IIFE may touch `localStorage` directly.

d) **DOM handle queries** (one per data-fc-* selector — store as locals).

e) **State variables:** `let prefs = readPrefs(); let store = readStore(); let queue = []; let currentCard = null; let session = { dues: 0, total: 0, new: 0, good: 0, bad: 0 }; let freeRevision = false;`.

f) **`buildSession(theme)`** — RESEARCH §7.4 verbatim: filter window.BANK by theme (or all), split into `seenDue` and `newPool`, apply `newCardsPerDay` cap (subtracting `SRS.countNew(store, today)`), random shuffle (Fisher-Yates inline, 5 lines), return queue. Initialize `session.dues = seenDue.length; session.total = seenDue.length; session.new = 0;`. Update `[data-fc-cap]` input value from `prefs.newCardsPerDay`.

g) **`renderCard(card)`** — set `[data-fc-question].textContent = card.question` (NEVER innerHTML — XSS guard, security threat T-03-03-01). Hide verso (`verso.hidden = true`). Set `[data-fc-reveal]` enabled. Focus reveal button. If `card === null` → call `renderEmpty()` instead.

h) **`revealCard()`** — populate `[data-fc-answer].textContent = card.answer`, `[data-fc-explanation].textContent = card.explanation`, build the source line via textContent + a `<code>` element for `card.source.ref` and an `<a href>` for the URL (DO NOT innerHTML the URL — construct via `document.createElement('a'); a.href = url; a.textContent = url; a.rel = 'noopener noreferrer'; a.target = '_blank';`). Show verso (`verso.hidden = false`). Focus the first grade button.

i) **`gradeCard(grade)`** — `const today = window.SRS.todayLocal(); const row = store[currentCard.id] || null; const wasNew = !row; const newRow = window.SRS.schedule(row, grade, today); store[currentCard.id] = newRow; writeStore(store);` Then update session counters: `session.dues -= 1; if (wasNew) session.new += 1; if (grade === 'rate') session.bad += 1; else session.good += 1;` Then render bandeau, advance queue (`currentCard = queue.shift() || null; renderCard(currentCard);`).

j) **`renderBandeau()`** — set `[data-fc-dues].textContent = session.dues + '/' + session.total;` set `[data-fc-dues].dataset.state = (session.dues > 0 ? 'has-dues' : 'zero');` set `[data-fc-new].textContent = session.new + '/' + prefs.newCardsPerDay;` Update sr-only live region `[data-fc-aria-live].textContent = session.dues + ' cartes dues, ' + session.new + ' nouvelles introduites sur ' + prefs.newCardsPerDay + ' aujourd\\'hui';`.

k) **`renderEmpty()`** — hide `.fc-card`; show `[data-fc-empty]`; compute next-due date by scanning `store` for the minimum `due > today` value; render via `[data-fc-next-due].textContent = ...` (or the fallback string from UI-SPEC if no future due exists); set `[data-fc-stats-good].textContent = session.good`, `[data-fc-stats-bad].textContent = session.bad`. Focus `[data-fc-free-revision]`.

l) **`startFreeRevision()`** — `freeRevision = true;` hide `[data-fc-empty]`; show `.fc-card`; render the free-revision banner element AND swap `.fc-grades` for `.fc-free-controls` (create these via createElement + appendChild; DO NOT innerHTML). The next-card path: pick `pool[Math.floor(Math.random() * pool.length)]`, render via `renderCard()` BUT — critical — the verso section in free mode shows ONLY the `Carte suivante` and `Quitter la révision libre` buttons (NO grade buttons). The free-revision next-card function MUST NOT reference `writeStore`, `store[id] = ...`, or `window.SRS.schedule` — RESEARCH §5.2 structural separation. (`window.SRS.schedule` is allowed in the normal `gradeCard` path only.)

m) **Event wiring (panel-scoped, NEVER document):**
- Theme `<select>` `change` → `writePrefs({ lastTheme: select.value });` → re-build session, render bandeau, render first card.
- Réglages input `change` → clamp to [1,50] integer; `writePrefs({ newCardsPerDay: value });` → re-derive `newSlots` for next session (or apply immediately to the bandeau cap denominator).
- Reveal button `click` → `revealCard();`. Reveal button `keydown` Space/Enter → `e.preventDefault(); revealCard();`.
- Card body (`[data-fc-card]`) `click` while `verso.hidden` → `revealCard()` (tap-to-reveal on touch).
- Grade buttons `click` → `gradeCard(btn.dataset.fcGrade);`.
- `panel.addEventListener('keydown', ...)` — gate: `if (verso.hidden) return; if (e.target.matches('select, input, textarea')) return;` then switch on `e.key`: `'1'` → rate, `'2'` → dur, `'3'` → bien, `'4'` → facile (each with `e.preventDefault()`). In free-revision mode, `'n'`/`'N'` → next, `'q'`/`'Q'` → quit.
- Free-revision next-card click → next random card (no writeStore). Quit click → `freeRevision = false; renderEmpty();`.
- Empty-queue `[data-fc-free-revision]` click → `startFreeRevision()`.

n) **Boot:** call `buildSession(prefs.lastTheme); currentCard = queue.shift() || null; if (currentCard) renderCard(currentCard); else renderEmpty(); renderBandeau();`. Restore `select.value = prefs.lastTheme` and `[data-fc-cap].value = prefs.newCardsPerDay` BEFORE wiring listeners so the change-event doesn't fire spuriously.

Close the IIFE, close the `<script>` tag.

CRITICAL prohibitions (PATTERNS.md anti-patterns):
- NO `import`, NO `<script type="module">`.
- NO inline `onclick`/`onkeydown` attributes in the injected markup.
- NO `document.addEventListener('keydown', ...)` — every listener bound to `#panel-flashcards` or a descendant.
- NO direct `localStorage.getItem`/`setItem` scattered across the IIFE — must go through readPrefs/writePrefs/readStore/writeStore.
- NO `localStorage.setItem('qhse-prefs-v1', JSON.stringify({lastTheme: ...}))` without the Object.assign merge — destroys P4/P5 keys.
- NO mutation of `window.BANK` (Phase 2 froze it).
- NO `new Date(yyyymmdd)` parse — use `SRS.addDays` for math, `SRS.todayLocal()` for "today".
- NO `innerHTML` assignment ANYWHERE in the IIFE (XSS guard — bank items contain free-form regulatory prose that could one day include `<` characters). Use `textContent`, `createElement`, `appendChild` exclusively for bank-content rendering.
- NO flip/fade/slide animation; no transitions added inline.
- NO modification/removal of the existing tablist IIFE at lines 88-139 — the new `<script>` block goes AFTER it.
- NO modification of the `<div role="tabpanel" id="panel-flashcards" ...>` wrapper attributes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('C:/Users/Lasmoles/mes-apps-claude/qhse-cesi/outils.html','utf8');if(!h.includes('<script src=\"srs.js\" defer></script>'))throw new Error('srs.js script tag missing');if(!h.includes('data-fc-reveal'))throw new Error('data-fc-reveal missing');if(!h.includes('Tous les thèmes'))throw new Error('theme picker default option missing');if(!h.includes('1 · Raté')||!h.includes('4 · Facile'))throw new Error('grade buttons missing');if(!h.includes('window.SRS'))throw new Error('IIFE missing SRS reference');if(!h.includes('qhse-srs-v1')||!h.includes('qhse-prefs-v1'))throw new Error('localStorage keys missing');if(/onclick=|onkeydown=/.test(h))throw new Error('inline event handler forbidden');if(h.indexOf('innerHTML')>-1){const segs=h.split('innerHTML');console.warn('innerHTML found '+(segs.length-1)+' times — review for XSS');}console.log('outils.html structural checks pass');"</automated>
  </verify>
  <done>
    outils.html: line 16-17 region contains both `<script src=\"outils-data.js\" defer></script>` AND `<script src=\"srs.js\" defer></script>`; `#panel-flashcards` wrapper attributes unchanged but inner DOM replaced with the scaffold; placeholder `<p>` removed; existing tablist IIFE at lines 88-139 untouched; new `<script>` block appended after with the Flashcards view IIFE; no inline `on*` handlers anywhere; no innerHTML used for bank content.
  </done>
  <acceptance_criteria>
    - outils.html contains the literals: `<script src="srs.js" defer></script>`, `data-fc-reveal`, `data-fc-grade="rate"`, `data-fc-grade="dur"`, `data-fc-grade="bien"`, `data-fc-grade="facile"`, `data-fc-theme`, `data-fc-cap`, `data-fc-empty`, `data-fc-free-revision`, `data-fc-next-due`, `qhse-srs-v1`, `qhse-prefs-v1`, `window.SRS`, `window.BANK`, `Tous les thèmes`, `Cartes mémoire avec répétition espacée`, `Bravo — file vide pour aujourd'hui.`, `Continuer en révision libre`, `Révéler`, `Espace ou Entrée`.
    - outils.html contains exactly the 15 theme slugs as `<option value="duerp">` ... `<option value="rncp">` in the locked source order; preceded by `<option value="all">`.
    - outils.html does NOT contain: `onclick=`, `onkeydown=`, `<script type="module">`, `import `, `localStorage.getItem('qhse-srs-v1')` outside the readStore function, `localStorage.setItem('qhse-prefs-v1', JSON.stringify({`. (Acceptable: a single setItem inside writePrefs after Object.assign merge.)
    - outils.html still contains the existing tablist IIFE — search for the literal string `aria-selected` returns matches AND the `tab.addEventListener('keydown'` pattern from line 125 is preserved.
    - outils.html still contains the existing `<div role="tabpanel" id="panel-flashcards" aria-labelledby="tab-flashcards" tabindex="0">` line (the wrapper attributes are byte-identical).
    - outils.html does NOT contain the original placeholder text `Ce mode arrive en Phase 3 — révision par cartes avec répétition espacée`.
    - `node -e "const h=require('fs').readFileSync('C:/Users/Lasmoles/mes-apps-claude/qhse-cesi/outils.html','utf8'); const innerHTMLCount=(h.match(/innerHTML/g)||[]).length; if(innerHTMLCount>0)throw new Error('innerHTML used '+innerHTMLCount+' times — replace with textContent + createElement (XSS T-03-03-01)'); console.log('XSS guard ok: zero innerHTML uses');"` succeeds.
    - `node -e "const h=require('fs').readFileSync('C:/Users/Lasmoles/mes-apps-claude/qhse-cesi/outils.html','utf8'); const docKeydown=(h.match(/document\.addEventListener\(['\"]keydown/g)||[]).length; if(docKeydown>0)throw new Error('document-level keydown forbidden — must be panel-scoped'); console.log('keydown scoping ok');"` succeeds.
    - `node -e "const h=require('fs').readFileSync('C:/Users/Lasmoles/mes-apps-claude/qhse-cesi/outils.html','utf8'); const lsCalls=(h.match(/localStorage\.(getItem|setItem)/g)||[]).length; if(lsCalls>8)throw new Error('expected <= 8 localStorage call sites (2 per fn × 4 fns); found '+lsCalls+' — must be encapsulated in readPrefs/writePrefs/readStore/writeStore'); console.log('localStorage encapsulation ok: '+lsCalls+' call sites');"` succeeds (8 = upper bound: each of the 4 functions touches one key once read + once write).
    - Visiting `outils.html` in a browser (file:// or localhost): clicking the Flashcards tab shows the bandeau + theme picker + a recto question (not the placeholder); pressing Space reveals the verso with 4 grade buttons; clicking "3 · Bien" advances to a new card AND `localStorage.getItem('qhse-srs-v1')` returns a non-empty JSON with the graded card's id as a key.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outils-data.js bank content → DOM render | Bank prose is hand-authored at commit time but contains regulatory text that may include `<`, `>`, `&` — the view MUST escape via textContent, not innerHTML |
| localStorage → IIFE state | The owner (or a future debug session) may manually edit `qhse-srs-v1`; corrupt JSON must not crash boot |
| Cross-tab session → in-memory state | Owner opens two tabs, grades in tab A; tab B has stale in-memory state — last-write-wins per tab |
| browser → window.SRS | Plan 03-01 already mitigated double-load + side-effect-key risks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering / XSS | Bank `question`/`answer`/`explanation`/`source.url` rendered into the verso | mitigate | ALL bank content rendered via `element.textContent = card.field` OR `document.createElement` + `appendChild`. ZERO `innerHTML` assignments anywhere in the IIFE. Acceptance criteria asserts `innerHTML` count === 0. The `<a href>` for `source.url` is constructed with `a.href = url` (not interpolated into a string template) so even a malicious URL containing quotes/`<` cannot break out of the attribute. |
| T-03-03-02 | Denial of Service | `QuotaExceededError` on `localStorage.setItem` (Safari private mode, full quota) | mitigate | Every `localStorage.setItem` wrapped in try/catch with `console.warn` fallback (RESEARCH §4.2). UI continues to function in-memory; state survives the session but not the reload. Acceptable graceful degradation per CONTEXT.md. No alert toast (single-user tool, no need for UI nag). |
| T-03-03-03 | Tampering / Data Integrity | Corrupt or manually-edited `qhse-srs-v1` JSON | mitigate | `readStore()` and `readPrefs()` wrap `JSON.parse` in try/catch. On failure: return `{}` for store, return DEFAULT_PREFS. Boot continues with empty state — owner loses progress but UI works. `console.warn` records the corruption for postmortem. |
| T-03-03-04 | Tampering | Cross-tab race (owner opens two tabs, grades in A, switches to B) | accept | Out of P3 scope per CONTEXT.md §code_context. Acceptable: last-write-wins per tab. Document as known limitation in 03-04 polish plan. No `storage` event listener added in P3 (would require state-machine for queue rebuild, scope creep). |
| T-03-03-05 | Information Disclosure | localStorage state is unencrypted | accept | No PII, no credentials in `qhse-srs-v1` (just ease/interval/due integers + card IDs). No backend, no sync — data never leaves the device. Owner's local-device attack surface is out of scope for v2.0. |
| T-03-03-06 | Elevation of Privilege | Free-revision mode bypass — a flag-based guard could miss a branch and mutate the store | mitigate | RESEARCH §5.2 structural separation: free-revision code path does NOT reference `writeStore`, `store[id] = ...`, or `window.SRS.schedule`. Plan 03-02 verify-srs.cjs asserts `Object.keys(SRS)` has zero side-effecting keys (proves persistence is the VIEW's responsibility). Grep-able by inspection. |
| T-03-03-07 | Tampering | Bank `source.url` containing `javascript:` scheme | mitigate | The `<a>` constructor pattern (`a.href = url`) sanitizes via the URL parser; modern browsers refuse to navigate `javascript:` from a click on an anchor element with rel="noopener noreferrer". Defense in depth: optionally validate `url.startsWith('http')` before assignment. (Bank URLs are content-verified per BANK-04 — all https — so the structural mitigation is already in place; this is belt-and-suspenders.) |
| T-03-03-08 | Tampering | Future P4/P5 code overwrites Phase 3's `qhse-prefs-v1` keys (lastTheme, lastMode, newCardsPerDay) | mitigate | `writePrefs(partial)` does `Object.assign({}, existing, partial)` — Phase 3's keys survive when P4 adds `lastQuizTheme`. Plan 03-02 asserts this in the "unknown keys survive" round-trip test. |

**Severity:** none high. All XSS (T-03-03-01) and persistence (T-03-03-02, T-03-03-03) threats structurally mitigated; cross-tab and unencrypted-localStorage accepted with documented rationale. No new packages → no T-{phase}-SC supply-chain entry.
</threat_model>

<verification>
- outils.html has new `<script src="srs.js" defer></script>` in `<head>` after the existing outils-data.js script.
- `#panel-flashcards` placeholder `<p>` replaced; DOM scaffold has all data-fc-* hooks; ARIA contract (sr-only landmark, aria-live mirror, aria-labels on grade buttons) present.
- Existing tablist IIFE (lines 88-139) unchanged — verifiable via grep for the literal `tab.addEventListener('keydown', e =>`.
- New view IIFE block appended after the existing `<script>` block; uses `'use strict'`; bound listeners to panel only.
- chassis.css `.fc-*` block additive only; pre-existing rules byte-identical.
- Manual smoke test (deferred to Plan 03-04): open outils.html in a browser, click Flashcards tab, select a theme, click Révéler, grade a card, reload, confirm state persists.
</verification>

<success_criteria>
- ROADMAP SC1: Owner opens Flashcards, picks theme, sees recto, clicks Révéler, sees verso (answer + explanation + source) — visible UI behavior.
- ROADMAP SC2: 4 grade buttons advance + persist to `qhse-srs-v1` (verifiable via DevTools Application panel).
- ROADMAP SC3: Bandeau dues counter reflects `row.due <= today` filter.
- ROADMAP SC4: Reload restores last theme + cap; SRS store intact.
- ROADMAP SC5: Grading "raté" → card disappears from today's queue; bandeau decrements; SRS-03 schema (`introduced` field) present on every persisted row.
- No regression on Phase 1 (ARIA tablist still works) or Phase 2 (`window.BANK` still loads and is read-only).
- Atomic commit + push to main (single commit covering both file edits — they are tightly coupled).
</success_criteria>

<output>
Create `.planning/phases/03-flashcards-srs/03-03-SUMMARY.md` when done. Atomic commit + push.
</output>
</output>
