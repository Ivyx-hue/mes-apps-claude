---
phase: 03-flashcards-srs
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .planning/phases/03-flashcards-srs/verify-srs.cjs
autonomous: true
requirements:
  - SRS-01
  - SRS-02
  - SRS-04
  - PERSIST-01
tags:
  - verify-gate
  - sm-2
  - node
  - ci

must_haves:
  truths:
    - "`node .planning/phases/03-flashcards-srs/verify-srs.cjs --final` exits 0 against the Plan 03-01 srs.js"
    - "All 8 SM-2 edge cases E1-E8 from RESEARCH §1.3 are named PASS lines in the gate output"
    - "Schema round-trip assertion proves `qhse-srs-v1` shape survives `JSON.stringify` → `JSON.parse` deepStrictEqual"
    - "`newCardsPerDay = 10` cap assertion simulates the view's intro loop against 15 candidate cards and confirms exactly 10 introduced"
    - "Free-revision purity assertion confirms `Object.keys(SRS)` exposes ZERO side-effecting keys (matches `/persist|save|write|store|set/i`)"
    - "Date arithmetic asserts addDays correctness across leap year (2024-02-28 → 2024-02-29) AND CET→CEST DST boundary (2026-03-28 + 2 → 2026-03-30)"
  artifacts:
    - path: ".planning/phases/03-flashcards-srs/verify-srs.cjs"
      provides: "Executable ROADMAP SC2/SC3/SC4 + PERSIST-01 contract; Node gate that runs locally pre-push"
      contains: "global.window = {}"
      min_lines: 120
  key_links:
    - from: ".planning/phases/03-flashcards-srs/verify-srs.cjs"
      to: "qhse-cesi/srs.js"
      via: "Node require() with global.window shim"
      pattern: "require\\(.*srs\\.js"
    - from: ".planning/phases/03-flashcards-srs/verify-srs.cjs"
      to: ".planning/ROADMAP.md success criteria 2, 3, 4 (the SRS half)"
      via: "named pass()/fail() lines labeled SC{n}/{REQ-ID}"
      pattern: "PASS \\[SC[0-9]/(SRS|PERSIST)-"
---

<objective>
Create `.planning/phases/03-flashcards-srs/verify-srs.cjs` — the Node CLI verification gate for Phase 3's SRS module. Asserts (a) SM-2 math for 8 named edge cases, (b) `qhse-srs-v1` schema round-trip, (c) due-date filtering across past/today/future + leap year + DST, (d) `newCardsPerDay = 10` cap enforcement via simulated view loop, (e) free-revision purity (no side-effecting keys on `window.SRS`). Exit 0 = all PASS; exit 1 = any FAIL. Runs locally pre-push, mirrors Phase 2's `verify-bank.cjs --final` discipline.

Purpose: This is the executable success-criteria contract for SRS-01, SRS-02, SRS-04, and PERSIST-01 (schema half). The same gate becomes the regression guard when Plan 03-03 wires the view — if the view's writePrefs/writeStore round-trip ever drifts from the asserted schema, this gate catches it.

Output: One file (`verify-srs.cjs`), ~150 lines, uses only Node built-ins (`assert`, `path`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-flashcards-srs/03-CONTEXT.md
@.planning/phases/03-flashcards-srs/03-RESEARCH.md
@.planning/phases/03-flashcards-srs/03-PATTERNS.md
@.planning/phases/02-content-bank/verify-bank.cjs
@qhse-cesi/srs.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author verify-srs.cjs — Node gate with 5 assertion groups (a-e) mirroring verify-bank.cjs shape</name>
  <files>.planning/phases/03-flashcards-srs/verify-srs.cjs</files>
  <read_first>
    - .planning/phases/02-content-bank/verify-bank.cjs lines 1-50 (module-load shim, `global.window = {}`, require, fail-fast on missing global — adopt the entire bootstrap block)
    - .planning/phases/02-content-bank/verify-bank.cjs full file (named pass()/fail() pattern, allPassed mutable flag, --final exit gate at line 443-450 — adopt the shape)
    - .planning/phases/03-flashcards-srs/03-RESEARCH.md §1.3 (8 edge cases E1-E8 — each becomes a named check())
    - .planning/phases/03-flashcards-srs/03-RESEARCH.md §6.2 lines 542-665 (the full executable verify-srs.cjs structure — RESEARCH provides the assertion block verbatim; adapt to verify-bank.cjs naming style)
    - .planning/phases/03-flashcards-srs/03-PATTERNS.md §`.planning/phases/03-flashcards-srs/verify-srs.cjs` (file-load shim verbatim adoption + 5 gate sections + final-exit pattern + anti-patterns list)
    - qhse-cesi/srs.js (the module being verified — confirm function signatures match what the assertions call)
  </read_first>
  <action>
Create `.planning/phases/03-flashcards-srs/verify-srs.cjs` (NEW). Start with the verify-bank.cjs header block, substituting:
- File path: `.planning/phases/03-flashcards-srs/verify-srs.cjs`
- Purpose: "Verification gate for Phase 3 SM-2 scheduler (qhse-cesi/srs.js)."
- Default mode: `node verify-srs.cjs` runs ALL assertions (5 groups, see below). `--final` is an alias for the same — kept for symmetry with verify-bank.cjs which had distinct modes.
- Exit codes: same convention (0 = pass, 1 = fail).

Bootstrap (mirror verify-bank.cjs:14-34):
```
'use strict';
const path = require('path');
const assert = require('assert');
const srsPath = path.resolve(__dirname, '../../../qhse-cesi/srs.js');
global.window = {};
try { require(srsPath); } catch (e) { console.error('FAIL: could not load srs.js from', srsPath); console.error(e.message); process.exit(1); }
const SRS = global.window.SRS;
if (!SRS || typeof SRS.schedule !== 'function') { console.error('FAIL: window.SRS not exported (or schedule function missing) after loading', srsPath); process.exit(1); }
```

Helpers (mirror verify-bank.cjs:148-157):
- `let allPassed = true;`
- `function pass(label) { console.log('PASS [' + label + ']'); }`
- `function fail(label, reason) { console.error('FAIL [' + label + '] ' + reason); allPassed = false; }`
- `function check(label, fn) { try { fn(); pass(label); } catch (e) { fail(label, e.message); } }`

Implement the 5 assertion groups, each preceded by a `// ----` banner comment. Each `check()` label uses the convention `SC{n}/{REQ-ID} {short-name}` (verify-bank.cjs naming style):

**Group (a) — SM-2 math (8 edge cases E1-E8 from RESEARCH §1.3):**
Adopt RESEARCH §6.2 lines 563-608 verbatim, renaming each `check()` label to:
- `SC2/SRS-01 E1 first-grade-ever bien on new card`
- `SC2/SRS-01 E2 first raté ever`
- `SC2/SRS-01 E3 ten consecutive raté ease bottoms at 1.3`
- `SC2/SRS-01 E4 raté on brand-new card no negative interval` — assert `SRS.schedule(null, 'rate', '2026-05-23').interval === 1` AND `> 0`
- `SC2/SRS-01 E5 ease floor preserved on alternating rate/dur` — `let r=null; for (let i=0;i<5;i++) r=SRS.schedule(r,'rate','2026-05-23'); for (let j=0;j<5;j++) r=SRS.schedule(r,'dur','2026-05-23'); assert.strictEqual(r.ease, 1.3);`
- `SC2/SRS-01 E6 ceil rounding bien rep3 ease 2.5 interval 6` (the ceil(6*2.5)=15 case)
- `SC2/SRS-01 E7 facile on new card`
- `SC2/SRS-01 E8 introduced field stable across grades`

**Group (b) — Schema round-trip (PERSIST-01):**
```
check('SC4/PERSIST-01 qhse-srs-v1 schema JSON round-trip', () => {
  const store = { 'duerp-flashcard-001': { ease: 2.5, interval: 6, due: '2026-05-29', lapses: 0, reps: 2, introduced: '2026-05-22' } };
  const roundtripped = JSON.parse(JSON.stringify(store));
  assert.deepStrictEqual(roundtripped, store);
});
check('SC4/PERSIST-01 qhse-prefs-v1 schema JSON round-trip preserves unknown keys', () => {
  // Future-proof: a P4-written `lastQuizTheme` must survive a P3-style merge.
  const existing = { lastTheme: 'duerp', lastMode: 'flashcards', newCardsPerDay: 10, lastQuizTheme: 'rps' };
  const partial = { lastTheme: 'iso-9001' };
  const merged = Object.assign({}, existing, partial);
  assert.strictEqual(merged.lastTheme, 'iso-9001');
  assert.strictEqual(merged.lastQuizTheme, 'rps');  // unknown key survives
  assert.strictEqual(merged.newCardsPerDay, 10);
});
```

**Group (c) — Due-date filtering + addDays robustness (SRS-02 + SRS-04):**
```
check('SC3/SRS-02 isDue past/today/future', () => {
  assert.strictEqual(SRS.isDue({ due: '2026-05-23' }, '2026-05-23'), true);
  assert.strictEqual(SRS.isDue({ due: '2026-05-20' }, '2026-05-23'), true);
  assert.strictEqual(SRS.isDue({ due: '2026-05-24' }, '2026-05-23'), false);
});
check('SC3/SRS-04 addDays year-boundary 2026-12-31 + 1 → 2027-01-01', () => {
  assert.strictEqual(SRS.addDays('2026-12-31', 1), '2027-01-01');
});
check('SC3/SRS-04 addDays leap-year 2024-02-28 + 1 → 2024-02-29', () => {
  assert.strictEqual(SRS.addDays('2024-02-28', 1), '2024-02-29');
});
check('SC3/SRS-04 addDays non-leap 2026-02-28 + 1 → 2026-03-01', () => {
  assert.strictEqual(SRS.addDays('2026-02-28', 1), '2026-03-01');
});
check('SC3/SRS-04 addDays DST CET→CEST 2026-03-28 + 2 → 2026-03-30', () => {
  assert.strictEqual(SRS.addDays('2026-03-28', 2), '2026-03-30');
});
check('SC3/SRS-02 filterDue returns only cards whose row.due <= today', () => {
  const cards = [{ id: 'a' }, { id: 'b' }, { id: 'c' }];
  const store = { a: { due: '2026-05-20' }, b: { due: '2026-05-25' }, c: { due: '2026-05-23' } };
  const due = SRS.filterDue(cards, store, '2026-05-23');
  assert.strictEqual(due.length, 2);
  assert.strictEqual(due.map(c => c.id).sort().join(','), 'a,c');
});
```

**Group (d) — newCardsPerDay cap enforcement (FLASH-01 pacing):**
Mirror RESEARCH §6.2 lines 639-655. Simulate the view's intro loop: 15 candidate cards, cap=10, scheduler called for each only if under cap. Assert exactly 10 entries in the store have `introduced === today`.

**Group (e) — Free-revision purity (D-06 structural guarantee):**
```
check('SC5/SRS-04 free-revision purity — window.SRS has no side-effecting keys', () => {
  const allKeys = Object.keys(SRS);
  const forbidden = allKeys.filter(k => /persist|save|write|store|set/i.test(k));
  assert.strictEqual(forbidden.length, 0, 'window.SRS exposes side-effecting key(s): ' + forbidden.join(','));
});
check('SC5/SRS-04 SRS.schedule is pure — same input twice yields equal output', () => {
  const a = SRS.schedule(null, 'bien', '2026-05-23');
  const b = SRS.schedule(null, 'bien', '2026-05-23');
  assert.deepStrictEqual(a, b);
});
check('SC5/SRS-04 SRS.schedule does not mutate the input row', () => {
  const row = { ease: 2.5, interval: 6, due: '2026-05-29', lapses: 0, reps: 2, introduced: '2026-05-22' };
  const snapshot = JSON.parse(JSON.stringify(row));
  SRS.schedule(row, 'bien', '2026-05-23');
  assert.deepStrictEqual(row, snapshot);
});
```

Final exit block (mirror verify-bank.cjs:443-450):
```
console.log('\n' + '='.repeat(70));
if (allPassed) {
  console.log('--final: ALL Phase 3 SRS gates PASS — window.SRS verified for SC2/SC3/SC4/SC5');
  process.exit(0);
} else {
  console.error('--final: ONE OR MORE ASSERTIONS FAILED — see FAIL lines above');
  process.exit(1);
}
```

CRITICAL prohibitions (PATTERNS.md anti-patterns):
- NO `npm install`, NO test-runner deps (Jest, Vitest, Mocha) — Node built-ins only.
- NO `import` — file extension is `.cjs`; CommonJS contract.
- NO `localStorage` reference (use `JSON.stringify`/`JSON.parse` directly to simulate persistence).
- NO `new Date()` inside any assertion (pass literal yyyy-mm-dd strings — keeps gate deterministic across DST/wall-clock).
- NO call to `SRS.schedule(row, grade)` with `today` undefined.
- NO modification of verify-bank.cjs.
  </action>
  <verify>
    <automated>cd C:/Users/Lasmoles/mes-apps-claude && node .planning/phases/03-flashcards-srs/verify-srs.cjs && echo "GATE_PASS"</automated>
  </verify>
  <done>
    `verify-srs.cjs` exists at the canonical path; running `node .planning/phases/03-flashcards-srs/verify-srs.cjs` from the repo root exits 0 with output ending `--final: ALL Phase 3 SRS gates PASS — window.SRS verified for SC2/SC3/SC4/SC5` followed by `GATE_PASS`; deliberately breaking srs.js (e.g. flipping a constant) causes exit 1 with a named FAIL line.
  </done>
  <acceptance_criteria>
    - File `.planning/phases/03-flashcards-srs/verify-srs.cjs` exists.
    - File contains the literals: `global.window = {}`, `require(srsPath)`, `SRS.schedule`, `Object.keys(SRS).filter`, `process.exit(0)`, `process.exit(1)`, `allPassed`.
    - File does NOT contain: `import `, `npm`, `localStorage`, `new Date()`, `require('jest')`, `require('mocha')`, `require('vitest')`.
    - File contains AT LEAST 8 distinct `check(` calls whose label substring starts with `SC2/SRS-01 E` (the 8 edge cases E1-E8).
    - File contains AT LEAST 2 `check(` calls with label substring containing `addDays` AND at least one with `leap-year` or `2024-02-29` AND at least one with `DST` or `2026-03-30`.
    - File contains the literal regex `/persist|save|write|store|set/i` (the free-revision purity gate).
    - `node .planning/phases/03-flashcards-srs/verify-srs.cjs` exits 0; stdout contains the lines `PASS [SC2/SRS-01 E1` (or similar with E1), `PASS [SC3/SRS-04 addDays year-boundary`, `PASS [SC5/SRS-04 free-revision purity`, and final line matches `--final: ALL Phase 3 SRS gates PASS`.
    - Tampering test: temporarily setting `EASE_FLOOR = 0` in srs.js → re-running the gate exits 1 with `FAIL [SC2/SRS-01 E3` or `E5` printed. (Executor should NOT actually commit this — only confirm the gate would catch it; then revert.)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev shell → Node | The script runs only on the developer's machine pre-push; no production runtime exposure |
| Node → srs.js (file load) | A shimmed `global.window = {}` is the entry; if srs.js had side effects beyond window-attach, they'd run here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | A future commit modifies srs.js constants without re-running this gate | mitigate | Document in CHECKPOINT.md / SUMMARY.md that this gate MUST run pre-push (mirrors verify-bank.cjs Phase 2 discipline). Owner runs `node verify-srs.cjs` as the last command before `git push`. |
| T-03-02-02 | Repudiation | Gate output not captured → "I thought it passed" claim | accept | Owner runs gate interactively and sees PASS/FAIL lines on screen; not auto-recorded. Acceptable for single-user project — same trust model as verify-bank.cjs. |
| T-03-02-03 | Denial of Service | Infinite loop in an assertion → gate hangs | mitigate | Every loop has a fixed integer upper bound (10 raté grades, 15 candidate cards). No `while (...)` constructs. No I/O inside loops. |
| T-03-02-04 | Tampering | Supply chain — npm dep with malicious payload | mitigate | Zero dependencies. Only Node built-ins (`assert`, `path`). No package.json change. No T-{phase}-SC entry needed. |

**Severity:** none high. Gate is a deterministic, pure-JS, single-purpose CLI with zero external dependencies.
</threat_model>

<verification>
- File created at the exact path `.planning/phases/03-flashcards-srs/verify-srs.cjs`.
- 5 assertion groups (a-e) present; at least 8 SM-2 edge-case checks (E1-E8); at least 5 date-arithmetic checks; at least 1 cap-enforcement check; at least 1 purity check.
- Gate exit code = 0 on the canonical srs.js from Plan 03-01.
- Gate uses only Node built-ins (verify by grepping for `require(` — only `path` and `assert` strings should appear).
</verification>

<success_criteria>
- `node .planning/phases/03-flashcards-srs/verify-srs.cjs` exits 0 against the canonical srs.js.
- All 5 RESEARCH §6.2 gate sections covered (math, schema, due, cap, purity).
- The gate is the executable contract for SC2/SC3/SC4/SC5 (the four ROADMAP success criteria that can be machine-asserted; SC1 — owner-visible reveal flow — is owner-verified in Plan 03-04).
- Atomic commit + push to main (single commit covering this file).
</success_criteria>

<output>
Create `.planning/phases/03-flashcards-srs/03-02-SUMMARY.md` when done. Atomic commit + push.
</output>
</output>
